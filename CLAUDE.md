# ⚠️ RÈGLES ABSOLUES

- **Git** : toujours committer et pousser directement sur `main` (pas de branche de feature), sauf demande contraire. **Un commit distinct par demande** de l'utilisateur.
- **Docs à jour** : après toute modif fonctionnelle, mettre à jour `CLAUDE.md` (technique) et `README.md` (visible utilisateur) dans le même commit — sans attendre qu'on le demande.
- **Éditer le dépôt principal**, jamais un worktree temporaire (`.claude/worktrees/*`).
- **⚠ Pas de build tool** : toute l'app est UN seul `<script type="text/babel">` compilé au runtime. La moindre erreur de syntaxe fait échouer la compilation entière → `#root` vide → **écran tout noir**, sans message d'erreur. Après toute modif d'`index.html`, vérifier que la page monte avant de commit.

---

# Spotify+ · Nouvelles Sorties

## Vue d'ensemble
PWA **sans backend** : toutes les données en local (sql.js SQLite WASM + IndexedDB), hébergée statiquement sur GitHub Pages. Chaque artiste suivi est scrappé depuis **sa propre** `last_scraped_at` (défaut `2026-03-15`). L'utilisateur parcourt son feed, écoute titre par titre ; en fin de titre → marqué écouté + auto-avance + disparition animée. Aucun ajout auto en playlist.

C'est un **hub perso** multi-sections : Musique (feed), Actu, Météo, Finance (+ Mes Actifs), Bon Plan, TV Time, Note, Revente, Santé, Vêtement, Frigo, To do, Maps, Mot de passe, Remember/Alertes, Stats, League of Legends, GitHub/Speedrun.

## Fichiers clés

| Fichier | Rôle |
|---|---|
| `index.html` | **App complète** (React 18 CDN + Babel + sql.js), tout en un fichier |
| `manifest.json` | Config PWA |
| `service-worker.js` | Cache app shell + vendor (offline). **Network-first** pour l'app shell (fetch `{cache:'no-store'}` pour bypasser le `max-age=600` de GitHub Pages), ne cache que `res.ok`, clé normalisée `./index.html`. Bump la version du cache après changement |
| `vendor/sql-wasm.js` / `.wasm` | sql.js 1.10.2 auto-hébergé |
| `vendor/leaflet.js` / `.css` | Leaflet 1.9.4 auto-hébergé, chargé **lazy** (`loadLeaflet()`) au 1er affichage de la carte Maps |
| `vendor/megajs.js` | **megajs 1.3.10** (build UMD navigateur officielle, MIT, ~230 Ko) auto-hébergée → pose `window.mega` (`Storage`/`File`/`MutableFile`/`API`). Client MEGA complet (login PBKDF2 + master key + upload/download chiffrés). Chargé **lazy** (`loadMega()`, pattern loadLeaflet) au 1er usage de la sauvegarde MEGA, précaché par le SW |
| `vendor/motus-words.js` / `motus-dico.js` | Données du jeu Motus (850 mots à deviner par longueur 6-9 × 5 difficultés + dictionnaire de validation ~125k mots, ~1,1 Mo) reprises du projet perso Pierre-Portfolio/Motus, wrappées en IIFE → posent `window.MOTUS_WORDS`/`window.MOTUS_DICO` (Set par longueur). Chargées **lazy** (`loadMotusData()`, pattern loadLeaflet) au 1er affichage du jeu, précachées par le SW |
| `icon-192.png` / `icon-512.png` | Icônes PWA (racine) |
| `data/actu.json` | Sources Actu pré-fetchées par GitHub Action (`update-actu.yml`, cron 2 h) via `scripts/fetch_actu.py`. Lu par `actuFetchStatic()` same-origin ; > 24 h → repli fetchers client (proxies CORS). **Images d'article** : les liens Google News (opaques, sans image) sont décodés côté Action (`gn_decode`, POST interne DotsSplashUi/batchexecute) → `og_image()` de la page réelle → champ `image` des items ; cache `images` (id GN → URL, `""` = tenté sans og:image) reconstruit à chaque run, budget `IMG_BUDGET=40` décodages/run, échec réseau retenté au run suivant. Client inchangé (`actuThumb` : image sinon favicon). **Liens réels (7.6.66)** : le décodage est factorisé dans `gn_real_url(link, id, links_old, links_new, budget)` + cache `links` (id GN → URL réelle) mutualisé entre `enrich_images` et **`resolve_links`** (appelée sur la seule section `linkedin` APRÈS les images) → un article n'est décodé qu'une fois, et le champ `link` des items LinkedIn est réécrit vers l'URL **linkedin.com**. `resolve_links` **renvoie la liste des items gardés** : résolu sur linkedin.com → lien réécrit ; résolu AILLEURS → item **jeté** (la recherche `site:` a élargi) ; **non** résolu (hors budget / décodage KO / réseau) → gardé tel quel avec son lien GN (l'absence de preuve n'est pas une preuve d'absence) |
| `data/indices.json` | Vrais indices (NASDAQ-100/S&P 500/CAC 40) via `scripts/fetch_indices.py` (Stooq→Yahoo) + `update-indices.yml`. Lu same-origin (zéro proxy) |

## Stack & sécurité

- **React 18.3.1 + Babel standalone 7.29.7** via CDN unpkg — **versions épinglées + SRI sha384 + crossorigin**. Ne jamais changer une version sans recalculer le hash.
- **sql.js 1.10.2 auto-hébergé** : `initSqlJs({ locateFile: f => './vendor/' + f })`.
- **⚠ CSP** (`<meta>` dans `<head>`) : `script-src` = `'self'` + unpkg (React/Babel, SRI) + **`accounts.google.com`** (Google Identity Services, script non versionné donc SANS SRI). `connect-src` limité à `'self'` + les origines réellement utilisées (**accounts.google.com** (GIS), api.spotify.com, accounts.spotify.com, api.themoviedb.org, www.googleapis.com, api.open-meteo.com + geocoding, api.bigdatacloud.net/photon.komoot.io/nominatim, api.coingecko.com, api.frankfurter.app, api.gold-api.com, api.twelvedata.com, stooq.com, lol-api-summoner.op.gg, lol-web-api.op.gg, proxies CORS codetabs/cors.eu.org/allorigins, api.github.com, www.speedrun.com, hn.algolia.com, hacker-news.firebaseio.com, haveibeenpwned.com, www.cheapshark.com, ledenicheur.fr, world.openfoodfacts.org, ddragon.leagueoflegends.com, api.dropboxapi.com/content.dropboxapi.com, api.pcloud.com/eapi.pcloud.com + joker *.pcloud.com (hôtes de contenu éphémères), g.api.mega.co.nz + jokers *.mega.co.nz/*.mega.nz (serveurs de fichiers dynamiques), tile.openstreetmap.org, www.themuse.com, remotive.com, generativelanguage.googleapis.com, **api.anthropic.com** (Revente — appel navigateur direct autorisé par l'en-tête `anthropic-dangerous-direct-browser-access`)). `frame-src` : `'self'` + **accounts.google.com** (iframes internes de GIS) + piece-jointe-carto.developpement-durable.gouv.fr (iframe carte transports IDF) + earthquakes.volcanodiscovery.com (catastrophes naturelles : volcans & séismes) + world-on-fire.com (Guerre) + waqi.info (qualité de l'air) + infoeau.fr (qualité de l'eau) + statistiques-locales.insee.fr (carte INSEE, mode iframe officiel) + odisse.santepubliquefrance.fr (santé) — les iframes du collapse « Cartographie » d'Actu (l'oval auroral NOAA est une IMAGE, couvert par `img-src https:`, pas de frame-src) — + www.youtube-nocookie.com (lecture EN PLACE des vidéos YouTube d'Actu). `img-src` inclut `https:` + **`blob:`** (photos Vêtement téléchargées depuis Drive avec un Bearer → `URL.createObjectURL`) + i.ytimg.com + *.ggpht.com/*.googleusercontent.com + image.tmdb.org + ddragon. `'unsafe-inline'`/`'unsafe-eval'` requis par Babel, `'wasm-unsafe-eval'` par sql.js. **Toute nouvelle origine (fetch/script/image) DOIT être ajoutée à la CSP sinon le navigateur la bloque.**
- **IndexedDB** : binaire SQLite (clé `spotifyplus_db`), connexion unique cachée (`_idbPromise`). `navigator.storage.persist()` au login.
- **Diagnostic de connexion (7.6.68)** : TOUTES les causes d'échec du retour OAuth finissaient sur un `setAuthState('login')` MUET (l'utilisateur revoyait la page de connexion sans explication, impossible à réparer à l'aveugle). Désormais la raison réelle est posée via `authErrSet(msg)` / relue par `authErrTake()` (**sessionStorage** `spotifyplus_auth_error` — pas un module-level, car `refreshToken()` **recharge la page** dans ses 2 cas de sortie et le message doit survivre à ce reload), remontée dans le store (`authError`) et affichée en bandeau rouge dans la carte de connexion de `Home` (`const { authError } = useStore()`). Cas couverts : `?error=…` sans `code` (consentement refusé / appli non autorisée — tombait avant dans le `if (!token)` et n'affichait rien) · `state` anti-CSRF non concordant · `pkce_verifier` disparu (autre navigateur, données de site effacées, navigation privée) · échec réseau du POST `/api/token` (le verifier n'est alors PAS jeté) · erreur renvoyée par le token endpoint (`error_description`/`error`, ex. « Invalid authorization code ») · **quota localStorage** (`setItem` throw dans `loginWithSpotify` ET dans `exchangeCode` — sans garde on partait vers Spotify avec un verifier jamais écrit, ou le jeton reçu n'était pas enregistré → boucle de connexion silencieuse) · **`/me` refusé** : statut HTTP + message Spotify mot pour mot, avec une reco dédiée en **403** (« ajoute-toi dans User Management du Dashboard » — cause n°1 quand l'appli est en Development Mode) et en 401 · exception du bloc d'init. `refreshToken()` documente aussi ses 2 déconnexions (session héritée sans refresh token, `invalid_grant`).
- **Auth** : OAuth 2.0 PKCE 100% client, token en localStorage. `state` anti-CSRF (`pkce_state`). `refreshToken()` protégé par mutex (`_refreshPromise`, refresh tokens Spotify à usage unique/rotation) ; refresh token roté re-stocké ; session héritée sans refresh → `logoutClear()`+reload ; `invalid_grant` → idem. `apiGet/apiPut/apiDel` retry 1× après refresh sur 401.
- **lsSet(key, value)** : écriture localStorage centralisée — `console.warn` + alerte utilisateur 1×/session (`_lsWarned`) sur quota plein. Route les setters persistants (saveTvItems, saveTodos, saveListenLog, saveVaultBlob, saveRappels, saveRemembers, saveModuleToggles…). Les caches re-fetchables restent en `catch {}` muet.
- **Garde multi-onglets** : `BroadcastChannel('spotifyplus_tabs')` → bandeau d'avertissement (`otherTab`), car chaque `saveDB()` exporte toute la DB.

### Config Spotify
```js
CLIENT_ID = '672e41f0308f4378b4f2331844e08b20'
REDIRECT_URI = 'https://pierre-portfolio.github.io/NewSpotifyRelease/'
SCOPES = 'user-follow-read user-read-private user-read-currently-playing user-read-playback-state user-modify-playback-state user-library-read user-library-modify'
// user-read-playback-state (6.4.5) : GET /me/player/queue (capture Découvertes par la lecture).
// Token pré-scope → 403 sur la file → log « déconnecte/reconnecte-toi » 1×/session (_dwCapDenied).
```

### `APP_VERSION` (actuellement `'7.6.83'`)
Constante module-level. Format `MAJ.MIN.U` = nombre de commits **du projet** (≈711, recalé par l'utilisateur en juillet 2026 : 710 commits + le commit courant) découpé : `patch = N%10`, `minor = floor(N/10)%10`, `major = floor(N/100)` (278→2.7.8, 1001→10.0.1). **⚠ Suivre le compteur PROJET (~711), pas `git rev-list --count` de ce fork**. À incrémenter **à la main à chaque commit** (pas de build tool pour l'injecter). Affichée sous « Purger les écoutes » et en badge sur la page de connexion.

### Délai de scraping
`delayChoice` (10/20/30 s, **défaut 10 s depuis 7.6.79** — demande utilisateur, était 30 s ; state + `delayChoiceRef` initialisés à 10, non persisté donc chaque chargement repart sur 10 s) + jitter : `delayChoice*1000 + Math.random()*2000 + 1000`.

---

## Schéma SQL local (4 tables)

```sql
tracks (id PK, spotify_uri UNIQUE, artist_name, title, release_title,
        release_type, release_date, cover_url, duration_ms,
        listened DEFAULT 0, liked DEFAULT 0, listened_at, added_at DEFAULT datetime('now'))

artists_scraped (spotify_id PK, last_scraped_at, name, image_url, popularity, followers,
        genres /*JSON*/, spotify_url, last_release_count DEFAULT 0,
        total_tracks_added DEFAULT 0, last_scan_status /*ok|partial|error*/, scan_count DEFAULT 0)

stats (id=1 CHECK, total_listened, listened_this_month, listened_this_year,
        last_reset_month /*YYYY-MM*/, last_reset_year,
        total_listened_ms DEFAULT 0, total_liked DEFAULT 0)  -- 2 dernières colonnes migrées

purged_uris (spotify_uri PK)  -- jamais réinsérés par un re-scan
```
Migrations idempotentes dans `initDB()` (ALTER TABLE en try/catch).

## Module DB (fonctions module-level)
`initDB()` (charge IndexedDB ou crée schéma) · `saveDB()` (export→IndexedDB, **sérialisé** via `_savePromise`/`_saveQueued`, pas de debounce) · `importDiscoverWeekly()` · `dbRun/dbAll/dbGet` · `loadListenStatsFromDB()` · `loadLikedTracksFromDB()` · `loadHistoryFromDB()` (listened=1, max 200, `listened_at DESC`) · `loadArtistsFromDB()` · `mapFeedRow(t)` · `loadFeedFromDB({artistSearch, filterType, sortBy, genreFilter})` (WHERE listened=0, **filtre + tri EN SQL** puis LIMIT 1000 → balaie TOUTE la base même au-delà de 1000).

Item feed : `{ id, spotifyUri, label, artist, title, subtitle, date, rawDate, image, isNew, liked, duration_ms }`.

`saveDB()` appelé après : chaque artiste scrapé, chaque écoute, chaque purge, `endSync`, `visibilitychange→hidden`. Toujours `INSERT OR IGNORE` pour tracks ; **UPSERT `ON CONFLICT(spotify_id) DO UPDATE`** pour artists_scraped (PAS `INSERT OR REPLACE`).

### Likes — migration API Spotify fév. 2026
`/me/tracks/contains` et `PUT/DELETE /me/tracks` **supprimés**. Remplaçants : `libraryContains(uris)` → `GET /me/library/contains?uris=` (max 40) · `librarySave/libraryRemove` → `PUT/DELETE /me/library?uris=` (**URIs en query string même pour PUT**, pas de body) · `libraryScopeAlert(result)` (403 = token pré-scope → alerte reco). `apiDel` mappe aussi 403→`{error:'forbidden'}`.

`syncInitialLikes()` : au `dbReady`, jusqu'à 300 tracks, `libraryContains` par batch de 40, TTL 24 h (`spotifyplus_likes_synced_at`).

### Découvertes de la semaine (`importDiscoverWeekly`)
Cherche dans `/me/playlists` toutes les playlists « découvertes de la semaine »/« discover weekly ». **Priorité aux copies PERSO** (owner ≠ 'spotify', contenu lisible). `fetchPlaylistItems(id)` essaie `/items` PUIS `/tracks`. Insère en `release_type='discover_weekly'`. **Cadence par SEMAINE ISO (6.3.6)** : `DW_LS_KEY` = clé `dwWeekKey()` (« YYYY-Www ») de la dernière semaine importée → ré-import une fois par NOUVELLE semaine (les Découvertes se rafraîchissent le lundi ; remplace l'ancien cooldown glissant 7 j qui dérivait). Échec → cooldown 6 h (`DW_FAIL_KEY`). **Ré-lecture directe** : l'ID de la dernière playlist LISIBLE est mémorisé (`DW_PID_KEY`) et relu en priorité chaque semaine (avant de re-balayer toutes les playlists) — import fiable/rapide, posé aussi par `dwManualAccess` sur succès. ⚠ Le contenu des playlists éditoriales Spotify n'est souvent pas lisible → log explique les contournements (lecture-capture ci-dessous, ou copie perso). Recherche des candidates factorisée dans `dwFindCandidates()`. **CAPTURE PAR LA LECTURE (6.4.5)** — `dwCaptureFromContext(contextUri, addLog)` appelée à chaque tick du poll player quand `r.context.type === 'playlist'` : le contenu éditorial est illisible mais ses MÉTADONNÉES (nom) le sont et la **file d'attente** (`GET /me/player/queue`, scope `user-read-playback-state`) expose currently_playing + ~20 suivants pendant l'écoute → si le nom matche `DW_NAME_RE` (cache session `_dwCtxIsDW` par playlist id), import en `discover_weekly` (skip titres déjà en base — même écoutés — + `purged_uris`), throttle `_dwCapLastAt` 1×/60 s, `saveDB()` + log si ajouts, puis `reloadFeedRef.current()` (ref posée à la définition de `reloadFeed` — la closure du tick, deps `[authState]`, figerait sinon des filtres périmés). 403/401 sur la file = token pré-scope → `_dwCapDenied` (stop session) + log reco reconnexion. → il suffit de LANCER la lecture de la playlist 1×/semaine (bouton violet) pour remplir « En attente », plus besoin de copie perso. **⚠ 7.6.65 — PLUS AUCUN IMPORT AUTOMATIQUE (demande utilisateur « retire le forcing »)** : l'appel `importDiscoverWeekly(addInitLog)` qui tournait à CHAQUE login (gate 1×/semaine ISO + re-tentative après le cooldown 6 h) est **SUPPRIMÉ** de l'init du store — aucune requête playlist ne part sans geste de l'utilisateur (`initMsgs`/`addInitLog` et le `setLogs(initMsgs)` associé sont devenus morts et retirés). Le **SEUL** appelant est `dwManualAccess` (le bouton), qui passe désormais **`{ force: true }`** à `importDiscoverWeekly(addLog, opts)` : ni `DW_LS_KEY` ni `DW_FAIL_KEY` ne peuvent avaler un clic (les garde-fous restent codés pour un futur appelant auto ; la fenêtre 429 `_rlUntil` s'applique toujours). La **capture pendant l'écoute** (`dwCaptureFromContext`) reste active : elle ne coûte aucune requête dédiée (elle lit la file déjà interrogée par le poll player). **Accès manuel** : bouton `DiscoverWeeklyButton` (`DW_PURPLE`) dans Scrapping **AU-DESSUS des Logs** (7.6.65 — c'est le déclencheur, il se voit en premier et les Logs juste en dessous montrent le résultat du clic ; desktop + mobile). **Rappel hebdo (7.6.8)** : passe en **jaune** (`DW_YELLOW`, préfixe 🟡, gras) à chaque NOUVELLE semaine ISO (donc dès le lundi) tant qu'il n'a pas été cliqué ; un clic mémorise `dwWeekKey()` dans `DW_BTN_ACK_KEY` (`spotifyplus_dw_btn_ack`) → reprend sa couleur initiale jusqu'au lundi suivant. → `dwManualAccess(log)` ignore les cooldowns, log un diagnostic détaillé par playlist (lisible/403), relance l'import si contenu lisible (cooldowns levés), `reloadFeed()` (nouvelle méthode store, re-query avec filtres actifs) puis ouvre la playlist dans un nouvel onglet. **Ouverture = TOUJOURS MA playlist, jamais une playlist commune (7.6.50)** : `dwEditorialUrl(candidates)` ne fait **PLUS AUCUNE RECHERCHE**. ⚠ Cause du bug « ça ouvre la mauvaise playlist / une playlist commune » : « Découvertes de la semaine » est **PERSONNALISÉE (une par compte)** alors que `GET /v1/search?type=playlist` est **GLOBAL** — il renvoyait donc la fiche de quelqu'un d'autre. Aucun endpoint public ne permet de retrouver la DW personnalisée d'un compte : il faut partir de quelque chose que CE compte a touché. Trois pistes, dans l'ordre : (1) **`DW_OPEN_KEY`** (`spotifyplus_dw_open_id`) = l'id de la playlist DW **réellement écoutée**, posé par `dwRememberOpenId(pid)` depuis `dwCaptureFromContext` (le contexte du player prouve que c'est la sienne) → `open.spotify.com/playlist/<id>` ; (2) la DW **owner 'spotify' présente dans SA bibliothèque** (`/me/playlists` : Spotify y place l'exemplaire personnalisé) ; (3) sa **copie perso** (figée mais sienne). Sinon `null` → page de recherche Spotify + log expliquant qu'il suffit de LANCER la playlist une fois pour que l'id soit mémorisé. **⚠ 7.6.58 — URL FIXE EN DUR (demande utilisateur)** : l'utilisateur a fourni le lien EXACT de SA « Découvertes de la semaine » → constante module-level **`DW_FIXED_URL = 'https://open.spotify.com/playlist/37i9dQZEVXcVrEUmwjvgjE'`**, renvoyée **en tout premier** par `dwEditorialUrl` (avant les 3 pistes, qui restent en repli si la constante est vidée). Conséquence : le bouton ouvre TOUJOURS ce lien, quel que soit l'état de la bibliothèque/du cache ; la branche « aucune candidate » de `dwManualAccess` ne renvoie donc plus jamais la page de recherche Spotify (log adapté : « lien fixe configuré »). Le diagnostic + l'import des titres lisibles sont inchangés. Changer de playlist = changer cette seule constante. **⚠ 7.6.59 — la playlist fixe est aussi la SOURCE de l'import hebdo** (demande utilisateur « requête cette playlist dès ma 1re connexion de la semaine ») : `DW_FIXED_URL` est remontée en tête du bloc Découvertes (avant `importDiscoverWeekly`, qui l'utilise) et `dwFixedPlaylistId()` en extrait l'id (accepte URL, `spotify:playlist:<id>` ou id brut ; `null` si la constante est vide → replis inchangés). `importDiscoverWeekly` gagne une **étape 0** : `fetchPlaylistItems(fixedPid)` AVANT `DW_PID_KEY` et avant `dwFindCandidates()` → à la 1re connexion de la semaine (gate `DW_LS_KEY` inchangée), les titres partent dans « En attente » sans balayer `/me/playlists`. Les étapes 1 et 2 sautent l'id déjà tenté (`rememberedPid !== fixedPid`, `continue` sur `pl.id === fixedPid`) ; le message « playlist introuvable » devient conditionnel (`fixedPid` → renvoie vers la capture par la lecture). Même étape 0 dans `dwManualAccess` (lecture de la playlist configurée en tête du diagnostic : lisible → `DW_LS_KEY`/`DW_FAIL_KEY` effacées + `importDiscoverWeekly` + retour immédiat de l'URL, sinon on enchaîne sur le balayage). ⚠ **Non vérifiable côté agent** : Spotify peut refuser le CONTENU d'une playlist ÉDITORIALE (403 → `items` vide, migration 2026) ; dans ce cas l'étape 0 ne ramène rien et la capture par la lecture (`dwCaptureFromContext`) reste le vrai filet. **⚠ 7.6.60 — les 3 garde-fous en tête d'`importDiscoverWeekly` LOGGENT désormais** (« rien dans les Logs à la connexion » = symptôme constaté) : `lastOk === thisWeek` → « déjà importées cette semaine (YYYY-Www) » ; cooldown `DW_FAIL_KEY` → « nouvelle tentative dans X h/min » (reste calculé sur `DW_FAIL_AGE_MS`) ; `_rlUntil` → « fenêtre rate-limit 429 ». Ils sortaient tous les trois AVANT le premier `addLog`, donc une connexion pouvait ne produire AUCUNE ligne — impossible de distinguer « déjà fait » d'un import qui n'a jamais tourné. Rappel : `dwManualAccess` (bouton violet) ignore ces trois garde-fous, c'est l'outil de diagnostic. ⚠ `dwCaptureFromContext` mémorise l'id **avant** la lecture de la file et même quand le scope file est refusé (`_dwCapDenied` déplacé après `dwRememberOpenId`). Store expose aussi `log`.

---

## Scraping (`startSync`)

### État unifié `syncState` : `'idle' | 'running' | 'paused' | 'rl_waiting'`
`setSync(st)` pose synchroniquement `syncStateRef.current` puis le state. Booléens `scraping`/`paused`/`rlWaiting` **dérivés**.

### Refus de démarrage (en tête, AVANT toute requête)
1. `syncStateRef.current !== 'idle'` (guard par ref, anti double-clic)
2. `Date.now() < _rlUntil` (fenêtre 429 persistée)
3. Quota 24 h : `loadQuota().count >= 100` ou fenêtre `until` active

### Arrêts — `endSync(reason)` + `checkpoint()`
`endSync('completed'|'daily_limit'|'error')` : reset, `setSync('idle')`, `saveDB()` final. `'completed'` nettoie `spotifyplus_sync_progress` ; autres raisons relisent le localStorage → bouton « ↩ Reprendre » immédiat.

`checkpoint()` attendu avant **chaque** appel (via `apiGetSafe`). UNE boucle d'attente pour la pause manuelle ET la fenêtre 429 (`_rlUntil`) — c'est lui qui pose `rl_waiting` + countdown et repasse `running` à l'expiration. Aucune requête pendant une pause.

### Rate-limit 429 (`apiGetSafe`, local à `startSync`)
`checkpoint()` en tête. Sur `RATE_LIMIT` : **stop dès le 1er 429** → `_rlSet(max(Retry-After, 15 min))` (fenêtre persistée partagée), `setBlockedUntil`, throw → `endSync('error')`. Body d'erreur HTTP (403, 5xx, 401 non rattrapé) → throw (ne termine plus en silence). Les 4 appels passent par `apiGetSafe` : `/me`, page artistes, albums, tracks.

### Fenêtre rate-limit persistée (`_rlUntil` = `spotifyplus_blocked_until`)
Source de vérité unique, relue au chargement du module → survit au F5, s'applique à TOUS les appels (player compris). `_rlNotify → setBlockedUntil` (bandeau + Lancer désactivé). Boot pendant fenêtre : `/me`→`rate_limited` → mode **connecté dégradé** (`user=null`) au lieu d'éjecter. `_parseRetryAfterMs` blinde le header contre NaN.

### Boucle artiste
- `cutoff = (scrapedDates[id] || '2026-03-15').slice(0,10)` (tronqué au jour)
- `/artists/{id}/albums?include_groups=album,single&limit=10&market=FR` — **⚠ limit max = 10** depuis fév. 2026 (`limit=20` → 400). Ne jamais dépasser.
- Filtre `cutoff ≤ release_date ≤ aujourd'hui` via `releaseInRange` (gère la précision year/month/day)
- Chaque album → `/albums/{id}/tracks?limit=50`
- Dédoublonnage via `seenUris` + `knownUrisRef` (+ `purged_uris`) : skip album seulement si `uris.every` connu
- **Écriture « légère »** dès la liste d'albums lue : `INSERT…ON CONFLICT` métadonnées + `last_scan_status='partial'` + `last_scraped_at=cutoff` (jamais avancée) → l'artiste apparaît même si un 429 coupe ensuite. **UPSERT final** après artiste terminé : avance `last_scraped_at=datetime('now')`, `status='ok'`, cumule `total_tracks_added`/`scan_count`. Liste inaccessible → `UPDATE status='error'` (pas d'INSERT, date non avancée).
- Progression : `spotifyplus_sync_progress` (`{artists_scanned, total_artists, last_artist_name, page_url, page_offset}`) après chaque artiste

### Quota 100 artistes / fenêtre GLISSANTE 24 h
localStorage `spotifyplus_daily_scrapings` = `{count, until, last}`. Helpers `loadQuota()`/`saveQuota(count, until, last)`/`ensureQuotaWindow()` (auto-réparant : ouvre une fenêtre si `count≥100` sans `until` valide — corrige l'ancien format `{date, count}`). `loadQuota()` remet count à 0 si `until` dépassé **OU** `Date.now()-last ≥ 24h` (inactivité). `dailyCount` incrémenté **APRÈS** la requête albums (un throw ne brûle plus le quota). `until` gelé à l'instant du 100e. `quotaUntil` (state) → countdown à 2 endroits (`DateRangePanel`, `ScrapingStatusPanel`). Helpers `fmtRemaining(ms)`, `fmtQuotaWhen(until)`.

### Reprise
`resumableSession` (au login si progression existe) → bouton « ↩ Reprendre ». `resumeSync()` → `startSync({skipCount, resumeUrl: page_url, resumeOffset: page_offset})` : repart directement sur la bonne page (plus de re-pagination burst). Compat ancienne progression sans `page_url`.

### Optimisations
Feed batché par artiste (`artistFeedItems` → 1 `setFeed`). Refresh Artistes throttlé 1×/1,5 s (`refreshArtists`). Horloge partagée `useSharedTick`/`_clock` (1 seul `setInterval` pour tous les countdowns). **Interdits** : précompiler le JSX (casse le mono-fichier), debounce de `saveDB` (perte de données mobile).

---

## Écoute / auto-avance

Marquage écouté quand `now?.uri` change (URI précédente dans le feed & `dbReady`) : `UPDATE tracks listened=1, listened_at` + `UPDATE stats` + `saveDB` + `recordListen()` + animation slide-right (600 ms). `listenedUrisRef` anti-doublon. Écritures DB **hors** updater `setFeed` (item lu via `feedRef.current`).

**Clic « Suivant » (`markListened(uri)`)** : les 3 players + `navigateFeed(+1)` capturent l'URI quittée AVANT de jouer le suivant, puis `markListened(leaving)` — retrait immédiat, corrige le double-clic rapide où l'URI intermédiaire échappe au poll 5 s. Idempotent. **Repli de position (7.6.7)** : `navigateFeed` mémorise la dernière position connue dans `filteredFeed` (`currentFeedIdxRef`, mis à jour à chaque nav + dans l'effet `now?.uri` quand le titre est dans la liste). Quand plusieurs filtres EXCLUENT le titre en cours de la liste, `filteredFeedIndex.get` renvoyait -1 → « suivant » repartait à l'index 0 (début de liste). Désormais `baseIdx` = position réelle sinon repli → la nav continue là où on était. ⚠ `markListened` n'utilise QUE la position réelle (`realIdx`), jamais le repli (sinon on marquerait « écouté » le mauvais titre).

**3 mécanismes d'auto-avance** (triple couverture, `advancedForRef` anti-doublon, `prevNowRef` = tick précédent) :
1. Effet `now?.uri` : URI null/hors feed **ET** titre quitté proche de la fin (`nearEnd`, garde anti-vol de lecture)
2. Effet `now?.current` : `remaining ≤ 3 s` & titre dans le feed
3. Effet `now?.playing` : transition `true→false` & `remaining ≤ 8 s` (cas principal : fin naturelle)

⚠ Auto-avance placée AVANT le `return` sur `listenedUrisRef`. ⚠ Limitation Spotify : fin de titre = `200 + is_playing:false` même URI → ni l'effet URI ni current ne se déclenchent (d'où l'effet 3).

**Poll player** : tick 5 s, bail-out si `now` inchangé. Onglet caché → aucun appel (tick de rattrapage au retour). Fenêtre 429 → garde l'état affiché (pas de `null`).

**Curseur optimiste `_effUri`** : module-level `_navUri`/`_navAt` posés par `playTrack()` ; `_effUri(pollUri)` renvoie `_navUri` tant que `< 6500 ms` (poll périmé jusqu'à 5 s). Les 3 players + `navigateFeed` calculent `currentIndex` via `_effUri(now?.uri)`.

---

## Stats / Purge

`stats` : `total_listened`, `listened_this_month/year`, `total_listened_ms`, `total_liked` (likes posés **dans l'app** via `setTrackLiked`, jamais par `syncInitialLikes` ; % likés = `total_liked/total_listened`, persistant). Reset mois/année au démarrage via `last_reset_*` (mois **LOCAL**, pas UTC).

**Purge** (`purgeListened`) : URIs → `purged_uris` (INSERT OR IGNORE), puis `DELETE FROM tracks WHERE listened=1` (likés compris). `stats` non touchée (% likés préservé). Journal `spotifyplus_listens` (voir Graphique) survit à la purge.

`removeFromFeed` (croix rouge × / swipe gauche) : traite le titre **comme écouté** (`listened=1` + `listened_at` + incrément stats + `recordListen`), plus aucun `DELETE` → apparaît dans l'Historique. Idempotent. **Undo** : snapshot dans `undoToast`, toast « ↩ Annuler » 5 s (`UndoToast`), `undoRemove()` remet `listened=0` + ré-décrémente si `counted` + `unrecordListen()` + réinsère à la position d'origine.

---

## Store (api du `StoreProvider`)

État : `authState` · `user` (null en mode dégradé 429) · `otherTab` · `dbReady` · `syncState` (+ dérivés `scraping`/`paused`/`rlWaiting`) · `blockedUntil` · `feed` · `logs` · `now` · `stats` · `listenStats` · `likedTracks` · `history` · `artists` · `loopEnabled` (localStorage) · `moduleToggles` · `delayChoice` · `dailyScrapings` · `quotaUntil` · `filteredFeed` (+ `filteredFeedIndex` Map URI→index) · `filterType` · `sortBy` (défaut `'artist'`) · `artistSearch` · `genreFilter` · `resumableSession`.

⚠ Changement de `filterType`/`sortBy`/`artistSearch`/`genreFilter` → effet debouncé 150 ms qui re-query `loadFeedFromDB` (filtre+tri EN SQL, balaie toute la base). `lastQueryRef` skippe le 1er run + les re-renders sans changement.

Méthodes : `startSync({skipCount, resumeUrl, resumeOffset})` · `resumeSync()` · `togglePause()` · `purgeListened()` · `restoreBackup(data)` · `resetQuota()` · `removeFromFeed(uri)` · `setTrackLiked(uri, bool)` · `syncInitialLikes()` · `navigateFeed(dir)` · `markListened(uri)` · `resetFilters()` · `logout()` · `seek(ms)` · `setLoopEnabled(bool)` · `setModuleEnabled(id, bool)` · `setDelayChoice(n)`.

### Paramétrage des modules
`TOGGLE_MODULES` = modules basculables `{id, label, icon, color, desktop, mobile}` (Actu/Météo/Finance/Frigo/Jeux/Bon Plan/TV Time/Musique/Note/Santé/Sport/Vêtement/To do/Maps/Mot de passe/Remember/Stats ; Scrapping & À propos non basculables). `loadModuleToggles()`/`saveModuleToggles()` (localStorage `spotifyplus_modules`), `moduleEnabled(toggles, id)` (absent = activé, seul `false` désactive). UI = toggles dans « ⚙️ PARAMÉTRAGE » (À propos). Onglet actif désactivé → retour à `home`. Inclus dans buildBackup. **Visibilité accueil (7.2.9, généralisée 7.4.2)** : store `homeModules`/`setModuleOnHome(id, on)` (localStorage `spotifyplus_home_modules`, `loadHomeModules`/`saveHomeModules`/`moduleOnHome(toggles, id)` = affiché par défaut, seul `false` masque) → petit bouton 🏠 dans « ⚙️ PARAMÉTRAGE » (à droite du toggle d'activation) : coloré = affiché sur l'accueil, grisé = masqué, inactif si module désactivé. **Impacte DÉSORMAIS desktop ET mobile** : `HubHome` mobile dérive lui aussi de `TOGGLE_MODULES` filtré par `moduleEnabled`+`moduleOnHome` (Musique 1er puis alpha, plus de `HUB_CARDS`/`MOBILE_ORDER` — `HUB_CARDS` conservé mais inutilisé) → par défaut TOUS les modules sur l'accueil mobile, « À propos » ajouté à part comme **barre allongée pleine largeur** (non basculable, `mobileKey:'stats'`). **INCLUS dans buildBackup** (clé `home_modules`, REMPLACÉE au restore, section d'export « Paramétrage ») + rechargé sans reload par `restoreBackup` (`setHomeModules(loadHomeModules())`).

---

## Sauvegarde / Restauration

`buildBackup()` complet = `{ stats, artists, music (table tracks), todos, todos_done, rappels, rappels_time, remembers, tvtime (+ YouTube), tmdb_key, yt_key, maps, finance (historique prix), food (profil + totaux kcal/jour + **`weights`** = journal des pesées, PAS la composition des repas), bonplan (jeux suivis + prix cibles), mdp_vault (chiffré), notes (notes — les chiffrées restent chiffrées), modules, home_modules (visibilité accueil 🏠), listens, `**`frigo`**` (contenu + péremption), `**`vetement`**` (inventaire + fiches SANS photos), `**`emploi`**` (salaire+secteur), `**`sport`**` (exos cochés), `**`gemini_key/github_token/chess_user/speedrun_user`**` (chaînes brutes), `**`lol`**` (`{key,cfg}`), `**`games`**` (`{hi:{<id>:score}, last, gh_level, osu_level}`), `**`prefs`**` (`{tvtime_filter, tvtime_sort, loop}`) }` (élargi en **7.6.2**). Helper `lsRaw(k)` (lecture brute) dans buildBackup ; `putRaw(k,v)` (écriture brute non-JSON) dans applyBackup. Restore : frigo/emploi/sport REMPLACÉS ; vetement REMPLACÉ mais **préserve la photo locale d'une fiche de même id** (photos jamais dans l'export) ; clés/pseudos REMPLACÉS si non vides (comme tmdb_key) ; `games.hi` FUSIONNÉ au MAX ; prefs remplacées. Nouvelles sections d'export partiel : Frigo, Vêtement, Emploi, Sport, « Comptes & clés » ; games ajouté à Stats, prefs à Paramétrage. **Caches re-fetchables toujours exclus** : actu, indices, dependabot/github_stats, speedrun/lol/chess (STATS), yt_subs, actifs, tv eps. Pesées : `food.weights` FUSIONNÉ par `weightMerge` (le local gagne à id égal, doublons jour+poids ignorés) → un ré-import ne duplique jamais l'historique. **Exclu volontairement** : composition des repas Nourriture (`spotifyplus_food_meals`), **photos Vêtement + images TierList** (data URLs lourdes).

`restoreBackup(data)` (complet OU partiel, clés absentes ignorées) : artists (dates jamais régressées, cumuls au MAX), music (listened/liked au MAX), stats (fusion cumuls), todos/rappels/remembers/maps REMPLACÉS, tvtime FUSIONNÉ (union `type+id`, progression la plus avancée), finance FUSIONNÉ (union série, densité 30 min, cap), food FUSIONNÉ (profil remplacé ; jours : le local gagne, jours absents complétés), listens FUSIONNÉ (union, cap 5000), notes FUSIONNÉES (par id, `updatedAt` le plus récent gagne), tmdb_key/yt_key REMPLACÉES si non vides, mdp_vault REMPLACÉ (backup dans `spotifyplus_mdp_vault_prev`).

**Dropbox** (`DROPBOX_APP_KEY` renseignée, PKCE offline) et **Google Drive** (`GDRIVE_CLIENT_ID`, token ~1 h sans refresh) : sauvegardent/restaurent le backup complet (overwrite). ⚠ **7.6.36 — Google a COUPÉ le flux implicite par redirection** (`response_type=token` → page de consentement « **Erreur 400 : invalid_request** », requête pourtant conforme : client_id/redirect_uri/scopes corrects). Remplacé par **Google Identity Services, modèle « token »** : `loadGis()` charge LAZY `https://accounts.google.com/gsi/client` (script NON versionné → **pas de SRI possible**, ajouté à `script-src`/`connect-src`/`frame-src` de la CSP), **préchargé au boot** (`if (gdriveConfigured()) loadGis()`) pour que `gdriveRequestToken()` → `initTokenClient(…).requestAccessToken()` parte SYNCHRONE d'un clic (sinon **popup bloquée**). `gdriveLogin(opts)` (branchable en `onClick`, `opts.scopes` pour un consentement incrémental) : succès → `gdriveStoreToken` + **`window.location.reload()`** (les panneaux lisent `gdriveConnected()` dans leurs initialisateurs de state, comme au retour de l'ancienne redirection) ; `popup_closed` → rien ; `popup_failed_to_open` → **message explicite** (plus de repli, cf. 7.6.48) ; **autre erreur avec le scope RESTREINT** → **2e essai automatique sur le `fallback`** passé à `gdriveGisFlow(wanted, store, fallback)`, ici `GDRIVE_SCOPE_BASE` (`drive.file` seul). **Scopes séparés** : `GDRIVE_SCOPE_BASE` (`drive.file` = sauvegarde, photos Vêtement/Revente) + `GDRIVE_SCOPE_READ` (`drive.readonly`, **restreint**, seulement Finance → Mes Actifs) ; `GDRIVE_SCOPES` = les deux. **⚠ 7.6.67 — LE SCOPE YOUTUBE A SON PROPRE CONSENTEMENT ET SON PROPRE JETON** (« ça marchait avant » : restriction NOUVELLE côté Google, rien n'a changé côté client OAuth) : demander `auth/youtube` avec `auth/drive.file` dans la MÊME requête renvoie désormais « **Accès bloqué : erreur d'autorisation — This request contains scopes that cannot be requested together: [.../auth/youtube, .../auth/drive.file]** » (Erreur 400 `invalid_request`), APRÈS le choix du compte — le consentement incrémental ne sauve rien (`include_granted_scopes` refait une requête mixte → même erreur). → `GDRIVE_SCOPE_YT` est **retiré de `GDRIVE_SCOPE_BASE`** et vit dans un jeton distinct : clés **`ytg_access`/`ytg_expiry`/`ytg_scopes`** (+ `ytg_declined`), helpers **`ytgStoreToken`/`ytgToken`/`ytgConnected`/`ytgLogin`/`ytgAutoLogin`/`ytgLogout`** calqués sur les `gdrive*`. `gdriveRequestToken(scopes, silent, store)` et `gdriveGisFlow(wanted, store, fallback)` prennent la **destination du jeton en paramètre** (défaut `gdriveStoreToken`) — c'est tout ce qui change côté flux GIS. **⚠ 7.6.82 — LA SÉPARATION DES JETONS NE SUFFISAIT PAS, le 400 persistait** : `initTokenClient` met **`include_granted_scopes` à TRUE par défaut** → Google **recolle dans la requête tous les scopes déjà accordés au `client_id`**. `ytgLogin` ne demandait que `auth/youtube` mais la requête réellement envoyée était `scope=…/auth/youtube …/auth/drive.readonly …/auth/drive.file & include_granted_scopes=true` (lisible dans le détail de la page d'erreur Google fournie par l'utilisateur) → combinaison interdite → « Erreur 400 : invalid_request » **APRÈS** le choix du compte, l'écran de consentement YouTube s'étant affiché normalement avant (d'où le diagnostic trompeur « c'est la PWA / la config Cloud »). → **`include_granted_scopes: false`** passé à `initTokenClient` dans `gdriveRequestToken`, donc pour **TOUS** les appels et pas seulement YouTube : le bug est **symétrique** (une fois `auth/youtube` accordé, une demande Drive y aurait ramené `youtube` et cassé la sauvegarde à son tour). Conséquence : chaque jeton ne porte plus QUE les scopes explicitement demandés — sans effet sur le reste, `gdriveLogin` demandant déjà `GDRIVE_SCOPES` (base+read) en un bloc et `gdriveHasScope` lisant `r.scope` réellement renvoyé. ⚠ Non vérifiable côté agent (le flux OAuth réel n'est pas testable ici) : la config passée à GIS a été vérifiée par test unitaire contre un faux `google.accounts.oauth2` (scope demandé = youtube seul, `include_granted_scopes === false`). ⚠ `_gdriveLoginRedirectLegacy` garde son `include_granted_scopes:'true'` — code MORT conservé pour documenter la requête rejetée, ne jamais le rebrancher tel quel. **Compat** : `ytgToken()` retombe sur le jeton Drive si celui-ci porte encore `auth/youtube` (`gdriveHasScope`), donc les sessions ouvertes avant la mise à jour continuent jusqu'à expiration (~1 h). `ytSubApi`/`ytSubPost` utilisent `ytgToken()` et `ytgLogout()` sur 401 ; `ActuYoutubeSection` dérive `connected` de `ytgConnected()`, ses 2 boutons appellent `ytgLogin` et un encart explique que l'autorisation YouTube est **séparée** de celle de Drive ; `ActuPanel` appelle `ytgAutoLogin()` (et non plus `gdriveAutoLogin()`). Conséquence utilisateur : **deux consentements à accorder une fois** (Drive dans À propos, YouTube dans Actu). **⚠ 7.6.71 — l'échec du consentement YOUTUBE a son propre message** (« c'est YouTube qui déconne, pas Google Drive ») : `gdriveGisFlow(wanted, store, fallback, onFail)` prend un 4e paramètre (défaut `gdriveLoginFailed`) et `ytgLogin` passe **`ytgLoginFailed(type)`** — jusque-là toute panne du consentement YouTube affichait « Connexion à **Google Drive** impossible », ce qui envoyait le diagnostic sur la mauvaise autorisation. `ytgLoginFailed` reste silencieux sur `popup_closed`, explique le cas **PWA installée** (jeton renvoyé vers `storagerelay://` → page Google « Erreur 400 » À LA FIN du consentement → se connecter depuis un onglet Chrome NORMAL, même origine ⇒ même `localStorage`) et, hors PWA, renvoie vers les pop-up + la vérification côté Google Cloud (scope `auth/youtube` déclaré sur l'écran de consentement, **YouTube Data API v3** activée). Le même mode d'emploi est affiché **dans l'UI** (encart 🛟 sous l'avertissement « autorisation séparée » de `ActuYoutubeSection`, visible avant même de cliquer). ⚠ Non vérifiable côté agent : le flux OAuth réel n'est pas testable ici (message et rendu testés en isolation dans le navigateur). Scopes accordés mémorisés (`gdrive_scopes`, posé par GIS *et* par le fragment du repli) → `gdriveHasScope(s)` (**absent = tout accordé**, compat tokens pré-7.6.36) ; `MesActifsPanel` dérive `canRead` → bandeau + bouton « Autoriser la lecture du Drive » au lieu d'un 403 garanti. ⚠ **7.6.48 — PLUS AUCUN REPLI PAR REDIRECTION** : le flux implicite servait encore de secours quand la popup GIS ne s'ouvrait pas, ce qui **éjectait l'utilisateur de l'app** vers la page Google « Erreur 400 : invalid_request » (rejet APRÈS le choix du compte — l'écran de connexion, lui, s'affiche normalement : l'origine JS `https://pierre-portfolio.github.io` et l'URI de redirection SONT bien déclarées côté Google, vérifié en direct). `gdriveLoginRedirect` devient `_gdriveLoginRedirectLegacy` (**plus jamais appelée**, conservée pour documenter la requête rejetée) et l'échec passe par **`gdriveLoginFailed(type)`** : `popup_closed` → silence, sinon `alert()` — « autorise les pop-up » en navigateur, message dédié si **`gdrivePwaMode()`** (`display-mode: standalone` ou `navigator.standalone`). ⚠ **C'est la PWA INSTALLÉE qui casse** : Google y renvoie le jeton vers un `storagerelay://…` que le Custom Tab Android ne sait pas résoudre → 400 juste après le choix du compte. **Aucun contournement côté page n'existe** (code+PKCE exigerait un `client_secret`, donc un backend) → la parade est de se connecter **une fois depuis un onglet Chrome normal** : même origine ⇒ **même `localStorage`** ⇒ la PWA relit le jeton. `gdriveHandleRedirect()` traite toujours un retour `#access_token=` (anti-fixation : préfixe `gdrive.` + nonce `gdrive_state`), retour OAuth distingué par `state` (`dropbox`/`gdrive.<nonce>`) ; marqueur `spotifyplus_oauth_return` rouvre l'onglet d'origine. **Reconnexion AUTO** : `gdriveAutoLogin()` (mount de `VosEcoutesPanel`/`VetementPanel`/`MesActifsPanel` ; `ActuPanel` appelle `ytgAutoLogin()` depuis 7.6.67) ne redirige PLUS (ça partait sans geste utilisateur → droit sur l'erreur 400) : il tente un **renouvellement SILENCIEUX** GIS (`prompt:''`) qui n'aboutit que si le consentement est déjà accordé, puis reload ; échec = silence (garde-fous inchangés : `_gdriveAutoAsked` 1×/chargement, cooldown 24 h `gdrive_declined`). **Garde layout mobile** : Chrome Android peut cocher « Version pour ordinateur » au retour OAuth (viewport ~980px) → `useIsMobile` reste mobile si (tactile ET min(screen.w,h) < 820) OU indice `spotifyplus_layout_hint` (posé avant chaque redirect OAuth quand innerWidth < 820, valable 15 min). Proposition de sauvegarde hebdo (seul l'export complet « Tous » pose `spotifyplus_last_backup`).

**pCloud** (3e destination, **7.6.35**) : OAuth 2.0 **implicite** (le flux `code` de pCloud exige un client_secret → impossible dans du code public), jeton **sans expiration** (révocable côté compte). Client ID lu par `pcloudClientId()` = constante `PCLOUD_CLIENT_ID_CONST` **ou** localStorage `spotifyplus_pcloud_client_id` (`PCLOUD_CLIENT_LS`, éditable dans le module **API** via `API_KEY_LS.pcloud` **et** en saisie inline dans la carte « non configurée » de `PCloudSync` → configurable depuis le téléphone sans commit). `pcloudLogin()` (state `pcloud.<nonce>`, mêmes marqueurs `spotifyplus_oauth_return`/`layout_hint` que Dropbox) · `pcloudHandleRedirect()` appelé à côté de `gdriveHandleRedirect()` dans l'init : lit le jeton dans le **fragment OU la query** (les deux formes existent selon les versions de pCloud), vérifie le nonce (anti-fixation) et nettoie l'URL. ⚠ **2 régions** : le retour donne `hostname` et/ou `locationid` (1=US `api.pcloud.com`, 2=EU `eapi.pcloud.com`) → mémorisé dans `pcloud_host` (interroger la mauvaise région = « invalid access token »). ⚠ **L'API pCloud répond TOUJOURS en HTTP 200** avec un champ `result` ≠ 0 → `pcloudApi()` teste `result` (pas seulement `res.ok`), `pcloudErr(code)` traduit (2009 = pas de sauvegarde, 1000/2000/2094/2095 = session expirée → déconnexion auto de l'UI). `pcloudUpload` = `POST /uploadfile` en `FormData` (écrase le fichier de même nom) · `pcloudDownload` = `getfilelink` puis fetch de l'hôte de contenu éphémère renvoyé (`c###.pcloud.com`) → **joker `https://*.pcloud.com` dans la CSP**.

**MEGA** (4e destination, **7.6.35**) : ⚠ **MEGA n'a PAS d'OAuth** — connexion **email + mot de passe**, toute la crypto (PBKDF2, master key, chiffrement du fichier) faite dans le navigateur par **megajs vendorisée** (`loadMega()`). `megaSetCred(email, pw, remember)` / `megaCred()` / `megaLogout()` : les identifiants ne sont écrits en localStorage (`spotifyplus_mega_cred`) **QUE si la case « mémoriser sur cet appareil » est cochée** (décochée par défaut — une session MEGA ouvre TOUT le compte, contrairement au jeton `drive.file` limité) ; sinon ils vivent en mémoire jusqu'au rechargement. `megaStorage()` met la session en cache module-level (`_megaStoragePromise`) — sinon chaque appel referait un PBKDF2 100k — avec `keepalive:false` (pas de long-poll `sc?sn=` permanent) et `userAgent:null` (le navigateur interdit d'écrire cet en-tête ; megajs le détecte aussi via `getShouldAvoidUA`). `megaUpload` écrit à la racine puis **supprime les versions précédentes SEULEMENT après un upload réussi** (MEGA autorise les doublons de nom ; une coupure ne doit jamais laisser le compte sans sauvegarde) · `megaDownload` prend le fichier le plus récent (`timestamp`). `megaErrMsg(e)` traduit les codes MEGA (-9 email inconnu, -15 session expirée, -26 2FA, réseau…). **2FA non gérée** (megajs accepte `secondFactorCode`, non exposé). CSP : `https://g.api.mega.co.nz` + jokers `https://*.mega.co.nz` / `https://*.mega.nz` (les serveurs de fichiers sont des hôtes dynamiques `gfs*.userstorage.mega.co.nz`). ⚠ **MEGA est INJOIGNABLE depuis l'environnement d'agent** (proxy réseau) : chargement de la lib, messages d'erreur et non-persistance des identifiants vérifiés dans le navigateur, mais **le cycle réel login/upload/download n'a PAS pu être testé** — à valider avec un vrai compte.

Les 4 destinations partagent l'UI (`DropboxSync`/`GoogleDriveSync`/`PCloudSync`/`MegaSync`, prop `mode='save'|'restore'`, vert = sauvegarder / bleu = restaurer) et sont rendues dans `VosEcoutesPanel` (saves groupés, puis restores groupés).

**Revente (7.6.45)** : clé `revente` (articles + annonces rédigées, **jamais les photos** — elles vivent dans Drive `HUB_Pierre/Revente` et se rattachent seules aux ids restaurés) + `claude_key` (brute, dans la section « Comptes & clés ») ; restore = **fusion par id, le local gagne**, section d'export dédiée.

Export : `EXPORT_SECTIONS` (boutons dans « ↧ EXPORTER » d'À propos) → `downloadBackup(sec)`, fichier `spotifyplus-<key>-<date>.json`.

---

## Modules (composants principaux)

**Layout** : `WebApp` (desktop, sidebar gauche scraping + onglets en haut, une seule vue centrale) · `MobileApp` (barre haut : profil + 3 onglets principaux Scrapping/En attente/❤Likés + menu ⋯) · `CompactPlayer` (viewport court `innerHeight<500`) · `HubHome` (accueil connecté ; **desktop (7.2.9)** : grille **3 colonnes** construite depuis `TOGGLE_MODULES`, **Musique en 1er puis ordre alphabétique**, filtrée par `moduleEnabled` ET `moduleOnHome` (bouton 🏠 du Paramétrage), conteneur scrollable `overflow:auto` + `justify-content:safe center` ; **mobile** : inchangé, 2 colonnes depuis `HUB_CARDS`) · `Home` (login « Hub de Pierre »). Retour accueil = clic sur le profil. Défaut view/tab = `'home'`.

**Ordre unique desktop+mobile** (onglets/menu/accueil/login) : Actu, Alertes, API, Bon Plan, Finance, Frigo, Jeux, Maps, Météo, Mot de Passe, Musique, Note, Revente, Santé, Sport, To Do, TV Time, Vêtement, Stats, À propos (Scrapping en tête). ⚠ Le module « Santé » (ex-Nourriture) garde sa **position** (id/composant internes inchangés = `food`/`NourriturePanel`) — seul le libellé visible a changé, il n'est donc pas re-trié à la lettre S. **Arc-en-ciel** violet→rouge appliqué partout. ⚠ Les NOMS de constantes couleur sont historiques (`METEO_BLUE` est cyan-vert, `MDP_AMBER` vert-cyan…) — lire la valeur. ⚠ Tout nouvel onglet mobile DOIT avoir son bloc de rendu dans `MobileApp` (sinon page noire) en plus de l'entrée `allOverflowTabs`.

**API** (`APIPanel`, indigo-bleu `API_COLOR='#6a78f2'`, entre Alertes et Bon Plan — **7.6.11**) : liste 100 % locale de TOUTES les API réseau de l'appli (`API_LIST`, miroir de la CSP connect-src, groupées par `API_CATS` : Musique/TV·Vidéo/Météo&Géo/Finance/Actu/Emploi/Bon Plan·Santé/Jeux/Sauvegarde/IA/Proxies CORS ; chaque entrée `{id, name, host, role, cat, key}`, `key:true`=clé/OAuth perso → badge 🔑). **Un interrupteur par API** (gabarit `role="switch"` du Paramétrage) persisté dans `spotifyplus_api_export` (`loadApiExport`/`saveApiExport`, `apiExported(map,id)` = coché par défaut, seul `false` exclut). Bouton **⬇ Exporter (N)** = télécharge un JSON `{app, exported_at, count, apis:[…cochées]}` (`spotifyplus-api-<date>.json`), + **un SEUL bouton bascule** (`allOn` = tout coché ? → « Tout décocher », sinon « Tout cocher », 7.6.19). **LIGNES EN ROUGE quand la clé n'est pas renseignée (7.6.55)** : `apiMissingFields(a)` renvoie les libellés des champs vides → ligne teintée `API_RED='#e05050'` (fond `14`, `borderLeft` 3 px, nom + hôte en rouge) + sous-ligne « ⚠ à renseigner : … », et un compteur « N sans clé » à côté de « 47 API ». Deux sources : (1) les API à éditeur inline (`API_KEY_LS`, tableau accepté) → lecture directe du localStorage ; (2) `API_CRED_CHECK` = tests dédiés pour les identifiants qui vivent ailleurs (Spotify `spotify_token`/`spotify_refresh`, Dropbox `dropboxConnected()`, pCloud `pcloudClientId()`, MEGA `megaCred()`, Twelve Data / Alpha Vantage = clés gratuites EN DUR donc toujours OK). ⚠ Une API `key:true` **sans test connu n'est jamais signalée** (pas de faux rouge). `ApiKeyEditor` reçoit `onChanged` → `bumpKeys` (useReducer) pour que le rouge disparaisse dès l'enregistrement. **Édition inline des clés (7.6.20)** : sous les lignes dont la clé est saisie par l'utilisateur et stockée en localStorage (`API_KEY_LS` = TMDB `spotifyplus_tmdb_key`, YouTube `spotifyplus_yt_key` pour `googleapis`, GitHub `spotifyplus_github_token`, Gemini `spotifyplus_gemini_key`), un `ApiKeyEditor` affiche la clé courante et la rend modifiable (enregistre au blur/Entrée/💾 via `lsSet`, champ vidé = clé retirée). **Une valeur d'`API_KEY_LS` peut être un TABLEAU (7.6.30)** quand l'API demande 2 champs — `steam_api` : SteamID64 `spotifyplus_steam_id` + clé Web API `spotifyplus_steam_key` — le rendu fait `[].concat(cfg).map(…)`. **Entrées Steam (7.6.30)** : `steam_api` (api.steampowered.com, cat Jeux (stats), 🔑) et `steam_store` (store.steampowered.com, cat Bon Plan / Santé). Les OAuth (Spotify/Dropbox/Drive) et les clés gratuites en dur (Twelve Data/Alpha Vantage) n'ont PAS d'éditeur. Module normal (id `api` : `TOGGLE_MODULES`, `navItems`, `allOverflowTabs` + bloc `MobileApp`, `SECTION`, présent sur l'accueil via `TOGGLE_MODULES`). PAS dans buildBackup, PAS de chip `HUB_FEATURES`.

**Feed** : `FeedList` (filtres type/genre/artiste + tri, bannières de date mobile, empty-state seulement si `feed.length===0 && !filtersOn`) · `FeedItem` (`React.memo` + props explicites, **ne consomme PAS `useStore`** ; `key={item.id}` = URI, jamais `id+i`) · swipe gauche=suppr / droite=prev.

**Players** : `PlayerBar` (desktop) · `MobilePlayer` (25vh) · `SeekBar` (mouse+touch, listeners attachés dans `onMouseDown` pas useEffect). Bouton loop partagé `loopEnabled`. Like : `isLiked` init local, `libraryContains` seulement si titre inconnu.

**Musique** (`MusiquePanel`, violet) : fusion 3 collapses `LikerPanel` (❤ Likés) / `HistoryPanel` (🕘 Historique) / `ArtistsPanel` (🎤 Artistes suivis). Anciens onglets dédiés supprimés (valeurs héritées mappées sur `musique`).

**Stats** (`StatsPanel`) : collapses `StatsCollapse` imbriqués — 🎵 MUSIQUES (restantes + sous-collapses écoutées/temps/graphique) · ✅ TO DO · 🎬 FILMS (temps total + top 5 séries + nombre/temps/restant/terminées, YouTube fusionné — **vidéos longues ET Shorts YouTube comptés SÉPARÉMENT (7.6.16)** : `tvTimeStats` sépare `ytVideos`/`ytMin`/`ytRemainMin` (≥ `YT_MIN_MINUTES`) de `ytShorts`/`ytShortMin`/`ytShortRemainMin` (< 3 min) ; cartes « SHORTS YOUTUBE » / « TEMPS SHORTS » / « TEMPS RESTANT SHORTS » affichées sous les cartes vidéos quand `hasYtShorts`) · 🗺️ MAPS · **🩺 SANTÉ** (7.6.37, entre Maps et Alertes — un seul sous-collapse **⚖️ POIDS** = `WeightStatsSection`) · 🔔 REMEMBERS · 🎮 JEUX (Speedrun + LoL + chess.com + **🕹️ Steam** (7.6.31, sous chess.com) + **🕹️ Jeux Hub** en dernier) · 🐙 GITHUB. **🕹️ Jeux Hub** (`GamesHubStatsSection`, `GAMES_HUB_STATS`) : records LOCAUX des mini-jeux de la section Jeux (100% hors-ligne, zéro requête) — meilleur score 2048/Flappy/Osu/Guitar Hero/Piano Tiles/Snake via `gameHi('spotifyplus_game_hi_<id>')`, nb d'éléments classés TierList (`TIERLIST_LS`) ; carte `statCardEl` par jeu (« — » / « pas encore joué » si jamais joué), en-tête « N mini-jeux disponibles · M avec un record établi ». Les jeux sans record persistant (Bataille, Démineur, Go, Inposteur, Memory, Morpion, Motus, Puissance 4, UnderCoverDraw) ne sont pas listés. **⚖️ Poids** (`WeightStatsSection`) : lit `spotifyplus_food_weights` au montage (donc à l'ouverture du collapse), 100 % local — 4 cartes `statCardEl` (dernière pesée, évolution totale, sur 30 j via `weightRefBefore(series,30)`, nombre de pesées) puis **une courbe par mesure renseignée** (`WeightLineChart`, SVG maison, ≤ 60 derniers points, échelle uniforme pour garder les points ronds, min/max à gauche + dates aux extrémités, `<title>` par point) ; delta coloré par `weightDelta` (vert = va dans le bon sens selon `lower` du champ). Aucune pesée → message renvoyant vers Santé → ⚖️ Poids. `ListeningCharts`/`BarChart` (14 j, SVG maison) lit le **journal persistant** `spotifyplus_listens` (`recordListen`/`unrecordListen`, survit à la purge). `AdvancedStats` (top 5 artistes, répartition, moy/30 j — repart de zéro après purge). Helper `fmtMonthsDaysHours(ms)`.

**À propos** (`VosEcoutesPanel`) : ⚙️ Paramétrage · ↧ Exporter (repliable) · DropboxSync/GoogleDriveSync (save) · ↥ Restaurer · Dropbox/Drive (restore) · 🔄 Réinit quota 24 h · 🗑 Purger · ⎋ Déconnecter (rouge) · « Version X ».

**Actu** (`ActuPanel`, bleu-indigo) — collapses en **ordre ALPHABÉTIQUE** (titre, emoji ignoré) aux 2 niveaux : 📰 Actualité (**9 sous-collapses depuis 7.6.74** ; le **3e depuis 7.6.75** — déplacé de la dernière place à **ENTRE Actu Mondial et LinkedIn**, demande utilisateur, hors alphabet — est **📚 WIKIPÉDIA — dernières nouveautés** : `ActuWikiSection`, source **`fr.wikipedia.org/api/rest_v1/feed/featured/AAAA/MM/JJ`** (API Wikimedia, **sans clé, CORS ouvert**, origine DÉJÀ en connect-src pour les logos de fuites) appelée **en direct, sans proxy** → indépendante de `data/actu.json` et de la GitHub Action. Deux blocs : `news` (encadré « Actualités » de l'accueil) et `mostread` (articles les plus consultés, avec vignettes et nb de vues). ⚠ **Les deux ne sont jamais servis pour la même date** — `news` n'existe que certains jours et `mostread` **jamais pour le jour courant** (publié a posteriori) → `wikiFetchLatest` interroge **J, J-1, J-2** et garde le premier bloc non vide de CHACUN (sinon la section serait vide un jour sur deux) ; la date retenue est affichée dans chaque sous-titre. ⚠ `news[].story` est du **HTML Wikimedia** : jamais injecté (`dangerouslySetInnerHTML` proscrit) — `wikiStripHtml` en extrait le TEXTE via `DOMParser` (n'exécute ni script ni chargement). Cache localStorage `spotifyplus_actu_wiki` TTL 30 min (`WIKI_TTL`), bouton ↻ propre à la section, fetch au MONTAGE donc à la 1re ouverture du collapse. **PAS dans buildBackup** (cache re-fetchable) ; les autres sous-collapses restent : Tendances Google Trends EN TÊTE, Actu Mondial, 💼 LinkedIn (**7.6.56** : `actuFetchLinkedin` interroge Google News **restreint au domaine** — `site:linkedin.com` puis `site:linkedin.com/pulse OR site:linkedin.com/posts` — pour ramener des articles **PUBLIÉS SUR** LinkedIn ; même cascade dans `scripts/fetch_actu.py`. **⚠ 7.6.66 — « je veux pas ceux de Google News mais bien ceux de LinkedIn » (API LinkedIn ?)** : (a) les 2 requêtes de repli `q=LinkedIn` / `LinkedIn OR "réseau professionnel"…` — qui ramenaient de la **PRESSE PARLANT** de LinkedIn — sont **SUPPRIMÉES** (remplacées par `site:fr.linkedin.com`) ; (b) nouveau garde-fou **`actuIsLinkedin(a)`** (+ `actuLinkHost`, `ACTU_LI_HOST_RE=/(^|\.)linkedin\.com$/`, miroir Python `is_linkedin`/`LINKEDIN_HOST_RE`) : un item n'est gardé que si l'hôte de `sourceUrl` **ou** celui de `link` est linkedin.com, ou à défaut si le libellé de source commence par « LinkedIn » — appliqué **au fetcher ET au rendu** ; (c) le fetcher essaie les requêtes **une par une jusqu'à ce qu'il reste des items APRÈS filtrage** (`actuFetchGNews([q])` par requête, plus une seule cascade globale) → mieux vaut une section vide que du hors-sujet ; (d) les liens `news.google.com` des items LinkedIn sont remplacés par la **vraie URL linkedin.com** côté Action (`resolve_links`/`gn_real_url` + cache `links`, cf. `data/actu.json`) — le repli client, lui, garde le lien Google News (le décodage exige un POST vers un endpoint interne Google, sans CORS) ; (e) badge du collapse « · Google News » → **« · linkedin.com »** + note de bas de section expliquant la limite. **⚠ 7.6.63 — FRAÎCHEUR (« des sujets qui datent de ya 300 jours »)** : une recherche `site:` est classée par PERTINENCE par Google News, pas par date. Les 3 requêtes (client ET Action) sont donc bornées par l'opérateur **`when:30d`** et le résultat repasse par **`actuRecentFirst(items, ACTU_LI_MAX_DAYS=30)`** (helpers module-level `actuItemTime`/`actuRecentFirst` : jette les parutions > 30 j, trie **de la plus récente à la plus ancienne**, item sans date lisible gardé mais rejeté en fin de liste) — ceinture et bretelles, `when:` étant parfois ignoré sur les requêtes à opérateurs. ⚠ Le filtre linkedin.com ET le tri sont **refaits au RENDU** de la section (`d.linkedin`), pas seulement dans le fetcher : la source par défaut est `data/actu.json`, qui peut venir d'un run d'Action antérieur (donc contenir de la presse « au sujet de » LinkedIn et des liens GN) ; liste vidée par le filtre → message dédié « Aucune publication LinkedIn dans les 30 derniers jours » (≠ « proxy injoignable »). Miroir Python : `LINKEDIN_MAX_DAYS`/`item_time`/`recent_first` (parsedate_to_datetime + ISO). ⚠ **Aucune API LinkedIn n'est utilisable depuis une page statique, et lire le fil « Actus » de SON compte est IMPOSSIBLE** : pas d'API publique de lecture (API réservée aux partenaires validés, OAuth exigeant un `client_secret` donc un backend, aucun en-tête CORS), **flux RSS Pulse supprimé**, pages publiques en 999 pour les robots → Google News reste le seul ANNUAIRE, d'où ce montage « découverte via GN, contenu et liens 100 % linkedin.com ». ⚠ **Non vérifiable côté agent** : le proxy réseau bloque `linkedin.com` ET `news.google.com` (403 CONNECT) → filtres/parsing testés unitairement (Node + Python), pas contre les vraies réponses ; `data/actu.json` ne portera des liens linkedin.com qu'après le prochain run de l'Action (cron 2 h). **placé JUSTE SOUS Wikipédia** (donc toujours sous Actu Mondial, 7.6.75), demande utilisateur — hors alphabet), Bourse&crypto, IA&Science Google News + Hacker News, 🛡️ Cyber Secu (failles/ransomwares/cyberattaques Google News — **placé ENTRE IA et Insolite**, position demandée, prime sur l'alphabet), Insolite, Jeux) · 🔓 Dernières fuites de données (10, bonjourlafuite → repli HIBP ; **icône de l'entreprise piratée (6.4.3)** : composant `LeakIcon` — favicon deviné `leakThumb` affiché immédiatement, remplacé par la **vignette Wikipédia FR** (logo) trouvée via `leakWikiIcon(leakCompanyName(l))` (API MediaWiki `generator=search&prop=pageimages`, keyless CORS `origin=*`, `fr.wikipedia.org` en connect-src), cache localStorage `spotifyplus_leak_icons` (nom → URL, '' = sans résultat, purge > 120 entrées) + cache session `_leakIconMem` ; items HIBP avec `l.domain` gardent le favicon exact ; `row()` d'ActuPanel accepte désormais un élément React comme vignette ; **fond BLANC + padding sous l'icône (6.4.6)** — logos souvent transparents, illisibles sur fond sombre) · 🗺️ **Cartographie** (7.6.21, collapse conteneur placé AU-DESSUS des « Dernières fuites de données ») — sous-collapses d'iframes chargées LAZY (StatsCollapse ne monte ses enfants qu'à l'ouverture), chacun rendu par le composant réutilisable **`MapFrame`** (`{src, title, credit, color, image}`), **triés par ordre ALPHABÉTIQUE du titre (emoji ignoré) depuis 7.6.26** : **🌌 Aurores boréales** (`AURORA_MAP_URL` = services.swpc.noaa.gov image `aurora-forecast-northern-hemisphere.jpg`, oval auroral hémisphère nord — **7.6.29 : IMAGE NOAA rendue via `image` (spaceweatherlive.com refusait l'iframe)**, en tête alphabétique), **📊 Carte INSEE** (**7.6.51** : `INSEE_MAP_URL` = **`https://www.insee.fr/fr/outil-interactif/7737357/map.html`** — Cartographie INFRACOMMUNALE, lien EXPLICITEMENT demandé par l'utilisateur ; `www.insee.fr` ajouté à `frame-src`. ⚠ Ce domaine avait refusé l'iframe en 7.6.29 → la carte porte une **`note`** expliquant le repli « ↗ Nouvel onglet » + un lien vers `INSEE_MAP_ALT` (`statistiques-locales.insee.fr?…&iframe=1`, nativement intégrable, conservé comme secours)). **⚠ 7.6.62 — INVERSÉ (« carte insee bug »)** : le cadre restait vide/gris puisque www.insee.fr refuse bel et bien l'iframe → la carte EMBARQUÉE est désormais `INSEE_MAP_ALT` et le bouton « ↗ Nouvel onglet » pointe sur `INSEE_MAP_URL` (la cartographie infracommunale demandée) via la **nouvelle prop `href` de `MapFrame`** (cible externe ≠ source embarquée ; absente = comportement d'avant). `www.insee.fr` reste en `frame-src` (inoffensif, et documente la tentative)., **🌪️ Catastrophes naturelles** (`DISASTER_MAP_URL` = **earthquakes.volcanodiscovery.com**, widget iframe volcans & séismes en temps réel — **7.6.29 : gdacs.org refusait l'iframe**), **🐾 Espèces animales** (**7.6.40**, `EspeceMapSection` — **seule carte du collapse qui n'est PAS une iframe**) : mappemonde « où vit quelle espèce » = **Leaflet** (déjà vendorisé + `loadLeaflet()` lazy) avec, en surcouche des tuiles OSM, les **tuiles de densité d'occurrences GBIF** (Global Biodiversity Information Facility, ~3 Md d'observations, **API publique SANS CLÉ**) — comme ce sont des **PNG** (couverts par `img-src https:`), aucun risque de X-Frame-Options, contrairement aux iframes qui ont déjà cassé plusieurs cartes ici. Helpers module-level : `gbifGet(path, timeout)` (fetch + AbortController 12 s, `null` sur échec), `gbifVernacular(r)` (nom FR sinon EN, `vernacularNames` absent/hétérogène toléré), `gbifSearchAnimals(q)` → `[{key, name, vern, rank}]` (`/v1/species/search` bornée au **backbone** `GBIF_BACKBONE` + règne **Animalia** `GBIF_ANIMALIA=1`, `status=ACCEPTED` ; **filtrage des rangs CÔTÉ CLIENT** via `GBIF_RANKS_OK` — aucun paramètre multi-valué à deviner — dédoublonnage par clé, `nubKey || key` car l'API cartes veut une clé de backbone ; repli `/v1/species/suggest` ; **`null` = réseau KO**, `[]` = 0 résultat). Surcouche = `/v2/map/occurrence/density/{z}/{x}/{y}@1x.png?srs=EPSG:3857&style=purpleYellow.point&taxonKey=` (⚠ `{z}/{x}/{y}` = jokers Leaflet, pas de l'interpolation JS) remplacée à chaque changement d'espèce ; `taxonKey` accepte tout rang (espèce/genre/famille). Compteur d'observations via `/v1/occurrence/search?limit=0` → `count`. UI : champ de recherche (nom courant OU scientifique) + 6 chips d'accès rapide + liste de résultats cliquables + bandeau de l'espèce. **Plein écran maison** : contrairement à `MapFrame`, PAS de portal — le parent DOM ne doit PAS changer (Leaflet perdrait son conteneur) → seul le style du bloc bascule en `position:fixed inset:0`, puis `invalidateSize()` ; Échap ferme. `api.gbif.org` ajouté à **connect-src** et à `API_LIST` (cat. Météo & Géo). ⚠ **GBIF est INJOIGNABLE depuis l'environnement d'agent (proxy 403)** : parsing défensif, mais la forme réelle des réponses n'a pas pu être vérifiée en direct — à valider dans le navigateur. **⚔️ Guerre** (`WAR_MAP_URL`, world-on-fire.com), **🌫️ Qualité de l'air** (`AIR_MAP_URL`, waqi.info — **7.6.51 : recadrée sur PARIS** `#/c/48.8566/2.3522/10.5z`, le cadrage 47.148/2.225/6.7z tombait au milieu de la France), **💧 Qualité de l'eau** (`WATER_MAP_URL` = **infoeau.fr/carte**, qualité de l'eau potable par commune — **7.6.29 : dansmoneau.fr refusait l'iframe**. ⚠ **7.6.51 — le clic sur « Afficher la carte » NE PEUT PAS être automatisé** : la same-origin policy interdit tout script dans une iframe cross-origin (aucun contournement côté page). Une `note` le dit à l'utilisateur ; seul `allow-forms` (ajouté au sandbox) garantit que SON clic aboutit), **🚇 Réseaux de Transport Paris** (`TRANSPORT_MAP_URL`, carte statique IDF du ministère, URL millésimée à mettre à jour si 404), **🏥 Santé** (`SANTE_MAP_URL` = odisse.santepubliquefrance.fr, indicateurs de santé par région / ODISSé — 7.6.26). ⚠ Les origines iframe sont en `frame-src` de la CSP ; **la prop `image` de `MapFrame` (7.6.29)** rend un `<img>` (object-fit contain, fond noir) au lieu d'un `<iframe>` quand la source est une image statique (couverte par `img-src https:`) — utilisée par l'oval auroral NOAA. **⚠ Ces sites gouvernementaux/institutionnels (INSEE www, GDACS, dansmoneau, SpaceWeatherLive) refusent l'`<iframe>` (X-Frame-Options / CSP frame-ancestors) ; corrigés en pointant vers des sources nativement intégrables. Impossible à tester depuis l'environnement d'agent (proxy réseau bloque ces hôtes) — à vérifier dans le navigateur.** **7.6.51 — `MapFrame` durci** : (a) prop **`note`** = ligne d'explication sous la carte (fusionnée avec `credit`) ; (b) le lien « ↗ Nouvel onglet » devient un vrai **bouton** à côté de « ⛶ Plein écran » ; (c) le **`sandbox`** de l'iframe autorise désormais **`allow-forms`** + `allow-popups-to-escape-sandbox` — sans `allow-forms`, TOUT bouton de la carte embarquée déclenchant une soumission de formulaire était silencieusement bloqué par le navigateur (cause probable des « cartes qui ne font rien »). **Plein écran (7.6.22)** : `MapFrame` a un bouton **« ⛶ Plein écran »** → overlay `position:fixed inset:0` via `ReactDOM.createPortal(…, document.body)` (l'iframe couvre tout l'écran, scroll de la page bloqué) + petit bouton **« ← Retour »** en haut à gauche (touche **Échap** aussi) pour revenir ; le changement de parent DOM recharge l'iframe (acceptable pour une carte). Un lien discret **« ↗ Nouvel onglet »** reste en repli si un site refuse l'embarquement · ▶ Vidéos YouTube abonnements (`ActuYoutubeSection`, OAuth scope `youtube`, bouton 🕒 → playlist « Hub Pierre ». **Lecture en place / nouvel onglet (7.2.1)** : clic sur la miniature (overlay ▶) → lecture EMBED dans le site (iframe `youtube-nocookie.com/embed?autoplay=1`, state `playingId`, origine en `frame-src`) ; bouton **↗** à côté de 🕒 → ouvre la vidéo dans un nouvel onglet du navigateur ; le titre reste un lien nouvel onglet. **Onglets ▶️ Vidéo / 🩳 Short en tête** (2 boutons 40% de large + compteur ; les Shorts < `YT_MIN_MINUTES` sont **CONSERVÉS ici**, contrairement à TV Time — durées stockées en secondes `durSec` via `ytParseDurationSec`, `ytSubIsShort(v)` = durée connue < 180 s, cache versionné `v: YTSUB_CACHE_V = 2`, pré-v2 = périmé). **Lazyload infini par onglet** : sentinelle `IntersectionObserver` dont le **root = conteneur scrollable parent** (remonte jusqu'à un `overflowY:auto|scroll`, `rootMargin:250px`) — le contenu vit dans le `<div overflowY:auto>` de WebApp/MobileApp ; changer d'onglet remet à la 1re page). Priorité `data/actu.json`, repli proxies CORS. Vignettes via `actuThumb` (photo article ou favicon). Cache 30 min. **PAS dans buildBackup.**

**Météo** (`MeteoPanel`, bleu) : Light 3 j / Full 7 j (non persisté). Recherche ville (`geocodeCity`). 4 lieux (Voisins-le-Bretonneux, Massy, Boulogne-Billancourt, position actuelle via `reverseGeocode` chaîne BigDataCloud→Photon→Nominatim, fallback hors-ligne `nearestCityFR`/`FR_CITIES`). Open-Meteo (sans clé). Clic jour → `MeteoDetailModal` (détail horaire).

**Finance** (`FinancePanel`, cyan) : gestionnaire **module-level `_finance`** (survit au démontage, `subs:Set`). Chargement **lazy par mode** au mount (`startFinanceLight`/`startFinanceFull`, plus au login). 4 modes : Light / Full / **🧮 Calculatrice** (`FinanceCalcPanel`, entre Full et Mes Actifs : calculatrice de poche locale — accumulateur + op en attente `calcApply`/`calcFmt` (toPrecision(12) anti-bruit flottant), clavier physique supporté hors champs de saisie, historique 5 calculs en session, zéro réseau/persistance) / Mes Actifs. **Badge d'en-tête de section CRYPTO (7.6.10, revu 7.6.17)** : au lieu du badge « EUR/USD », affiche « X BTC » = ma position PEPE **convertie en Bitcoin** via `pepeBtc(cg)` (`PEPE_HOLDINGS * pepe.usd / bitcoin.usd`, en NOMBRE de BTC, plus en % — demande utilisateur) ; `PEPE_HOLDINGS = 556442765` est une **variable cachée** (aucune UI de saisie). Format : 6 décimales < 0,001, sinon 4 < 1, sinon 3. `FinanceSection` accepte une prop `badge` optionnelle (sinon EUR/USD). Ordre : Crypto (CoinGecko) → Indices (`data/indices.json` same-origin) → Stock picking (Twelve Data + repli Stooq via `corsFetchText` proxies) → Matières (Pétrole/Sucre via Alpha Vantage, Or/Argent gold-api) → Monnaie (EUR/USD Twelve Data → repli Frankfurter). File TD `tdSpaced` (`TD_GAP_MS=8000`, plan 8 req/min). Flag `TD_ENABLED`. **📈 Graphique au clic** (`FinanceChartModal`, SVG maison) : 4 sources (coingecko/frankfurter/stooq/alpha), cache session `_chartCache`, **repli historique local** `spotifyplus_finance_history` (`finHistSnapshot`, MAX 1 pt/jour, valeurs Light seulement, ~1 an). **Mes Actifs** (3e onglet, doré) : patrimoine depuis le Google Sheets « Finance » du Drive (export CSV 1re feuille via `actifsFetch`, scope `drive.readonly`, cache 6 h). **PAS dans buildBackup** (sauf finance history).

**Bon Plan** (`ComparePanel`, bleu-indigo) — **refonte v6.0.4**, 5 collapses **(⭐ Ma liste EN PREMIER, ordre imposé v6.1.8)** : ⭐ Ma liste → 🕹️ Bibliothèque Steam (7.6.30) → 🔥 Promos du moment → 🎮 Recherche jeux → 🔎 Tous produits. 🔥 **Promos du moment** (**replié par défaut — 6.4.7**, demande utilisateur : `cmpFetchDeals` = CheapShark `/deals?sortBy=DealRating&pageSize=30`, cache LS `spotifyplus_cmp_deals` 30 min, filtres client Tous/−50 %/−75 %/AAA (retail ≥ $29), **tri par % de réduction décroissant** (plus grosse promo en tête), vignettes ; **⚠ `/deals` nomme ses prix `salePrice`/`normalPrice`** — ≠ `/games` qui utilise `price`/`retailPrice` — helpers `dSale`/`dNormal` avec fallback anciens noms, lire `d.price` ici affichait « NaN € ») · ⭐ **Ma liste** (`CompareWatch` : jeux suivis via `CmpStar` partagé, LS **persistant** `spotifyplus_cmp_watch` (lsSet), refresh groupé `cmpRefreshWatch` 1 req `/games?ids=` max 1×/30 min au montage, **prix cible EUR** optionnel → ✓ vert sous la cible ; **clé backup `bonplan`**, fusion par gameID le local gagne) · 🎮 **Recherche jeux** (CheapShark, prix ≈ EUR via `cmpUsdToEur`, ⭐ sur chaque résultat ; **repli Steam si CheapShark ne trouve rien (7.2.7, fiabilisé 7.3.2)** : `cmpSteamSearch(q)` interroge l'API Steam `storesearch` (`store.steampowered.com/api/storesearch/?cc=FR`) **via les proxies CORS `corsFetchText`** (Steam sans en-tête CORS) → vignette + lien direct + **prix RÉEL en EUR**. ⚠ storesearch **n'inclut PAS toujours le prix** (souvent absent même pour un jeu payant → tout s'affichait « — » = « ne fonctionne pas ») : les prix manquants sont désormais complétés via **`cmpSteamPrice(appid)`** (`appdetails?filters=price_overview`, `price_overview.final` en centimes ; succès sans price_overview = **Gratuit**/0), en parallèle borné aux 8 premiers. `cmpSteamSearch` renvoie **`null`** si les proxies sont injoignables (UI « Steam injoignable ») vs `[]` (aucun jeu). Affiché sous « Aucun jeu trouvé sur CheapShark ». Répond au besoin « chercher le prix quand aucun résultat » ; **liens de recherche par boutique `CMP_SHOPS`/`CmpShopLinks`** — Steam/G2A/Instant Gaming/Eneba/Kinguin/Gamivo/CDKeys, simples liens de navigation car AUCUNE de ces boutiques n'a d'API keyless (Steam sans CORS, keyshops sans API ouverte) — affichés sous les offres d'un jeu déplié (titre exact) et sous « Aucun jeu trouvé » (requête brute)) · 🔎 **Tous produits** (**refonte v3 — 7.6.49 : PLUS AUCUN SCRAPPING**, demande utilisateur). Les versions précédentes (iTunes Search via proxies + les 3 bases « Open * Facts ») ne ramenaient que des catalogues DESCRIPTIFS, sans prix marchand ni promo. Désormais **une requête IA avec RECHERCHE WEB côté fournisseur** : `cmpAiSearch(provider, q)` → **Claude** (`claudeGenerate` + outil serveur `web_search_20250305`, `max_uses: CMP_AI_SEARCHES`=6) ou **Gemini** (`geminiGenerate(parts, {tools:[{google_search:{}}]})`), au choix (`CMP_AI_LS` = `spotifyplus_cmp_ai_provider`, défaut claude) avec **les clés déjà en place** (Revente `spotifyplus_claude_key` / Frigo `spotifyplus_gemini_key`). ⚠ `claudeGenerate` refuse de poser `output_config.format` ET `tools` ensemble (incompatibles) → avec outils on parse le JSON du texte via `geminiExtract`. **Repli** : si le fournisseur rejette l'outil de recherche, un 2e appel part SANS outil et l'UI affiche « réponse non ancrée, prix possiblement périmés » (`grounded:false`). `cmpAiPrompt(q)` = la requête EXACTE (affichable dans l'UI par le bouton **✎ requête**) : rôle chasseur de bons plans FR, liste `CMP_AI_SOURCES` (amazon.fr, fnac, darty, cdiscount, boulanger, ldlc, materiel.net, rueducommerce, leclerc, carrefour, auchan, rakuten, backmarket, leboncoin, vinted, idealo, ledenicheur, dealabs, google shopping), `CMP_AI_MAX`=8 offres triées par prix, interdiction d'inventer prix/URL, sortie JSON `{produit, prix_reference, offres:[{marchand, prix, prix_barre, remise, etat, url, commentaire}], verdict, sources}`. `cmpAiOffer(o)` normalise (virgule décimale, remise recalculée depuis le prix barré, `safeHref` sur l'URL). UI : sélecteur Claude/Gemini + état de la clé, ✎ requête, offres avec badge −X %, prix barré, lien marchand, verdict, chips des sources, lien leDénicheur de vérification. **iTunes Search / Open Products Facts / Open Beauty Facts RETIRÉS** de la CSP et d'`API_LIST` (Open FOOD Facts reste : utilisé par Santé) L'état suivis vit dans `ComparePanel` (`isWatched`/`toggleWatch` passés aux sections). Caches promos/boutiques re-fetchables PAS dans buildBackup.

**🕹️ Bibliothèque Steam** (`SteamWishlistSection`, bleu `STEAM_COLOR='#66c0f4'`, 2e collapse de Bon Plan — **7.6.30**) : affiche **ma liste de souhaits Steam** (icône, nom, prix, promo). 2 endpoints Steam **publics sans clé**, le SteamID64 suffit (profil + wishlist publics) : `IWishlistService/GetWishlist/v1?steamid=` → appids, puis `IStoreBrowseService/GetItems/v1?input_json=` (chunks de **50 appids par requête**, `context {language:'french', country_code:'FR'}`) → nom + assets + `best_purchase_option`. Helpers module-level : `steamGetJson(url)` (**fetch DIRECT borné à 8 s** puis repli `corsFetchText` ; 401/403 → `{__denied:true}`, pas de cascade de proxies inutile), `steamNum(v)` (Steam renvoie ses entiers en CHAÎNE), `steamAssetUrl(it, appid)` (`asset_url_format` + `${FILENAME}` sinon capsule CDN `cdn.cloudflare.steamstatic.com/steam/apps/<id>/capsule_231x87.jpg`), `steamNormalizeItem` → `{appid, name, image, free, priceCents, origCents, discount, url}` (remise recalculée si `discount_pct` absent), `steamStoreItems(appids)`, `steamFetchWishlist(force)` (cache `spotifyplus_steam_wishlist` TTL 30 min, cap `STEAM_WL_MAX=200`, tri **promo décroissante** puis priorité ; erreurs `CONFIG`/`PRIVATE`/`NET`), `steamPromoItems(minPct)` (lit le CACHE seul → utilisé par Alertes/Stats), `SteamGameRow`, `SteamConfigForm` (SteamID64 validé `/^\d{17}$/` + clé Web API optionnelle, partagé Bon Plan/Stats). UI : compteurs (suivis / en promo / ≥ 80 %), chips **Tous / En promo**, badge −X % + prix barré, bouton ⚙ (config) et ↻. Config = `spotifyplus_steam_id` (+ `spotifyplus_steam_key` pour les stats), éditable aussi dans le module **API**. **CSP** : `api.steampowered.com` + `store.steampowered.com` ajoutés à connect-src. ⚠ **Caches `spotifyplus_steam_wishlist` / `spotifyplus_steam_stats` volontairement HORS buildBackup, hors `EXPORT_SECTIONS` et hors `restoreBackup`** (demande utilisateur : ni import/export ni sauvegarde) — seuls le SteamID/la clé vivent en localStorage. ⚠ **Steam est INJOIGNABLE depuis l'environnement d'agent (proxy réseau 403)** : parsing défensif, mais le format réel des payloads n'a PAS pu être vérifié en direct — à valider dans le navigateur.

**TV Time** (`TvTimePanel`, ambre — **`selfHeading:true` depuis 7.6.52**, le panneau rend lui-même « TV TIME » + l'icône 📚 alignée à droite ; le heading dédié a été retiré de `MobileApp`) :

**📚 LIVRES (7.6.52)** — même concept que Films/Séries/YouTube, pour la lecture. Activé par l'**icône 📚 alignée avec le titre « TV TIME »** (`BOOK_LS` = `spotifyplus_tvtime_books`, `booksEnabled()`/`setBooksEnabled()`, badge = nb de livres) → un onglet **« 📚 Livres »** apparaît **À DROITE de « ▶️ YT »** dans la barre de filtres (`mediaFilter === 'book'`, valeur acceptée par `TVTIME_FILTER_LS`). **API sans clé** : `bookSearch(q)` interroge **Google Books v1** (`www.googleapis.com/books/v1/volumes?country=FR`, CORS ouvert, origine déjà en CSP — titre, auteurs, année, couverture ET `pageCount`) puis, si 0 résultat, **Open Library** (`openlibrary.org/search.json`, ajoutée à connect-src + à `API_LIST` cat. TV / Vidéo) ; renvoie `[]` si aucun livre et **`null` si les DEUX sont injoignables**. `bookFromGoogle`/`bookFromOpenLibrary` → item `{ id:'gb:…'|'ol:…', type:'book', title, author, year, poster (URL ABSOLUE), pages, link }`. **Les livres vivent dans la MÊME liste `spotifyplus_tvtime`** → export / import / backup Drive-Dropbox-pCloud-MEGA marchent sans rien ajouter (`_tvItemKey` = `type+id`) ; `mergeTvItem` prend le MAX de `pagesRead`/`pages` (jamais de régression). Progression : `setBookPage`/`bumpBook` (±`BOOK_PAGE_STEP`=10) / `askBookPage` (✎ = `prompt`) ; atteindre la dernière page passe le livre en **LU** (et `setStatus(...,'done')` force `pagesRead = pages`). 3 sections `secTitle` : 📖 EN COURS DE LECTURE / 📕 À LIRE / ✓ LUS (clés `bkReading`/`bkTowatch`/`bkDone`). Couleur `BOOK_COLOR='#c98b3f'`. Clic sur la couverture = ouverture de la fiche Google Books / Open Library (pas de modale). ⚠ **Garde-fous** : `filmTv` exclut `type==='book'`, `tvRefreshShows` ne les envoie jamais à TMDB, `tvTimeStats` les `continue` (hors compteurs films/séries/YouTube). Le corps du panneau s'affiche désormais si `hasKey || booksOn` (les livres n'exigent aucune clé), la recherche TMDB restant gated sur `hasKey`.

Suivi séries/films via **TMDB v3** (clé perso `spotifyplus_tmdb_key`, pas de plafond journalier).  `tmdbFetchItem` = 1 requête récupère tout, mis en cache (`spotifyplus_tvtime`) → compteurs/stats ne redemandent rien. `tvRefreshShows` (TTL 24 h) ré-interroge seulement les séries suivies non terminées. Item `{id, type:'movie'|'tv'|'youtube', title, poster, year, status:'towatch'|'watching'|'paused'|'done', ...}`. Source de vérité suivi = `tvWatchedSet` (clés `S{n}E{e}`, cochage individuel). Fiches `TvDetailModal`/`YtDetailModal` (saisons dépliables épisode par épisode, `/tv/{id}/season/{n}` caché 7 j). **Vignette YouTube cliquable (7.6.28, refondue en LECTEUR PLEIN ÉCRAN en 7.6.78)** : dans `YtDetailModal`, la miniature de chaque vidéo (avec overlay ▶) est un **bouton** qui appelle `playFrom(v)` → `onPlay(flatVideos, idx)` où `flatVideos` = toutes les vidéos de la fiche **dans l'ordre affiché** (années décroissantes) et `idx` = la vidéo cliquée → lecture PLEIN ÉCRAN dans l'app (cf. `YtFullscreenPlayer` ci-dessous), le reste de la fiche servant de file d'attente pour le bouton ⏭. La vidéo est marquée vue à son LANCEMENT (règle inchangée, `markSeen` supprimé au profit de `ytMarkVideoSeen` côté panneau) ; la coche ✓ reste pour (dé)cocher manuellement, le titre reste un lien nouvel onglet SANS marquage (repli si l'embed est refusé). Sections repliables : En cours/En pause/À voir/Prochaines sorties/Vus. Handlers : `setStatus`, `withEps` (statut auto done/watching), `ignoreUnwatched` (🙈 valider sans voir → revient en cours à la prochaine sortie), `abandonSeries` (🚫/⏹ done+`dropped`), `undoLast` (↩ pile 30). Labels `manga`/`cartoon`. **YouTube** (clé `spotifyplus_yt_key`) : chaînes/playlists via lien, vidéos groupées par année, filtre Shorts `< 3 min` (tag `keepShorts` désactive), `unlimited` (cap 500 → 50k), check auto nouvelles vidéos au login (`ytCheckNewVideos`, TTL 6 h, skip `dropped`). **Tag « garder UNIQUEMENT les Shorts » (`shortsOnly`, 7.6.13)** : bouton sous « garder les Shorts » (`onShortsOnly`/`setShortsOnly`, exclusif de `keepShorts` — active l'un désactive l'autre). `ytBuildItem` filtre alors `videosRaw.filter(v => (v.dur||0) < YT_MIN_MINUTES)` (complément exact du filtre normal) → les saisons n'affichent QUE des Shorts, et `ytCheckNewVideos` ne détecte de nouveauté (donc ne notifie « En cours ») que pour un Short qui sort. Propagé partout (build/refresh/loadAll/checkNew/mergeTvImport). **À l'ajout d'une chaîne 100 % Shorts (7.6.13)** : le build normal échoue (aucune vidéo ≥ 3 min) → `addYoutube` retente en `shortsOnly` et, si des Shorts existent, `confirm()` demande si c'est volontaire → ajout avec `shortsOnly` (pas de re-demande si la chaîne est déjà suivie en shortsOnly). **Bouton 🕒 « playlist » (7.2.6)** : dans la section YouTube **EN COURS**, entre ✓ et 🙈 (`wlBtn`) → `queueRemaining(it)` ajoute TOUTES les vidéos restantes (non vues) à la playlist « Hub Pierre » via `ytWatchLaterAdd`. Anti-doublon = **même set local `wlAdded`/`loadYtWlAdded` que la section Actu** (les vidéos déjà ajoutées par le Hub sont skippées) ; bouton **grisé** quand toutes les restantes y sont déjà (ou aucune restante). ⚠ La présence est vérifiée via ce set local (ce que le Hub a ajouté), PAS par un fetch du contenu réel de la playlist (économie de quota). FUSIONNÉ au backup (`mergeTvImport`).

**⚠ 7.6.78 — LECTEUR YOUTUBE PLEIN ÉCRAN + FILE DES SHORTS RESTANTS** (demande utilisateur) :
- **`YtFullscreenPlayer({queue, index, onIndex, onClose, onPlaying})`** (module-level, juste avant `TvTimePanel`) : `position:fixed inset:0 zIndex:400` (au-dessus de `YtDetailModal`, z 200), iframe `youtube-nocookie.com/embed/<id>?autoplay=1&rel=0&playsinline=1` plein viewport (origine DÉJÀ en `frame-src`, rien à ajouter à la CSP) + **barre d'actions EN OVERLAY** en haut : **← Retour · ⏮ · ⏭ Suivant · compteur `i+1/N`** (`pointerEvents:'none'` sur la barre, `'auto'` sur les boutons pour ne pas voler les clics à la vidéo), titre + chaîne en bas. Clavier : **← / →** naviguent, **Échap** ferme. **Plein écran NATIF best effort** (`requestFullscreen()` au montage, `exitFullscreen()` au démontage, tous deux en try/catch + `.catch()`) : refusé hors geste utilisateur et absent sur iOS → sans conséquence, l'overlay couvre déjà tout le viewport. ⚠ **Échap en plein écran natif** sort du plein écran (géré par le navigateur) → on ne ferme le lecteur QUE si `document.fullscreenElement` est nul, sinon un seul Échap ferait les deux. ⚠ L'effet clavier est **SANS tableau de deps** (ré-attaché à chaque render) pour ne jamais figer `index` dans une closure.
- **⚠ AUCUNE auto-avance en fin de vidéo** : la détecter exige l'**IFrame API de YouTube**, donc le script `www.youtube.com/iframe_api` — interdit par la CSP (`script-src` = `'self'` + unpkg + accounts.google.com) et qu'il n'est pas question d'ouvrir pour ça. Le passage à la suivante est donc MANUEL (⏭ / →). C'est une limite assumée, pas un oubli.
- **Bouton 🩳 dans l'en-tête de `TvTimePanel`**, **à GAUCHE de l'icône 📚** (position demandée après coup ; il visait d'abord l'en-tête de la section YouTube « EN COURS ») : badge = `ytShortsQueue().length`, désactivé/grisé à 0. `ytShortsQueue()` balaie **tous** les items `type==='youtube'` dont `status !== 'done'` et retient les vidéos non vues avec `(v.dur||0) < YT_MIN_MINUTES` — ⚠ `dur` est en **minutes ARRONDIES** dans TV Time (≠ `durSec` de la section Actu) et une durée INCONNUE vaut 0, donc traitée comme un Short : même convention que le filtre `shortsOnly` d'`ytBuildItem`. Ordre = chaîne par chaîne, chronologique (l'ordre de stockage). Entrées de file = `ytQueueEntry(it, v)` = `{id, title, date, dur, itemId, channel}` — **`itemId` est indispensable** : une file mélange plusieurs chaînes, il faut savoir laquelle marquer.
- **`ytMarkVideoSeen(itemId, videoId)`** (appelée par `onPlaying` à chaque changement d'index) : lit **`itemsRef.current`** (miroir de `items` réassigné à chaque render) et non la closure `items` — plusieurs vidéos peuvent être marquées avant un re-render, la closure perdrait les marquages précédents. Idempotent, bascule en `done` quand tout est vu (même règle que `ytSetWatched`), **hors pile d'annulation** (une file de Shorts saturerait les 30 entrées de `undoStack`).
- State `player = {queue, i} | null` dans `TvTimePanel`, overlay rendu tout en bas du panneau à côté des fiches.

**⚠ 7.6.80 — BOUTON 🕒 « À REGARDER PLUS TARD » PAR VIDÉO** (demande utilisateur) : dans `YtDetailModal`, chaque vidéo **NON COCHÉE** d'une saison porte un bouton **🕒** (composant local `WatchLater`, à gauche de la coche ✓) qui ajoute **cette seule vidéo** à la playlist « Hub Pierre » — équivalent unitaire du 🕒 de la chaîne entière (`wlBtn`/`queueRemaining`). Côté `TvTimePanel`, `queueOneVideo(v)` appelle `ytWatchLaterAdd` et partage TOUT avec le bouton de chaîne : le set local `wlAdded`/`saveYtWlAdded` (anti-doublon commun, donc une vidéo déjà envoyée par le 🕒 de la chaîne s'affiche déjà ajoutée) et l'état `wlBusyId` (**une seule écriture playlist à la fois** — pendant un ajout, tous les 🕒 sont grisés). Les alertes d'erreur (AUTH/SCOPE/QUOTA/réseau) sont factorisées dans `wlAlert(err)`. Rendu : 🕒 rouge = ajoutable · 🕒 vert bordure verte = déjà dans la playlist (bouton inerte) · « … » = ajout en cours. Props passées à la fiche : `wlAdded`, `wlBusyId`, `onWatchLater` (absente ⇒ aucun bouton, la fiche reste utilisable telle quelle). ⚠ Comme pour le bouton de chaîne, la présence dans la playlist est jugée sur le set LOCAL (ce que le Hub a ajouté), pas par un fetch du contenu réel (économie de quota).

**⚠ 7.6.81 — RETRAIT AUTOMATIQUE À « VUE » + PLAYLIST RENOMMÉE « Hub Pierre »** (demande utilisateur) :
- ⚠ **La « À regarder plus tard » NATIVE de YouTube (`list=WL`) reste inaccessible** : Google a retiré `watchLater`/`watchHistory` de l'API Data v3 le **12 septembre 2016** (toute lecture/écriture → erreur, `playlistItems.insert` avec `playlistId:'WL'` → 404 `playlistNotFound`). Aucun scope ne débloque ça, aucun endpoint de remplacement → la playlist dédiée du Hub reste la seule option. Question déjà posée une fois, la réponse ne changera pas.
- **`YTSUB_WL_NAME = 'Hub Pierre'`** (était « À regarder plus tard (Hub) »). ⚠ Changer cette constante ne suffit PAS : l'id de la playlist est caché dans `YTSUB_WL_ID_LS`, donc l'app continuerait d'écrire dans une playlist portant l'ANCIEN nom (renommage invisible), et un cache vidé en aurait créé une SECONDE. D'où deux garde-fous : (a) **`YTSUB_WL_OLD_NAMES`** = anciens titres, cherchés en repli dans le balayage `playlists?mine=true` → la playlist existante est retrouvée, pas dupliquée ; (b) **`ytWlSyncName(pid)`** (via le nouveau helper `ytSubPut` = `playlists.update`) **RENOMME la playlist existante** et mémorise le nom appliqué dans **`YTSUB_WL_NAMED_LS`** → au plus 1 aller-retour par changement de nom, best effort (un échec est retenté au prochain appel, il ne bloque jamais l'ajout/le retrait).
- **`ytSubDelete(path, params)`** (DELETE, 204 sans corps) + **`ytWatchLaterRemove(videoId)`** : ⚠ `playlistItems.delete` veut l'id de l'**ITEM de playlist**, pas celui de la vidéo → `playlistItems.list?playlistId=…&videoId=…` d'abord. `ytWlPlaylistId(skipCache, create)` gagne un 2e paramètre : le retrait passe **`create = false`** (pas de playlist ⇒ rien à retirer, surtout pas une playlist à créer). Retry unique sur `NET`/`NOTFOUND` après invalidation de l'id caché (id périmé). Coût quota : 1 unité (list) + 50 par suppression.
- **`wlForget(ids)`** dans `TvTimePanel`, branché sur **les 3 chemins qui marquent une vidéo vue** : `ytSetWatched` (coche manuelle / année entière → seulement les ids NOUVELLEMENT cochés), `ytMarkVideoSeen` (lecteur plein écran) et `setStatus(item,'done')` sur une chaîne. ⚠ Il ne traite **que les ids présents dans `wlAdded`** — sinon chaque coche déclencherait un aller-retour API inutile et on toucherait à des vidéos ajoutées à la playlist par l'utilisateur lui-même. Silencieux (aucune alerte) : un échec laisse l'id dans `wlAdded`, le retrait sera retenté au prochain marquage. La persistance relit `loadYtWlAdded()` avant d'écrire (source de vérité disque, pas d'écrasement d'un ajout concurrent).
- ⚠ **Non vérifiable côté agent** (l'API YouTube exige un OAuth utilisateur) : `ytWatchLaterRemove`/`ytWlSyncName`/`ytWlPlaylistId` ont été testés contre un **faux serveur** (Node) couvrant : playlist trouvée sous l'ancien nom → renommée sans doublon, renommage joué UNE seule fois, vidéo absente → succès sans DELETE, playlist supprimée → aucun retrait ni création, chemin d'ajout qui recrée bien la playlist au nouveau nom.

**Note** (`NotePanel`, vert `NOTE_COLOR='#6fd345'`, entre Musique et Santé — **6.5.0** ; refonte espaces séparés **7.6.12**) : prise de notes locale (`spotifyplus_notes`, `loadNotes`/`saveNotes`), **`selfHeading:true`** (le panneau rend son propre titre cliquable). **DEUX ESPACES** (`mode` 'normal'|'enc') : (1) « **🗒️ NOTE** » = notes en clair `{id, title, text, locked:false, updatedAt}` UNIQUEMENT — les notes cryptées n'apparaissent PAS (même leur existence est masquée) ; (2) « **NOTE CRYPTÉ** » (titre SANS cadena 🔒 depuis **7.6.14** — retiré des deux titres) atteint en **cliquant sur le titre** (`openGate`) → saisie d'un **MOT DE PASSE GLOBAL** (plus de mdp par note). Un **vérificateur** `spotifyplus_notes_enc_check` = `mdpEncrypt(gpw,{v:1})` (`loadNoteEncCheck`/`saveNoteEncCheck`) valide la saisie même sans note cryptée : absent → phase `setup` (mdp ×2 crée le vérificateur), présent → phase `unlock` (déchiffre le vérificateur). Le mdp global (`gpw`) reste en session ; un effet déchiffre toutes les notes cryptées (`dec` map id→{title,text}). Note cryptée = `{id, locked:true, enc, updatedAt}` où `enc = mdpEncrypt(gpw, {title, text})` — **AUCUN titre en clair**. Bouton « + Nouvelle note cryptée » en mode enc, « ← Revenir aux notes » en bas. **Bascule public ⇄ privé depuis l'éditeur (7.6.33)** : dans la barre de l'éditeur, **ENTRE « ✓ Enregistrer » et « Annuler »**, un bouton **« 🔒 Passer privé »** (espace normal) / **« 🗒️ Passer public »** (espace crypté) enregistre la note dans l'AUTRE espace (même `id`, elle quitte donc l'espace courant). Helpers `writeNote(entry, now)` / `saveAsClear()` / `saveAsEnc(pw)` factorisés (utilisés aussi par `saveNote`) ; `toggleVisibility()` : privé → public demande une **confirmation** (`window.confirm`, la note ne sera plus chiffrée) ; public → privé chiffre avec `gpw` s'il est en session, sinon **ouvre la porte** (`toPrivate=true` → `gate` 'setup' ou 'unlock', libellé du bouton de la porte « 🔒 Passer privé », texte d'aide adapté) et la conversion se fait à la validation via `gateDone(pw)` **sans entrer dans l'espace crypté**. `cancelGate` remet `toPrivate=false`. State `msg` = confirmation affichée sous le titre (« Note déplacée dans les notes cryptées / normales »), effacée à la prochaine action (`openNew`/`openNote`/`openGate`/`leaveEnc`). **Migration douce** : anciennes notes `{title clair, enc:{text}}` (mdp individuel) → échec du déchiffrement global (`dec[id]===null`) → déverrouillage per-note inline (`askId`/`unlockLegacy`), re-chiffrées au mdp global à l'enregistrement. `mdpEncrypt`/`mdpDecrypt`/`isVaultBlob` réutilisés (AES-GCM 256 + PBKDF2). Basculable (`TOGGLE_MODULES` id `note`), export `EXPORT_SECTIONS` clé `note` (champs `notes` + `notes_enc_check`), buildBackup inclut `notes_enc_check`, restore FUSIONNÉ par id (`updatedAt` max) + vérificateur REMPLACÉ. **PAS de carte sur l'accueil connecté** (demande) ; **accessible depuis la page de CONNEXION** (state `tool` de `Home`, panneau superposé — tout est local, aucune connexion requise ; le même state sert aussi la 🧮 Calculatrice, `FinanceCalcPanel` réutilisé tel quel). **6.5.4** : Note et Calculatrice sont des **chips de la grille `HUB_FEATURES`** (même rendu/dérive `hub-drift` que les autres tuiles, à leur place alphabétique) — 4e élément optionnel `toolKey` ('notes'/'calc') qui rend la chip cliquable (plus de rangée de boutons dédiée sous la carte de connexion). ⚠ Mot de passe global des notes cryptées oublié = notes irrécupérables.

**Revente** (`ReventePanel`, vert `REVENTE_COLOR='#86d941'`, entre Note et Santé — **7.6.45**) : rédaction assistée d'annonces **Vinted / Leboncoin / eBay**. ⚠ **La publication automatique est IMPOSSIBLE sans backend et ce n'est pas un manque d'implémentation** : Vinted et Leboncoin n'exposent aucune API publique de dépôt d'annonce pour un particulier (seulement des API partenaires sous contrat), et l'API Sell d'eBay exige un échange OAuth avec un `client_secret` (donc un serveur) et n'envoie pas d'en-têtes CORS. Le module couvre donc tout le faisable : l'IA rédige, on copie, on ouvre le formulaire.
- **Données** : `spotifyplus_revente` = `{annonces:[…], provider:'claude'|'gemini'}` (`revLoad`/`revSave` via `lsSet`). Article = `{id, nom, cat, marque, taille, couleur, etat, prixAchat, prix, notes, createdAt, listings:{<plateforme>:{titre,description}}, status:{<plateforme>:'brouillon'|'en_ligne'|'vendu'}, motsCles:[], prixConseille}`.
- **`REV_PLATFORMS`** : `{id, label, color, titleMax, descMax, pct, url, style}` — Vinted (60/1000, `vinted.fr/items/new`), Leboncoin (50/4000, `leboncoin.fr/deposer-une-annonce`), eBay (80/4000, `ebay.fr/sl/sell`, **`pct: REV_EBAY_PCT = 20`**). `revPrice(base, pct)` applique la majoration (eBay +20 %, demande utilisateur) ; `style` décrit le ton attendu et part dans le prompt.
- **IA — 2 fournisseurs** (sélecteur persistant `provider`) : **Claude** (`claudeGenerate`, `CLAUDE_MODEL='claude-opus-5'`, clé `spotifyplus_claude_key`) ou **Gemini** (réutilise `geminiGenerate`, même clé que le Frigo). `revGenerate(provider, annonce, images)` construit le prompt (`revPrompt`), envoie jusqu'à `REV_MAX_AI_PHOTOS`=3 photos en vision, et normalise la réponse (troncature à `titleMax`/`descMax`, plateformes inconnues ignorées).
- **⚠ Appel navigateur direct à l'API Anthropic** : `POST api.anthropic.com/v1/messages` avec `x-api-key` + `anthropic-version: 2023-06-01` + **`anthropic-dangerous-direct-browser-access: true`** — c'est CET en-tête qui débloque le CORS côté Anthropic (mécanisme derrière le `dangerouslyAllowBrowser` du SDK JS). Origine **ajoutée à la CSP connect-src**. Utilise les **structured outputs** (`output_config.format` + `REV_AI_SCHEMA`, JSON garanti) et **`effort:'low'`** (⚠ sur Opus 5 la réflexion est ACTIVE par défaut et `max_tokens` plafonne réflexion + réponse ensemble → `max_tokens: 8000`). `stop_reason==='refusal'` géré. Parsing via `geminiExtract` (encaisse le JSON brut de Claude comme les ``` de Gemini).
- **Photos → Google Drive** (`HUB_Pierre/Revente`, helpers génériques de Vêtement) : jusqu'à `REV_MAX_PHOTOS`=5 par article, un fichier par photo nommé **`<idAnnonce>__<n>.<ext>`** (`revPhotoName`/`revParseName`) → le lien article ↔ photos est porté par le NOM, rien en local, le Drive fait autorité. `revPhotoBlob` (contain 1200 px, WebP 0.85 → repli JPEG). Vignettes via `VetPhotoImg` (réutilisé tel quel). ⚠ **`revImgToData(url)`** convertit une photo Drive en base64 pour la vision via `<img>` + canvas et **JAMAIS via `fetch('blob:…')`** — la CSP `connect-src` liste des origines https, pas le schéma `blob:`.
- **UI** : bandeau d'avertissement (pas de publication auto) + état Drive, puis 3 collapses — **⚙️ Assistant IA** (fournisseur + clé), **➕ Nouvel article** (formulaire), **📋 Mes annonces** (compteurs en ligne / vendus / CA / marge, puis une ligne par article dépliable → photos, prix, ✨ Générer, mots-clés, et **une carte par plateforme** avec titre/description + compteurs de caractères, `RevCopyBtn` ⧉ Titre/Description/Prix et lien ↗ Déposer). `RevCopyBtn` = `navigator.clipboard` avec accusé ✓/✕ 2 s.
- **Intégration** module normale (id `revente` : `TOGGLE_MODULES`, `navItems`, `allOverflowTabs` + bloc `MobileApp`, `SECTION`, accueil via `TOGGLE_MODULES`) + entrée `anthropic` dans `API_LIST`/`API_KEY_LS` (cat IA, 🔑). **buildBackup** : clé `revente` (articles + annonces, JAMAIS les photos) + `claude_key` (ajoutée à la section d'export « Comptes & clés ») ; section d'export dédiée `revente` ; restore = **fusion par id, le local gagne**.

**Vêtement** (`VetementPanel`, orange `VETEMENT_COLOR='#f59a2e'`, entre TV Time et Stats) — **7.2.4** : garde-robe locale (`spotifyplus_vetements` = `{counts, items, brands}`, `vetLoad`/`vetSave` via `lsSet`) — **sauf les PHOTOS, qui vivent dans Google Drive depuis 7.6.32 (voir plus bas)**. 3 collapses : **👕 Inventaire** (compteur ± par type — `VET_TYPES` T-shirt/Chemise/Pull/Sweat/Veste/Manteau/Pantalon/Jean/Short/Sous-vêtements/Chaussettes/Chaussures) · **🧥 Mes vêtements** (**7.3.1 : PILOTÉ par l'inventaire**, plus de bouton ➕ ; vide au départ, un **sous-collapse par TYPE présent** contenant **une fiche par unité** : nom, **taille** (champ texte libre `taille`, ex. M/40/42, badge « T.x » dans le titre — 7.3.6), **photo** (sur Drive, cf. ci-dessous), **état** `VET_ETATS` (Neuf/Bon état/Usé/Abîmé), **lieu** `VET_LIEUX` (Saint-Quentin-en-Yvelines / Massy), et **✕ Supprimer avec confirmation** « je le jette » qui décrémente aussi l'inventaire — plus de sélecteur TYPE dans la fiche). **Couplage counts↔fiches** : les fiches sont la source de vérité, `counts` en est le décompte (`vetCounts(items)`) ; **+1** dans l'inventaire (`setCount`) crée une fiche vide (`vetBlankItem`), **−1** retire la fiche la plus récente du type, la saisie directe (`setCountVal`) réconcilie ; `vetLoad` migre les anciens inventaires (compteur > nb de fiches) en complétant avec des fiches vides. **❤️ Mes marques (7.6.9, refonte lignes 7.6.18)** : 3e collapse, liste des marques aimées `brands:[{id, name, articles:[]}]` (nom + chips de types d'articles `VET_BRAND_ARTICLES` = Haut/Pantalon/Jean/Short/Veste/Manteau/Chaussures/Sac/Bob-Casquette/Accessoire/Sous-vêtements, plus larges que l'inventaire) ; `addBrand`/`updBrand`/`delBrand`/`toggleBrandArticle` ; `saveItems` et `saveBrands` conservent toujours l'autre champ (`...data`). **Chaque marque = une LIGNE FINE (façon TV Time)** : nom + résumé des types (`articles.join(' · ')`) + boutons ✏️ Éditer / ✕ Supprimer ; le **formulaire d'édition** (input nom + chips d'articles + ✓ OK) ne s'affiche qu'au clic sur ✏️ (state local `editBrandId`). `addBrand` crée la marque ET ouvre son formulaire ; `delBrand` referme le formulaire s'il était ouvert. Basculable (`TOGGLE_MODULES` id `vetement`), présent partout comme un module normal (onglet desktop `navItems`, menu mobile `allOverflowTabs` + bloc de rendu `MobileApp`, `SECTION`, chip de la grille `HUB_FEATURES`). PAS sur l'accueil connecté (`HUB_CARDS`).

**⚠ PHOTOS → GOOGLE DRIVE (7.6.32)** — l'ancien stockage en **data URL dans localStorage** est **supprimé** (quota ~5 Mo partagé par TOUT le Hub → perte silencieuse de données, et rien n'était sauvegardé). Les photos vivent désormais dans le Drive de l'utilisateur, sous **`HUB_Pierre/Vetement`** (les 2 dossiers créés à la volée), **un fichier par fiche NOMMÉ AVEC SON ID** (`<id>.webp` / `.jpg`, cf. `vetPhotoName`/`vetIdFromName` + table `VET_MIME_EXT`) → le lien fiche ↔ photo est porté par le **nom du fichier**, rien n'est stocké en local, le Drive fait autorité. Conséquence : les photos **survivent** à une réinstallation et **suivent** sur un autre appareil dès que le backup (qui contient les ids de fiches) est restauré. `buildBackup` inclut désormais `brands` (perdus jusque-là) et jamais les photos.
- **Helpers Drive génériques** (à côté de `gdriveUpload`/`gdriveDownload`, réutilisables par d'autres modules médias) : `GDRIVE_HUB_FOLDER='HUB_Pierre'` · `gdriveEnsureFolder(token, name, parentId, path)` (cherche puis CRÉE, id caché en localStorage `gdrive_folder_<path>` ; si le parent trouvé est inaccessible en écriture — dossier créé hors app, scope `drive.file` — repli sur la racine) · `gdriveHubSubfolder(token, sub)` · `gdriveHubFolderReset(sub)` · `gdriveListFolder` (paginé) · `gdriveUploadMedia({fileId, name, parentId, blob})` (PATCH si `fileId`, sinon **création en 2 temps** métadonnées JSON puis contenu — évite d'assembler du multipart/related binaire) · `gdriveDeleteFile` · `gdriveBlobUrl(fileId)`/`gdriveBlobForget(fileId)`.
- **Affichage** : un fichier Drive privé n'a pas d'URL affichable → téléchargement avec le Bearer puis `URL.createObjectURL` ⇒ **`blob:` AJOUTÉ à `img-src` dans la CSP**. Cache mémoire par `fileId` (durée de vie = la page) + dédoublonnage des requêtes concurrentes. Composant `VetPhotoImg({fileId, size, rev, radius})` (⏳ pendant le chargement, ⚠ sur échec) ; le state `photoRev` + `gdriveBlobForget` invalident la vignette quand une photo est REMPLACÉE sur le même `fileId`.
- **Panel** : state `photos` (idFiche → fileId), `drive` (`idle|loading|ready|off|error`), `driveMsg`, `busyPhoto`, `photoRev` ; `syncDrive()` (ensure dossier → `gdriveListFolder` → index par `vetIdFromName`) au mount avec `gdriveAutoLogin()` ; bandeau d'état en tête (dossier + nb de photos + ↻ Rafraîchir / Connecter Google Drive). `onPhoto` : `vetPhotoBlob` (contain **640 px**, `toBlob` WebP 0.8 → repli JPEG 0.85, ⚠ Safari encode en PNG quand on demande du WebP → on vérifie `blob.type`) puis upload, avec **1 retry** après `gdriveHubFolderReset` si le dossier caché a disparu. `delPhoto(id, silent)` supprime le fichier Drive ; `delItem` l'appelle en silencieux (jeter un vêtement jette sa photo).
- **Migration one-shot** (`migratedRef`) : au 1er `drive === 'ready'`, les `it.photo` encore en `data:` partent sur Drive (`vetDataUrlToBlob` — décodage MANUEL, un `fetch('data:…')` serait bloqué par `connect-src`) et **seules celles réellement uploadées** sont retirées du localStorage.

**Frigo** (`FrigoPanel`, bleu `FRIGO_COLOR='#47a8de'`, entre Finance et Jeux ; **renommé « Frigo Massy » → « Frigo » en 7.2.5**) : gestion du frigo 100 % locale (`spotifyplus_frigo` = `{sections:{principal,congel,placard}}`, `frigoLoad`/`frigoSave` via `lsSet`). **Assistant IA (7.6.0, `FrigoAiSection`, en tête du panneau)** — via **Google Gemini** (`GEMINI_MODEL='gemini-2.0-flash'`, multimodal) avec **clé perso stockée en local** (`spotifyplus_gemini_key`, `geminiKey()`, JAMAIS commitée → repo public OK, pattern des clés TMDB/YouTube ; saisie/retrait dans le collapse, lien vers aistudio.google.com/apikey). Helpers module-level : `geminiGenerate(parts)` (POST `generativelanguage.googleapis.com/v1beta/models/{model}:generateContent?key=`, erreurs 400/403/429 explicites), `geminiExtract(txt, open, close)` (isole un JSON `[…]`/`{…}` en retirant les ``` ), `frigoPhotoData(file)` (photo → base64 JPEG ~900 px pour l'entrée vision). **3 fonctions** : (1) **📷 Photo → ingrédients** — `<input capture>` → `frigoPhotoData` → `geminiGenerate([texte, {inline_data}])` renvoie un tableau JSON d'aliments → chips cochables + sélecteur de compartiment + « Pré-remplir » (appelle `onAdd`) ; (2) **🍳 10 recettes avec mon frigo** — envoie la liste des aliments du frigo → 10 objets `{titre, jai, manque}` ; (3) **📖 Recette d'un plat** — nom de plat → `{titre, ingredients, etapes}`. Origine `generativelanguage.googleapis.com` ajoutée à la CSP connect-src. **3 collapses** = `FRIGO_SECTIONS` (titres corrigés : **Compartiment principal** ex-« Partiment principal », **Congélateur** ex-« surgeler », **Placard**), chacun un `FrigoSection` : ajout d'ingrédient (nom + **date de péremption** optionnelle), édition inline de la date, suppression ✕. Statut de péremption via `frigoExpStatus` (expired/soon ≤ 3 j/ok). **Section Alertes auto (`FrigoAlertSection`)** : ajoutée dans `RappelPanel` (juste AVANT « 🔔 Mes rappels » qui est en dernière position depuis 7.3.7), lecture seule, dérive de `frigoDueItems()` (aliments périmés ou ≤ 3 j) → générée/vidée automatiquement selon le contenu du Frigo. **REPLIÉE par défaut (7.6.61, demande utilisateur)** : plus de `defaultOpen={due.length > 0}` (elle se dépliait toute seule dès qu'un aliment arrivait à péremption) — le nombre d'aliments concernés reste dans le titre replié. Intégration module normale (id `frigo` : `TOGGLE_MODULES`, `navItems`, `allOverflowTabs` + bloc `MobileApp`, `SECTION`, chip `HUB_FEATURES`). PAS sur `HUB_CARDS`, PAS dans buildBackup.

**Emploi** (`EmploiPanel`, bleu `EMPLOI_COLOR='#57a0e0'`, entre Bon Plan et Finance — **7.5.9**) : marché de l'emploi 100 % client, 2 collapses. **💰 Mon salaire vs secteur** (`EmploiSalarySection`) : saisie du salaire brut (/mois ou /an) + choix du secteur (`EMPLOI_SECTORS`, 16 secteurs avec **salaire brut annuel moyen INDICATIF** — ordres de grandeur France, pas une source temps réel) → écart € et % vs la moyenne + double barre « Moi / Secteur ». Config persistée `spotifyplus_emploi` (`emploiLoad`/`emploiSave` via `lsSet`). **📋 Offres du moment** (`EmploiOffersSection`) : recherche métier + lieu → `emploiSearchJobs` fusionne **2 APIs keyless CORS** : The Muse (`www.themuse.com/api/public/jobs`, filtre lieu + filtrage client par mot-clé) + Remotive (`remotive.com/api/remote-jobs?search=`, remote), `emploiGetJson` = fetch direct puis repli `corsFetchText` (proxies), dédoublonnage titre+société, cartes cliquables (lien `safeHref` nouvel onglet, badge source). Origines `www.themuse.com` + `remotive.com` ajoutées à la CSP connect-src. Intégration module normale (id `emploi` : `TOGGLE_MODULES`, `navItems`, `allOverflowTabs` + bloc `MobileApp`, `SECTION`, sur l'accueil via `TOGGLE_MODULES`). PAS de chip `HUB_FEATURES` (offres = réseau), PAS dans buildBackup (sauf config salaire non incluse pour l'instant).

**Sport** (`SportPanel`, lime `SPORT_COLOR='#c3d93f'`, entre Santé et To Do) : suivi d'entraînement 100 % local (`spotifyplus_sport`, `sportLoad`/`sportSave` via `lsSet`). **7.5.8** : un **sous-collapse par partie du corps** (`SPORT_GROUPS`, composant `SportGroup`), chacun contenant une **grille de tuiles-exercices**. **Clic sur une tuile → elle se grise** (grayscale + ✅ + libellé barré, marque « fait », persistant) ; **bouton ↺ Réinitialiser** par partie remet à zéro les grisés de cette partie. Compteur `N/total` dans le titre du sous-collapse. State `{ [groupKey]: { [exoName]: true } }`.

**⚠ 7.6.72 — CATALOGUE BASIC-FIT + FICHES ILLUSTRÉES (demande utilisateur)** : `SPORT_GROUPS` passe de listes de **chaînes** à des listes d'**objets** `{ n:nom, m:machine Basic-Fit, mus:'Muscle · Muscle', icon:vignette, f:[frame1, frame2], s:[3 étapes], t:erreur à éviter }` — **9 groupes / 70 exercices depuis 7.6.76** calqués sur le matériel réellement présent en salle Basic-Fit France (cardio, machines guidées, poulies, poids libres) : Running/Cardio, Full body, Dos, Pecs, **Épaules** (nouveau), Biceps, Triceps, **Abdos/Gainage** (nouveau), Jambes. ⚠ Les clés de persistance sont les **noms d'exercices** (`done[e.n]`) : les anciens noms (7.5.8) ne matchent plus → les coches d'avant la mise à jour ne sont pas reprises (aucune migration, ce ne sont que des cases du jour).
- **Images** : planches **Everkinetic** (CC BY-SA) et photos de machines hébergées sur **Wikimedia Commons**. Chaque exercice a 2 phases (`Nom-1` / `Nom-2` = départ / fin du mouvement) → `f` = les deux vignettes 500 px, `icon` = la 1re en 250 px. **Les 124 fichiers et les 168 URLs ont été vérifiés un par un** (API Commons à la génération, puis chargement réel dans le navigateur). Pas de nouvelle origine CSP : `img-src` liste déjà `https:`. Le trait est noir sur fond transparent → **vignettes rendues sur fond BLANC** (`SportExoImg`, même correctif que les logos de fuites d'Actu), repli **emoji 🏋️** sur `onError` (hors ligne / image retirée de Commons).
- **Appui long = fiche** : `SPORT_HOLD_MS` (450 ms) sur la VIGNETTE (`onPointerDown/Up/Leave/Cancel`, ref `holdRef={timer,fired}`) ouvre `SportExoModal` ; `fired` neutralise le clic qui suit (sinon relâcher cocherait « fait ») — pattern des tags du coffre Mot de Passe. La fiche donne machine, chips de muscles, les 3 étapes numérotées, l'erreur à éviter, et la **démo animée** `SportExoDemo` : alternance des 2 phases toutes les `SPORT_DEMO_MS` (900 ms) = le « gif » demandé, avec bouton ⏸/▶ et compteur de phase. Échap ou clic sur le fond ferme. Une seule frame (photos de machines cardio) → image fixe, pas de bouton.
- **⚠ 7.6.76 — 2 exos AJOUTÉS sur la machine pec deck / butterfly** (demande utilisateur, lien `docteur-fitness.com/pec-deck-ou-butterfly`) : **Dos → « Pec deck inversé (butterfly dos) »** (assis FACE au dossier, deltoïdes postérieurs/rhomboïdes) et **Pecs → « Pec deck unilatéral »** (un bras à la fois, en plus du « Butterfly (pec deck) » déjà présent). ⚠ **Les images de docteur-fitness.com n'ont PAS pu être récupérées** : le site répond **403 au proxy de l'environnement d'agent** (comme Wikimedia, également bloqué → impossible de chercher de NOUVEAUX fichiers Commons ou de vérifier une URL inédite). Les 2 exos réutilisent donc les frames **déjà vérifiées** `Butterfly-machine-1/2.png` (c'est littéralement la même machine) ; pour la version dos, `f` est **inversé** (`[frame2, frame1]` = bras rapprochés → bras écartés) de sorte que l'animation joue bien dans le sens du mouvement, et le champ `t` prévient que l'illustration montre la machine prise dans l'autre sens. Aucun risque d'image cassée (URLs déjà en service ailleurs dans la table).
- **Régénération** : la table est produite par un script (scratchpad `gen2.py` + `groups.json` → `sport_groups.js`, splice UTF-8/CRLF dans `index.html`) — refaire tourner ce script si des images disparaissent de Commons. **PAS dans buildBackup** (état du jour, re-cochable). Même intégration qu'un module normal (id `sport` : `TOGGLE_MODULES`, `navItems`, `allOverflowTabs` + bloc `MobileApp`, `SECTION`). PAS sur `HUB_CARDS`, **PAS de chip `HUB_FEATURES`** (retiré de la grille de la page de connexion en 7.4.0, comme la chip « Nouvelles sorties »), PAS dans buildBackup.

**Jeux** (`JeuxPanel`, bleu-cyan `GAMES_COLOR='#39b2d1'`, entre Finance et Maps) : ludothèque 100% locale (zéro réseau). Un seul onglet avec un **menu déroulant maison en haut à droite aligné avec le titre « JEUX »** (`selfHeading:true` dans `SECTION.games`) pour choisir parmi **17 mini-jeux**. ⚠ Ce menu remplace la selectbox `<select>` native (dont la liste OS remplissait tout l'écran, non ergonomique) : bouton (label du jeu courant + chevron ▾) qui ouvre un **popup compact** (gabarit du menu ⋯ des modules — `C.surfaceHigh`, bordure, overlay `position:fixed inset:0` pour fermer au clic extérieur, `maxHeight:60vh` scrollable, item actif surligné en `GAMES_COLOR`), state local `open` (`GAMES`, ordre ALPHABÉTIQUE, chiffres en tête ; **GROUPÉ par nombre de joueurs** via `GAME_GROUPS` (1 / 2 / 3-8 joueurs) — plus de « · N joueurs » dans les libellés : `min`/`max` par jeu, `gamePlayersLabel(g)` → suffixe « ( Nj ) » / « ( a - bj ) », les jeux multijoueurs (> 1) sont **soulignés en bleu** `GAMES_COLOR`) : 2048, Bataille navale, Démineur, Doodle Jump, Flappy Bird, Go (2 joueurs), Guitar Hero, Inposteur, Memory, Morpion (2 joueurs), Motus, Osu, Piano Tiles, Puissance 4 (2 joueurs), Snake, TierList, UnderCoverDraw (3-8 joueurs). Dernier jeu ouvert mémorisé (`spotifyplus_games_last`). `GameHost` = `switch` qui rend le composant du jeu sélectionné (repli `GameComingSoon`). Chaque jeu est un composant autonome, 100% local : **2048** (`Game2048`, fusion de tuiles, clavier+swipe via `useSwipe`), **Bataille navale** (`GameBataille` — **2 joueurs passe-l'appareil (7.2.2)**, plus de bot : **placement MANUEL case par case (7.3.0)** — chaque joueur pose SES navires (`BN_SIZES` [4,3,3,2,2]) en touchant une case (ancre haut/gauche), orientation `placeOrient` h/v basculée par 🔄, aperçu sous le curseur `hoverCell` (valide = couleur joueur, invalide = rouge), `shipCells`/`placeShip`/`undoShip`, flotte démarrée VIDE (`emptyFleet`), 🎲 « Aléatoire » (`bnPlace`) et ↺ Annuler conservés, « Flotte prête » actif seulement à 5/5 navires posés ; phases `place0→place1→pass→play→over`, `fleets[p]`/`shots[p]`, 1 tir par tour puis écran « passe l'appareil » qui masque le plateau, grille de tir = navires cachés / grille propre = navires visibles + tirs reçus, `BN_COLORS` par joueur), **Démineur** (`GameDemineur`, 9×9/10 mines, 1re case sûre, mode 🚩), **Doodle Jump** (`GameDoodle` — **7.5.1**, portage maison du jeu culte, 100 % canvas/aucun asset externe : le doodler (créature verte dessinée) **saute automatiquement** de plateforme en plateforme (gravité `D_GRAV`/rebond `D_JUMP`), on le dirige gauche/droite (**bords traversants**), la **caméra monte** avec lui (`D_CAM` = 42 % de la hauteur : scroll quand `py < D_CAM && vy < 0` → tout redescend, `climb`+=Δ) et le **score = altitude** (`Math.floor(climb/10)`, record `gameHi('doodle')`). Plateformes `doodleSpawnRow` : **vertes** fixes, **bleues** mobiles (`vx`, rebond aux murs), **marron cassantes** (`break` : on passe à travers, `p.dead`, particules — jamais 2 d'affilée via `lastType` pour garder la montée jouable), **blanches** à usage unique (`white` : `dead` après 1 rebond). **Ressorts** (`D_SPRING`) & **trampolines** (`D_TRAMPO`) = super saut ; **🎩 chapeau à hélice** (`prop`, 95 f) & **🚀 jetpack** (`jet`, 150 f) = vol (montée forcée + invincibilité). **👾 Monstres** (3 types dont OVNI, détruits par un **tir** — balles vers le haut, `shoot` throttle 8 f ; **7.5.2 : sauter DESSUS l'écrase et fait rebondir** — `vy>0 && prevFeet<=m.y+14` → `alive=false`+rebond `D_JUMP`+particules ; tout autre contact hors vol = mort) et **🕳️ trous noirs** (`holes`, mort si `hypot < r*0.6`). Difficulté progressive (`diff = score/700` → plus de plateformes mobiles/cassantes, monstres > score 60, trous > score 350). **Écart entre plateformes borné (7.5.4, « certains sauts sont impossibles »)** : portée d'un saut ≈ `D_JUMP²/(2·D_GRAV)` ≈ 146 px → l'écart généré est **clampé à `D_GAP_MAX`=120** (jamais de saut infaisable) ; un écart au-delà de `D_GAP_SAFE`=92 (saut « limite ») spawn une **dalle blanche** (rebond garanti à usage unique) **+ un monstre type 1 qui patrouille de droite à gauche** (`vx`, placé du côté opposé à la dalle pour rester évitable, destructible au tir/écrasement), `doodleSpawnRow(s, ny, risky)` ; jamais deux dalles blanches/cassantes d'affilée (toujours une plateforme solide entre deux). **Boost de rebond même case (7.5.5, porté à +3 % en 7.5.6)** : rebondir plusieurs fois de SUITE sur la MÊME plateforme (objet identité `s.lastPlat`) augmente la HAUTEUR du saut de **+3 % composé** par rebond (`s.bounceStreak`, `bv *= Math.pow(1.03, streak/2)` car h ∝ v²) ; toucher une AUTRE plateforme (ou écraser un monstre) remet `lastPlat`/`bounceStreak` à zéro → saut de base. **7.5.6 : texte d'aide au-dessus du jeu RETIRÉ** (plus de prop `hint` sur le `GameWrap` de `GameDoodle`) et **plateau agrandi** (`maxWidth:'min(100%, 62vh)'` au lieu de `maxWidth:W`=340) pour remplir davantage l'écran. **Vitesse ralentie d'~20 % (7.5.2, « trop rapide »)** : vitesses ×0.8, gravité ×0.64 → hauteur de saut (v²/2g) conservée, tout se déroule plus lentement. Boucle `requestAnimationFrame` sur une ref `g.current` (refs anti-closure, atterrissage tunnel-safe via `prevFeet`/`feet` qui encadrent `p.y`). Contrôles **100 % sur le canvas (7.5.3, plus de boutons ◀ ▶ / 🔫 Tir)** : **glisser le doigt sur le canvas** (le doodler suit la position X, `targetX`), **double-clic / double-tap** sur le plateau pour tirer (`lastTapRef`, seuil 320 ms), **flèches/ZQSD + Espace** au clavier. **Bords traversants durcis (7.5.3)** : la position wrap en modulo `s.px = ((s.px % W) + W) % W` (sortie à droite → retour à gauche et inversement) et le drag suit le **chemin le plus court** à travers la couture (`dx ±= W` si `|dx| > W/2`) → le wrap marche aussi au doigt. Dessins module-level `doodleDoodler`/`doodlePlatform`/`doodleMonster`/`doodleItem`/`doodleHole`/`doodleBg` (fond carnet crème). Record dans stats 🕹️ Jeux Hub (`GAMES_HUB_STATS`, métrique « altitude max »)), **Flappy Bird** (`GameFlappy`, canvas + `requestAnimationFrame`, tap/Espace ; **assets fidèles au jeu d'origine (7.3.8)** : `flappyDraw` dessine un ciel dégradé cyan + nuages en parallaxe, des tuyaux verts à dégradé et **lèvre** (`flappyPipe`), un sol sableux à hachures défilantes, et un **oiseau jaune** (`flappyBird` : corps/ventre/aile battante/œil/bec) qui **s'incline selon la vitesse verticale**. **Difficulté progressive (7.3.8)** : la **vitesse augmente de +0,35 px/frame toutes les 50 pts** (`spd = 2.1 + floor(score/50)*0.35`, spawn distance-based tous les 195 px pour garder l'espacement constant), et **à partir de 100 pts les tuyaux oscillent verticalement** (`moving`, `p.top = base + sin(t*0.045 + phase)*amp`, `amp` 24-50 px, clampé aux marges `minTop`/`maxDrop`) ; **`WebkitTapHighlightColor:'transparent'` + `userSelect:'none'`** sur le canvas et l'overlay (7.3.9) → supprime le flash bleu foncé du tap-highlight mobile à chaque battement d'ailes), **Go** (`GameGo`, 9×9 SVG, capture par groupe `goGroup`/`goNeighbors` + ko simple + passe), **Guitar Hero** (`GameGuitarHero`, rythme : notes qui tombent sur 4 couloirs, `GH_LANE_KEYS` D/F/J/K + boutons tactiles, zone de frappe `GH_HIT_Y`/`GH_HIT_WIN`, boucle 30 fps via `applyNotes` (ref+state = source unique anti-closure), **manche 60 s** `GH_ROUND` + **4 niveaux** `GH_LEVELS` (ex-Très facile retiré, tout décalé d'un cran) réglant vitesse/cadence + `chord` = nb de notes lâchées **simultanément** (Difficile/Extrême = 2, plusieurs tuiles en même temps ; Extrême ≈ 30 % plus rapide que Difficile), niveau persistant `spotifyplus_game_gh_level` (défaut 2, clampé 1-4), combo, note ratée ou frappe à vide → combo cassé, record `gameHi('guitar')`), **Inposteur** (`GameInposteur`, pass-and-play type Undercover, `INPOSTEUR_PAIRS`, phases setup→reveal→discuss→result), **Memory** (`GameMemory` — **2 joueurs passe-l'appareil (7.3.4)**, plus de solo : `turn`/`scores[2]`, `MEM_COLORS` par joueur, paire trouvée = 1 pt + on rejoue / raté = la main passe, vainqueur = plus de paires, égalité gérée ; plus de record `gameHi`. **Dimension choisie avant la partie (7.4.7)** : écran de choix `dim===null` → boutons `MEM_SIZES` 4×4 / 6×6 / 8×8 (`start(d)`), pool `MEMORY_EMOJIS` porté à **32 emojis** pour couvrir jusqu'à 32 paires, `memDeck(dim)` tronque au nombre de paires `dim²/2`, taille/gap de carte adaptés à `dim`, bouton « ⇦ Taille » pour revenir au choix), **Morpion** (`GameMorpion`, `MORPION_WINS` ; **variante 3 pions max** `MORPION_MAX=3` : state `order` = pions dans l'ordre de pose, au 4e pion d'un joueur son plus ancien est retiré → **jamais d'égalité, il y a toujours un vainqueur** ; **aucun avertissement visuel** du pion condamné (au joueur de s'en souvenir — demande utilisateur), plus de `draw`), **Motus** (`GameMotus` — **portage complet du projet perso Pierre-Portfolio/Motus (6.6.6)**, remplace l'ancienne version 6 lettres/35 mots : écran d'accueil du jeu (longueur 6-9 × 5 difficultés `MOTUS_DIFFS` avec jauges colorées), 850 mots à deviner + **validation dico ~125k mots** (mot inconnu → secousse `motusShake` + flash), 1re lettre offerte verrouillée, code couleur **authentique** `MOTUS_UI` (🟥 rouge carré bien placé / 🟡 ROND jaune mal placé — `borderRadius:'50%'` — / 🟦 bleu absent), `motusEval` 2 passes avec comptage des doubles, révélation case par case `motusFlip` 220 ms/case (saisie bloquée pendant `revealing`, fin de partie via `endTimer`), clavier AZERTY écran + physique (préventDefault seulement en phase play), touches sans rétrogradation. **Mode 2 joueurs (duel, 7.3.5)** : sélecteur Solo/2 joueurs sur l'écran d'accueil du jeu (`mode` 'solo'|'duo') → **même mot mystère**, **tour par tour** sur le même téléphone, chacun sa grille (`duoGuesses[2]`, `duoCur`, `duoDone[2]`), écran **« passe l'appareil »** (phase `pass` → `beginTurn` révèle la grille du joueur courant) entre chaque tour pour **ne pas voir les propositions de l'autre** ; le **1er à trouver gagne** (démarrage tiré au sort, `winner` 0/1/-1, phase `over`), match nul si les deux épuisent les 6 essais. `submit` route sur la logique duo quand `mode==='duo'`. L'entrée du menu Jeux reste « 1 joueur » (le mode se choisit dans le jeu). ⚠ Données LOURDES hors index.html : `loadMotusData()` charge lazy `vendor/motus-words.js`/`motus-dico.js` → `window.MOTUS_WORDS`/`MOTUS_DICO` ; bouton Jouer désactivé pendant le chargement, erreur réseau → ↻ Réessayer), **Osu** (`GameOsu`, cercles DOM à cliquer, combo, **manche de 60 s** `OSU_ROUND`, **4 niveaux de difficulté** `OSU_LEVELS` (ex-Très facile retiré, tout décalé d'un cran) réglant cadence d'apparition `spawn`/durée de vie `life`/taille `size` + `multi` = nb de cercles apparaissant **en même temps** (Difficile/Extrême = 2 ; Extrême ≈ 30 % plus rapide que Difficile) — niveau persistant `spotifyplus_game_osu_level` (défaut 2, clampé 1-4), sélecteur verrouillé pendant la manche, bonus de score `60+level*40` par cercle, anim `osuAppr` calée sur `life`), **Piano Tiles** (`GamePiano`, solo « ne touche pas la tuile blanche » — **7.4.8, refonte 7.4.9** : **un seul mode** (plus de `PIANO_LEVELS`/sélecteur). Au `start` le plateau est VIDE, la boucle attend `PIANO_START_DELAY=1000` ms avant de spawn la 1re rangée → **1 s sans tuile puis les tuiles arrivent d'EN HAUT** (spawn contigu `makeRow(topY - PIANO_ROWH)`). **Difficulté progressive AVEC LE TEMPS** (`elapsedRef` +33 ms/tick) : `pianoSpeedAt(ms)` 1.1 → ~3.2 en ~35 s, `pianoDoubleProbAt(ms)` = proba d'une **rangée à 2 tuiles** (2 colonnes distinctes) dès ~8 s jusqu'à 0.42. On tape la/les tuile(s) noire(s) de la rangée la plus BASSE : `tapLane` cherche une non-tapée de la colonne dans la bande `y >= maxY - PIANO_ROWH*0.5` (gère les rangées à 2 tuiles, chacune doit être tapée) ; colonne sans tuile en bas OU tuile noire passée `y>=100` = `over`. Boucle setInterval ~30 fps sur `tilesRef`/`applyTiles` (refs anti-closure) + `scoreRef`/`phaseRef`/`endRef`, `PIANO_ROWS=4` visibles. Zones de tap par colonne au-dessus des tuiles + touches D F J K, record `gameHi('piano')`), **Puissance 4** (`GamePuissance4`, `p4Win` ; **mécanique ligne complète** — après chaque `drop`, `clearFullRows(g)` reconstruit la grille sans les rangées horizontales entièrement remplies (`row.every(v=>v!==0)`) et ré-empile des rangées vides EN HAUT → les jetons au-dessus descendent d'un cran, façon Tetris. Ordre : victoire `p4Win` testée AVANT le clear (priorité à l'alignement de 4), puis re-testée après le clear si des jetons sont retombés), **Snake** (`GameSnake`, `setInterval` + refs anti-closure, dpad), **TierList** (`GameTierList` — **portage complet du projet perso Pierre-Portfolio/LocalTierList (6.6.7)**, remplace l'ancienne version à chips texte : tier list à **IMAGES** — upload multiple `tlFileToDataURL` (**7.5.0 : lit le fichier via `FileReader.readAsDataURL` et NON `URL.createObjectURL` — la CSP `img-src` autorise `data:`/`https:` mais PAS `blob:`, donc un blob URL était bloqué au chargement de l'`<img>` → `onerror` → `null`, l'import ne marchait pas**, puis canvas 150×150 cover, WebP 0.8 → repli JPEG 0.85, data URL, illisible → `null` sans bloquer), **drag & drop Pointer Events** souris+tactile (seuil `TL_DRAG_THRESHOLD` 8 px, image `position:fixed` + `pointerEvents:'none'` pendant le drag, drop calculé via `elementFromPoint`→`[data-tl-zone]` AVANT le reset du style, surlignage bleu de la zone survolée ; **7.5.2 : lâcher SUR une autre image (`[data-tl-img]` porte `data-tl-zone`/`data-tl-idx`) → `swapImg` échange leurs places** = réordonner 2 photos dans un même rang (ou permuter entre rangs/réserve) ; **7.5.5 : le CÔTÉ visé de l'image cible décide** (`getBoundingClientRect`, `frac = (x-left)/width`) — moitié gauche (`<0.34`) → `insertImg` AVANT, moitié droite (`>0.66`) → `insertImg` APRÈS (insérer ENTRE 2 images en décalant les autres, gère le recul d'index quand on retire dans la même zone avant la cible), centre → `swapImg` — ; sinon lâcher sur le fond du rang = `moveImg` (ajoute à la fin comme avant), **auto-scroll rAF près des bords du conteneur scrollable ancêtre** `tlScrollParent` — pas window), mode édition (renommer/couleur/supprimer un rang → images rendues à la réserve/ajouter/♻️ reset ; tap sur image = suppression confirm, détection `[data-tl-edit]`), rangs par défaut S/A/B/C vert→rouge `TL_DEFAULT_TIERS`. ⚠ Persistance **clé `tierlist-v1`** = MÊME clé/format que l'app autonome LocalTierList (même origine github.io → **sauvegarde partagée**, voulu) via `lsSet`, debounce 300 ms seulement pour nom/couleur, restauration défensive `tlLoad`/`tlValidImg` (préfixe `data:image/` obligatoire, regex couleur). ⚠ Ne jamais remettre width/height à '' dans `resetDragStyle` (posés par le style JSX, React ne les ré-écrit pas). PAS dans buildBackup — data URLs lourdes), **UnderCoverDraw** (`GameUnderCover` — **portage du projet perso Pierre-Portfolio/UnderCoverDraw (6.6.8)**, id `undercover` : jeu de société **dessin & déduction** « passe-l'appareil » **3-8 joueurs avec prénoms** (saisies préservées entre +/−, `Joueur N` par défaut) — la majorité reçoit un mot A, l'imposteur (créneau aléatoire dans un **ordre de passage mélangé**) un mot B au **dessin quasi identique** ; phases `home → setup → pass → reveal → draw → result`. Base = `UCD_DB` (couples_de_mots.json du projet d'origine **inline**, 20 catégories) aplatie en `UCD_PAIRS` (344 couples). Écran pass : avatar initiale + dégradé `UCD_COLORS`, points de progression ; écran reveal : **AUCUN indice de rôle** (cadre identique pour tous, règle d'origine) ; résultat : mot commun (cyan `UCD_UI.cit`) VS mot imposteur (rose `UCD_UI.imp`) + rôle de chaque joueur, 🔄 nouvelle manche (mêmes joueurs, compteur `round`). ≠ Inposteur (mots génériques décrits à l'oral, nb d'imposteurs variable) — les deux coexistent). Records dans `spotifyplus_game_hi_<id>` (`gameHi`/`setGameHi`). Helpers partagés : `gameBtn`, `GameWrap` (conseil + barre score/reset + contenu centré), `useSwipe`. Les jeux 2 joueurs se jouent sur le même téléphone. ⚠ Une valeur de prop `bar={<>…</>}` avec plusieurs `{expr && <button/>}` doit fermer CHAQUE `{}` (un `}` manquant → écran noir Babel). Basculable (`TOGGLE_MODULES` id `games`) ; **PAS de carte sur l'accueil connecté** (`HubHome`) ; **accessible depuis la page de CONNEXION** (chip « Jeux » de `HUB_FEATURES` avec `toolKey='games'` → panneau superposé, comme Note/Calculatrice, tout est local). **PAS dans buildBackup.** ⚠ Nouvel onglet mobile → bloc de rendu `{tab === 'games' && <JeuxPanel/>}` présent dans `MobileApp`.

**Santé** (ex-**Nourriture** ; libellé « Santé » partout, mais id/composant internes = `food`/`NourriturePanel`, vert-jaune `FOOD_COLOR='#9bd93f'`, entre Musique et To Do) : suivi calories façon Lifesum. Profil `spotifyplus_food_profile` (poids/taille/âge/sexe/activité/objectif) → besoin quotidien **Mifflin-St Jeor** (`foodBmr`/`foodTarget`, plancher 1200 kcal, objectifs -500/0/+300) + IMC. Repas du jour par catégorie (`FOOD_CATS` petit-déj/déjeuner/dîner/encas) : composition dans `spotifyplus_food_meals` (**LOCAL uniquement, jamais exporté**, purge > 90 j dans `saveFoodMeals`), totaux quotidiens dans `spotifyplus_food_days` (`{date: kcal}`, exportés, clé backup `food`). Jauge du jour + **historique 30 j** (`_lastNDays(30)`, 6.4.9). **Édition rétroactive 7 j (6.4.9)** : state `selDay` (défaut aujourd'hui) = jour édité par la section repas — chips des 7 derniers jours dans la section + bouton ✏️ sur les lignes récentes de l'historique ; `commitMeals` recalcule et persiste le total du jour ÉDITÉ (`selDay`) à chaque ajout/suppression. ⚠ Un jour restauré depuis un backup (total sans composition) voit son total recalculé depuis la seule composition locale si on l'édite. **Section « 📅 AUJOURD'HUI » repliable** (state local `todayOpen`, ouverte par défaut, chevron dans l'en-tête cliquable — le compteur kcal reste visible replié) + **croix × « Effacer la recherche »** à droite du bouton 🔎 (visible si input/résultats/erreur non vides — reset `name`/`results`/`searchErr`) + **texte « ⏳ Recherche des calories en cours… »** affiché pendant `searching`. **Calories auto via Open Food Facts** (base ouverte GRATUITE sans clé, CORS natif, origine `world.openfoodfacts.org` en CSP connect-src) : bouton 🔎 → `foodSearchNutrition(query)` (`cgi/search.pl`, champs `product_name(_fr)/brands/nutriments`, parsing défensif `energy-kcal_100g` → `energy-kcal` → `energy_100g`/4.184 kJ, dédoublonné, ≤ 12 résultats, cache session module-level `FOOD_SEARCH_CACHE`). **Recherche auto-résiliente (6.4.8)** : search.pl est lent/instable (le 1er appel échouait souvent → il fallait recliquer la loupe 2-3 fois) → 2 tentatives directes (timeout 12 s, pause 800 ms) puis repli proxies CORS (`corsFetchText`) avant de remonter l'erreur. Chaque résultat = `FoodResultRow` avec **portion en grammes ajustable** (défaut 100) → kcal = `round(kcal100 × g/100)` en direct, bouton + ajoute le repas (label suffixé `(N g)` si ≠ 100). **Saisie manuelle des kcal conservée en repli** (aliment absent de la base / réseau indispo → `searchErr`).

**⚠ 7.6.73 — MACROS (glucides / protéines / graisses) + SUGGESTIONS IA EN TÊTE** (demande utilisateur) :
- **Table `FOOD_MACROS`** = `[{key:'c'|'p'|'f', label, short, color, kcalPerG:4|4|9}]` (⚠ `short` explicite : « Glucides » et « Graisses » commencent par la même lettre). `foodMacroTotals(list)` → `{c,p,f, known, unknown, kcalKnown}` ; `FoodMacroBar({macros, height, labels})` = barre empilée **en part de CALORIES** (4/4/9 kcal/g — la masse seule serait trompeuse) + légende « ● Glucides 42 g · 44 % ».
- **Source des valeurs** : `foodSearchNutrition` lit en plus `carbohydrates/proteins/fat` (`*_100g`, repli sans suffixe) → `c100/p100/f100` **nullables** (beaucoup de fiches OFF ne les portent pas ; jamais 0 par défaut). `FoodResultRow` met les macros à l'échelle de la portion **en direct** (comme les kcal) et les affiche sous la ligne ; sans macros, il l'écrit au lieu d'inventer.
- **Stockage** : les macros de la portion sont écrites DANS le repas (`{c,p,f}` en g, + `ai` si l'estimation vient d'une IA) → totaux et détail par journée sans re-requête. Repas saisi à la main ou restauré d'un backup = pas de macros → compté à part (`unknown`).
- **Visible à la validation d'un ingrédient** : barre de répartition du jour sous la jauge kcal (dès qu'un aliment validé porte ses macros) + chips `Gluc./Prot./Gras` sous chaque repas de la journée.

**⚠ 7.6.77 — 3 BARRES SÉPARÉES + REPÈRES QUOTIDIENS DANS LE PROFIL** (demande utilisateur « 3 barres plutôt qu'une » + « aligner avec les calories ce qui est conseillé par jour selon la corpulence ») :
- **`foodMacroTargets(profile)`** (juste après `FOOD_MACROS`) → `{c, p, f, kcal}` en **grammes/jour** : protéines = `FOOD_PROT_PER_KG` (**1,6 g/kg** de poids, repère adulte actif) **bornées à 35 % des calories** (⚠ sans ce plafond, 1,6 g/kg appliqué au poids TOTAL d'une forte surcharge pondérale — 140 kg → 224 g = 42 % des kcal — écraserait les glucides ; avec, les glucides restent ≥ 40 %), graisses = **`FOOD_FAT_KCAL_PCT` 25 %** des calories, glucides = **le reste** → la somme retombe toujours sur `foodTarget(p)` (±1 kcal d'arrondi). `null` si le poids manque. Ordres de grandeur grand public, **pas une prescription** — rien n'est personnalisé au-delà du poids et de l'objectif calorique déjà saisis.
- **`FoodMacroBars({macros, targets})`** = **une barre par macro** sous la jauge de calories du jour, remplie vers le repère (`g / cible` + %, dépassement en rouge `#e05050`). ⚠ **Information différente de `FoodMacroBar`** (barre empilée conservée) : l'empilée dit la **RÉPARTITION** des kcal avalées, les 3 barres disent l'**AVANCEMENT** vers la cible. Sans profil (donc `targets === null`), la barre se remplit avec la part de calories pour garder du sens. `FoodMacroBar` reste utilisée par `FoodResultRow` et le dépliage d'une journée de l'historique 30 j.
- **`MON PROFIL`** : `macroTargets` rendu **sur la même ligne que `🎯 N kcal/jour`** (sous-flex `flexWrap` dans le bloc profil) → `Gluc. 269 g · Prot. 147 g · Gras 62 g` teintés par `FOOD_MACROS[].color`, `title` détaillant la règle de calcul.
- **APPUI LONG sur une ligne de l'historique 30 j** (`FOOD_HOLD_MS`=450, ref `histHold={timer,fired}`, `fired` neutralise le clic ✏️ qui suivrait) → dépliage des macros de CETTE journée, recalculées depuis `spotifyplus_food_meals` (⚠ purge à 90 j, donc au-delà : message « aucune macro pour cette journée » — un jour restauré d'un backup n'a que son total).
- **Les 2 PREMIERS résultats de recherche sont Claude et Gemini** : `foodAiSuggest(provider, q)` (prompt `foodAiPrompt`, schéma `FOOD_AI_SCHEMA` en structured outputs pour Claude, `geminiExtract` pour les deux) renvoie un résultat **de même forme** qu'une fiche OFF (`{name, kcal100, c100, p100, f100, ai, note}`) → mêmes lignes, même portion ajustable, même bouton +. Clés réutilisées : `spotifyplus_claude_key` (Revente) et `spotifyplus_gemini_key` (Frigo) — un fournisseur sans clé n'apparaît pas. Les 3 requêtes partent **en parallèle** (state `aiRows` = `loading|ok|none|err` par fournisseur, chacune s'affiche dès qu'elle répond ; une IA lente ne retarde pas Open Food Facts). Lignes **badgées ✨** (teinte par fournisseur) : une estimation IA n'est pas une mesure.

**⚠ 7.6.83 — 📷 PHOTO DU PLAT → PLAT + CALORIES + MACROS** (demande utilisateur) : bouton **📷** à droite de la loupe 🔎 (même ligne de saisie) → `<input type="file" accept="image/*" capture="environment">` caché (ouvre l'appareil photo sur mobile) → `frigoPhotoData(file)` (réutilisé du Frigo : redimensionnement ~900 px + base64 JPEG **en mémoire**) → `foodPhotoAnalyze(provider, img)` envoyé **EN PARALLÈLE** à Claude (bloc `image` + `FOOD_PHOTO_SCHEMA` en structured outputs) et Gemini (`inline_data`), avec les **mêmes clés** que les suggestions texte (`spotifyplus_claude_key` / `spotifyplus_gemini_key` ; aucune clé → une seule ligne « Analyse impossible »).
  - Le prompt (`FOOD_PHOTO_PROMPT`) demande les valeurs **POUR LA PORTION VISIBLE**, pas pour 100 g : `{plat, portion_g, kcal, glucides, proteines, graisses, aliments[], note}`. `foodPhotoAnalyze` **ramène tout à 100 g** (`kcal100`/`c100`/`p100`/`f100`) et expose `portionG` → le résultat a exactement la **forme d'une fiche Open Food Facts**, donc il est rendu par `FoodResultRow` (nouvelle prop **`defaultGrams`**, qui pré-remplit le champ grammes avec le poids estimé) et s'ajoute par le même `addFromResult` (entrée persistée avec ses macros + `ai:'<fournisseur>'`). Corriger les grammes recalcule kcal ET macros en direct. ⚠ `portion_g` absent/aberrant (< 10 g ou > 3000 g) → **250 g** par défaut plutôt qu'un rejet ; `plat` vide, `kcal` illisible ou réponse non-JSON → `null` (« aucun plat reconnu »).
  - **⚠ LA PHOTO N'EST JAMAIS CONSERVÉE** (contrainte explicite de la demande — ne pas « mettre en cache » ça un jour) : l'aperçu est un **object URL** (state `photoPrev` + ref `photoUrlRef`) **révoqué** par `setPreview`/`dropPhoto` et au démontage ; le base64 n'est référencé que par les 2 promesses en vol. Aucune écriture localStorage, aucun upload Drive, rien dans `buildBackup` — seul le repas ajouté (nom + kcal + macros) est persisté, comme une saisie normale. `clearSearch` (× de la barre de recherche) et l'ajout d'un résultat appellent `dropPhoto`.
  - State `photoRows` = `[{provider, state:'loading'|'ok'|'none'|'err', data, err}]` (même gabarit qu'`aiRows`), bloc rendu **AU-DESSUS** des suggestions texte. Aucune origine à ajouter à la CSP (Anthropic et Gemini y sont déjà).
  - ⚠ Vérifié dans le navigateur (Playwright, `fetch` stubbé) : les deux requêtes partent bien avec l'image en JPEG base64, les 2 lignes s'affichent avec la portion estimée pré-remplie, l'ajout persiste macros + `ai`, la photo disparaît et **aucune data URL d'image n'atterrit en localStorage**. Le comportement des VRAIS modèles sur une vraie assiette n'est évidemment pas testable ici.

**⚖️ POIDS — journal de pesées (`PoidsSection`, 7.6.37)** : collapse `StatsCollapse` rendu dans `NourriturePanel` **juste sous « 👤 MON PROFIL »**. Persistance 100 % locale `spotifyplus_food_weights` (`WEIGHT_KEY`, `loadWeights`/`saveWeights`, tri chronologique `weightSort`, cap `WEIGHT_MAX=2000`). Une pesée = `{ id, date:'YYYY-MM-DD', poids, imc, graisse, eau, metabolisme, viscerale, muscle, proteine, osseuse, ageCorp }` — **seuls `date` et `poids` sont obligatoires**, les 9 autres (mesures d'une balance à impédancemétrie) sont facultatifs. Table `WEIGHT_FIELDS` = `{key, label, unit, dec, icon, color, lower}` (ordre du formulaire, de la fiche ET des graphiques ; `lower:true` = une baisse est une amélioration → delta vert). Helpers module-level : `weightNum` (accepte la **virgule décimale FR**), `weightFmt`, `weightSort`, `weightNormalize` (assainit une pesée importée : date ISO + poids > 0 obligatoires, sinon rejetée), `weightMerge(local, incoming)` (le LOCAL gagne à id égal, doublons `date|poids` ignorés), `weightSeries(list,key)`, `weightRefBefore(series,days)`, `weightDelta(cur,prev,field)`, `weightDateFr`. UI : carte « dernière pesée » (poids + delta vs précédente + total), **+ Nouvelle pesée** (formulaire grille auto-fill, ✏️ édite en place, × supprime avec `confirm`), historique plus récent en tête avec toutes les mesures en chips. ⚠ **Les inputs du formulaire sont en `type="text" inputMode="decimal"`, PAS `type="number"`** : un input number REJETTE la virgule du clavier FR (`value` repasse à `''` → saisie perdue en silence) ; `weightNum` parse « 81,6 ». **IMC auto** : non saisi + taille du profil connue → `poids / (taille/100)²`. **Le poids du profil suit la pesée la plus récente** (`onProfileWeight` → `syncProfileWeight` dans `NourriturePanel`, met aussi à jour le formulaire de profil) → le métabolisme de base et l'objectif kcal restent justes. **Export / import** : boutons ⬇/⬆ de la section — l'export écrit un **buildBackup PARTIEL** `{app, version, exported_at, scope:['food'], food:{weights}}` (`spotifyplus-poids-<date>.json`) donc relisible aussi par le ↥ Restaurer d'À propos ; l'import accepte un tableau brut, `{weights:[…]}` ou `{food:{weights:[…]}}` et fusionne via `weightMerge` (ré-import idempotent). Les pesées voyagent aussi dans le backup global (clé `food.weights`, export partiel « Santé », Drive/Dropbox/pCloud/MEGA). Graphiques dans **Stats → 🩺 SANTÉ → ⚖️ POIDS**.

**To do** (`TodoPanel`, vert) : `spotifyplus_todos`, buckets `TODO_BUCKETS` (Quotidien/Aujourd'hui/semaine/mois/année/un jour/**Activité avec Cécile** — 7e rubrique ajoutée en 7.6.53, en DERNIER après « À faire un jour », id `cecile`, icône 💞) en carrousel. ⭐ favorites. Quotidien : × = « valider pour aujourd'hui » (`doneDate`, repart le lendemain). Complétion → `recordTodoDone` (`spotifyplus_todos_done`, → `todoDoneStats()`). **⚠ 7.6.69 — dans `cecile`, le × NE SUPPRIME PLUS** (demande utilisateur) : `isCecile` → `toggleDone(id)` pose `done:true` + `doneAt` sur la tâche (jamais de `remove`), qui reste dans la liste **grisée (opacité 0.5, texte `C.muted`) + barrée** et est **reléguée en bas** (`items` re-trié `!done` puis `done`, ordre de saisie préservé dans chaque groupe) ; le bouton affiche alors ✓ et **reste cliquable** pour rouvrir l'activité (contrairement au ✓ figé du Quotidien). Le sous-titre du carrousel ajoute « · N faite(s) » (`doneCount`) et une ligne d'aide dédiée s'affiche sous la liste. Le drapeau **`counted`** garantit qu'un aller-retour ✓/↺ ne compte **qu'une fois** dans `recordTodoDone`. `move()` efface `done` quand la tâche quitte `cecile` (ailleurs le × supprime toujours). Variable de rendu partagée `faded = doneToday || doneCecile` (grisé + barré des deux cas).

**Maps** (`MapsPanel`, jaune) : trajets Google Timeline importés (localStorage `spotifyplus_maps`, cache `_mapsCache`). `parseGoogleTimeline` (2 formats : `semanticSegments` + `timelineObjects`). `mapsMergeImport` (jours remplacés). Carte Leaflet lazy (`MapsMap`, tuiles OSM assombries, marqueurs `circleMarker`, `mapsEsc` échappe le HTML). ⚠ L'app ne trace JAMAIS la position (import manuel officiel).

**Mot de Passe** (`GestionMdpPanel`, ambre) : coffre chiffré **AES-GCM 256 + PBKDF2 600k** (Web Crypto native). localStorage `spotifyplus_mdp_vault` = `{v, salt, iv, ct}` seulement, mot de passe maître jamais stocké. Phases setup/locked/unlocked. Session `_mdpSession` (effacée au reload), verrouillage auto inactivité `MDP_IDLE_MS=10min` (timer module-level). `safeHref` assainit les URL (bloque `javascript:`). ⚠ Mot de passe oublié = irrécupérable (bouton reset repart en setup). **Contenu déchiffré = `{ entries:[{id,identifiant,mdp,url,comment,tags:[tagId]}], tags:[{id,name,color}] }`** (7.6.1) — `mdpNormalizeVault(data)` migre un ANCIEN coffre qui chiffrait directement le tableau d'entrées (`Array` → `{entries, tags:[]}`), re-chiffré au 1er déverrouillage. `persist(nextEntries, nextTags)` chiffre l'objet complet (args omis = valeur courante). Tout vit dans le blob chiffré → **automatiquement dans buildBackup (`mdp_vault`), l'export/import du panneau, et la synchro Drive/Dropbox** (rien à ajouter). **7.6.1** : (1) **bouton ✏️ d'édition** par entrée (formulaire inline `editForm`/`editId`, `startEdit`/`saveEdit`/`cancelEdit`) ; (2) **réordonnancement drag & drop** via poignée ⠿ (`startDrag`, pointer events souris+tactile, `elementFromPoint`→`[data-mdp-row]`, ordre de travail dans une closure `cur`, commit `persist(cur)` au `pointerup`) — actif seulement SANS filtre/recherche/édition (`reorderable`) ; (3) **tags** : palette cyclique `MDP_TAG_COLORS`, barre de filtre en haut (chip « Tous » + un chip par tag avec × de suppression + **petit bouton `+`** ouvrant un champ de création `createTag`), `deleteTag` retire aussi le tag de toutes les entrées, `tagPicker(selected, setF)` (chips cliquables dans les formulaires ajout/édition), `tagChip` (affichage sous chaque entrée), filtre combiné avec la recherche. **7.6.34** : (1) **confirmation avant suppression d'une entrée** (`remove(id)` → `confirm('Supprimer « <identifiant/url/commentaire> » ?')`, la suppression étant définitive et sans corbeille) ; (2) **liste GROUPÉE en collapses** (`StatsCollapse`, `defaultOpen`) — **un collapse par tag** (titre = nom du tag + nombre, couleur du tag) et un collapse **« Reste »** EN DERNIER pour les entrées sans tag (ou dont les tags ont été supprimés) ; une entrée multi-taguée apparaît dans CHACUN de ses collapses ; les collapses vides sont masqués (donc un filtre de tag actif n'affiche que le sien) ; le rendu d'une ligne est factorisé dans `renderEntry(e)` ; (3) **ordre des collapses choisi par l'utilisateur** : chaque tag porte un champ `order` (numérique) saisi dans un **2e champ « Ordre »** du formulaire de création (bouton `+`), défaut = max+1. Helpers module-level `mdpTagOrd(t, i)` (tag sans `order` → rejeté après tous les autres via `MDP_TAG_ORD_END=1e9` + index de création) et `mdpSortTags(tags)` (ordre puis nom) ; `sortedTags` sert à la barre de filtre, au `tagPicker` ET aux collapses. (4) **Édition d'un tag** (nom + ordre) par **double-clic** (`onDoubleClick`) ou **appui long 3 s** (`MDP_TAG_HOLD_MS`, `holdStart`/`holdClear` sur `onPointerDown`/`Up`/`Leave`/`Cancel`, ref `holdRef={timer,fired}` ; `fired` neutralise le clic qui suit l'appui long via `clickTagFilter`, sinon le filtre basculerait aussi) → formulaire inline `editTagId`/`tagEdit` (`openTagEdit`/`saveTagEdit`, Entrée valide, Échap annule). `order` vit dans le blob chiffré → suivi automatiquement par backup/export/Drive/Dropbox.

**« Version pour ordinateur » Chrome Android** : réglage NAVIGATEUR, **impossible à décocher depuis une page web** (aucune API). Mitigations : `useIsMobile` force le layout mobile malgré le viewport desktop (garde `touchSmall` + hint OAuth `spotifyplus_layout_hint`) + **bandeau d'aide ambre dans `MobileApp`** (détection tactile petit écran physique + `innerWidth ≥ 900` → explique menu ⋮ → décocher ; fermable, state `deskHintOff`). Ne pas promettre de le désactiver automatiquement.

**Finance — en-tête** : `FinancePanel` rend LUI-MÊME son titre « FINANCE » + les 4 boutons de mode (Light/Full/Calculatrice/Mes Actifs, **sans icône 🧮**) sur la même ligne (gabarit Météo/Alertes) — `selfHeading:true` dans `SECTION.finance` (desktop) et plus de heading dédié dans `MobileApp`.

**Remember/Alertes** (`RappelPanel`, titre indigo `#6f7bf5`, contenu violet `RAPPEL_PURPLE`) : **🗓 CALENDRIER MENSUEL (7.6.70)** — bouton 🗓 **à GAUCHE de « 📅 Dates à venir »** (même ligne que le titre ALERTES, state `showCal`, replié par défaut) → rend `RappelCalendar` ENTRE la barre de titre et le bandeau des rappels échus, sans quitter la vue courante (le bouton « Dates à venir » reste indépendant). Helpers module-level : `RAPPEL_CAL_DOW` (semaine **lundi→dimanche**), `rappelDayKey(d)` (clé locale `YYYY-MM-DD`, jamais `toISOString()` qui décalerait d'un jour en UTC), `rappelCalCells(y, m)` (cellules `null` de remplissage jusqu'au 1er — `offset = (getDay()+6)%7` — puis un objet par jour). `RappelCalendar({events})` : `byDate` = index date→alertes, navigation ‹ / › + bouton « Auj. », grille 7 colonnes de cases carrées (`aspectRatio`) portant le n° du jour + **un point par alerte** (max 3) et le `title` = libellés ; **aujourd'hui** cerclé d'orange, jour **sélectionné** en violet plein ; sous la grille, le **détail du jour cliqué** (libellé + pastille ↻ récurrence + heure + badge `rappelBadge`) ou « Aucune alerte ce jour-là ». La source est **`allDated`** — l'ancien `upcoming` a été scindé : `allDated` garde TOUTES les dates (passées comprises, donc le calendrier montre les retards) et `upcoming = allDated.filter(days >= 0)` alimente la liste comme avant. 100 % local, aucune requête. Reste du panneau : rubriques fixes `RAPPEL_SECTIONS` + items `RAPPEL_EXTRA` (League of Legend, **Epic Games**) + sections auto Frigo/Steam + **« 🔔 Mes rappels » persos (`spotifyplus_remember_custom`) EN DERNIÈRE POSITION (7.3.7)**. **ORDRE DES COLLAPSES (7.6.54, demande utilisateur)** : Administratif · **Anniversaire** (remonté juste sous Administratif) · Logement · Véhicule · Médecin · Travail · **🛰️ Dev** (ex-« Veilles Techno », RENOMMÉ + déplacé sous Travail ; contient 🤖 Dependabot) · Finance · Sport · **🧊 Frigo** (`FrigoAlertSection`, remonté juste sous Sport) · **🎮 Jeux** (contient **🕹️ Steam** — `SteamAlertSection`, placée AU-DESSUS de — **🏁 Speedrun**) · Autre · 🔔 Mes rappels. ⚠ Frigo et Steam ne sont plus rendus en bas de panneau mais **dans le flux des rubriques** (conditions `sec.key === 'sport'` / à l'intérieur du collapse Jeux). Dates `spotifyplus_rappels` + heures optionnelles `spotifyplus_rappels_time` (input heure masqué, affiché au clic sur le libellé). `notifyDueReminders()` (J-30/J-7/J-1/J0 + retard, 1×/jour). `syncRemembersToTodos()` (≤ 3 j → bucket Aujourd'hui). **Rappels RÉCURRENTS (7.6.39)** : table module-level `RAPPEL_RECUR` = `{ "<section>.<item>": N mois }` (aujourd'hui `anniversaire.couple_cecile` = **6 mois**) → la date saisie reste la date d'**ORIGINE** (seule stockée/modifiable dans `spotifyplus_rappels`, donc rien à changer au backup) et `rappelNextDate(key, date)` calcule la **PROCHAINE occurrence ≥ aujourd'hui** (`rappelAddMonths` borne le jour au dernier jour du mois cible : 31 août + 6 mois → 28/29 févr., contrairement à `setMonth` qui déborderait sur mars ; nb de périodes calculé directement, la boucle finale (garde 24) ne rattrape que le bornage du jour). Branché dans `rememberAllWithDelta()` (donc notifications, To do auto, stats, « PROCHAINES ÉCHÉANCES » de Stats), le bandeau `due` et la vue « 📅 Dates à venir » de `RappelPanel` → un rappel récurrent **resonne à chaque échéance et ne tombe JAMAIS « en retard »**. `rappelRecurLabel(key)` rend la pastille « ↻ 6 mois » (+ date de la prochaine occurrence dans la liste des rubriques, `title` au survol). ⚠ Le champ `<input type="date">` continue d'afficher/écrire la date d'origine, jamais l'occurrence calculée. **⚠ Epic Games = rappel NORMAL depuis 7.6.57 (demande utilisateur « remets-le comme tous les autres »)** : toute la mécanique HEBDOMADAIRE introduite en 7.6.46/7.6.47 pour cet item a été **SUPPRIMÉE** — plus de `RAPPEL_WEEKLY`, `RAPPEL_DOW_FR`, `rappelWeeklyDate()`, `todoHasRemember()`, ni de drapeau `weekly` sur les entrées de `rememberAllWithDelta()`. `jeux.epicgames` reste un item de `RAPPEL_EXTRA` rendu **exactement comme les autres** dans le collapse « 🎮 Jeux » (libellé cliquable pour l'heure optionnelle, badge de jours restants, `<input type="date">`), sa date vit dans `spotifyplus_rappels` (donc sauvegardée/restaurée comme les autres), et il suit les règles communes : notifications J-30/J-7/J-1/J0 + retard, ajout auto au To do à ≤ 3 j, tombe « en retard » si la date est passée. `rappelTimeOf(key, times)` ne renvoie plus que l'heure saisie ; `rappelRecurLabel` ne connaît plus que `RAPPEL_RECUR` (mensuel). **Veilles Techno** : 🤖 Dependabot (`DependabotSection`, token GitHub PAT `ghToken()`, `ghFetchDependabot` GraphQL, fetch direct api.github.com). **Jeux** : 🏁 Speedrun (`SpeedrunSection`, pseudo speedrun.com, endpoints publics). **Section Steam auto (`SteamAlertSection`, 7.6.30)** : placée juste APRÈS `FrigoAlertSection` (donc avant « 🔔 Mes rappels »), lecture seule, dérive de `steamPromoItems()` = jeux de la wishlist à **≥ `STEAM_PROMO_PCT` (80) %** ; rafraîchit le cache au mount via `steamFetchWishlist(false)` (no-op si < 30 min). SteamID absent → message renvoyant vers Bon Plan. Bandeaux d'alerte en tête.

**League of Legends** (`LolStatsSection` dans 🎮 JEUX de Stats) — **migration 6.0.7 : API interne op.gg SANS CLÉ en source primaire** (demande utilisateur, la clé Riot Development expirait sans arrêt). Endpoints (non documentés, relevés dans OPGG.py, parsing défensif) : recherche `lol-api-summoner.op.gg/api/v3/{region}/summoners?riot_id=`, profil `/api/{region}/summoners/{id}/summary` (league_stats), partie en cours `lol-api-summoner.op.gg/api/{region}/spectates/{id}` (**corrigé 7.2.8** : les anciennes URLs étaient MORTES → parties jamais détectées ; `lol-web-api.op.gg/.../bypass/spectates` injoignable via proxies + `.../summoners/{id}/spectates` = 404 « route could not be found ». Le bon endpoint est sur l'hôte à CORS ouvert `lol-api-summoner.op.gg` : hors partie → 404 JSON « data not found » = pas en partie). `opggGet(url, opts)` = fetch direct (CORS op.gg) puis proxies CORS, **timeout 8 s/tentative** (AbortController — sans quoi lol-web-api bloquait 30 s+) ; `opts` optionnel `{directOnly, timeout}` (repli borné : pas de cascade de proxies). **Détection partie durcie (7.4.2)** : `opggSpectate` essaie DÉSORMAIS plusieurs endpoints (lol-api-summoner en cascade complète, puis 2 formes lol-web-api en fetch **direct court** `directOnly`+timeout 5 s ; tous en `noStore` → `{cache:'no-store'}` pour toujours voir la partie ACTUELLE, pas une version cachée) et ne conclut « pas en partie » (`null`) qu'après avoir vu un JSON SANS participants sur TOUS — un endpoint périmé qui répond sans participants ne fige plus la détection ; parsing des participants tolérant via helpers module-level `opggSpectateParts` (cherche `participants` à plusieurs niveaux + `teams[].participants`) et `opggSpectateNormalize` (champs alternatifs `summoner_id`/`champion_id`/`team_key`|`team_id`/`created_at`…). `{error:true}` seulement si AUCUN JSON reçu (→ repli Riot). ⚠ op.gg est INJOIGNABLE depuis l'environnement d'agent (proxy 403) → ce durcissement n'a pas pu être testé en direct. **Bouton 🩺 « Diag partie » (7.4.4)** dans la barre de la section LoL (visible si `data.source==='opgg'`) → `opggSpectateDiagnose(platform, summonerId)` interroge chaque endpoint spectate (direct puis proxy codetabs de secours) et affiche la réponse BRUTE (statut HTTP, taille, extrait 500 car, `parts` = participants normalisés) dans une zone monospace scrollable — sert à identifier EN PARTIE l'endpoint valide + la forme réelle du payload quand la détection échoue (à copier-coller). **Bouton « ↻ Actualiser » (7.4.5)** : `refresh` re-teste TOUJOURS le spectate (partie en cours re-fetchée à chaque clic, `no-store`) — et si le profil op.gg hoquette (`p.error`) mais qu'un profil est déjà chargé, on GARDE l'affichage et on re-teste quand même le spectate avec ce profil (`prof = p.error ? data : p`) : un raté réseau sur le profil ne bloque plus la re-détection d'un changement de partie ; erreur bloquante seulement si AUCUN profil (`!prof`). Hôtes en CSP connect-src. Tout est **normalisé au format Riot** (`opggLeagueToEntries` → queueType/tier/rank/LP/wins/losses ; live → `{queueLabel, mins, participants:[{puuid, riotId, championId, teamId}]}` où `puuid` = summoner_id op.gg, clé opaque) → UI inchangée. Mapping `LOL_OPGG_REGION` (euw1→euw…). **Repli automatique Riot** si op.gg échoue ET clé enregistrée (`spotifyplus_lol_key`, OPTIONNELLE désormais — Personal API Key permanente recommandée) : `lolRiotProfile`/`lolRiotLive` (spectator-v5) via `lolApiGet` (proxies). `lolFetchRanks(profile, live)` : rangs des 10 joueurs (summary op.gg ou league-v4 selon `live.source`, cache 15 min par id, 3 workers). Cache profil exige `cache.source` (invalide les caches pré-migration). Config `spotifyplus_lol_cfg` `{riotId, platform}` ; maîtrises seulement en source riot (op.gg → masquées). **UI (6.2.0)** : carte **🏅 « MEILLEURS JOUEURS · Top ≈ X % »** — helper `topCard(entry, tag)` (6.4.0) — **désormais SOLO/DUO uniquement (7.3.3)** : `topCard(solo,'SOLO/DUO')` entre le pseudo et CLASSÉE SOLO/DUO (la carte `topCard(flex,'FLEX')` a été RETIRÉE à la demande de l'utilisateur ; le classement flex reste via `rankRow(flex,'CLASSÉE FLEXIBLE')`) — estimation par palier via la table statique `LOL_TOP_PCT` + `lolTopPercent(entry)` (distribution soloQ publiée — même table utilisée pour la flex, aucune distribution flex keyless). **Collapse « 🏆 CLASSEMENT » (6.5.3)** : la ligne « classement : ( rang / total ) » est RETIRÉE des tuiles Top % — tous les classements vivent dans un `StatsCollapse` dédié (`classementCollapse()`, sous la bannière partie en cours) au format « rang / total » : Solo/Duo & Flexible = `lolEstimatedRank(entry)` / `LOL_RANKED_POP` (≈ 2 500 000, préfixe ≈) ; Ladder op.gg (LP) / Champions / Niveau / Maîtrises = données réelles si le payload op.gg les expose — helper module-level `opggProfileRanks(s)` (parsing tolérant `ladder_rank`/`level_rank`/`champion_rank`/`mastery_rank`… sur le summary ET la fiche de recherche, fusion dans `profile.ranks`), sinon « — / — » (rien n'est inventé ; ladder_rank est le seul régulièrement présent). Ancien cache profil sans `ranks` → « — / — » jusqu'au prochain refresh ; **CARTE « PARTIE EN COURS » refondue façon tableau op.gg (7.6.64, capture fournie par l'utilisateur)** : `liveCard` rend un bandeau d'en-tête (file · pastille LIVE clignotante · `mapLabel` si op.gg le donne · chrono) puis **UN BLOC PAR ÉQUIPE** — `LOL_TEAM_BLUE='#5383e8'` / `LOL_TEAM_RED='#e84057'`, **bleue = team 100, rouge = team 200 comme en jeu** (⚠ surtout pas « bleue = mon équipe » : on peut jouer côté rouge, le libellé « (LA TIENNE) » signale l'équipe du joueur) — chacun avec en-tête teinté portant la **MOYENNE DE TIER** et les **BANS**, puis une ligne-tableau par joueur : icône champion, pseudo + `#tag`, champion + niveau, rang « Tier Division (N LP) », **barre de winrate** + % coloré + nb de parties. Helpers module-level testables : `lolTierValue(entry)` (palier×4 + division, I = meilleur), `lolTierFromValue(v)` (retour au libellé FR, pas de division au-delà de Diamant), `lolAvgTier(entries)` (ignore les non classés, `null` si personne n'est classé) ; `opggSpectateNormalize` expose désormais `mapLabel`, `bans` (via **`opggSpectateBans(d)`** — accepte `banned_champions` à la racine ET `teams[].banned_champions`, ids illisibles jetés) et, par joueur, `level`/`position` ; `lolRiotLive` remplit `bans` depuis `bannedChampions` (championId -1 ignoré) et `level` depuis `summonerLevel`. ⚠ **NON repris faute de données keyless** : icônes de sorts d'invocateur et de runes, colonnes « information de champion » et KDA de saison (op.gg les calcule côté serveur) — rien n'est simulé, une note le dit sous la carte. ⚠ op.gg étant injoignable depuis l'environnement d'agent, la forme réelle des champs `mapLabel`/`bans`/`level` n'a PAS pu être vérifiée : tout est optionnel et l'absence ne casse rien. **bannière colorée « 🎮 Aucune partie en cours »** (fond/bordure `LOL_COLOR`) déplacée SOUS le classement flexible (remplace l'ancien texte gris italique au-dessus des classements) ; **pendant le fetch spectate** (état `liveSearching`, posé autour du `lolFetchLive` de `refresh`), bannière pointillée « 🔎 Recherche d'une partie en cours… » à la place (la bannière « Aucune partie » attend `!liveSearching`).

**GitHub/Speedrun stats** (`GithubStatsSection`/`SpeedrunStatsSection`) : réutilisent les accès de Veilles Techno. `ghFetchProfile` (1 GraphQL viewer, streak), `srFetchStats` (personal-bests ; cache versionné `SR_STATS_V`, expose `gameList` = jeux courus + nb de PB). **Speedrun en 3 sous-collapses (6.3.4)** : 🏆 Podium (records du monde, podiums, personal bests) · 🎮 Jeux (jeu favori + liste `gameList`) · ℹ️ Autre info (jeux courus PUIS temps cumulé — ordre 6.4.2). PAS dans buildBackup. Ordre GitHub (sans carte Dépôts, 6.5.2) : Abonnés → Étoiles reçues → Dépôt star → Langage préféré → Contributions → Commits → Forks → Série active (en dernier, sous Forks — demande utilisateur).

**Steam — stats** (`SteamStatsSection`, collapse 🕹️ STEAM dans 🎮 JEUX, **sous chess.com** — **7.6.31**) : mes statistiques Steam. ⚠ Contrairement à la wishlist (keyless), ces endpoints EXIGENT la **clé Web API** (`spotifyplus_steam_key`) EN PLUS du SteamID64 → sinon `SteamConfigForm` (le même que Bon Plan) est affiché. `steamFetchStats(force)` interroge **4 endpoints en parallèle** (`ISteamUser/GetPlayerSummaries/v2`, `IPlayerService/GetOwnedGames/v1?include_appinfo=1&include_played_free_games=1`, `IPlayerService/GetSteamLevel/v1`, `IPlayerService/GetRecentlyPlayedGames/v1`) via `steamGetJson`, cache `spotifyplus_steam_stats` TTL 30 min ; erreurs `CONFIG`/`KEY` (401/403)/`PRIVATE` (200 mais enveloppe vide = profil privé)/`NET`. Cartes `statCardEl` : Niveau Steam · Jeux possédés (+ nb lancés) · Temps de jeu total (`steamFmtPlaytime`, sous-titre en jours) · Top 3 des jeux les plus joués 🏆🥈🥉 · Jamais lancés · Ces 2 semaines · **Liste de souhaits** (lue dans le cache wishlist de Bon Plan, AUCUNE requête en plus, sous-titre = nb à ≥ 80 %). Boutons ⚙ (config) et ↻. **PAS dans buildBackup** (cache re-fetchable, comme la wishlist).

**Counter-Strike 2** (`CounterStrikeStatsSection`, collapse 🔫 COUNTER-STRIKE dans 🎮 JEUX de Stats, **entre Hearthstone et Jeux Hub** — **7.6.43**) : stats CS2 **à vie** (éliminations + morts + ratio K/D, % de tirs dans la tête, précision = tirs touchés/tirés, parties gagnées + winrate, temps de jeu + rounds, MVP, **arme favorite**, bombes posées/désamorcées). **AUCUNE configuration propre** : réutilise le SteamID64 + la clé Web API de la section Steam (`steamId()`/`steamKey()`, formulaire partagé `SteamConfigForm`) et `steamGetJson` (fetch direct borné 8 s puis repli proxies CORS). Endpoint `ISteamUserStats/GetUserStatsForGame/v2` sur `CS_APPID` 730 : Valve y publie les compteurs à vie, dont un `total_kills_<arme>` par arme → table `CS_WEAPONS` (libellés FR, couteau inclus) et `topWeapon` = plus gros compteur. `csFetchStats(force)` : cache `spotifyplus_cs` TTL 30 min, erreurs `CONFIG`/`KEY` (401/403)/`PRIVATE` (200 mais `stats` vide = **« Détails du jeu » non public** ou jeu jamais lancé)/`NET` ; ⚠ `CONFIG` ne pose **PAS** de message d'erreur (le rendu rebascule seul sur le formulaire — sinon « Oublier » afficherait une fausse erreur réseau). Aucune origine à ajouter (api.steampowered.com déjà en CSP). **PAS dans buildBackup** (cache re-fetchable, comme la wishlist et les stats Steam). ⚠ **Steam est INJOIGNABLE depuis l'environnement d'agent (proxy 403)** : parsing défensif, forme réelle du payload non vérifiée en direct.

**Hearthstone** (`HearthstoneStatsSection`, collapse 🔥 HEARTHSTONE dans 🎮 JEUX de Stats, **entre Clash Royale et Jeux Hub** — **7.6.42**) : ⚠ **Hearthstone n'a AUCUNE API de stats de JOUEUR** — ni keyless ni officielle. Contrairement à WoW, l'API Blizzard ne publie que des **données de jeu** (cartes, dos de cartes, codes de deck, métadonnées) et **aucun endpoint de profil/collection** : il n'y a donc rien de personnel à afficher, même au prix d'un OAuth. La section montre les stats de la **BASE DE CARTES** via **HearthstoneJSON** (`HS_URL` = `api.hearthstonejson.com/v1/latest/frFR/cards.collectible.json`, communautaire, **SANS CLÉ**) — strictement mieux que l'API Blizzard ici, qui exigerait un `client_secret` impossible à cacher dans du code public. Un bandeau explicatif le dit à l'utilisateur en tête de section. `hsFetchStats(force)` : fetch direct (timeout 15 s) puis repli `corsFetchText`, **ne met en cache QUE les stats calculées** (`spotifyplus_hs` — les ~4 000 cartes brutes rempliraient le localStorage), **TTL 7 j** (la base ne bouge qu'à chaque extension). Cartes `statCardEl` : total collectionnable + nb d'extensions · répartition par type · légendaires · classe la plus fournie · coût moyen en mana (+ carte la plus chère), puis une **courbe de mana** (barres maison 0→7+). Tables de traduction `HS_CLASS_FR`/`HS_RARITY_FR`/`HS_TYPE_FR`. `api.hearthstonejson.com` en connect-src + entrée `API_LIST` (cat. Jeux (stats), sans 🔑). **PAS dans buildBackup**. ⚠ **Injoignable depuis l'environnement d'agent** : parsing défensif, forme réelle du JSON non vérifiée en direct.

**Clash Royale** (`ClashRoyaleStatsSection`, collapse 👑 CLASH ROYALE dans 🎮 JEUX de Stats, **entre Steam et Jeux Hub** — **7.6.41**) : stats de joueur (trophées + record + arène, niveau roi, victoires/défaites + % de victoires, victoires 3 couronnes, bilan des dernières parties via `/battlelog`, cartes débloquées + carte favorite, clan + dons, jours de guerre gagnés). ⚠ **Aucune API keyless n'existe** pour les stats perso : l'API officielle Supercell exige un token lié à une **IP FIXE**, qu'un navigateur n'a pas → on passe par le **proxy RoyaleAPI** (`CR_PROXY` = `proxy.royaleapi.dev/v1`, contournement documenté officiellement, renvoie les en-têtes CORS) et le token est créé en whitelistant **l'IP DU PROXY** `CR_WHITELIST_IP` (45.79.218.79), pas celle de l'utilisateur — la carte de config explique la marche à suivre en 3 étapes. Config = `spotifyplus_cr_tag` + `spotifyplus_cr_key` (helpers `crTag()`/`crKey()`, éditables aussi dans le module **API** via `API_KEY_LS.clashroyale`, forme TABLEAU comme `steam_api`) ; `crNormTag(t)` retire le `#`, met en MAJUSCULES et **normalise O → 0** (l'alphabet Supercell n'a pas de O). `crFetchStats(force)` : cache `spotifyplus_cr` TTL 30 min, erreurs `CONFIG`/`KEY` (401/403 = token invalide ou mauvaise IP)/`TAG` (404)/`NET` ; le `/battlelog` est **best effort** (son échec ne masque pas le profil). `proxy.royaleapi.dev` en connect-src + entrée `API_LIST` (cat. Jeux (stats), 🔑). **PAS dans buildBackup** (cache re-fetchable, comme Steam/chess.com). ⚠ **Injoignable depuis l'environnement d'agent** (proxy réseau) : parsing défensif, forme réelle des réponses non vérifiée en direct.

**chess.com** (`ChessStatsSection`, collapse ♟️ CHESS.COM dans 🎮 JEUX, **sous LoL** — ordre Jeux : Speedrun → LoL → chess.com) : **API publique keyless à CORS natif** `api.chess.com/pub/player/{user}` + `/stats` → fetch direct (origine en connect-src, avatars via le `https:` d'img-src). Config = pseudo seul (`spotifyplus_chess_user`, helper `chessUser()`), cache `spotifyplus_chess` TTL 30 min (`chessFetchStats(force)`, re-fetchable → PAS dans buildBackup). Cartes `statCardEl` : élo par cadence (rapide/blitz/bullet/daily — `last.rating`, sub = record + V-D-N), puzzles (`tactics.highest`), puzzle rush. Erreurs `CONFIG`/`USER` (404)/`NET`. Couleur `CHESS_COLOR = '#81b64c'`.

---

## Bugs connus / fixes

- **`releaseInRange(dateStr, cutoff, ceiling)`** : `release_date` peut être year/month/day. `new Date("2026")`→1er janv → une sortie datée à l'année seule passait sous le cutoff. Traite la date comme période `[début, fin]` et garde si chevauchement. Utilisé aux 2 sites de filtrage.
- **Auto-avance restart** : effet « redémarrage » exige `prevNowRef.current.uri === now.uri` (sinon changement de titre/seek pris pour rebouclage).
- **Écran noir = erreur Babel** : ex. redéclaration `days` (paramètre + `const days`). Renommer. Aucun filet de sécurité.
- **Reset mensuel LOCAL** : `getFullYear()`/`getMonth()`, pas `toISOString()` (UTC → bascule à ~01h FR).
- **Service worker** : ne cache que `res.ok` (sinon 404 écrase l'app) + fetch app shell `{cache:'no-store'}` (sinon `max-age=600` fige la version). Ne jamais revenir en cache-first pour l'app shell.
- **TV Time loupe** : `onClick={() => search()}` (pas `search`, sinon l'event devient le `type`).
- **FeedItem clé** : `key={item.id}`, jamais `id+i` (remount + `React.memo` inopérant).
- **`_effUri`** : voir Écoute (curseur optimiste clic suivant rapide).
- **PlayerBar** : lire le label dans `filteredFeed[currentIndex]`, pas `feed[currentIndex]`.
- **SeekBar** : listeners `mousemove`/`mouseup` dans `onMouseDown`, pas useEffect (sinon clic rapide raté).
- **sql.js** : `_db.run(sql, params)` = tableau positionnel pour les `?`, jamais un objet.
- **Babel hoisting** : tout hook avec tableau de deps (et tout `useMemo`) doit être déclaré APRÈS les variables qu'il liste (sinon deps `[undefined]` figées, `useMemo` lit `undefined`).
- **`apiGet`** : `return null` sur 204 (sinon `.json()` throw) ; `{rate_limited:true}` (pas null) quand le guard bloque.
- **Coffre MDP** : `safeHref` avant `<a href>` (CSP `unsafe-inline` → `javascript:` s'exécuterait).

## Limites
iOS/Safari : IndexedDB ~50 Mo, purge auto si app non ouverte 7 j → privilégier Android. sql.js charge toute la DB en RAM → purger régulièrement. Pas de sync multi-devices (hors Dropbox/Drive manuel).
