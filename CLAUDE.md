# ⚠️ RÈGLE ABSOLUE — Git : toujours pousser sur `main` + commit par demande

- **Toujours committer et pousser directement sur la branche `main`** (pas de branche de feature dédiée), sauf demande explicite contraire de l'utilisateur.
- **Faire un commit distinct entre chaque demande distincte** de l'utilisateur : une demande = un commit clair et descriptif (ne pas empiler plusieurs demandes non liées dans un seul commit).

---

# ⚠️ RÈGLE ABSOLUE — Maintenir README.md et CLAUDE.md à jour

Après **chaque modification fonctionnelle** (nouvelle feature, changement de comportement, ajout de composant, modification d'architecture), mettre à jour **systématiquement** :

- **`CLAUDE.md`** : logique technique, composants, store, fonctions module-level, comportements
- **`README.md`** : fonctionnalités visibles par l'utilisateur, stack, structure du projet

**Ne pas attendre que l'utilisateur le demande.** Si un changement de code mérite d'être documenté, le faire dans le même commit ou dans un commit/PR dédié immédiatement après.

---

# ⚠️ ERREUR RÉCURRENTE DE CLAUDE — Worktree vs dépôt principal

Claude travaille souvent dans un **worktree temporaire** (`\.claude\worktrees\...`) au lieu du dépôt principal.
Les modifications dans le worktree ne sont **pas visibles dans GitHub Desktop** et ne peuvent pas être committées.

**Règle absolue : toujours éditer les fichiers dans `C:\Users\petil\Documents\GitHub\NewSpotifyRelease\`**
Jamais dans `.claude\worktrees\*`.

Si des modifications ont été faites dans le worktree par erreur, les copier vers le dépôt principal avant de conclure.

---

# Spotify+ · Nouvelles Sorties — Contexte projet

## Vue d'ensemble
Application web PWA **sans backend** — toutes les données sont stockées localement via sql.js (SQLite WebAssembly) + IndexedDB. Hébergée statiquement sur GitHub Pages.

Chaque artiste est scrappé **depuis sa propre date de dernier scan** (`last_scraped_at` dans `artists_scraped`), pas depuis une date globale. Un artiste jamais scrappé repart de 2026-03-15.

L'utilisateur parcourt son feed de découverte, écoute les titres un par un via le player intégré. Quand un titre se termine, il est automatiquement marqué comme écouté, le suivant se lance automatiquement, et le titre disparaît du feed avec une animation. Aucun ajout automatique en playlist.

---

## Fichiers clés

| Fichier | Rôle |
|---|---|
| `index.html` | **App complète** — React 18 CDN + Babel + sql.js, tout en un seul fichier |
| `manifest.json` | Config PWA (nom, icônes, display standalone) |
| `service-worker.js` | Cache app shell + vendor pour usage offline (v8 : bump cache ; v7 : + Leaflet précaché ; clé de cache normalisée, ne cache que les réponses `res.ok`, fetch app shell en `{ cache: 'no-store' }` pour bypasser le cache HTTP `max-age=600` de GitHub Pages) |
| `vendor/sql-wasm.js` | sql.js 1.10.2 **auto-hébergé** (hash sha384 vérifié = ancien SRI cdnjs) |
| `vendor/sql-wasm.wasm` | Binaire WebAssembly SQLite auto-hébergé (un .wasm ne peut pas avoir de SRI) |
| `vendor/leaflet.js` / `vendor/leaflet.css` | **Leaflet 1.9.4 auto-hébergé** (tarball npm officiel) — carte de la section Maps, chargé **lazy** par `loadLeaflet()` au 1er affichage de la carte. `vendor/images/` = assets Leaflet par défaut (jamais fetchés : marqueurs 100% vectoriels via `circleMarker`) |
| `icon-192.png` | Icône PWA 192×192 (présente à la racine, utilisée aussi par les notifications) |
| `icon-512.png` | Icône PWA 512×512 (présente à la racine) |
| `data/maps.json` | **Placeholder hérité** (`{ updated_at, maps:{} }`) — toujours pas lu par l'app. Le module Maps fonctionne par **import de fichier** (localStorage `spotifyplus_maps`), pas par sync de ce JSON |
| `data/indices.json` | **Vrais indices** (NASDAQ-100/S&P 500/CAC 40) générés par la GitHub Action — lu par l'app en same-origin (CORS natif, zéro proxy) |
| `scripts/fetch_indices.py` | Script Python (stdlib only) qui récupère les indices côté serveur (Stooq en priorité, repli Yahoo cookie+crumb) et écrit `data/indices.json` |
| `.github/workflows/update-indices.yml` | GitHub Action cron (toutes les 2 h, lun–ven, 06–22 UTC) + `workflow_dispatch` qui lance le script et commit le JSON si changé |

---

## Stack (`index.html`)

- **React 18.3.1** via CDN (pas de build tool), Babel standalone **7.29.7**
- **⚠️ CDN épinglés + SRI** : les 3 `<script>` CDN restants (react, react-dom, babel — unpkg) ont des versions exactes + attribut `integrity` (sha384) + `crossorigin="anonymous"`. **Ne jamais changer une version sans recalculer le hash SRI** (sinon le script est bloqué par le navigateur).
- **sql.js 1.10.2 auto-hébergé** dans `vendor/` — `initSqlJs({ locateFile: f => './vendor/' + f })`. Le `.wasm` ne peut pas avoir de SRI : l'auto-hébergement ferme le dernier vecteur CDN.
- **⚠️ CSP** (`<meta http-equiv="Content-Security-Policy">` dans le `<head>`) : `connect-src` limité à `'self'` + `api.spotify.com` + `accounts.spotify.com` + `api.themoviedb.org` (TV Time — API TMDB v3 ; `img-src` inclut aussi `image.tmdb.org` pour les affiches + `i.ytimg.com` pour les miniatures de vidéos YouTube + `*.ggpht.com`/`*.googleusercontent.com` pour les **avatars de chaînes** YouTube (sinon l'image d'une chaîne suivie est bloquée par la CSP et n'apparaît pas), et `tile.openstreetmap.org` + `*.tile.openstreetmap.org` pour les tuiles de la carte Maps) + `api.open-meteo.com` (météo) + `api.bigdatacloud.net` + `photon.komoot.io` + `nominatim.openstreetmap.org` (reverse-geocoding du nom de ville — chaîne de 3 providers, premier qui répond) + `api.coingecko.com` + `api.frankfurter.app` + `api.gold-api.com` + `api.twelvedata.com` + `stooq.com` + `api.codetabs.com` + `cors.eu.org` + `api.allorigins.win` (Finance : crypto, EUR/USD, or ; actions via Twelve Data (clé, CORS natif) ; indices + pétrole via Yahoo Finance (proxy CORS) ; repli Stooq via proxies CORS codetabs/cors.eu.org/allorigins) — même en cas d'XSS, le token ne peut pas être exfiltré vers un domaine attaquant. `'unsafe-inline'`/`'unsafe-eval'` requis par Babel standalone, `'wasm-unsafe-eval'` par sql.js. **Toute nouvelle origine (script, image, fetch) doit être ajoutée à la CSP sinon elle est bloquée par le navigateur.**
- **IndexedDB** : persistance du binaire SQLite (clé `spotifyplus_db` dans le store `db` de la base `spotifyplus`). Connexion **unique mise en cache** (`_idbPromise` dans `idbOpen`) — ne pas rouvrir à chaque get/set. `navigator.storage.persist()` est demandé au login (réduit le risque d'éviction iOS).
- **Fonts** : DM Sans (UI) + DM Mono (labels, logs, valeurs)
- **Auth** : OAuth 2.0 PKCE — 100% client-side, token stocké en `localStorage`
  - Paramètre `state` anti-CSRF généré au login (`pkce_state`), vérifié au callback avant `exchangeCode`
  - `refreshToken()` est protégé par un **mutex** (`_refreshPromise`) : les refresh tokens Spotify sont à usage unique (rotation) — deux refresh simultanés (poll player + scraping) invalideraient le compte. Le refresh token **roté est re-stocké**.
  - Sur `invalid_grant` (refresh token définitivement mort) : `logoutClear()` + reload — sinon le poll player enverrait des 401 toutes les 5s pour toujours
  - `apiGet`, `apiPut` et `apiDel` retry **une fois** après refresh sur HTTP 401 (`apiPost` supprimé — inutilisé)
- **Aucun appel backend** — l'app appelle uniquement l'API Spotify + sql.js local
- **Garde multi-onglets** : `BroadcastChannel('spotifyplus_tabs')` (ping/pong) — le deuxième onglet ouvert affiche un bandeau d'avertissement (`otherTab` dans le store, rendu dans `Shell`), car chaque `saveDB()` exporte toute la DB : deux onglets s'écraseraient mutuellement

### Config Spotify
```js
CLIENT_ID    = '672e41f0308f4378b4f2331844e08b20'
REDIRECT_URI = 'https://pierre-portfolio.github.io/NewSpotifyRelease/'
SCOPES       = 'user-follow-read user-read-private user-read-currently-playing user-modify-playback-state user-library-read user-library-modify'
```

### Version de l'app — `APP_VERSION`
Constante module-level `APP_VERSION` (**actuellement `'4.4.8'`**, commit ≈448), format `MAJ.MIN.U` = **le nombre de commits du projet avec des points séparant le dernier chiffre (patch), l'avant-dernier (minor) et tout le reste (major)** : `patch = N % 10`, `minor = floor(N/10) % 10`, `major = floor(N/100)`. Exemples : 278 commits → `2.7.8`, 270 → `2.7.0`, 300 → `3.0.0`, 1001 → `10.0.1`, 1234 → `12.3.4`. Affichée en gris sous le bouton « 🗑 Purger les écoutes » de `VosEcoutesPanel` (« Version 3.0.0 »). **⚠ Le compteur suit l'historique du PROJET (≈300), pas `git rev-list --count HEAD` de ce dépôt-fork qui est bien plus bas (~65)** — incrémenter à la main en suivant le compteur projet.
**⚠️ Pas de build tool pour l'injecter** : la incrémenter **manuellement à chaque commit** (le compteur = `git rev-list --count HEAD` après le commit ; le commit qui change `APP_VERSION` compte lui-même, donc poser la valeur du futur commit puis appliquer le découpage ci-dessus).

### Délai de scraping
Le délai entre chaque artiste est configurable via un sélecteur (10 / 20 / 30s) auquel s'ajoute un **jitter aléatoire de 1 à 3 secondes**.
```js
const delay = delayChoice * 1000 + Math.random() * 2000 + 1000;
```

---

## Schéma SQL local (4 tables)

```sql
CREATE TABLE IF NOT EXISTS tracks (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  spotify_uri TEXT UNIQUE NOT NULL,
  artist_name TEXT NOT NULL,
  title TEXT NOT NULL,
  release_title TEXT,       -- nom de l'album (null pour les singles)
  release_type TEXT,        -- 'single', 'album', 'ep'...
  release_date TEXT,
  cover_url TEXT,
  duration_ms INTEGER,
  listened INTEGER DEFAULT 0,
  liked INTEGER DEFAULT 0,  -- 1 si liké via le player (colonne migrée via ALTER TABLE)
  listened_at TEXT,         -- horodatage UTC de l'écoute (colonne migrée) — alimente l'Historique
  added_at TEXT DEFAULT (datetime('now'))
);

CREATE TABLE IF NOT EXISTS artists_scraped (
  spotify_id TEXT PRIMARY KEY,
  last_scraped_at TEXT NOT NULL,
  name TEXT,                            -- nom de l'artiste (depuis /me/following, colonne migrée)
  image_url TEXT,                       -- pochette artiste (images[1].url = 300px, repli images[0])
  popularity INTEGER,                   -- 0-100
  followers INTEGER,                    -- followers.total
  genres TEXT,                          -- JSON.stringify(genres[])
  spotify_url TEXT,                     -- external_urls.spotify
  last_release_count INTEGER DEFAULT 0, -- sorties trouvées au dernier scan (artistReleases)
  total_tracks_added INTEGER DEFAULT 0, -- cumul des titres ajoutés pour cet artiste
  last_scan_status TEXT,                -- 'ok' | 'partial' (scan interrompu par un 429) | 'error' (albums inaccessibles)
  scan_count INTEGER DEFAULT 0          -- nombre de scans réussis
);

CREATE TABLE IF NOT EXISTS stats (
  id INTEGER PRIMARY KEY CHECK (id = 1),
  total_listened INTEGER DEFAULT 0,
  listened_this_month INTEGER DEFAULT 0,
  listened_this_year INTEGER DEFAULT 0,
  last_reset_month TEXT,    -- format 'YYYY-MM'
  last_reset_year TEXT      -- format 'YYYY'
);

CREATE TABLE IF NOT EXISTS purged_uris (
  spotify_uri TEXT PRIMARY KEY   -- URIs des titres purgés : jamais réinsérés par un re-scan
);
```

---

## Module DB (fonctions module-level dans `index.html`)

```js
let _db = null;                        // instance sql.js Database

async function initDB()                // charge depuis IndexedDB ou crée le schéma + migration liked
async function saveDB()                // exporte _db.export() → IndexedDB
async function importDiscoverWeekly(addLog)  // import hebdo playlist "Découvertes de la semaine"
function dbRun(sql, params=[])         // execute sans retour
function dbAll(sql, params=[])         // retourne tableau d'objets
function dbGet(sql, params=[])         // retourne le premier objet ou null
function loadListenStatsFromDB()       // retourne { remaining, remaining_ms, this_month, this_year, all_time, listened_ms, total_liked }
function loadLikedTracksFromDB()       // retourne les tracks WHERE liked=1 (max 500) mappées en items feed
function loadHistoryFromDB()           // retourne les tracks WHERE listened=1 (max 200) ORDER BY listened_at DESC — plus récent en haut (Historique)
function loadArtistsFromDB()           // retourne tous les artists_scraped (ORDER BY last_scraped_at DESC) mappés : { id, name, image, popularity, followers, genres[], spotifyUrl, lastScrapedAt, lastReleaseCount, totalTracksAdded, lastScanStatus, scanCount } — alimente la section Artistes
function mapFeedRow(t)                  // mappe une ligne DB `tracks` → item du feed (id, spotifyUri, label, artist, title, subtitle, date, image, isNew, liked, rawDate, duration_ms)
function loadFeedFromDB({ artistSearch, filterType, sortBy })   // titres WHERE listened=0, **filtre ET tri appliqués EN SQL** puis LIMIT 1000. `filterType` → WHERE sur `release_type` ('single' / 'discover_weekly' / album = NULL ou NOT IN(single,dw)) ; `sortBy` → ORDER BY `release_date ASC` / `artist_name COLLATE NOCASE` / `id ASC` ; `artistSearch` → `LIKE '%…%'`. Comme le cap LIMIT 1000 s'applique **après** filtre+tri, **tout filtre/tri actif balaie TOUTE la base** (Singles, plus anciens, artiste recherché) même au-delà de la 1000e ligne. Appelé par le chargement initial (défauts) et par l'effet de filtres
```

### Migrations DB (idempotentes)
`initDB()` exécute dans des try/catch après chaque chargement (nouveau ou depuis IndexedDB) — sans effet si la colonne existe déjà :
- `ALTER TABLE tracks ADD COLUMN liked INTEGER DEFAULT 0`
- `ALTER TABLE tracks ADD COLUMN listened_at TEXT` — horodatage UTC posé via `datetime('now')` au moment où un titre passe `listened = 1` (marquage auto fin de titre + clic croix rouge × via `removeFromFeed`) ; alimente l'Historique
- `ALTER TABLE stats ADD COLUMN total_listened_ms INTEGER DEFAULT 0`
- `ALTER TABLE stats ADD COLUMN total_liked INTEGER DEFAULT 0` — compteur de likes posés **dans l'app** (via `setTrackLiked` uniquement, PAS par `syncInitialLikes`) ; persistant, non purgeable
- `ALTER TABLE artists_scraped ADD COLUMN name / image_url / popularity / followers / genres / spotify_url / last_release_count / total_tracks_added / last_scan_status / scan_count` — **métadonnées artiste gratuites** (déjà présentes dans l'objet `/me/following`, aucune requête en plus) + compteurs de scan. Renseignées dans l'`INSERT … ON CONFLICT` (UPSERT) après chaque artiste scrapé avec succès

### Champs des items feed
Chaque item du feed contient : `id, spotifyUri, label, artist, title, subtitle, date, rawDate, image, isNew, liked, duration_ms`
- `rawDate` : date ISO brute (`YYYY-MM-DD`) utilisée pour le tri par date de sortie
- `liked` : 0 ou 1, synchronisé depuis DB et mis à jour en temps réel par `setTrackLiked`
- `duration_ms` : durée en millisecondes — utilisée pour le seek à 25% au clic next

### syncInitialLikes
Appelée via `useEffect` dès que `dbReady` passe à `true`. Récupère jusqu'à 300 tracks non écoutés, interroge `/me/library/contains` (helper `libraryContains`) par batch de **40 URIs** (max imposé par l'API), met à jour `liked` en DB + feed array + `likedTracks` state.
**TTL 24h** (`localStorage spotifyplus_likes_synced_at`) : jusqu'à 8 requêtes par appel, inutile à chaque reload de page.

### Helpers library — migration API Spotify février 2026
`/me/tracks/contains` et `PUT/DELETE /me/tracks` ont été **supprimés** par Spotify (appliqué aux apps Development Mode existantes le 9 mars 2026). Remplaçants génériques module-level dans `index.html` :
- `libraryContains(uris)` → `GET /me/library/contains?uris=…` (max 40 URIs, retourne un tableau de booléens ou `null` si erreur/403/rate-limit)
- `librarySave(uri)` / `libraryRemove(uri)` → `PUT/DELETE /me/library?uris=…` — **URIs en query string, même pour le PUT** (pas de body)
- `libraryScopeAlert(result)` : si l'API répond `403 forbidden` (token émis avant l'ajout des scopes `user-library-read/modify`), affiche une alerte demandant une déconnexion/reconnexion et retourne `true` (le caller annule sa mise à jour d'état)
- ⚠ `apiDel` mappe désormais aussi le statut 403 sur `{ error: 'forbidden' }` (comme `apiPut`)

### FeedItem — swipe mobile
- `onTouchStart` : capture `touchStartX`
- `onTouchMove` : calcule `swipeDx`, applique `translateX` + teinte de fond (rouge=gauche, accent=droite)
- `onTouchEnd` : `swipeDx < -60` → `removeFromFeed` · `swipeDx > 60` → `navigateFeed(-1)`

### Notification fin de session
Déclenchée quand `dailyCount >= QUOTA_MAX` (100) dans `startSync` via le helper module-level `notify(title, body)` (permission demandée si `'default'`, silencieuse si `'denied'`).
**⚠️ Mobile (Android Chrome)** : `new Notification()` lève `Illegal constructor` quand un service worker est actif. `notify()` passe donc par `navigator.serviceWorker.ready → reg.showNotification()` en priorité, avec fallback `new Notification()` dans un try/catch — ne jamais réintroduire un `new Notification()` direct dans la synchro.

### Notification « scraping de nouveau disponible »
Un `useEffect([quotaUntil, blockedUntil])` du `StoreProvider` programme un `setTimeout` qui se déclenche à l'expiration de la **fenêtre bloquante la plus tardive** (`Math.max(quotaUntil, blockedUntil)`) et appelle `notify('Spotify+ — Scraping disponible', …)`. La permission est demandée immédiatement quand un blocage commence (geste utilisateur récent du clic Lancer). `notifiedAvailAtRef` évite de re-notifier deux fois pour la même fenêtre ; le timeout est nettoyé/reprogrammé à chaque changement de `quotaUntil`/`blockedUntil`.

### Découvertes de la semaine (`importDiscoverWeekly`)
- Appelée dans l'init **avant** le chargement du feed (les tracks DW sont visibles dès le login)
- Early-return si `Date.now() < _rlUntil` (fenêtre rate-limit active) — retente au prochain login
- Cherche dans `GET /me/playlists` (toutes pages) **TOUTES** les playlists dont le nom contient `'découvertes de la semaine'`/`'discover weekly'`, ou (owner Spotify) `'découvertes'`/`'discover'` → tableau `candidates` (plus une seule `.find()`)
- **Priorité aux copies PERSO** : `candidates` est trié pour placer les playlists **owner ≠ 'spotify'** (copies utilisateur, contenu lisible) AVANT l'éditoriale Spotify. On tente `fetchPlaylistItems` sur chaque candidate dans l'ordre jusqu'à en trouver une dont le contenu est **réellement lisible** (`items.length > 0`)
- Insère les tracks avec `release_type = 'discover_weekly'`, `release_title = 'Découvertes de la semaine'`
- **Deux cooldowns distincts** : `DW_LS_KEY` (`spotifyplus_dw_last_import`, **7 j**) posé seulement **après un import réussi** (contenu lu) ; `DW_FAIL_KEY` (`spotifyplus_dw_last_fail`, **6 h**) posé sur **échec** (playlist introuvable / contenu inaccessible). → un échec transitoire ne bloque plus l'import pendant 7 jours, il retente dans 6 h (auto-réparant). Le succès efface `DW_FAIL_KEY`.
- **⚠ Migration Spotify** : le helper `fetchPlaylistItems(id)` essaie **le nouvel endpoint `/playlists/{id}/items` PUIS l'ancien `/playlists/{id}/tracks`** (`additional_types=track`) et lit `entry.item || entry.track`. Le contenu des playlists n'est renvoyé **que** pour les playlists **possédées/collaboratives** — "Découvertes de la semaine" appartenant à Spotify, l'API répond 403 ou sans `items` **même si l'utilisateur l'a ajoutée à sa bibliothèque** (« liker » ne rend PAS le contenu lisible). Si aucune candidate n'est lisible : log ⚠ qui explique le **contournement = créer une COPIE PERSO** de la playlist sur Spotify (menu ⋯ → « Ajouter à une autre playlist » → « Nouvelle playlist », garder « découvertes » dans le nom) — cette copie (owner = utilisateur) sera trouvée et importée automatiquement au prochain essai. + `DW_FAIL_KEY` (nouvel essai dans 6 h).
- Les messages sont collectés dans `initMsgs[]` et affichés dans le panneau Logs au login
- Tag **Découvertes** (violet `oklch(0.65 0.18 300)`) dans le feed, tag **DW** sur mobile

### Règles d'écriture DB
- `saveDB()` est appelé **après chaque artiste scrapé**, **après chaque écoute marquée**, **après une purge**, **à chaque fin de synchro** (`endSync`) et **quand l'app passe en arrière-plan** (`visibilitychange → hidden`)
- `saveDB()` est **sérialisé** (`_savePromise` + `_saveQueued`) : jamais deux exports/écritures IndexedDB en parallèle. Pas de debounce temporel — chaque appel garantit une écriture incluant ses données (boucle do/while qui ré-exporte si un appel est arrivé pendant l'écriture). **Ne pas remplacer par un debounce** : risque de perte de données si l'OS tue l'onglet mobile.
- Toujours utiliser `INSERT OR IGNORE` pour les tracks (UNIQUE sur `spotify_uri`)
- Utiliser un **UPSERT `INSERT … ON CONFLICT(spotify_id) DO UPDATE`** pour `artists_scraped` (PAS `INSERT OR REPLACE` qui effacerait les cumuls `total_tracks_added`/`scan_count`) — mais **uniquement si le fetch des albums a réussi** (sinon la fenêtre de scan est brûlée sans avoir rien scanné)

---

## Logique de scraping (dans `startSync`)

### État de synchro unifié — `syncState` (enum)
**Un seul état remplace les 3 booléens** `scraping`/`paused`/`rlWaiting` :
`syncState : 'idle' | 'running' | 'paused' (pause manuelle) | 'rl_waiting' (attente 429)`
- `setSync(st)` pose **synchroniquement** `syncStateRef.current` puis `setSyncState` — le guard anti double-clic de `startSync` et le `checkpoint()` lisent l'état réel, pas celui du dernier render
- Les booléens `scraping` (`!== 'idle'`), `paused`, `rlWaiting` sont **dérivés** et toujours exposés dans l'api du store (l'UI ne change pas)
- Pause pendant `rl_waiting` → passe à `paused` ; à la reprise, le checkpoint re-détecte la fenêtre 429 et re-pose `rl_waiting`

### Conditions de NON-démarrage — toutes en tête de `startSync`, AVANT toute requête
1. `syncStateRef.current !== 'idle'` → return (guard par **ref** : insensible au double-clic avant re-render)
2. `Date.now() < _rlUntil` (fenêtre rate-limit persistée) → log + bandeau, return
3. **Quota 24h** : fenêtre glissante encore active (`loadQuota().until > Date.now()`) → log "prochaine synchro dans X h Y min", return — avant, cliquer Lancer/Reprendre avec le quota consommé dépensait `/me` + les pages d'artistes pour rien

### Arrêts de synchro unifiés — `endSync(reason)` + `checkpoint()`
**Tous les chemins d'arrêt passent par `endSync(reason)`** (`'completed' | 'daily_limit' | 'error'`) :
- reset `nextCall` / `nextCallTotal` / `eta`, `setSync('idle')`
- `'completed'` → supprime `spotifyplus_sync_progress` + `setResumableSession(null)`
- autres raisons → **relit le localStorage et re-set `resumableSession`** : le bouton "↩ Reprendre" apparaît immédiatement, sans reload
- `await saveDB()` final dans tous les cas

**`checkpoint()`** est attendu avant **chaque** appel API de la synchro (via `apiGetSafe`). **UNE seule boucle d'attente** pour : la pause manuelle (`syncStateRef === 'paused'`) ET la fenêtre rate-limit globale (`Date.now() < _rlUntil`, posée par n'importe quel 429, player compris). Quand il attend sur la fenêtre 429, c'est lui qui pose `rl_waiting` + le countdown (`setNextCall` dérivé de `_rlUntil`) et qui repasse en `running` à l'expiration. **Plus aucune requête ne part pendant une pause, et plus de sleep parallèle dans `apiGetSafe`.**

### Gestion du rate-limit 429 (wrapper `apiGetSafe`)
Défini localement dans `startSync` :
- `await checkpoint()` en tête de chaque tentative
- Sur erreur `RATE_LIMIT` : **stop complet dès le PREMIER 429** → `_rlSet(max(Retry-After, 15 min))` (fenêtre **persistée**, partagée par tous les appels), `setBlockedUntil`, throw `SYNC_RATE_ABORT` → catch → `endSync('error')` (progression sauvegardée, bouton ↩ Reprendre immédiat). Plus de streak/disjoncteur à 3 : l'ancien comportement (pause `Retry-After` + retry jusqu'à 3 429 consécutifs) a été retiré à la demande de l'utilisateur.
- Retour `{ rate_limited: true }` (bloqué par le guard global) → `continue` : re-checkpoint puis retry, **jamais** traité comme une réponse valide
- Body d'erreur HTTP (`data.error` : 401 non rattrapé, 403, 5xx) → **throw** (la synchro s'arrête en gardant la progression — elle ne termine plus en silence comme si tout était scanné)

Les 4 appels utilisent `apiGetSafe` : `/me`, page artistes, albums d'un artiste, tracks d'un album.

### Fenêtre rate-limit persistée `spotifyplus_blocked_until` (= `_rlUntil`)
- **Une seule source de vérité** : `_rlUntil` (module-level) est écrit dans localStorage à chaque `_rlSet()` (429 player OU stop synchro min. 15 min) et **relu au chargement du module** — un blocage survit au F5 pour TOUS les appels (player, login, synchro), pas seulement pour le bouton Lancer
- `startSync` refuse de démarrer tant que `Date.now() < _rlUntil`
- `_rlNotify → setBlockedUntil` : tout 429 (player compris) affiche le bandeau et désactive Lancer
- `DateRangePanel` désactive Lancer/Reprendre + bandeau countdown "🚫 Rate limit Spotify — réessaie dans X min"
- Au boot pendant une fenêtre active : `/me` retourne `rate_limited` → l'app entre quand même en mode **connecté dégradé** (`user=null`) au lieu d'éjecter vers le login
- `Retry-After` est blindé par `_parseRetryAfterMs` (un header malformé donnait `NaN` → guard inopérant + retry immédiat sur une API qui vient de throttle)

1. Vérifie le compte `/me` (via `apiGetSafe` — un échec **throw**, ne termine plus la synchro en silence)
2. Charge les dates de scrapping depuis `SELECT spotify_id, last_scraped_at FROM artists_scraped`
3. Pagine `/me/following?type=artist&limit=50` en streaming (échec de page → **throw**, pas `break`)
4. Pour chaque artiste :
   - `cutoff = (scrapedDates[artist.id] || '2026-03-15').slice(0, 10)` — **tronqué au jour** : une sortie publiée le jour du dernier scan mais indexée après n'est plus ratée (doublons absorbés par `INSERT OR IGNORE`)
   - `/artists/{id}/albums?include_groups=album,single&limit=10&market=FR` — **⚠ limit max = 10 depuis la migration Spotify de février 2026** (appliquée aux apps existantes le 9 mars 2026 ; avant : max 50). `limit=20` → `400 "Invalid limit"`. Ne jamais remonter au-dessus de 10.
   - Filtre par `cutoff ≤ release_date ≤ aujourd'hui`
   - Pour chaque album dans la période → `/albums/{id}/tracks?limit=50`
   - Dédoublonnage via `seenUris` Set + `knownUrisRef` — skip **seulement si TOUTES les pistes sont connues** (`uris.every`) : un album dont la piste 1 est sortie en single avant n'est plus sauté en entier
   - **Singles** : `INSERT OR IGNORE` d'une seule ligne (`uris[0]`, `title = album.name`, `release_title = null`)
   - **Albums** : `INSERT OR IGNORE` d'une ligne par track (`title = t.name`, `release_title = album.name`)
5. **Dès que la liste d'albums est lue avec succès** (AVANT la boucle des pistes) → **écriture d'une ligne « légère »** : `INSERT … ON CONFLICT` qui pose les métadonnées + `last_scan_status='partial'` + `last_scraped_at = cutoffISO` (date existante ou défaut, **jamais avancée**) ; l'`ON CONFLICT` ne touche **que** les métadonnées (pas `last_scraped_at`/`scan_count`/`total_tracks_added`). Objectif : l'artiste **apparaît immédiatement** dans le panneau (avec `setArtists(loadArtistsFromDB())`) même si un **429 coupe le scan plus bas** — sans avancer sa date, donc aucun titre raté. Le statut reste `'partial'` tant que le scan n'est pas terminé.
   - Après chaque artiste **terminé** → **`INSERT … ON CONFLICT(spotify_id) DO UPDATE` (UPSERT) final** qui **avance `last_scraped_at = datetime('now')`**, passe `last_scan_status='ok'`, **cumule** `total_tracks_added` + `scan_count`, pose `last_release_count` (`artistReleases`) et rafraîchit les métadonnées. (`'partial'` n'est donc visible que pour un artiste interrompu entre les deux écritures.)
   - Si la liste d'albums est **inaccessible** (fetch échoué) → log ⚠ + `UPDATE last_scan_status='error'` qui n'affecte qu'une ligne existante (pas d'INSERT, date non avancée)
6. Après chaque artiste → `saveDB()` (async) + `localStorage.setItem('spotifyplus_sync_progress', { artists_scanned, total_artists, last_artist_name, page_url, page_offset })` — `page_url` = URL de la page `/me/following` en cours, `page_offset` = nombre d'artistes de cette page déjà traités
7. À la fin → `endSync('completed')` (saveDB final + nettoyage localStorage) + `setProgress(100)`

### Quota 100 artistes / fenêtre GLISSANTE de 24h
- **Ce n'est PLUS une limite par jour calendaire** (remise à zéro à minuit) mais une **fenêtre glissante de 24h** : atteindre 100 artistes démarre un compteur de 24h, et toute synchro suivante est refusée jusqu'à son expiration. Avant, on pouvait scraper 100 à 23h30 puis 100 de plus à 00h30.
- **Reset d'inactivité (24h depuis le dernier scrap)** : le compteur repart à 0 **24h après le dernier artiste scrapé**, MÊME si on n'a pas atteint 100. Avant, scraper 95 puis revenir 3 jours plus tard affichait encore 95 (le compteur ne baissait jamais tant que 100 n'était pas atteint, car seule la fenêtre `until` posée au 100e remettait à 0).
- Helpers module-level `loadQuota()` / `saveQuota(count, until, last)` autour de `localStorage` clé `spotifyplus_daily_scrapings` = `{ count, until, last }` (`until` = timestamp ms de fin de la fenêtre 24h, `0` tant que < 100 ; `last` = timestamp ms du **dernier artiste scrapé**, posé à `Date.now()` après chaque scrap dans `startSync`). `loadQuota()` **remet `count` à 0** dans **deux** cas : (1) `until` dépassé (fenêtre 100-atteint expirée) **OU** (2) **`Date.now() - last ≥ 24h`** (inactivité).
- **`ensureQuotaWindow()` (helper module-level, AUTO-RÉPARANT)** : si `count ≥ 100` sans fenêtre valide (`until` absent/expiré), ouvre et persiste une fenêtre de 24h **maintenant**, et retourne le timestamp de fin (`0` si quota non atteint). Corrige un **état hérité de l'ancien format `{ date, count:100 }`** (pré-fenêtre-glissante) qui laissait le compteur bloqué à 100 **sans `until`** : le garde `startSync` ne bloquait pas (until=0), la synchro démarrait, atteignait le quota en tête de boucle et s'arrêtait **sans jamais poser de date** → bloqué en boucle, aucun countdown affichable. Appelé aux **3 endroits** : init (expose la fenêtre au chargement), garde `startSync`, garde de boucle.
- Vérifiée en tête de boucle artiste ; à l'atteinte : log + notification navigateur + `endSync('daily_limit')` (le bouton Reprendre apparaît **immédiatement**) — et aussi vérifiée **en tête de `startSync`** via `loadQuota().count >= QUOTA_MAX` (refus avant toute requête)
- **Les deux logs de refus quota + la notification affichent la date/heure absolue de réouverture** (`toLocaleString('fr-FR', { dateStyle:'long', timeStyle:'short' })`) en plus de la durée relative
- **`dailyCount` est incrémenté APRÈS la requête albums** (pas avant) : un throw (erreur API, stop 429) ne brûle plus le quota d'un artiste qui n'a pas été scanné
- **La fenêtre 24h est gelée à l'instant exact du 100e artiste** : `until = Date.now() + 24h` est posé dans l'incrément qui atteint 100 (pas au tour de boucle suivant), `setQuotaUntil(until)` met à jour l'UI. `ensureQuotaWindow()` **ne ré-écrase pas** une fenêtre déjà active (préserve l'instant du 100e scan).
- `quotaUntil` (state + store) alimente un **bandeau de countdown** affiché à **deux endroits** (chacun avec son propre `setInterval` 1s) : dans `DateRangePanel` (désactive aussi Lancer/Reprendre via `anyBlock = blocked (429) || quotaBlocked`) ET sous la barre de progression de `ScrapingStatusPanel` (là où l'utilisateur lit déjà `X/100 sur 24h`). Texte : « ⏳ Quota de 100 artistes atteint — Prochaine synchro le **{fmtQuotaWhen(until)}** (dans {fmtRemaining(ms)}) ». Helpers module-level `fmtRemaining(ms)` (« X h Y min ») et `fmtQuotaWhen(until)` (« 19 juin à 18:10 »)

### Reprise de synchro après interruption / redémarrage
- La progression est sauvegardée dans `localStorage` (`spotifyplus_sync_progress`) après chaque artiste, **avec le curseur de pagination** (`page_url` + `page_offset`)
- Au login, si une progression existe → `setResumableSession(p)` → bouton **"↩ Reprendre"** affiché
- `resumeSync()` → `startSync({ skipCount, resumeUrl: page_url, resumeOffset: page_offset })` : la reprise **repart directement sur la bonne page** — avant, elle re-paginait `/me/following` depuis le début (~20 fetchs dos à dos sans délai pour sauter 1000 artistes = burst exactement au pire moment post-429)
- Compat : une ancienne progression sans `page_url` retombe sur l'ancien re-parcours avec saut (`globalArtistIndex <= skipCount → continue`)
- `localStorage` nettoyé quand la sync se termine normalement

### UX DateRangePanel — quand `resumableSession` existe
- Bouton **"↩ Reprendre la synchro en cours"** affiché **en premier**, pleine largeur, avec `artists_scanned / total_artists` et `last_artist_name`
- Bouton "Lancer" devient **"↺ Recommencer la synchro de 0"** (rouge foncé) avec `confirm()` avant d'exécuter

---

## Comportement "marquer comme écouté" + auto-avance

- `prevUriRef` stocke l'URI du son précédent
- Quand `now?.uri` change → l'URI précédente vient de se terminer
- Si elle est dans le feed **et que `dbReady` est true** :
  - `UPDATE tracks SET listened = 1 WHERE spotify_uri = ?`
  - `UPDATE stats SET total_listened = total_listened + 1, listened_this_month = ..., listened_this_year = ... WHERE id = 1`
  - `saveDB()` (async)
  - `setListenStats(loadListenStatsFromDB())`
- Animation slide-right + suppression du feed après 600ms
- `listenedUrisRef` évite les doubles appels dans la même session
- **Auto-avance** : si `now` devient `null` et que l'URI précédente était dans le feed → le titre suivant est lancé automatiquement (sauf si loop activé)
- **Détection redémarrage** : si la position repasse à 0 après avoir été proche de la fin sur le même URI → auto-avance (sauf loop)

**Important** : les items ajoutés pendant la sync courante SONT immédiatement en DB et peuvent être marqués écoutés dans la même session.

**⚠️ Clic « Suivant » manuel (helper `markListened`) :** le retrait du feed quand on **clique** sur Suivant ne doit PAS dépendre uniquement de l'effet « URI a changé ». Le poll player n'interroge Spotify que toutes les 5s : sur un **double-clic rapide** (2 fois en < 3s), `now.uri` saute de trackA → trackC, et le titre **intermédiaire trackB n'apparaît jamais dans `now.uri`** → l'effet URI ne le voit jamais → il restait dans le feed.
- `markListened(uri)` (`useCallback([dbReady])`, exposé dans l'api du store) : même logique que l'effet URI (UPDATE `tracks`/`stats` + `saveDB` + recharge stats/history + animation de retrait), **idempotent via `listenedUrisRef`** (un titre déjà compté par le poll n'est pas recompté).
- Les 3 players (`PlayerBar`, `MobilePlayer`, `CompactPlayer`) capturent l'URI du titre quitté (`filteredFeed[currentIndex]`) **avant** de jouer le suivant, puis appellent `markListened(leaving)` après `playTrack`. `navigateFeed(+1)` fait de même. Effet de bord bienvenu : le retrait est désormais **immédiat** (plus d'attente du poll 5s), même sur un clic simple.
- Ne se déclenche que si `currentIndex !== -1` (titre du feed en cours) → un titre hors feed lancé manuellement n'est jamais marqué.

**⚠️ Ordre critique dans l'effet URI :** l'auto-avance doit être placée **avant** `if (listenedUrisRef.current.has(prevUri)) return` — sinon elle est court-circuitée si le titre a déjà été traité dans la session.

**⚠️ Limitation Spotify :** quand un titre se termine, Spotify retourne `200 + is_playing:false` avec la même URI → `now?.uri` ne change pas ET `now?.current` est gelé → ni l'effet URI ni l'effet current ne se déclenchent.

**3 mécanismes d'auto-avance (triple couverture) :**
1. **Effet `now?.uri`** : `currentUri` est null ou hors feed **ET le titre quitté était proche de sa fin** (`prevNowRef` : `duration - current ≤ 12s` au dernier tick) → avance (cas Spotify radio/queue). **⚠ Garde anti-vol de lecture** : sans la condition `nearEnd`, lancer manuellement un titre hors feed (ou stopper la lecture) en plein milieu d'un titre du feed déclenchait `playTrack` et écrasait le choix de l'utilisateur. `prevNowRef` est mis à jour par l'effet 3 (déclaré APRÈS) → il contient encore l'état du tick précédent quand l'effet 1 s'exécute.
2. **Effet `now?.current`** : `remaining ≤ 3s` ET titre dans le feed → avance (fin imminente détectée en live)
3. **Effet `now?.playing`** : transition `true → false` ET `remaining ≤ 8s` ET titre dans le feed → avance **(cas principal : fin naturelle)**

`advancedForRef` (ref, pas state) empêche les doubles appels sur le même titre. Il est **remis à null dès qu'un nouveau titre démarre** (URI différente) — permet de ré-avancer si l'utilisateur réécoute le même titre plus tard dans la session. `prevNowRef` capture l'état complet (`playing`, `uri`, `current`, `duration`) du tick précédent pour détecter la transition.

**Écritures DB hors updater** : le marquage écouté (`UPDATE tracks/stats` + `saveDB`) est fait **en dehors** de l'updater `setFeed` (les updaters React doivent rester purs) — l'item est lu via `feedRef.current`.

**Poll player avec bail-out** : le tick 5s compare le nouvel état `now` au précédent et garde la même référence si rien n'a changé (lecture en pause) → pas de re-render global inutile.

**Poll player — économie de requêtes** :
- **Onglet caché** (`visibilityState === 'hidden'`) → le tick ne fait aucun appel ; au retour (`visible`), un tick de rattrapage part immédiatement
- **Fenêtre 429 active** → `apiGet` retourne `{ rate_limited: true }` et le tick **garde l'état `now` affiché tel quel** — avant, le `null` faisait disparaître le player et polluait les effets d'auto-avance alors que la musique jouait toujours

---

## Statistiques

- Table `stats` avec une seule ligne (id=1) — compteurs incrémentés à chaque écoute
- `remaining` = `SELECT COUNT(*) FROM tracks WHERE listened = 0` (recalculé)
- `remaining_ms` = `SELECT SUM(duration_ms) FROM tracks WHERE listened = 0` (recalculé)
- `listened_ms` = `SELECT SUM(duration_ms) FROM tracks WHERE listened = 1` (recalculé) — affiché dans `VosEcoutesPanel` + `now.duration * 1000` pour le titre en cours
- `total_liked` (colonne `stats`) = compteur de likes posés **dans l'app** : +1 à chaque like, −1 (plancher 0) à chaque unlike via `setTrackLiked` — **jamais** alimenté par `syncInitialLikes` (les likes Spotify importés ne comptent pas). Affiché dans `VosEcoutesPanel` en **% likés** = `total_liked / total_listened × 100`. Persistant : la purge ne touche pas la table `stats`
- Réinitialisation mois/année : vérifiée au démarrage via `last_reset_month` / `last_reset_year`

---

## Purge

- Bouton **"Purger les écoutes"** dans `VosEcoutesPanel` (desktop sidebar + mobile onglet Stats)
- Action : les URIs sont d'abord mémorisées dans `purged_uris` (`INSERT OR IGNORE … SELECT`), puis `DELETE FROM tracks WHERE listened = 1` (**likés compris**) → `saveDB()` → `setListenStats` + `setLikedTracks`
- **`purged_uris`** est chargée dans `knownUris` au boot et consultée par le scan (`newTracks = pistes ∉ seenUris`) et par `importDiscoverWeekly` : **un titre purgé ne réapparaît jamais** dans le feed, même si son album retombe dans une fenêtre de scan (cutoff tronqué au jour, scan `partial`)
- **L'onglet ❤ Likés est aussi vidé** des titres écoutés. Le **% likés** n'est PAS affecté : il vit dans la table `stats` (`total_liked / total_listened`), indépendante des lignes `tracks` supprimées
- `removeFromFeed` (bouton croix rouge × **et** swipe gauche mobile) traite le titre **comme écouté** : `listened = 1` + `listened_at` + **incrément des compteurs `stats`** (`total_listened`, `listened_this_month/year`, `total_listened_ms`) → le titre apparaît dans l'**Historique** et compte dans les **stats d'écoute**. **Plus aucun `DELETE`** : le titre (liké ou non) reste en DB avec `listened = 1` (nécessaire pour l'Historique), il sera ensuite purgé par le bouton Purger. **Idempotent via `listenedUrisRef`** : si le poll player a déjà compté le titre, il n'est pas recompté (on s'assure juste qu'il est marqué via `COALESCE(listened_at, …)`)
- Affiche une `alert` avec le nombre de titres supprimés

### Undo du retrait (`UndoToast`)
`removeFromFeed` pose un **snapshot** `{ uri, item, idx, durMs, counted }` (`idx` = position d'origine dans le feed) dans `undoToast` (state + `undoToastRef`). Le composant `UndoToast` (rendu dans `Shell` quand `connected`) affiche un toast **« ↩ Annuler » pendant 5 s** (auto-fermé via `clearUndo`). `undoRemove()` remet `listened = 0` + `listened_at = NULL`, **ré-décrémente les stats uniquement si `counted`** (le retrait avait bien compté l'écoute, plancher `MAX(...,0)`), retire l'URI de `listenedUrisRef`, et réinsère l'item **à sa position d'origine** dans le feed (`idx`). Exposés dans l'api du store : `undoToast`, `undoRemove`, `clearUndo`.

---

## Module Maps — trajets Google Timeline

### Stratégie « scraping » Google Maps (⚠ à connaître avant toute évolution)
Il n'existe **AUCUNE API publique** pour la Timeline Google, et depuis fin 2024 les trajets sont stockés **sur le téléphone** (plus consultables sur le web) — un scraping direct depuis une app statique est **impossible** (authentification Google + CORS). La récupération passe par l'**export officiel** que l'utilisateur importe manuellement :
- **Android/iOS** : Google Maps → profil → Vos trajets → ⚙ → « Exporter les données de vos trajets » → `location-history.json` / `Timeline.json` (format `semanticSegments`)
- **Google Takeout** : « Historique des positions (ancien) » → `Semantic Location History/AAAA_MOIS.json` (format `timelineObjects`)
- `Records.json` (points GPS bruts, centaines de Mo) **non supporté** volontairement
- Pistes d'automatisation future (non implémentées) : ré-import périodique manuel (recommandé 1×/semaine) ; une vraie automatisation exigerait une extension navigateur ou une sauvegarde de l'export sur Drive/Dropbox relue par l'app — **jamais** de tracking par l'app elle-même (choix produit explicite)

### Données — localStorage `spotifyplus_maps`
```js
{ version: 1, updated_at: ISO, days: {
  'YYYY-MM-DD': [           // jour LOCAL de départ, segments triés par start
    { kind:'move', mode:'walk|run|bike|drive|transit|fly|other', start, end,
      distance_m, path:[[lat,lon],…] },   // tracé simplifié (≤150 pts, 5 décimales)
    { kind:'visit', name, lat, lon, start, end },
] } }
```
Inclus dans `buildBackup`/`applyBackup` (clé `maps`, REMPLACÉ à la restauration — qui invalide aussi `_mapsCache`) + export partiel 🗺️ Maps + Dropbox.

### Helpers module-level
- `loadMaps()` / `saveMaps(data)` — lecture/écriture avec **cache mémoire `_mapsCache`** (évite de re-`JSON.parse` un blob de plusieurs Mo à chaque render de `StatsPanel`, qui re-rend au tick 5s du player). `saveMaps` alerte si quota localStorage dépassé
- `parseGoogleTimeline(json)` → `{ days, count }` — parse les **2 formats** : `semanticSegments` (visit / activity / timelinePath — les **timelinePath sont rattachés aux activités par chevauchement temporel** ±2 min : l'activité donne mode/distance, le timelinePath le tracé détaillé ; un timelinePath orphelin devient un trajet `other` avec distance haversine) et `timelineObjects` (activitySegment avec `simplifiedRawPath` sinon `waypointPath` sinon départ→arrivée ; placeVisit avec nom/adresse). Timestamps ISO **et** epoch ms (`startTimestampMs`), coords `latitudeE7`/`latE7`/`latLng:"48.7°, 2.0°"`/`"geo:48.7,2.0"` via `mapsParseLatLng` (+ `mapsCheckLL` borne lat/lon)
- `mapsMergeImport(parsed)` — les jours importés **REMPLACENT** les mêmes jours existants (Google = source de vérité), les autres conservés. **Garde-fou taille** : si le JSON dépasse ~4,2 Mo, re-simplifie tous les tracés (150 → 60 → 25 pts)
- `mapsSimplifyPath(path, maxPts=150)` (échantillonnage premier/dernier conservés + arrondi 5 décimales ≈ 1 m), `mapsPathDistanceM` / `mapsHaversineM`, `mapsDayKey(iso|Date)` (jour LOCAL, même convention que `_lastNDays`), `mapsMode(type)` (mapping tolérant des deux nommages Google `IN_PASSENGER_VEHICLE` / `in passenger vehicle`), `mapsVisitName(semanticType)` (Home→Domicile, Work→Travail), `mapsStats()`, `fmtMapsKm(m)`, `fmtMapsDur(ms)`
- `loadLeaflet()` — injection lazy de `vendor/leaflet.js` + `.css` (promesse module-level `_leafletPromise`), appelé au montage de `MapsMap`
- `mapsEsc(s)` — **échappement HTML obligatoire** pour tout texte importé injecté dans `bindPopup` (les noms de lieux viennent d'un fichier utilisateur → un backup piégé ne doit pas injecter de HTML, la CSP autorise `unsafe-inline`)

### Rendu carte (`MapsMap`)
- Instance Leaflet **créée une fois** dans un ref, `layerGroup` vidé/repeuplé à chaque changement de jour, `fitBounds` sur tous les points, **`map.remove()` au démontage** (changement d'onglet), `invalidateSize()` différé (80 ms) après montage
- Tuiles `https://tile.openstreetmap.org/{z}/{x}/{y}.png` avec `className:'maps-dark-tiles'` → filtre CSS `invert+hue-rotate` pour le thème dark (les polylignes SVG sont dans un pane séparé, non affectées). CSS Leaflet sombre (popups, contrôles, attribution) dans le `<style>` du `<head>`
- **Marqueurs 100% vectoriels** (`circleMarker`) — aucun fetch des images Leaflet par défaut

## Module Mes Actifs — patrimoine perso depuis le Google Sheets « Finance » (Drive)

**3e onglet de la section Finance** (à côté de Light/Full, accent doré `ACTIFS_GOLD = '#e0c060'`). Lit le classeur Google Sheets « Finance » de l'utilisateur sur son Drive et affiche un tableau de bord patrimoine.

### Récupération (module-level, près des helpers gdrive)
- **Scope** : `gdriveLogin()` demande désormais `drive.file` **+ `drive.readonly`** (lecture seule de tout le Drive). Un token émis avant l'ajout du scope → 403 sur la recherche → erreur `SCOPE` → bandeau « reconnecte Google Drive » avec bouton.
- `actifsFindFile(token)` : cherche un **dossier** dont le nom contient « finance », puis un **tableur Sheets dedans** (de préférence nommé Finance) ; **repli** : tableur nommé « Finance » n'importe où. Fichier trouvé caché dans `localStorage spotifyplus_actifs_file` (1 seule recherche ; invalidé sur 404 export ou refresh forcé).
- `actifsFetch(force)` : `GET /drive/v3/files/{id}/export?mimeType=text/csv` — l'API Drive exporte la **PREMIÈRE feuille** du classeur, qui est la feuille **« Actif »** (tout le patrimoine y est). 401 → `gdriveLogout()` + throw `AUTH` ; 403 → `SCOPE` ; introuvable → `NOTFOUND` ; parse vide → `EMPTY`.
- **Parsing** : `actifsParseCsvRows` (CSV complet : guillemets/virgules/sauts de ligne quotés), `actifsNum` (nombres FR « 1 234,56 € » ET US « 1,234.56 », %, espaces insécables), `actifsDate` (ISO `YYYY-MM-DD` **et `YYYY-MM` mois seul**, `DD/MM/YYYY` fr, `M/D/YYYY` US désambiguïsé si un des deux nombres > 12, **`M/YYYY` mois/année — le format réellement utilisé dans la colonne A de la feuille Actif, ex. « 6/2026 » / « 09/2024 » → 1er du mois**), `actifsNorm` (en-têtes minuscules sans accents/ponctuation). **Matching par EN-TÊTE normalisé, pas par lettre de colonne** : `ACTIFS_FIELDS` (scalaires : patrimoine, budget, immo, 50/30/20, pyramide, FIRE), `ACTIFS_COMPTES` (16 comptes/plateformes), `ACTIFS_PHYSIQUES` (13 collections). ⚠ `indexOf` = 1re occurrence : les doublons d'en-têtes (Voiture/Ordi/Displate réapparaissent dans le bloc « répartition Exotique » plus à droite) matchent la colonne VALEUR. → `{ months (desc, 1 objet/mois), totals (lignes « Total 20XX ») }`.
- **Cache** : `loadActifs()`/`saveActifs()` → `localStorage spotifyplus_actifs` + cache mémoire `_actifsCache`. **TTL 6h** (`ACTIFS_TTL`) : au montage du panneau, cache affiché immédiatement puis re-fetch auto si périmé ; bouton **↻ Actualiser** (force = re-recherche du fichier). Simple cache re-fetchable : **PAS inclus dans buildBackup**. CSP : rien à ajouter (`www.googleapis.com` déjà autorisé).

### UI (`MesActifsPanel` + `ActifSection`/`ActifKV`/`ActifBar`, juste avant `FinancePanel`)
- **Bandeaux d'état** empilés au-dessus du contenu (le cache reste affiché dessous) : non connecté (+ bouton Connecter), erreur (`SCOPE`/`AUTH` → bouton reconnexion), spinner de sync.
- **Sections** : 💰 PATRIMOINE (net en gros + Δ mois/Δ an colorés, brut, hors immo, physique/numérique avec %, immo brut/net, syndic + **courbe `FinanceChart` réutilisée** — chips série Net/Brut/Hors immo × période 1 an/Tout) · 📆 BUDGET DU MOIS (dernier mois avec `revenu` renseigné : revenus/salaire/ventes/refonte/dépenses/achats/dette/économie + taux d'épargne + **barres 50/30/20** avec cibles) · 🏦 COMPTES & PLATEFORMES (valeurs ≥ 0,5 € triées desc, barres de part, négatif en rouge) · 🧸 ACTIFS PHYSIQUES & COLLECTIONS (idem) · 🔺 PYRAMIDE 10/80/5/5 (fractions → %, couleur par étage) · 🔥 LIBERTÉ FINANCIÈRE (barre de progression capital→cible, épargne lissée, gain mensuel moyen coloré, rendement, 3 métriques « Liberté » en années) · 📅 TOTAUX ANNUELS (grille mono Année/Salaire/Dépenses/Économie) · pied : nom du fichier + date de modif Drive + date de sync + ↻.
- Helpers UI : `fmtEur(v, dec)` (fr-FR), `fmtEurSigned`, `fmtActifMonth`. `latest` = 1er mois avec `net`, `lastFlow` = 1er mois avec `revenu` (le budget peut être saisi avant le patrimoine).

---

## PWA

- `manifest.json` à la racine — `start_url: /NewSpotifyRelease/`, `display: standalone`
- `service-worker.js` (cache `spotifyplus-v7` — v7 ajoute `vendor/leaflet.js`/`.css` au précache) — **network-first pour l'app shell** (`navigate`, `/`, `/index.html`) avec fallback cache hors-ligne (**mise en cache uniquement si `res.ok`** — une 404/5xx ne remplace plus l'app en cache) ; le fetch réseau de l'app shell utilise **`{ cache: 'no-store' }`** (fetch par URL, pas la Request en mode `navigate`) pour **bypasser le cache HTTP du navigateur** (`Cache-Control: max-age=600` de GitHub Pages) — sinon le network-first récupérait un `index.html` périmé et la version restait figée. Cache-first pour le reste (dont `vendor/`, précaché). Handler `activate` qui purge les anciens caches + `skipWaiting`/`clients.claim`.
- **⚠️ Clé de cache NORMALISÉE** : l'app shell est toujours stocké sous `'./index.html'` (`c.put('./index.html', copy)`), jamais sous l'URL réelle de navigation — sinon le retour OAuth (`?code=...&state=...`) écrivait le code d'autorisation dans Cache Storage
- **⚠️ L'ancienne stratégie cache-first servait l'index.html du cache pour toujours** → les PWA installées ne recevaient jamais les mises à jour. Ne pas revenir en cache-first pour l'app shell.
- Enregistrement dans `<head>` : `navigator.serviceWorker.register('./service-worker.js')`
- Icônes PWA : `icon-192.png` et `icon-512.png` présentes à la racine du repo (référencées par `manifest.json` et `notify()`)

---

## UI React — composants principaux

| Composant | Rôle |
|---|---|
| `StoreProvider` | Context global — auth, dbReady, syncState (+ scraping/paused/rlWaiting dérivés), stats, feed, rate-limit, player, loopEnabled, otherTab |
| `Home` | Page de login (mobile + desktop) |
| `MapsPanel` | **Section Maps — « Mes trajets »** (couleur `MAPS_COLOR = '#e6d53f'`, jaune, onglet sous To do desktop `maps` + mobile overflow) — visuel type « Vos trajets » de Google Maps : **carte Leaflet** du jour sélectionné (polylignes colorées par mode `MAPS_MODES`, lieux en `circleMarker` jaune, popups horaires/distance, `fitBounds` par jour, tuiles OSM assombries via classe CSS `maps-dark-tiles`) + **timeline horodatée** (icône mode, distance, `HH:MM – HH:MM · durée`) + **résumé du jour** (chips distance/durée/trajets/lieux). **Navigation** : ‹ › = jour renseigné précédent/suivant, `<input date>` libre, bouton « Auj. ». **Import** (bouton 📥, input file JSON) : `parseGoogleTimeline()` supporte les **2 formats d'export Google** (voir section Module Maps). **L'app ne trace JAMAIS la position** — voir la stratégie « scraping » dans Module Maps. Sous-composant `MapsMap({ segs })` : instance Leaflet dans un ref (créée 1×, détruite au démontage, `invalidateSize` après montage), Leaflet chargé lazy via `loadLeaflet()` (vendor/, promesse cachée `_leafletPromise`, fallback « Carte indisponible » si échec) |
| `WebApp` | Layout desktop (sidebar gauche + contenu) — barre du haut avec **onglets `view`** dans l'ordre `scrapping` / `historique` / `artistes` / `meteo` / `finance` / `tvtime` / `todo` / `maps` / `mdp` / `remember` / `stats` / `ecoutes` (À propos), rendus à partir de `navItems` (barre `overflowX:auto` → défile si trop étroit) — **⚠ cet ordre est propre au DESKTOP** (le mobile garde le sien). **⚠ Plus de colonne de droite ni de bandeau** : le contenu central affiche **une seule vue à la fois** — le feed (`ScrapingStatusPanel` + `FeedList`) pour `scrapping`, `ArtistsPanel` pour `artistes`, sinon **la section sélectionnée seule** (heading coloré + panneau, via le map `SECTION` = `{ heading, color, node, count? }`, dans un conteneur `maxWidth:640`) — dont l'onglet `stats` (`StatsPanel`, les chiffres) placé **juste avant** `ecoutes` (`VosEcoutesPanel`, À propos). Les panneaux Météo/Finance/… **ne montent que quand leur onglet est actif** (chargement lazy, comme sur mobile). La sidebar gauche (`DateRangePanel` / `NextCallPanel` / `LogsPanel`) reste inchangée. **Desktop uniquement** — le mobile (`MobileApp`) garde ses onglets |
| `ArtistsPanel` | **Section Artistes** — liste des artistes scrappés (`store.artists` ← `loadArtistsFromDB`) avec **filtres** (recherche nom/id, statut Tous/OK/**Partiel**/Erreur, tri : scan récent / **scan ancien** / nom A→Z / popularité / titres ajoutés / nb de scans) + compteur `X/total`. Chaque ligne via `ArtistRow` : pochette artiste, nom (ou « Artiste inconnu »), **id Spotify** (mono), badge statut (✓ OK vert / ⏳ Partiel orange = scan coupé par un 429 / ⚠ Erreur rouge), lien ↗ Spotify, chips genres (max 3), métriques (📅 dernier scrap relatif, ★ popularité, 👥 abonnés via `fmtCompactNum`, 🔁 nb scans, 🎵 titres ajoutés, 🆕 sorties au dernier scan). Affiché en onglet sur mobile, en vue centrale sur desktop |
| `MobileApp` | Layout mobile — **barre du haut unique** (un peu plus haute, `padding:'13px 14px'`) : **à gauche le profil** (photo `UserChip` + nom + statut `product`, à la place de l'ancien logo « Spotify+ »), **à droite les onglets**. Onglets : **3 principaux affichés** (Scrapping / En attente / ❤ Likés) + **menu « ⋯ »** (overflow) regroupant **Histo / Artistes / Météo / Finance / TV Time / To do / Mot de Passe / Remember / Stats / À propos** (dans cet ordre, Histo et Artistes en tête ; **Stats** (clé `statspanel`, chiffres) juste au-dessus d'**À propos** (clé `stats`, export/purge/version)). **⚠ Le bouton « Déconnecter » a été RETIRÉ du menu ⋯** et déplacé **tout en bas d'À propos** (`VosEcoutesPanel`, après « Version X »). `overflowTabs` = tableau `[clé, libellé, couleur]` — **chaque onglet a une couleur de l'ARC-EN-CIEL** (3e position) appliquée au libellé du dropdown : Histo `#b06cf5` (violet) → Artistes `#6f7bf5` (indigo) → Météo `#4ea3f5` (bleu) → Finance `#2fc1c1` (cyan) → **TV Time `#38c98a` (vert-cyan)** → To Do `#45c552` (vert) → **Maps `#e6d53f` (jaune)** → Mot de Passe `#f5b81f` (ambre) → Remember `#f5813c` (orange) → **Stats `#ee6a48`** (rouge-orangé) → À propos `#e05050` (rouge, = couleur du bouton Déconnecter). Ordre : **TV Time est inséré juste au-dessus de To Do, Maps juste dessous**. `menuOpen` (state local) ouvre un dropdown avec overlay de fermeture au clic extérieur ; le bouton ⋯ prend la couleur accent quand l'onglet actif est dans l'overflow. (L'ancienne 2e barre profil + bouton Déconnecter a été supprimée.) |
| `CompactPlayer` | **Vue ultra-compacte** affichée à la place de `MobileApp` quand le viewport est très court (`useShortViewport`, `innerHeight < 500` — ex. fenêtre du bas en split-screen sur tel). Affiche titre/artiste + contrôles **précédent / play-pause / suivant / ❤ like**. Le bouton like reprend la logique du `MobilePlayer` (`isLiked` initialisé en local depuis `feed`/`likedTracks`, `libraryContains` seulement si le titre est inconnu ; écritures via `librarySave`/`libraryRemove` + `libraryScopeAlert` + `setTrackLiked`). Sélectionné dans `Shell` : `isMobile && shortViewport → <CompactPlayer/>` |
| `DateRangePanel` | Bouton Reprendre (si session en cours) / Lancer ou Recommencer de 0 / Pause. Bandeau countdown si blocage 429 (`blockedUntil`) **ou** quota 24h atteint (`quotaUntil`) — boutons désactivés via `anyBlock` |
| `ScrapingStatusPanel` | Stats temps réel (3 boîtes : Artistes `X/Y` + `X/100 sur 24h` / Sorties / Titres) + **countdown quota 24h** sous la barre de progression quand `quotaUntil` est actif |
| `NextCallPanel` | Countdown + "Temps total restant" (ETA sur **tous** les artistes restant à scraper, PAS plafonnée aux 100/jour) + "Temps total de la session" (temps pour finir les 100/jour) + sélecteur délai |
| `LogsPanel` | Journal en temps réel |
| `FeedList` | Feed avec filtre type (Tous/Singles/Albums/Découvertes), filtre **genre** (sélecteur alimenté par les genres des artistes scannés), filtre artiste (texte), tri (ajout/date/artiste), bannière titres masqués. **Bannières de date (mobile uniquement)** : séparateur "📅 20 juin 2026" inséré à chaque changement de jour de sortie (`rawDate`) entre deux items consécutifs — actif en tri "Ordre d'ajout" et "Date sortie ↑", désactivé en tri "Artiste A→Z". La clé React est portée par un `<React.Fragment key={item.id}>` (bannière + FeedItem), les clés restent stables. **Empty-state** : « Aucune musique en attente » seulement si `feed.length === 0 && !filtersOn` — sinon (filtre/recherche actif) la toolbar reste visible (sinon impossible d'effacer une recherche à 0 résultat). **Bannière titres masqués masquée dès qu'un filtre/tri est actif** (`filtersOn`) : la requête balaie alors toute la DB (filtre+tri en SQL), le cap des 1000 ne masque plus de résultats correspondants |
| `FeedItem` | Ligne du feed : égaliseur animé, bouton ⏰ snooze (fin de liste), bouton × supprimer, bouton ❤ like, swipe gauche=suppr / droite=prev. **`React.memo` + props explicites** (`isNowPlaying`, `removeFromFeed`, `setTrackLiked`, `navigateFeed`, `snoozeTrack`) — ne consomme PAS `useStore` (sinon les ~1000 lignes re-rendent à chaque tick du poll 5s) |
| `LikerPanel` | Liste des titres likés (liked=1 en DB) avec bouton unliker et lecture |
| `HistoryPanel` | **Historique** des titres écoutés non purgés (listened=1), trié `listened_at DESC` (plus récent en haut) — horodatage relatif (`formatListenedAt`) + bouton réécouter. Desktop : sidebar droite sous VOS ÉCOUTES · Mobile : onglet **Historique** |
| `ListeningCharts` / `BarChart` | **Section 📈 GRAPHIQUE** (collapse dans `StatsPanel`, juste sous 🎵 MUSIQUES) — deux bar charts en divs (aucune lib externe → CSP inchangée) : **écoutes / jour** (14 j) et **tâches terminées / jour** (14 j, depuis `spotifyplus_todos_done`). **⚠ Écoutes/jour lues depuis un JOURNAL PERSISTANT** (`spotifyplus_listens` = `[{ ts }]`, helpers `loadListenLog`/`saveListenLog`/`recordListen`/`unrecordListen`, cap 5000) et **PLUS depuis `tracks.listened_at`** : ce journal **survit à la purge** (contrairement aux lignes `tracks` supprimées) → le graphe garde son historique. `recordListen()` est appelé aux **3 sites** de marquage écouté (effet URI, `markListened`, `removeFromFeed` branche `counted`), `unrecordListen()` à l'undo. Inclus dans `buildBackup`/`applyBackup` (clé `listens`, **FUSIONNÉ** : union des timestamps, cap 5000) + export dédié 📊 Stats (`fields:['stats','listens']`). Helpers module-level `_lastNDays(n)`, `listensPerDay(n)`, `todosPerDay(n)`. `ListeningCharts` recalcule au montage (donc à l'ouverture du collapse) |
| `AdvancedStats` | **Stats avancées** (Stats, collapse « 📊 STATS AVANCÉES » ambre `#e0a050` sous le Graphique) — calculées au montage depuis les titres écoutés **non purgés** : top 5 artistes écoutés, répartition Singles/Albums/Découvertes (avec %), moyenne d'écoutes/jour sur 30 j (`listened_at >= datetime('now','-30 days')`). ⚠ Repart de zéro après une purge (les compteurs de la table `stats` restent eux persistants) |
| `DropboxSync` | ⚠ **Le bouton `mode="restore"` (« ↥ Restaurer depuis Dropbox ») est TOUJOURS visible dès que Dropbox est configuré** (`DROPBOX_APP_KEY` non vide), même **non connecté** : dans ce cas le clic lance d'abord `dropboxLogin()` (libellé « (connexion requise) ») ; une fois connecté il restaure (libellé « (dernière sauvegarde) »). **Sync Dropbox optionnelle** (dans À propos / `VosEcoutesPanel`, sous le bouton export) — OAuth 2.0 **PKCE + offline** (refresh token en localStorage). ⚠ Nécessite une **app Dropbox** créée par le propriétaire du repo : constante module-level `DROPBOX_APP_KEY` (**renseignée** : app `spotifyplus-backup`, Scoped · App folder, scopes `files.content.write`/`files.content.read` — une App key est publique par nature en PKCE, pas de secret dans le code ; si vide, l'UI affiche « non configurée » et aucun appel réseau ne part). Helpers module-level `dropboxLogin()` (redirect `www.dropbox.com/oauth2/authorize`, `state='dropbox'`, `token_access_type=offline`), `dropboxExchange(code)` / `dropboxToken()` (refresh auto) → `https://api.dropboxapi.com/oauth2/token`, `dropboxUpload(obj)` → `https://content.dropboxapi.com/2/files/upload` (overwrite `/spotifyplus-sauvegarde.json`), **`dropboxDownload()`** → `https://content.dropboxapi.com/2/files/download` (parse le JSON, 409 = aucune sauvegarde). Le **retour OAuth est distingué de Spotify** par `state==='dropbox'` dans l'init (échange puis reprise de l'init Spotify normale). CSP `connect-src` : `api.dropboxapi.com` + `content.dropboxapi.com`. **Bouton « ↥ Restaurer depuis Dropbox »** (sous « Sauvegarder ») : télécharge la dernière sauvegarde puis `store.restoreBackup(data)` après `confirm()` (mêmes règles de fusion que l'import fichier — dates de scan jamais régressées, finance/écoutes fusionnés, reste remplacé). Le contenu uploadé = `buildBackup()` **COMPLET** = `{ stats, artists (dont last_scraped_at), music (table `tracks` : pistes du feed en attente + historique + likes), todos, todos_done, rappels (Remember fixes : dates), rappels_time (Remember fixes : heures optionnelles), remembers (Remember persos, avec `time` optionnel), tvtime (TV Time, métadonnées TMDB en cache comprises **+ chaînes/playlists YouTube et leurs vidéos**), tmdb_key (clé API TMDB, chaîne brute `spotifyplus_tmdb_key`), yt_key (clé API YouTube Data v3, chaîne brute `spotifyplus_yt_key`), maps (trajets Google Timeline importés, `spotifyplus_maps`), finance (historique local des prix, `spotifyplus_finance_history`), mdp_vault (blob CHIFFRÉ du coffre), modules (Paramétrage : modules activés/désactivés) }` — **même objet que l'export « Tous » local**, en mode `overwrite` (chaque sauvegarde écrase la précédente sur Dropbox). Le libellé du bouton précise « (tout, écrase la dernière version) » |
| `GoogleDriveSync` | **Sync Google Drive optionnelle** (dans À propos / `VosEcoutesPanel`, **juste sous `DropboxSync`**, même UX) — ⚠ Google n'autorise **pas** le PKCE sans secret pour les clients « Application Web » (l'échange de code exige un `client_secret`, interdit dans du code public) → **flux OAuth 2.0 IMPLICITE** (`response_type=token`) : access token **~1h** en localStorage (`gdrive_access`/`gdrive_expiry`), **PAS de refresh token** — à l'expiration le bouton repasse automatiquement en « Connecter » (reconnexion en 1 clic, consentement déjà accordé). Nécessite un **ID client OAuth Google** créé par le propriétaire du repo (console.cloud.google.com : activer l'API Drive, client type « Application Web », `REDIRECT_URI` en origine + URI de redirection autorisées) : constante module-level `GDRIVE_CLIENT_ID` (**renseignée** : ID client OAuth « Application Web » du projet Google Cloud — l'ID client est public par nature en flux implicite, aucun secret ; si vide, l'UI affiche « non configurée » et aucun appel réseau ne part). **Scopes `drive.file` + `drive.readonly`** : `drive.file` pour la sauvegarde (l'app ne voit QUE les fichiers qu'elle a créés) ; `drive.readonly` (lecture seule de tout le Drive) requis par l'onglet **Mes Actifs** de Finance — un token émis avant l'ajout de ce scope donne un 403 sur la recherche Drive et l'UI Mes Actifs propose une reconnexion. Helpers module-level : `gdriveLogin()` (redirect `accounts.google.com/o/oauth2/v2/auth`, `state='gdrive'`), `gdriveHandleRedirect()` (appelé **en tête de l'init** : le retour arrive dans le **fragment** `#access_token=…&state=gdrive`, jamais envoyé au serveur — stocké puis URL nettoyée, ne collisionne pas avec les retours Spotify/Dropbox en `?code=`), `gdriveToken()`/`gdriveConnected()` (valides seulement si non expiré), `gdriveFindBackupId(token)` (recherche `files?q=name='…' and trashed=false`, **id caché dans `gdrive_file_id`** → 1 seule recherche puis PATCH direct ; invalidé sur 404), `gdriveUpload(obj)` (PATCH `upload/drive/v3/files/{id}?uploadType=media` si le fichier existe, sinon POST `uploadType=multipart` qui le crée), **`gdriveDownload()`** (GET `drive/v3/files/{id}?alt=media`, parse le JSON ; 401 → `gdriveLogout()`, 404 → invalide `gdrive_file_id`). Fichier `spotifyplus-sauvegarde.json` = `buildBackup()` **COMPLET** (même contenu que Dropbox), écrasé à chaque sauvegarde ; pose aussi `spotifyplus_last_backup`. Sur **401** (token révoqué) : `gdriveLogout()` + retour à l'état déconnecté. CSP `connect-src` : `www.googleapis.com` (le redirect `accounts.google.com` est une navigation, pas un fetch → rien à ajouter). **Bouton « ↥ Restaurer depuis Google Drive »** (sous « Sauvegarder ») : télécharge la dernière sauvegarde puis `store.restoreBackup(data)` après `confirm()` (mêmes règles de fusion que l'import fichier) |
| `StatsPanel` | **Section Stats** (heading `STATS`, onglet desktop `stats` + onglet mobile `statspanel`, **juste au-dessus d'À propos**) — **sections repliables `StatsCollapse`** : **🎵 MUSIQUES** (fermée par défaut) — **structure : carte « RESTANTES À ÉCOUTER » mise en avant tout en haut (non repliée), puis des SOUS-COLLAPSES imbriqués** : **🎧 MUSIQUES ÉCOUTÉES** (compteurs mois / année / all-time + `AdvancedStats`), **⏱ TEMPS D'ÉCOUTE & LIKES** (**temps d'écoute restant AU-DESSUS du temps total écouté**, tous deux au format **« X Mois X Jours X Heure »** via `fmtMonthsDaysHours`, mois et jours toujours affichés → « 0 mois 0 j 5 h » à moins de 24 h, puis **% titres likés** via l'app), **📈 GRAPHIQUE** (`ListeningCharts` — désormais sous-collapse de 🎵 MUSIQUES, plus un collapse frère), **✅ TO DO** (fermée par défaut : tâches terminées de la journée / du mois / de l'année via `todoDoneStats()`, + **★ Tâches compliquées** = total des tâches favorites effectuées), **🎬 FILMS** (fermée par défaut, couleur `TVTIME_COLOR`) — **structure : une carte « TEMPS DE VISIONNAGE TOTAL » (films + séries + YouTube) mise en avant tout en haut, suivie du sous-collapse « 🏆 TOP 5 SÉRIES » (les 5 séries qui génèrent le plus de temps — titre + `X ép. × Y min` + temps `fmtMonthsDaysHours`, via `tv.topSeries` ; utile pour repérer une série au `epRuntime` gonflé qui fausse le total), puis des SOUS-COLLAPSES imbriqués** (tous à l'intérieur du collapse 🎬 FILMS) : **🔢 NOMBRE** (**ÉPISODES VUS** en gros = `episodes`, sous-titre « X série(s) vus » = `series` / **VIDÉOS YOUTUBE** = `ytVideos` / films vus / **INCLUANT MANGAS** = `mangaTitles` / **INCLUANT DESSINS ANIMÉS** = `cartoonTitles`), **⏱ TEMPS DE VISIONNAGE** (temps séries `epMin` / **temps YouTube** `ytMin` / temps films `movieMin` / **INCLUANT MANGAS** `mangaMin` / **INCLUANT DESSINS ANIMÉS** `cartoonMin`), **⏳ TEMPS RESTANT** (temps restant films `movieRemainMin` / **temps restant YouTube** `ytRemainMin` / temps restant séries `epRemainMin`), **✅ SÉRIES TERMINÉES** (**séries en cours** `seriesOngoing` = séries non 'done' / **séries terminées** `seriesDone` = statut 'done'). **Plus de section YouTube dédiée** : les 3 métriques YouTube (vidéos vues, temps, temps restant) sont **fusionnées dans les cartes séries/films** (insérées entre séries et films / entre temps séries et temps films / entre temps restant films et séries), affichées uniquement si `ytChannels > 0` (`hasYt`). Tout via `tvTimeStats()`, 100% depuis le cache localStorage TV Time, **0 requête API** ; temps séries = `watchedEps × epRuntime` par série ; **temps restant** = films non vus (`movieRemainMin`) + épisodes non vus `(totalEps − watchedEps) × epRuntime` (`epRemainMin`). `tvTimeStats()` renvoie aussi les compteurs **cartoon** (`cartoonTitles`/`cartoonEpisodes`/`cartoonMin`, label `cartoon` posé en fiche, même mécanique que `manga`) et **`topSeries`** (top 5 des séries par temps de visionnage, `[{ title, min, eps, epRuntime }]` trié desc, affiché sous le temps total). Les temps sont formatés en **« X Mois X Jours X Heure »** via le helper local `fmtMonthsDaysHours(ms)` de `StatsPanel` (mois = 30 j, **mois et jours toujours affichés** → « 0 mois 0 j 5 h » à moins de 24 h)), **🗺️ MAPS** (fermée par défaut, couleur `MAPS_COLOR` : **distance parcourue** — ordre **inversé, total en premier** : depuis toujours / cette année / ce mois-ci / aujourd'hui + **🥇 mode préféré** ; **toutes les valeurs en jaune `MAPS_COLOR`** — via `mapsStats()`, 100% localStorage `spotifyplus_maps` (cache mémoire `_mapsCache`, 0 requête)), — les **stats avancées** (`AdvancedStats`) sont rendues **dans le sous-collapse 🎧 MUSIQUES ÉCOUTÉES** (il n'y a **plus de collapse dédié « 📊 STATS AVANCÉES »**), et `ListeningCharts` est rendu dans le sous-collapse **📈 GRAPHIQUE de 🎵 MUSIQUES**, **🔔 REMEMBERS** (fermée par défaut, couleur `#f5813c`) — **structure : la mini-timeline « prochaines échéances » (4 rappels à venir les plus proches, pastille colorée + libellé + badge) mise en avant tout en haut**, puis des SOUS-COLLAPSES : **⏰ ÉCHÉANCES** (en retard / dans 30 jours / prochaine échéance délai+libellé) et **📋 RAPPELS** (rappels persos / dates renseignées `X/total`) — via `rememberStats()` (module-level) et `rememberAllWithDelta()`, 100% localStorage `spotifyplus_rappels`/`spotifyplus_remember_custom`, 0 requête). Lit `listenStats` + `now` du store. `StatsCollapse({ title, color, defaultOpen, children })` = header cliquable (chevron ▶ qui pivote) + contenu conditionnel |
| `VosEcoutesPanel` | **Section À propos** (heading `À PROPOS`, onglet desktop `ecoutes` + onglet mobile `stats`) — **actions de données, dans l'ordre** : (1) **section repliable « ⚙️ PARAMÉTRAGE »** (`StatsCollapse`) = un **toggle switch** par module basculable (`TOGGLE_MODULES`) via `setModuleEnabled` — active/désactive l'onglet correspondant (desktop & mobile) en direct, réglage inclus dans l'export/import ; (2) **section repliable « ↧ EXPORTER MES DONNÉES »** (`StatsCollapse`, **fermée par défaut**) avec **11 boutons d'export séparés** définis par le tableau module-level `EXPORT_SECTIONS` = 📦 Tous (export complet) / 🎤 Artistes (`artists`) / 🎵 Musique (`music`, table `tracks` : feed en attente + historique + likes) / 🎬 TV Time (`tvtime`+`tmdb_key`+`yt_key`, séries/films **et chaînes/playlists YouTube**) / ✅ To do (`todos`+`todos_done`) / 🗺️ Maps (`maps`) / 📈 Finance (`finance`, historique local des prix) / 🔑 Mot de passe (`mdp_vault`, blob chiffré) / 🔔 Remember (`rappels`+`remembers`) / 📊 Stats (`stats`+`listens`) / ⚙️ Paramétrage (`modules`, modules activés/désactivés) — chaque bouton appelle `downloadBackup(sec)` : un export partiel = fichier `spotifyplus-<key>-<date>.json` contenant `app/version/exported_at/scope` + les clés choisies (restaurable par ↥ Restaurer, `applyBackup` ignore les clés absentes) ; **seul l'export complet « Tous » pose `spotifyplus_last_backup`** (proposition hebdo) ; (3) **`<DropboxSync mode="save" />`** puis (4) **`<GoogleDriveSync mode="save" />`** (boutons Sauvegarder / Connecter) ; (5) bouton **↥ Restaurer mes données** (input file JSON → `restoreBackup`, confirm avant application) ; (6) **`<DropboxSync mode="restore" />`** puis **`<GoogleDriveSync mode="restore" />`** (bouton Restaurer seul, visible uniquement si connecté) ; (7) bouton **🔄 Réinitialiser le quota 24h** (affiche `X/100` + `· bloqué` si fenêtre active, `confirm()` puis `resetQuota()`) ; (8) bouton **🗑 Purger les écoutes** ; (9) **bouton « ⎋ Déconnecter » (rouge)** — déplacé ici depuis le menu ⋯ mobile (`logout` du store) ; et **texte gris « Version {APP_VERSION} »** centré **sous le bouton Déconnecter** (tout en bas). Les composants `DropboxSync`/`GoogleDriveSync` prennent un **prop `mode`** (`'save'` défaut = Connecter/Sauvegarder/Déconnecter ; `'restore'` = bouton Restaurer seul, `null` si non connecté) → sauvegardes et restaurations placées à des endroits distincts. (Les chiffres/graphiques ont été déplacés dans `StatsPanel`.) |
| `ActuPanel` | **Section Actu** (titre `ACTU` en bleu-indigo `ACTU_COLOR = '#5f8def'`, onglet desktop `actu` + onglet mobile `actu`, **entre Artistes et Météo**) — agrégateur d'actualités **0 backend, sources gratuites sans clé**. **4 cartes** : **📰 Techno · IA · Science** via l'API **Hacker News/Algolia** (`GET https://hn.algolia.com/api/v1/search?tags=front_page`, **CORS natif**, ajouté à la CSP `connect-src` — titre + `▲ points` + lien) · **🇫🇷 Presse FR** (IA/science/techno) via le **flux RSS Google News** (`news.google.com/rss/search?q=…&hl=fr&gl=FR`, **proxy CORS `corsFetchText`** — le titre « Titre - Source » est séparé) · **🔥 Tendances du jour · France** via **Google Trends RSS** (`trends.google.fr/…/daily/rss?geo=FR`, repli `trends.google.com/trending/rss?geo=FR`, proxy CORS ; lit `<ht:approx_traffic>`) — **⚠ substitut aux tendances Twitter/X** (API X fermée, aucune source keyless CORS) · **🔓 Dernières fuites de données** = **bonjourlafuite.eu.org** (pas d'API : HTML récupéré via `corsFetchText`, **parseur heuristique** `actuFetchLeaks` qui extrait les blocs `article/li/tr/hN` **datés** ; si aucune entrée datée → `needsTuning` → message + lien vers le site). Helpers module-level : `loadActu`/`saveActu` (cache localStorage `spotifyplus_actu`, **TTL 30 min**, cache mémoire `_actuCache`), `actuDecode` (entités HTML + retrait balises), `actuAgo` (horodatage relatif), `actuParseRss` (RSS/Atom → `[{title,link,date,source}]`), `actuFetchHN`/`actuFetchPresse`/`actuFetchTrends`/`actuFetchLeaks`, `actuFetchAll` (Promise.all des 4, chaque fetcher catch en interne → jamais de reject). UI : bouton **↻ Actualiser**, cache affiché immédiatement au montage puis re-fetch auto si périmé. **Simple cache re-fetchable : PAS inclus dans buildBackup.** CSP : seul `hn.algolia.com` ajouté (le reste passe par les proxies `codetabs`/`cors.eu.org`/`allorigins` déjà autorisés). Basculable via Paramétrage (`TOGGLE_MODULES`, id `actu`) |
| `MeteoPanel` | **Section Météo** (titre `MÉTÉO` en bleu `METEO_BLUE = '#4ea3f5'`) — **section/onglet propre** : sidebar droite desktop (entre Météo et Historique) + **onglet dédié `meteo`** dans le menu overflow mobile (plus dans l'onglet Stats). Prévisions **3 jours** (aujourd'hui + 2) pour **4 lieux dans l'ordre** : Voisins-le-Bretonneux + **Massy** + Boulogne-Billancourt (coords codées en dur dans `METEO_LOCS`) puis **la position actuelle** (`navigator.geolocation.getCurrentPosition`, fallback « Position non autorisée » si refus/erreur). Le libellé de ce dernier lieu = **le nom de la ville détectée** (ex. « Boulogne-Billancourt ») — obtenue par reverse-geocoding des coords via `reverseGeocode(lat,lon)` qui **enchaîne 3 providers gratuits/sans clé, le premier qui renvoie un nom gagne** (selon position/réseau certains échouent → CORS, rate-limit, champ vide) : **1) BigDataCloud** (`GET https://api.bigdatacloud.net/data/reverse-geocode-client`, pensé navigateur, CORS permissif, pas de rate-limit — lit `city || locality || principalSubdivision`) ; **2) Photon/Komoot** (`GET https://photon.komoot.io/reverse?lang=fr`, basé OSM — lit `features[0].properties.city || name || locality || district || county || state`) ; **3) Nominatim/OSM** (`GET https://nominatim.openstreetmap.org/reverse?format=jsonv2&zoom=12&accept-language=fr`, rate-limit 1 req/s — lit `address.city || town || village || municipality || suburb || county || state`). **Si les 3 providers réseau échouent** (bloqueur pub/tracking, rate-limit, hors-ligne), fallback **HORS-LIGNE** `nearestCityFR(lat,lon)` : ville française la plus proche calculée localement (distance euclidienne corrigée en `cos(lat)`) depuis la liste embarquée `FR_CITIES` (dense en Île-de-France + grandes villes nationales) — **aucune requête réseau, jamais null**. « Ma position » ne s'affiche donc plus que si la **géolocalisation elle-même** échoue/est refusée (« Position non autorisée ») (ou « Position non autorisée » si la géoloc est refusée). Données via **Open-Meteo** (API gratuite sans clé) : `GET https://api.open-meteo.com/v1/forecast?daily=weather_code,temperature_2m_max,temperature_2m_min&forecast_days=<N>`. **Bouton Light/Full** (état `full`, **NON persisté** — toujours **Light** au (re)chargement/reconnexion) en haut à droite, même UX que Finance : **Light = 3 jours** (aujourd'hui + 2), **Full = 7 jours** (tuiles plus petites — emoji/police/padding réduits, gap resserré). Le nombre de jours passe dans `fetchMeteo(lat,lon,days=3)` / `fetchMeteoDetail(lat,lon,days=3)` (`forecast_days`) ; le `useEffect` de `MeteoPanel` dépend de `[full]` (refetch au bascule), et `MeteoLoc` reçoit le prop `full` (purge son détail caché + ferme la modal quand `full` change). `fetchMeteo(lat,lon)` mappe `daily` en `{date,code,tmax,tmin}` ; `wmoMeteo(code)` → `[emoji, libellé FR]` (codes WMO) ; `meteoDayLabel(iso,i)` → « Auj./Lun./… ». Chaque jour : emoji + tmax (en bleu, mono) + tmin. Sous-composant `MeteoLoc` (1 lieu, stocke aussi `lat`/`lon` pour le détail) ; fetch en parallèle dans un `useEffect([full])` avec garde `cancelled`. **Détail au clic** : cliquer un jour ouvre `MeteoDetailModal` (overlay) avec beaucoup plus de données pour ce jour, chargées à la demande via `fetchMeteoDetail(lat,lon,days)` (Open-Meteo, champs `apparent_temperature`, `precipitation_sum/probability/hours`, `wind_speed/gusts/direction`, `uv_index_max`, `sunrise/sunset` + détail **heure par heure** : temp, proba pluie, humidité). Helpers : `windCardinal(deg)` (rose des vents FR), `meteoFullDate(iso)`, `fmtHM(iso)` |
| `FinancePanel` | **Section Finance** (titre `FINANCE` en cyan `FINANCE_CYAN = '#2fc1c1'` — même couleur que l'onglet Finance du menu ⋯) — données live via **APIs gratuites sans clé** (choix utilisateur : tout keyless, fiabilité variable côté indices/actions). Affichée en section propre sidebar droite desktop (sous Météo) + **onglet dédié `finance`** mobile. **Ordre des sections** : CRYPTO → INDICES → STOCK PICKING → **MATIÈRES PREMIÈRES** → **MONNAIE** (MATIÈRES PREMIÈRES placée juste avant MONNAIE). Section **MONNAIE** en **bas de page (Light + Full)** = ligne **1 EUR → USD** (valeur `eurusd.toFixed(4)`). Sous-blocs `FinanceSection` (titre bleu + badge **EUR/USD** affiché dans chaque section). **Taux EUR/USD via Twelve Data** (`fetchTD('EUR/USD')`, forex tier gratuit, CORS natif) avec **repli Frankfurter** (`api.frankfurter.app/latest?from=EUR&to=USD`, BCE) si la clé TD est absente/épuisée contenant des `FinanceRow` (icône + nom + valeur mono + variation 24h colorée `FinancePct`). Helpers : `fmtPrice(v)` (gros nombres sans décimale, petits en `toPrecision(3)` pour PEPE), `FinancePct` (+vert/−rouge). **Boutons Light/Full/Mes Actifs** (état `mode` `'light'|'full'|'actifs'`, `full`/`actifs` dérivés, **NON persisté** — toujours **Light** au (re)chargement/reconnexion) en haut à droite : le 3e onglet **Mes Actifs** remplace TOUTES les sections marché par `<MesActifsPanel/>` (patrimoine perso depuis le Google Sheets « Finance » du Drive — voir la section **Module Mes Actifs** plus bas ; ne déclenche AUCUN appel des APIs de cours, `startFinanceFull` ne part que via le mode Full) ; en **Light** seules les lignes essentielles sont affichées (**Bitcoin, PEPE, Or, Pétrole, S&P 500, NASDAQ-100, NVIDIA, Take-Two, EUR/USD**), les autres (`{full && …}`) n'apparaissent qu'en **Full**. **Crypto** : `CRYPTO` = Bitcoin / **Ethereum / Solana / TAO (bittensor)** / XRP / PEPE via **CoinGecko** `api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum,solana,bittensor,ripple,pepe&vs_currencies=usd&include_24hr_change=true`. **Ethereum et Solana utilisent des logos SVG inline** (composants module-level `EthIcon` / `SolIcon`, gradient Solana `#00FFA3→#DC1FFF`, `icon={<EthIcon/>}` / `icon={<SolIcon/>}`) au lieu des anciens symboles texte Ξ / ◎ — SVG inline, aucune image externe, donc rien à ajouter à la CSP. Le prop `icon` de `FinanceRow` accepte donc texte/emoji **ou** élément React. **Pétrole/indices/actions via Twelve Data (clé) + repli Stooq** : helper `fetchQuote(tdSymbol, stooqSymbol)` essaie d'abord **Twelve Data** (`fetchTD` → `api.twelvedata.com/quote?symbol=…&apikey=TWELVE_DATA_KEY`, **CORS natif → aucun proxy**, lit `close` + `percent_change`), puis retombe sur **Stooq** (`fetchStooq(symbol)` → CSV `stooq.com/q/l/?s=…&f=sd2t2ohlcv&h&e=csv`, variation = (close−open)/open) si le symbole n'est pas couvert par le tier gratuit ou si la clé est absente. **`TWELVE_DATA_KEY`** = constante module-level (clé gratuite lecture seule ; **publique par nature** sur une app statique, ne jamais y mettre une clé payante/avec carte/accès compte). Symboles TD : `NVDA`/`TTWO`/`GOOGL`/`MSFT`/`AMZN`/`TSLA` (actions) — **uniquement des actions US, que TD gratuit couvre** ; symboles Stooq de repli : `nvda.us`/`ttwo.us`/`googl.us`/`msft.us`/`amzn.us`/`tsla.us`. **Les indices (NASDAQ-100/S&P 500/CAC 40) NE passent PLUS ni par TD ni par AV (ETF)** mais par **un JSON same-origin `data/indices.json`** généré par une **GitHub Action** (cf. INDICES ci-dessous) ; le pétrole reste via Alpha Vantage. **⚠ Chargement LAZY par mode** : au montage on ne charge QUE les valeurs Light (NDX, pétrole, NVDA, TTWO + crypto/or/EUR-USD) ; les valeurs **Full** (`.INX`, `PX1`, GOOGL/MSFT/AMZN/TSLA) sont chargées **à la demande** au **1er passage en mode Full** (`startFinanceFull`, idempotent via `_finance.startedFull`) — économise des requêtes quand l'utilisateur reste en Light. **⚠ Gestionnaire MODULE-LEVEL `_finance` (PAS dans le composant)** : objet `{ eurusd, crypto, gold, oil, idx, stocks{}, silver, sugar, loadingLight, loadingFull, startedLight, startedFull, subs:Set }` (`idx` = `{ ndq, spx, cac }` chacun `{ price, pct }`, lu depuis `data/indices.json`) + helpers `startFinanceLight()` / `startFinanceFull()`. **⚠ Lancé UNIQUEMENT au mount de `FinancePanel`** (`startFinanceLight()` dans son `useEffect([])`, idempotent) — **PLUS au login** : les APIs dédiées (Alpha Vantage, Twelve Data, CoinGecko, gold-api) ne partent que quand l'utilisateur ouvre la section/onglet Finance. (Sur mobile l'onglet `finance` est démonté tant qu'il n'est pas ouvert → 0 requête ; sur desktop le panneau est en permanence dans la sidebar droite donc il monte avec le layout connecté.) Le gestionnaire `_finance` survit au démontage de l'onglet mobile (les valeurs restent en cache). `FinancePanel` s'abonne via `_finance.subs.add(forceRender)` (useReducer) et lit `_finance` directement. **⚠ File TD `tdSpaced(fn)`** : Twelve Data plan **Basic 8 = 8 req/MINUTE** → TOUS les appels TD (`fetchTD`/`fetchQuote`) passent par une **file globale `_tdChain`** garantissant ≥ **`TD_GAP_MS` (8000 ms)** entre deux appels (sinon le KPI "Minutely maximum" dépasse 8 et TD throttle en 429). Crypto (CoinGecko), or (gold-api) et le repli Stooq ne comptent PAS pour TD → lancés en parallèle/immédiats. Light et Full partagent la même file (Full s'enchaîne derrière Light). **Loading clair** : bandeau bleu avec spinner (`@keyframes finspin`) tant que `loadingLight` (ou `full && loadingFull`) ; affiche **« Chargement de {valeur}… »** où `{valeur}` = `_finance.nextLabel`, posé par `tdSpaced(fn, label)` quand la tâche arrive **en tête de file** (donc toujours juste, même si Light et Full empilent en même temps). **Source EU choisie volontairement** : pas de mur de consentement RGPD (Yahoo redirige les IP UE vers une page de consentement → JSON cassé, abandonné). **⚠ Stooq n'envoie pas d'en-tête CORS** → `fetchStooq` passe par `corsFetchText(url)` qui essaie **plusieurs proxies CORS keyless SANS inscription, le 1er qui répond gagne** : `api.codetabs.com/v1/proxy/?quest=` → `cors.eu.org/` (proxy européen) → `api.allorigins.win/raw?url=` → fetch direct. (`corsproxy.io` abandonné : exige une inscription.) **⚠ Chargement SÉQUENTIEL** : les 10 cours sont récupérés **un par un** (boucle `for…of` avec pause 350 ms), pas en parallèle — tirer toutes les requêtes d'un coup sature le proxy (rate-limit) et fait échouer la plupart. Ordre : valeurs visibles en mode Light d'abord (`^ndq`, `cl.f`, `nvda.us`). **Matières premières** : `MATIÈRES PREMIÈRES` = Pétrole WTI via **Alpha Vantage** (`fetchAlphaOil()` → endpoint `function=WTI&interval=daily`, **CORS natif**, vrai prix $/baril, clé `ALPHA_VANTAGE_KEY` gratuite ~25 req/jour, lecture seule, publique par nature) + Or once (`fetchGold()` = `fetchMetal('XAU')` → **gold-api.com** `/price/{symbole}`, CORS natif, sans clé, chargé en parallèle) + **(Full)** Argent (`XAG`) via `fetchMetal` (`_finance.silver`, gold-api, parallèle dans `metalsTrack`) + **Sucre via Alpha Vantage** : `fetchAlphaCommodity('SUGAR')` (endpoint matière première **dédié** `function=SUGAR&interval=monthly`, **CORS natif**, vrai prix mondial avec variation mois/mois `{price, pct}`) — chargé **dans la piste AV séquentielle** de `startFinanceFull` (`_runAlphaTrack`). (Le Blé a été retiré.) `fetchAlphaOil()` est désormais un alias de `fetchAlphaCommodity('WTI', 'daily')`. **Flag `TD_ENABLED` (module-level)** : `false` désactive TOUS les appels Twelve Data (`fetchTD` renvoie `null` → EUR/USD repli Frankfurter, actions vides, pistes TD sautées) — utile en phase de test pour isoler les sources keyless. **Actuellement `true`** (TD actif pour EUR/USD + actions). **INDICES (vrais indices, via GitHub Action)** : `INDICES` = S&P 500 (`^GSPC`, Light) / NASDAQ-100 (`^NDX`, Light) / CAC 40 (`^FCHI`, Full) — **vrais niveaux d'indices, plus d'ETF**. ⚠ Aucune API gratuite à **CORS natif** ne donne ces indices au navigateur (TD/FMP/Finnhub les réservent au payant, AV n'a que les ETF, Yahoo n'a pas de CORS + mur RGPD UE). **Solution retenue** : une **GitHub Action** (`.github/workflows/update-indices.yml`, cron toutes les 2 h lun–ven 06–22 UTC + `workflow_dispatch`) exécute `scripts/fetch_indices.py` **CÔTÉ SERVEUR** (ni CORS ni mur RGPD à ce niveau) et commit `data/indices.json`. ⚠ Yahoo bloque les **IP datacenter** des runners en `429` sans session cookie/crumb → le script interroge **Stooq en priorité** (CSV `^ndq`/`^spx`/`^cac`, fiable depuis un CI) avec **repli Yahoo** (endpoint `chart` v8 + flux cookie+crumb). L'app lit ce fichier via `fetchIndicesJSON()` (`fetch('./data/indices.json?_=<ts>', {cache:'no-store'})`, **same-origin = CORS natif, zéro proxy, zéro clé**) **en mode Light** (1 seul fetch → les 3 indices, S&P/CAC déjà en mémoire pour le passage en Full). Données EOD/différé (pas temps réel), rafraîchies plusieurs fois/jour ouvré. Le script **préserve la valeur précédente** d'un symbole en cas d'échec ponctuel ; Les helpers inutilisés `fetchAlphaQuote`/`fetchYahooQuote`/`fetchIndexQuote`/`_runIndexTrack` ont été **supprimés** (plus aucun appel Yahoo côté navigateur → `query1.finance.yahoo.com` retiré de la CSP). `startFinanceLight`/`startFinanceFull` lancent **2 pistes en parallèle** (AV pétrole+sucre/blé + TD EUR-USD/actions) et n'éteignent `loadingLight`/`loadingFull` qu'au `Promise.all` des deux ; les indices (`fetchIndicesJSON`), la crypto et l'or partent en parallèle immédiat (non awaités). **Stock picking** : `STOCK PICKING` = NVIDIA (`nvda.us`, visible en Light) / Take-Two (`ttwo.us`) / Google (`googl.us`) / Microsoft (`msft.us`) / Amazon (`amzn.us`) / Tesla (`tsla.us`) — toutes sauf NVIDIA sont **mode Full uniquement**. Fetch dans un `useEffect([])` avec garde `cancelled`, fallback `—` sur échec. **Origines ajoutées à la CSP `connect-src`** au fur et à mesure. **📈 GRAPHIQUE AU CLIC (`FinanceChartModal`)** : chaque `FinanceRow` porte un prop `chart` = spec `{ kind, id, interval? }` + `onChart` (→ `setChartSpec` dans `FinancePanel`) — la ligne devient cliquable (curseur pointer, petit 📈 gris après le nom) et ouvre une **modal** (gabarit MeteoDetailModal) avec la **courbe historique du prix** en **SVG maison** (`FinanceChart` : aire dégradée + ligne 2px, couleur `FIN_UP`/`FIN_DOWN` selon la variation sur la période, 3 gridlines pointillées **avec le prix en étiquette sur l'axe Y** (fmtV au-dessus de chaque gridline), point final, **crosshair** au `onPointerMove`/`Down` avec readout `date · valeur` dans un slot fixe — fonctionne aussi au doigt via `touchAction:'none'`), min/max sous la courbe, dates début/fin. **4 sources d'historique** (helpers module-level, TOUTES déjà dans la CSP — rien à ajouter) : `kind:'coingecko'` (crypto : `/coins/{id}/market_chart?days=…`, 1 fetch PAR période car la granularité dépend de `days`), `kind:'frankfurter'` (EUR/USD : `fetchFxHistory()`, 1 an de fixings BCE en 1 requête), `kind:'stooq'` (indices `^spx`/`^ndq`/`^cac`, actions `nvda.us`…, or `xauusd`/argent `xagusd` : `fetchStooqHistory()` = CSV quotidien 1 an via `corsFetchText`), `kind:'alpha'` (pétrole WTI daily / sucre SUGAR monthly : `fetchAlphaSeries()` — ⚠ consomme 1 req du quota AV ~25/j au 1er clic seulement). **Cache session `_chartCache`** (module-level) : historiques complets cachés par source, découpés par période côté client (`CHART_RANGE_DAYS`), ré-échantillonnés ≤ 400 pts (`chartDownsample`). Périodes (`chartRanges(spec)`) : crypto `24h/7j/1m/1a`, alpha monthly `1a/5a/10a`, reste `7j/1m/3m/1a` — **défaut = `7j` quand la période existe** (indices/actions/métaux/pétrole/EUR-USD ouvrent sur la semaine), sinon 2e entrée (sucre → `5a`). Échec distant → **repli sur l'HISTORIQUE LOCAL échantillonné** : `_finNotify` appelle `finHistSnapshot()` qui enregistre les valeurs affichées de `_finance` dans localStorage `spotifyplus_finance_history` (`FIN_HIST_LS`, `{ version:1, series:{ '<clé>': [{t,v},…] } }`, clés `finHistKey(spec)` = `cg:bitcoin`/`stooq:^spx`/`alpha:WTI`/`fx:EURUSD`). **⚠ Enregistrement = MAX 1 point par jour calendaire et par donnée** (`_finDayKey`, plus `≥ 30 min`), et **UNIQUEMENT pour les valeurs du mode LIGHT** (Bitcoin, PEPE, S&P 500, NASDAQ-100, NVIDIA, Take-Two, Or, Pétrole, EUR/USD) — les valeurs **Full** (autres cryptos, CAC 40, autres actions, argent, sucre) **n'enregistrent RIEN**. **Conservation ~1 an** (`FIN_HIST_MAX_AGE` = 366 j, purge des points plus vieux + cap `FIN_HIST_MAX_PTS` = 370), cache mémoire `_finHistCache`, écriture localStorage seulement si un point a été ajouté. **⚠ Les graphiques `kind:'stooq'` (indices/actions/or) PRIVILÉGIENT l'historique local** (source distante Stooq via proxies CORS peu fiable) : la modal affiche `finHistPoints(spec, range)` directement s'il y a ≥ 2 points locaux, et ne tente le fetch distant qu'en dernier recours. Pour les autres sources (`coingecko`/`frankfurter`/`alpha`), fetch distant d'abord puis repli local si échec — bandeau « courbe reconstituée depuis l'historique local » ; si < 2 points locaux → message expliquant que la courbe se construira au fil des visites. **Inclus dans buildBackup/applyBackup** (clé `finance`, export dédié 📈 Finance + export « Tous » ; à la restauration : **FUSIONNÉ**, union par série + tri + densité 30 min + cap — jamais écrasé). **Aucune lib de chart** (SVG pur → CSP inchangée) |
| `TvTimePanel` | **Section TV Time** (titre `TV TIME` en vert-cyan `TVTIME_COLOR = '#38c98a'`, cran de l'arc-en-ciel entre Finance et To do) — **suivi perso séries & films via l'API TMDB v3** (gratuite : **PAS de plafond journalier** depuis 2019, seulement un rate-limit ~50 req/s — largement suffisant). Onglet desktop `tvtime` + onglet mobile (menu ⋯), **au-dessus de To do**. **Clé API requise** : générée par l'utilisateur sur themoviedb.org (Paramètres → API), saisie dans l'UI et stockée en `localStorage` (`spotifyplus_tmdb_key`, helper `tmdbKey()`) — repli constante module-level `TMDB_API_KEY` (vide). Sans clé : encart d'instructions, **aucun appel réseau** ; bouton ⚙ pour changer la clé. **⚠ OPTIMISATION REQUÊTES** : à l'ajout d'un titre, **1 seule requête** `/movie/{id}` ou `/tv/{id}` (`tmdbFetchItem(type, id)`) récupère TOUT (titre, affiche `poster_path`, année, durée film, saisons `[{n,eps}]` sans la saison 0, nb total d'épisodes, durée moyenne/ép. avec replis `episode_run_time[0]` → `last_episode_to_air.runtime` → 35 min, statut de production, prochain épisode `next_episode_to_air`) → mis en cache dans `localStorage` (`spotifyplus_tvtime`, helpers `loadTvItems`/`saveTvItems`) ; l'affichage, les compteurs ±1 épisode et les stats ne redemandent **JAMAIS rien** à l'API. Seul `tvRefreshShows(items, force)` ré-interroge `/tv/{id}` des séries suivies **non 'done' et non `Ended`/`Canceled`** avec **TTL 24h** (`refreshedAt`, auto au montage + bouton ↻ manuel) — il retourne une Map de champs frais (jamais `watchedEps`/`status`) appliquée **sur le dernier état persisté** (`applyFresh` relit `loadTvItems()` : une action utilisateur pendant le refresh n'est pas écrasée). Item = `{ id, type:'movie'\|'tv', title, poster, year, status:'towatch'\|'watching'\|'paused'\|'done', addedAt, doneAt? · film : runtime (min), releaseDate (date de sortie ISO, pour PROCHAINES SORTIES), refreshedAt · série : epRuntime, totalEps, seasons, tvStatus, nextEp:{date,s,e,name}\|null, refreshedAt, watchedEps, watchedSet, specialsSeen }` (+ champ commun **`manga`** ci-dessous). **`specialsSeen`** = tableau des **n° d'épisodes spéciaux vus** (cochage **INDIVIDUEL** — cocher un spécial ne coche PAS les précédents, contrairement aux saisons officielles qui restent cumulatives ; migration de l'ancien compteur plat `watchedSpecials` int → `[1..n]` via `tvSpecialsSeen(item)`). **`manga`** (bool, sur un **film OU une série**) = label posé dans la fiche `TvDetailModal`, **PAS affiché dans les listes**, exploité uniquement dans les stats. Helpers module-level : `tmdbGet(path, params)` (ajoute `api_key` + `language=fr-FR`, retourne null si erreur/clé absente), `tvWatchedSet(item)` (**SOURCE DE VÉRITÉ** du suivi — ensemble des épisodes vus, clés `"S{n}E{e}"` hors spéciaux ; **cochage INDIVIDUEL par épisode** → n'importe quelle saison/épisode cochable isolément ; migration : un ancien item sans `watchedSet` reconstitue l'ensemble depuis le compteur plat `watchedEps` en marquant les N premiers épisodes dans l'ordre. `watchedEps` est maintenu = `watchedSet.length` pour les stats/listes), `tvAllKeys(item)` (toutes les clés d'épisodes — « tout vu »), `tvLastWatched(item)` (dernier ép. vu dans l'ordre — bouton −1), `tvNextToWatch(item)` (**1er épisode NON vu** dans l'ordre des saisons « S2E5 » — clé absente de l'ensemble vu), `tvSeasonBreakdown(item)` (décompte des épisodes vus PAR SAISON, membres de l'ensemble → `[{ n, eps, watched }]`, chaque saison indépendante), `tvStatusFR(s)` (statut de production TMDB → FR), `tvDaysUntil(iso)` (jours avant une date, minuit local), `tvTimeStats()` (stats du collapse Stats Film). **Fiche `TvDetailModal`** (clic sur l'**affiche** — d'une série dans les listes, **OU de n'importe quel résultat de recherche** film comme série via le prop `onPoster` de `row`/`poster` ; state `detail` `{type,id}`, item relu dans `items` à chaque render). **Clic sur une affiche de résultat de recherche → `openDetail(r)`** : si le titre est déjà suivi, ouvre sa vraie fiche ; sinon **1 requête `tmdbFetchItem`** récupère tout et l'affiche en **aperçu** (state `preview`, non enregistré) — la **1re action** dans la fiche (statut / ± épisode / coche) l'**ajoute à la liste** via `commitPreview(extra)` puis bascule sur sa vraie fiche. **Films pris en charge** (`isMovie = item.type === 'movie'`) : la fiche affiche le **synopsis** (`item.overview`, ajouté au retour movie de `tmdbFetchItem`) + année/durée + les boutons de statut, **sans** les sections de suivi par épisode (barre de progression, « Continuer le suivi », « Toutes les saisons » — masquées par `{!isMovie && …}`). Pour une **série** : popup gabarit MeteoDetailModal avec **progression décortiquée par saison** façon TV Time — en-tête (affiche, année, nb saisons/ép., statut de production FR, barre + % global), sélecteur de statut (📌/▶/✓), carte **« Continuer le suivi »** (prochain ép. `S02 | E05` + gros bouton ✓ = +1 et − = −1, « Série entière vue ✓ » si fini), **une ligne par saison** (barre de progression, compteur `x/y` mono, **coche circulaire** : cocher/décocher ajoute/retire TOUS les épisodes de CETTE saison dans l'ensemble vu (`S{n}E1..E{eps}`), indépendamment des autres saisons — il est possible de cocher les saisons 28-32 sans toucher 1-27), ligne **« Épisodes spéciaux »** si `specialEps > 0` : pseudo-saison grisée **DÉPLIABLE et COCHABLE** (liste individuelle `specialsSeen`, coche globale « tout cocher/décocher » + **coches par épisode INDÉPENDANTES** via le prop `onSetSpecials` → `setSpecials(item, arr)` — cocher le spécial E4 n'ajoute pas E1..E3 ; épisodes chargés via `tvFetchSeasonEps(id, 0)`) — suivable mais **JAMAIS comptée** dans `totalEps`/le % (+ note explicative). **Saison DÉPLIABLE (liste des épisodes façon TV Time)** : le header d'une ligne saison est cliquable (chevron ▶ qui pivote, state local `openSeason` — une seule saison dépliée à la fois ; la coche saison fait `stopPropagation`) → carte par épisode : **vignette 16:9** (`still_path` via `TMDB_STILL` = `image.tmdb.org/t/p/w185`, placeholder 📺 sinon), **`S02 | E01`** gras mono, nom de l'épisode, **coche verte par épisode** — cocher/décocher un épisode isolé bascule sa clé `S{n}E{e}` dans l'ensemble vu (via le prop `onToggleEp(n, e, seen)` → `toggleEp`). **1 seule requête `/tv/{id}/season/{n}` PAR SAISON au 1er dépliage** (`tvFetchSeasonEps(tvId, n)`, module-level) puis **cache localStorage `spotifyplus_tvtime_eps`** (`TVTIME_EPS_LS`, TTL 7 j `TV_EPS_TTL`, **cap 80 saisons** `TV_EPS_CACHE_MAX` avec éviction des plus anciennes, + cache mémoire session `_tvEpsCache`) — sur échec réseau : cache périmé si présent, sinon **épisodes placeholder générés** (`E01..En` sans nom/vignette) pour que les coches restent utilisables hors-ligne ; state `epsMap`/`epsLoading` (« Chargement des épisodes… »). Ce cache est un simple cache re-fetchable : PAS inclus dans buildBackup. `specialEps` (nb d'ép. de la saison 0) est renseigné par `tmdbFetchItem` et rafraîchi par `tvRefreshShows` — les hors-série ne comptent JAMAIS dans `totalEps`/la progression (une série est « vue en entier » sans eux) — mais les spéciaux **vus** (`specialsSeen.length`) comptent dans les stats d'épisodes de `tvTimeStats()`. **Label manga** : `tvTimeStats()` renvoie aussi `mangaTitles`/`mangaEpisodes`/`mangaMin` (films & séries taggés `manga`) — affichés dans le collapse **🎬 FILMS** de `StatsPanel` (2 cartes « Mangas / animés » + « Temps mangas », **visibles seulement si `mangaTitles > 0`**). Toggle **🍥 Manga** dans `TvDetailModal` (sous le sélecteur de statut, films comme séries) via le prop `onManga` → `setManga(item, bool)`. **Label `cartoon` (« 🎨 Dessin animé », bleu `#7aa5ff`)** : même mécanique que `manga` (toggle côté à côté dans `TvDetailModal` ET `YtDetailModal` via le prop `onCartoon` → `setCartoon(item, bool)`, préservé par les refresh YouTube, fusionné à l'import via `mergeTvItem`) et **lu par les stats** (`tvTimeStats()` renvoie `cartoonTitles`/`cartoonEpisodes`/`cartoonMin`, affichés en « INCLUANT DESSINS ANIMÉS » dans les sous-collapses du 🎬 FILMS) — drapeau posé sur l'item, inclus dans le backup. **Statut AUTO (`withEps` dans `TvTimePanel`)** : tout changement de `watchedEps` (±1, coche saison, coche épisode) passe la série automatiquement en **'done'** (+ `doneAt`) quand `watchedEps ≥ totalEps` (les spéciaux ne sont pas requis), et la repasse en **'watching'** si le compteur redescend sous le total alors qu'elle était 'done'. UI : **recherche** avec **filtre du type recherché** (state local `searchType` `'all'\|'movie'\|'tv'`, **NON persisté — « Les deux » par défaut à chaque montage** ; rangée de boutons Les deux / 📺 Séries / 🎬 Films sous la barre de recherche, **affichée UNIQUEMENT quand l'input de recherche a le focus** — state `searchFocused` posé par `onFocus`/`onBlur` de l'input ; condition d'affichage `searchFocused || results !== null` (reste visible tant que des résultats sont là, pour changer de type après coup) ; les boutons ont `onMouseDown preventDefault` pour ne pas voler le focus de l'input) : `'all'` → `/search/multi` (films+séries en **1 requête**), sinon `/search/movie` ou `/search/tv` (toujours 1 requête ; ces endpoints ne renvoient pas `media_type` → posé depuis le filtre) ; `pickSearchType(t)` **relance automatiquement la recherche** avec le nouveau type si des résultats sont affichés ; **pagination « lazy load »** : `results` conserve TOUT le fetch (toutes pages), mais seuls `visibleCount` (8 par défaut) résultats sont affichés (`results.slice(0, visibleCount)`) ; bouton **« ▼ Voir plus de résultats »** (`loadMore`) affiché tant que `visibleCount < results.length` OU qu'il reste des pages TMDB (`searchMeta.page < searchMeta.totalPages`) — il ajoute 8 à `visibleCount` et, si le fetch courant est épuisé, **récupère la page TMDB suivante à la demande** (`page: searchMeta.page + 1`, dédoublonnage par `id+type`, spinner « Chargement… » via `loadingMore`) pour trouver un titre absent des 8 premiers. `searchMeta` = `{ q, type, page, totalPages }` posé par `search`, remis à 8 à chaque nouvelle recherche ; le compteur affiche `RÉSULTATS · {affichés}{+ s'il reste plus}`. Boutons 📌 À voir / ▶ En cours / ✓ Vu — ajouter un doublon met simplement à jour son statut ; **★ badge « déjà suivi »** sur chaque résultat déjà dans la liste **quel que soit son statut** (film comme série) — `mark = !!exist` : étoile + mention colorée selon le statut (« ★ Déjà vu » verte `#4caf50` pour 'done' / « ★ En cours » vert-cyan `TVTIME_COLOR` pour 'watching' / « ★ À voir » ambre `#f5b81f` pour 'towatch') dans le sous-titre, via `markColor`/`markLabel` et lookup `items.find` par `id`+`type`) ; **filtre segmenté Films/Séries** (« Tous · N / 📺 Séries · N / 🎬 Films · N », affiché dès qu'il y a ≥ 1 item) : state `mediaFilter` (`'all'\|'movie'\|'tv'`) **persisté en localStorage** (`spotifyplus_tvtime_filter`, constante `TVTIME_FILTER_LS`) — les sections EN COURS / À VOIR / VUS sont calculées sur la liste filtrée (`visible`), PROCHAINES SORTIES (100% séries) est **affichée dans tous les modes de filtre** (Tous / Séries / Films) ; **filtre par nom + tri des listes** (rangée sous le filtre Films/Séries, affichée dès qu'il y a ≥ 1 item) : input texte `listFilter` (**non persisté**, match `includes` insensible à la casse sur `title`, bouton × pour effacer) + `<select>` `sortBy` **persisté** (`spotifyplus_tvtime_sort`, constante `TVTIME_SORT_LS`, options module-level `TVTIME_SORTS` : `added_desc` Ajout récent (défaut) / `added_asc` Ajout ancien / `name` Nom A→Z (localeCompare fr) / `year_desc` Sortie récente / `year_asc` Sortie ancienne) — appliqués dans `visible` (comparateur `tvCmp` sur `addedAt`/`title`/`year`), donc aux 3 sections En cours / À voir / Vus ; PROCHAINES SORTIES garde son tri par date de diffusion ; **5 sections TOUTES repliables** (header cliquable + chevron ▶ qui pivote, state local `secOpen` `{ watching, paused, towatch, upcoming, done }` — **seule EN COURS ouverte par défaut** (En pause / À voir / Prochaines sorties / Vus fermées) ; le bouton ↻ de Prochaines sorties fait `stopPropagation` pour ne pas replier la section), **dans l'ordre** : **▶ EN COURS** (progression `x/y ép. · prochain SxEy` ou `à jour ✓`, boutons **− / ＋** épisode vu (bornés 0..totalEps), **⏸ Mettre en pause pour les SÉRIES uniquement** (`setStatus(it,'paused')` → section EN PAUSE), **📌 repasser en À voir pour les FILMS uniquement** (`setStatus(it,'towatch')`), ✓ terminé, ×) / **⏸ EN PAUSE** (séries au visionnage suspendu, entre EN COURS et À VOIR ; boutons **▶ Reprendre le visionnage** (`setStatus(it,'watching')` → EN COURS) et **⏹ Arrêter le visionnage** (`abandonSeries(it)` → VUS **en CONSERVANT la progression** : passe `status='done'` sans toucher `watchedEps`, donc ne valide PAS les épisodes restants, contrairement au ✓ « tout vu »), ×) / **📌 À VOIR** (▶ commencer, ✓ vu, ×) / **📅 PROCHAINES SORTIES** (séries avec `nextEp` daté **ET films non encore sortis** — le contenu suit le filtre : Séries → prochains épisodes `SxEy « nom » · date`, — **⚠ une série « à jour » (`tvCaughtUp(item)` : tous les épisodes DIFFUSÉS vus + `nextEp` dans le futur) est retirée d'EN COURS et n'apparaît QUE dans PROCHAINES SORTIES tant que le nouvel épisode n'est pas sorti — le jour de la diffusion (`tvDaysUntil(nextEp.date) ≤ 0`) elle réapparaît automatiquement dans EN COURS.** Films → films à venir `🎬 Sortie ciné · date`, Les deux → les deux fusionnés triés par date ; badge « dans X j / demain », bouton ↻ — entre À voir et Vus, affichée dans tous les modes de filtre. **Films : un film à date de sortie STRICTEMENT future (`isUpcomingMovie` = `tvDaysUntil(releaseDate) > 0`) vit UNIQUEMENT dans PROCHAINES SORTIES et est retiré de la liste À VOIR ; le jour de la sortie ou après (`tvDaysUntil ≤ 0`) il n'est plus « upcoming » → il réapparaît automatiquement dans À VOIR.** Item film enrichi de `releaseDate` par `tmdbFetchItem` ; les films non vus sans date / à venir sont backfillés/rafraîchis par `tvRefreshShows`) / **✓ VUS** (fermée par défaut, ↩ repasser en cours, ×). **✓ sur une série pose `watchedEps = totalEps`** (tout vu → alimente les stats). Suppression avec `confirm()`. Affiches via `https://image.tmdb.org/t/p/w92{poster_path}` (`TMDB_IMG`), placeholder emoji 🎬/📺 sinon. Inclus dans `buildBackup`/`applyBackup` (clé `tvtime`, **FUSIONNÉ à la restauration** — union par clé `type+id` via `mergeTvImport`/`mergeTvItem` : un import fusionne avec la liste locale au lieu de l'écraser, la progression la plus avancée est conservée (**union des ensembles d'épisodes vus `watchedSet`**, `watchedEps` recalculé, union des spéciaux `specialsSeen` et vidéos vues `watchedIds`, statut le plus avancé, `addedAt` le plus ancien) et les métadonnées de la version au `refreshedAt` le plus récent gagnent). **▶️ YOUTUBE (chaînes & playlists)** : sous la recherche TMDB, un **widget jumeau** permet d'ajouter une **chaîne ou une playlist YouTube par son lien** (`ytInput` + bouton ＋). Nécessite une **clé API YouTube Data v3 dédiée** (générée sur console.cloud.google.com, stockée en `localStorage` `spotifyplus_yt_key`, helper `ytKey()` — constante `YT_KEY_LS`, bouton ⚙ ; quota gratuit ~10 000 unités/j). `parseYoutubeInput(raw)` reconnaît les liens `/channel/UC…`, `/@handle`, `/user/…`, `/c/…`, `?list=PL…`/`playlist`, ou un id brut. `ytBuildItem(raw)` (module-level) résout la source (`ytResolveChannel` via `channels.list` `forHandle`/`forUsername`/`id`, repli `search.list`), pagine les vidéos (`ytFetchPlaylistVideos` sur la playlist « uploads » de la chaîne, **plafond `YT_MAX_VIDEOS = 500`** — ou **SANS limite** via `ytBuildItem(raw, { unlimited:true })` : `max=Infinity` lève le cap de pages à 1000 ≈ 50k vidéos ; drapeau `unlimited` posé sur l'item, **préservé** par les refresh/checks (`ytRefreshItem`/`ytCheckNewVideos`/`addYoutube`) et la fusion backup (`mergeTvItem`) ; **bouton « 🔓 Récupérer toutes les vidéos »** dans `YtDetailModal` → handler `ytLoadAll(item)` (confirm avant, re-fetch complet). Une fois illimitée, la fiche affiche « Plafond retiré » + un **bouton « 🔒 Reverrouiller »** → handler `ytRelock(item)` : ne conserve que les vidéos **cochées (vues)** + toutes celles de l'**année en cours**, supprime le reste (recalcule `seasons`/`totalEps`/`epRuntime`/`watchedIds`/statut), repasse `unlimited:false` — allège le stockage tout en gardant l'historique vu. 1 unité/page de 50) puis récupère les durées (`ytFetchDurations`, `videos.list?part=contentDetails`, batch de 50, `ytParseDuration` ISO 8601 → min). Item `{ id, type:'youtube', ytKind:'channel'\|'playlist', title, poster (URL miniature directe), year, status, seasons:[{n:année, eps}], totalEps, epRuntime (durée moy.), videos:[{id,title,date,dur(min),still}], watchedIds:[] (**cochage individuel** des vidéos vues), refreshedAt, manga }`. **Les vidéos sont regroupées en « saisons » = ANNÉES de publication**. Fiche dédiée **`YtDetailModal`** (clic sur la miniature, rendue à la place de `TvDetailModal` quand `detailItem.type==='youtube'`) : en-tête (miniature ronde, nb années/vidéos, barre + %), sélecteur de statut, toggle 🍥 Manga, bouton ↻ (`ytRefreshItem` = re-`ytBuildItem` en préservant `watchedIds`/statut/manga), puis **une ligne dépliable par année** (barre + `x/y` + coche « tout cocher l'année ») avec, dépliée, **une carte par vidéo** (miniature 16:9, titre lien `youtube.com/watch?v=`, date + durée, **coche verte individuelle** → `ytSetWatched(item, ids)` qui pose le statut auto 'done'/'watching'). **YouTube = 4e onglet du filtre segmenté** : `Tous / 📺 Séries / 🎬 Films / ▶️ YT` (la puce YT prend `YT_COLOR` quand active ; `Tous` = `filmTv` = items non-youtube, `YT` = `nbYoutube`). Le contenu est **piloté par le filtre** : quand `mediaFilter !== 'youtube'` → sections films/séries (EN COURS/EN PAUSE/À VOIR/PROCHAINES/VUS) ; quand `mediaFilter === 'youtube'` → **sections YouTube dédiées** (EN COURS / VUS uniquement — **pas de section À VOIR pour YouTube**, `secOpen.ytWatching/ytDone`, `ytItems` répartis par statut, en-têtes `secTitle(..., YT_COLOR)`) qui REMPLACENT les sections films/séries. Le filtre par nom (`listFilter`) et le tri s'appliquent aussi à `ytItems`. **↩ Retour en arrière** : bouton en bas du panneau (visible si la pile n'est pas vide) qui **annule la dernière action TV Time** — pile d'annulation en mémoire `undoStack` (cap 30) alimentée par `update()` (empile l'état `items` précédent avant chaque mutation user) ; `undoLast()` restaure le dernier état. Le refresh auto des métadonnées (`applyFresh`) **bypasse `update`** (écrit direct) pour ne pas polluer la pile. **Stats** : `tvTimeStats()` renvoie `ytChannels`/`ytVideos`/`ytMin` (temps = somme des durées des vidéos vues)/`ytRemainMin` → **3 cartes** (« Vidéos YouTube » / « Temps YouTube » / « Temps restant YouTube ») dans le collapse 🎬 FILMS, visibles si `ytChannels>0`. `tvRefreshShows` skippe les items YouTube (refresh via ↻ de la fiche). **Check auto « nouvelles vidéos » au login (`ytCheckNewVideos()`, module-level)** : appelé au passage en `connected` dans `StoreProvider` (+ au montage de `TvTimePanel` pour refléter dans un onglet déjà ouvert), il ré-interroge l'API YouTube pour chaque chaîne/playlist suivie (**TTL 6h par item** via `refreshedAt`, constante `YT_NEW_CHECK_TTL` — économie de quota), détecte les **nouvelles vidéos** (id absent des `videos` en cache) et **repasse EN COURS (`status='watching'`, `doneAt` effacé) toute série marquée « vue » (`'done'`) qui a une nouvelle vidéo**. Préserve `watchedIds`/statut (hors repassage)/`manga`/`cartoon`/`addedAt` ; écrit directement en localStorage (`saveTvItems`) puis recharge l'état local si changement. CSP : `www.googleapis.com` (déjà présent pour Drive) couvre l'API, `i.ytimg.com` ajouté à `img-src` pour les miniatures |
| `TodoPanel` | **Section To do** (titre `TO DO` en vert `TODO_GREEN = '#45c552'` — même couleur que l'onglet To Do du menu ⋯) — sidebar droite desktop **sous Finance** + **onglet dédié `todo`** mobile. Gestion de tâches persistées en `localStorage` (`spotifyplus_todos`, helpers `loadTodos`/`saveTodos`) classées par échéance via un **carrousel** : `TODO_BUCKETS` = **Quotidien** / Aujourd'hui / Dans la semaine / Dans le mois / Dans l'année / À faire un jour (Quotidien en premier, Aujourd'hui en deuxième). Flèches ‹ › + points indicateurs pour changer de bucket affiché ; champ + bouton **+** (ou Entrée) ajoutent une tâche **dans le bucket courant** ; chaque tâche a une **★ étoile** (`toggleFav`, jaune si favorite), des flèches ‹ › pour la **déplacer d'un cran d'échéance** (`move` — en sortant de `daily` on efface `doneDate`). Chaque todo = `{ id, text, bucket, fav, doneDate? }`. **Bucket `daily` (Quotidien)** : le **même bouton ×** (uniformisé) appelle `validateDaily` au lieu de `remove` = « valider pour aujourd'hui » — pose `doneDate = todoTodayStr()`, le × devient un **✓ vert**, grise/barre la tâche jusqu'à minuit, puis elle **repart active le lendemain** (comparaison `doneDate === todoTodayStr()` à chaque render + tick 60s qui force le re-render au changement de jour). Pour retirer définitivement une tâche du Quotidien : la déplacer avec ‹ › vers Aujourd'hui (là où le × la supprime). Les autres buckets gardent le **×** `remove` (suppression = terminée). **Toute complétion** (× d'un bucket normal **ou** ✓ Quotidien) appelle `recordTodoDone(fav)` → journal `spotifyplus_todos_done` (`[{ ts, fav }]`, helpers `loadTodoDone`/`saveTodoDone`, borné à 2000) lu par `todoDoneStats()` (→ stats To do de `VosEcoutesPanel`) |
| `GestionMdpPanel` | **Section Mot de Passe** (titre/header `MOT DE PASSE`, onglet « Mot de Passe », en ambre `MDP_AMBER = '#f5b81f'` — même couleur que l'onglet Mot de Passe du menu ⋯) — sidebar droite desktop **sous To do** + **onglet `mdp`** mobile. **Coffre d'identifiants CHIFFRÉ** par un mot de passe maître. Chiffrement **AES-GCM 256** + clé dérivée **PBKDF2 (600k itér., SHA-256 — blob v2 ; les coffres v1/300k sont re-chiffrés en v2 au déverrouillage)** via la **Web Crypto API** native (`crypto.subtle`, aucune lib, aucune origine réseau → rien à ajouter à la CSP). `localStorage` (`spotifyplus_mdp_vault`) ne contient que `{ v, salt, iv, ct }` chiffrés ; le mot de passe maître **n'est jamais stocké**. Helpers module-level `mdpEncrypt`/`mdpDecrypt`/`_mdpDeriveKey`/`_mdpB64`/`_mdpUnb64`, `loadVaultBlob`/`saveVaultBlob`/`isVaultBlob`. **3 phases** : `setup` (créer le mot de passe maître, min. 6, migre l'ancien clair `spotifyplus_mdp` puis l'efface), `locked` (déverrouiller), `unlocked` (liste + ajout). **Bouton « 🗑 Mot de passe oublié — réinitialiser le coffre »** sur l'écran verrouillé (`resetVault`, `confirm()`) : supprime le blob `spotifyplus_mdp_vault` (copie de secours dans `spotifyplus_mdp_vault_prev`) + l'ancien clair et repasse en phase `setup` pour définir un NOUVEAU mot de passe — seul moyen de repartir quand le mot de passe maître est oublié (données chiffrées irrécupérables par conception). Session en mémoire `_mdpSession` (mot de passe maître + entrées déchiffrées) survit au changement d'onglet, **effacée au reload** (verrouillage auto) + bouton **🔒 Verrouiller**. Chaque entrée = `{ id, identifiant, mdp, url, comment }` ; mot de passe **masqué** (👁/🙈) + copier 📋 (`mdpCopyPassword` — **presse-papiers purgé 10 min après la copie**, best-effort), lien ↗ URL, **×** supprimer. **Formulaire d'ajout repliable** (`showAdd`, masqué par défaut) + **champ de recherche** (`query`, filtre sur identifiant/url/commentaire). **Export/Import** : `↧ Exporter` télécharge le blob chiffré (`gestion-mdp-sauvegarde.json`, déchiffrable avec le mot de passe maître), `↥ Importer` restaure une sauvegarde puis redemande le déverrouillage — **si un coffre existe déjà : `confirm()` avant écrasement + copie de secours du blob actuel dans `spotifyplus_mdp_vault_prev`**. ⚠ Le chiffrement protège contre une **fuite passive** du stockage, **PAS** contre un malware actif (mémoire/keylogger). ⚠ Mot de passe maître oublié = **données irrécupérables** (pas de backend/reset) |
| `RappelPanel` | **Section Remember** (⚠ **seul le TITRE `REMEMBER` est en orange** DA `#f5813c` — même couleur que l'onglet Remember du menu ⋯ — appliqué **inline** au heading mobile ET au heading desktop `SECTION.remember` ; **le contenu du panneau reste en violet `RAPPEL_PURPLE = '#c792ea'`** : StatsCollapse, bordures d'inputs, bouton +. Ne pas re-teindre tout le panneau en orange, l'utilisateur veut le contraste titre orange / contenu violet ; onglet/header libellé **« Remember »**) — sidebar droite desktop **sous Mot de Passe** + **onglet `rappel`** mobile. **Chaque rubrique fixe + « 🔔 Mes rappels » est un `StatsCollapse` repliable** (comme À propos, fermé par défaut). Sections fixes (`RAPPEL_SECTIONS`, dans l'ordre : **Administratif** → Déclaration d'impôts / Carte d'identité / Passeport / Carte européenne ; **Logement** → AG / Détecteur de fumée / Nettoyer hotte cuisine / Nettoyer VMC / Nettoyer aérations fenêtres ; **Véhicule** → Contrôle technique / Assurance auto / Carte navigo / Permis de conduire ; **Médecin** → Généraliste / Dentiste / Dépistage / Check Up complet / ORL / Ophtalmologiste / Dermatologue / Ostéopathe / Kiné / Mutuelle ; **Travail** → Entretien annuel d'évaluation / Visite médicale du travail ; **Finance** → Faire ses comptes / DCA ; **Sport** → Licence Course à pied ; **Anniversaire** → Papa / Maman / Fête des pères / Fête des mères / Cécile ; **Autre** → Lunettes / Carte restaurant expiration) avec **date du prochain rappel** (`<input type=date>`) par item, persistées en `localStorage` (`spotifyplus_rappels`, clé `<section>.<item>`) + **heure OPTIONNELLE** (`<input type=time>`, affichée seulement quand une date est renseignée) persistée à part dans `spotifyplus_rappels_time` (même clé `<section>.<item>`, helpers `loadRappelTimes`/`saveRappelTimes` — supprimer la date efface l'heure). + **Rappels personnalisés** (libellé + date + **heure optionnelle**, ajout/suppression) persistés dans `spotifyplus_remember_custom` (`[{ id, label, date, time }]`, helpers `loadRemembers`/`saveRemembers`). Badge via `rappelDelta`/`rappelBadge` (à définir / en retard / aujourd'hui / dans X j). **Bandeau** des rappels échus en haut. **Titre `REMEMBER` (orange `#f5813c`) + bouton « 📅 Dates à venir » sur la MÊME LIGNE** (header interne au panneau, `justifyContent:space-between`) : le titre n'est **plus** rendu par le parent pour cette section (mobile : plus de `<div>REMEMBER</div>` ; desktop : `SECTION.remember.selfHeading = true` → la vue centrale saute son heading générique). Le bouton (state local `showUpcoming`) : bascule vers une **vue « dates à venir »** = tous les rappels datés aujourd'hui + futur (fixes + persos), triés de la plus proche à la plus lointaine (label + date `fmtRappelDate` + badge) ; **l'affichage par rubriques reste le défaut** (bouton repasse « ↩ Toutes les rubriques »). **`notifyDueReminders()`** (module-level, appelé au passage en `connected` dans `StoreProvider`) émet une **notification à l'ouverture de l'app** aux **jalons J-30 (1 mois), J-7, J-1 et le jour J** de chaque rappel (fixe ou perso), **plus les rappels en retard** (`days ≤ 0`), **throttlée 1×/jour** (`spotifyplus_remember_notified`). Le message indique l'échéance relative de chaque item (« dans 30 j » / « dans 7 j » / « demain » / « aujourd'hui » / « en retard de X j »). `rememberAllWithDelta()` retourne tous les rappels datés `{ key, label, days, date, time }` (`key` = identifiant stable `"<section>.<item>"` pour les fixes, `"custom.<id>"` pour les persos ; `time` = `"HH:MM"` optionnel) ; `rememberDueList()` (dérivé) collecte les rappels échus (date ≤ aujourd'hui, pour le bandeau). **Remember → To do (auto)** : `syncRemembersToTodos()` (module-level, `REMEMBER_TODO_LEAD_DAYS = 3`, appelé au passage en `connected` ET après chaque modif de date dans RappelPanel) ajoute automatiquement dans le **carrousel « Aujourd'hui »** du To do (`bucket:'today'`, texte `🔔 <label>`, champ `rememberKey`) tout rappel à **≤ 3 j de son échéance** (ou en retard). Dédoublonné **par occurrence** via `localStorage spotifyplus_remember_todo_added` = `{ "<key>": "<date>" }` : un même rappel/date n'est ajouté qu'**une seule fois** (même si l'utilisateur supprime ensuite la tâche), un **changement de date** recrée une occurrence |
| `PlayerBar` | Barre du bas desktop — prev/play-pause/next + **bouton loop** + SeekBar + position |
| `MobilePlayer` | Player mobile **25vh** — pochette + titre + artiste + SeekBar tactile + like + contrôles + loop |
| `SeekBar` | Barre de progression cliquable/draggable — mouse ET touch (`onTouchStart/Move/End`) |

---

## Store — état global

```js
// Clés exposées dans l'api du StoreProvider
authState        // 'loading' | 'login' | 'connected'
user             // objet Spotify /me (null si boot pendant une fenêtre 429 — mode dégradé)
otherTab         // boolean — un autre onglet de l'app est ouvert (bandeau d'avertissement)
dbReady          // boolean — DB sql.js initialisée et prête
syncState        // 'idle' | 'running' | 'paused' | 'rl_waiting' — source de vérité unique
scraping         // boolean DÉRIVÉ (syncState !== 'idle')
paused           // boolean DÉRIVÉ (syncState === 'paused') — pause MANUELLE
rlWaiting        // boolean DÉRIVÉ (syncState === 'rl_waiting') — attente auto 429
blockedUntil     // timestamp ms — fenêtre rate-limit persistée (localStorage spotifyplus_blocked_until = _rlUntil)
feed             // array d'items du feed
logs             // array de logs
now              // état lecture Spotify en cours
stats            // { artists, total, releases, tracks } — compteurs sync
listenStats      // { remaining, remaining_ms, this_month, this_year, all_time, listened_ms, total_liked }
likedTracks      // array d'items feed (tracks WHERE liked=1), rechargé après chaque like/unlike
history          // array d'items (tracks WHERE listened=1, max 200, plus récent en haut), rechargé à chaque écoute / removeFromFeed / purge
artists          // array d'artistes scrappés (table artists_scraped + métadonnées), rechargé au login (dbReady), après CHAQUE artiste scanné pendant la synchro (compteur en temps réel) et à chaque fin de synchro (endSync) — alimente la section Artistes
loopEnabled      // boolean — persisté dans localStorage (spotifyplus_loop, '1'/'0') : survit au changement d'onglet / F5
moduleToggles    // objet { [moduleId]: bool } — modules activés/désactivés (Paramétrage d'À propos), persisté en localStorage (spotifyplus_modules). Clé absente = activé, seul `false` désactive. Filtre les onglets de WebApp (navItems, clé desktop) et MobileApp (overflowTabs, clé mobile). Inclus dans buildBackup/applyBackup (clé `modules`, REMPLACÉ)
delayChoice      // 10 | 20 | 30 (secondes)
dailyScrapings   // number — artistes scrapés dans la fenêtre 24h en cours (localStorage spotifyplus_daily_scrapings)
quotaUntil       // timestamp ms — fin de la fenêtre glissante de 24h ouverte au 100e artiste (0 si inactive)
filteredFeed     // array — feed filtré + trié (useMemo, dépend de feed + filterType + sortBy + artistSearch)
filteredFeedIndex // Map URI → index dans filteredFeed (useMemo) — O(1) pour les players et les effets d'auto-avance (fini les findIndex O(n) au tick 5s)
filterType       // 'all' | 'single' | 'album' | 'dw'
sortBy           // 'default' | 'date_asc' (plus ancien en haut) | 'artist'
artistSearch     // string — filtre texte sur artist_name
genreFilter      // string ('' = tous) — filtre par genre, appliqué UNIQUEMENT en SQL (sous-requête sur artists_scraped.genres, motif JSON '"<genre>"') ; les tracks DW multi-artistes ne matchent pas. Sélecteur « Genre : tous » dans la toolbar de FeedList (options = genres agrégés des artistes scannés, top 30 par fréquence)
// ⚠ filterType / sortBy / artistSearch : tout changement déclenche un effet debouncé 150ms qui
//   re-query la DB via loadFeedFromDB (filtre+tri EN SQL) et remplace `feed` → les filtres et
//   les tris balaient TOUTE la base, même au-delà du cap des 1000 (pas seulement les 1000 en
//   mémoire). Retour aux défauts ('all'/'default'/'') → recharge le feed de base. Garde
//   `lastQueryRef` (clé JSON [search,filterType,sortBy]) : skippe le 1er run (défauts = init) et
//   les re-renders sans changement. Deps [artistSearch, filterType, sortBy, dbReady] → pas de
//   re-query à chaque artiste pendant la synchro

resumableSession // { artists_scanned, total_artists, last_artist_name, page_url, page_offset } | null

// Méthodes
startSync({ skipCount, resumeUrl, resumeOffset })  // tous les refus (déjà en cours / fenêtre 429 / quota jour) sont en tête, AVANT toute requête
resumeSync()                   // → startSync({ skipCount, resumeUrl: page_url, resumeOffset: page_offset }) — reprise par curseur
togglePause()                  // bascule syncState entre 'paused' et 'running' (no-op si 'idle')
purgeListened()                // mémorise les URIs dans purged_uris puis DELETE listened=1 (LIKÉS COMPRIS — l'onglet ❤ est vidé des titres écoutés ; le % likés vit dans stats et n'est pas affecté)
restoreBackup(data)            // restaure une sauvegarde buildBackup (complète OU partielle — les clés absentes sont ignorées) : UPSERT prudent artists_scraped (dates de scan jamais régressées, cumuls au MAX), music (table `tracks` : feed en attente + historique + likes — UPSERT, listened/liked jamais régressés au MAX, listened_at conservé ; recharge feed/history/likés), stats (reprise intégrale si appareil vierge, sinon fusion des cumuls), todos/rappels/remembers/maps REMPLACÉS, tvtime FUSIONNÉ (union par `type+id`, progression la plus avancée conservée — `mergeTvImport`), finance (historique local des prix) FUSIONNÉ (union par série, densité ≥ 30 min, cap 500 — jamais écrasé), tmdb_key/yt_key (clés API TMDB & YouTube, chaînes brutes) REMPLACÉES si présentes et non vides, mdp_vault REMPLACÉ avec copie de secours de l'existant dans spotifyplus_mdp_vault_prev. Bouton « ↥ Restaurer mes données » dans VosEcoutesPanel (input file JSON + confirm)
resetQuota()                   // remet le quota 24h à 0 (saveQuota(0,0) + setDailyScrapings(0) + setQuotaUntil(0)) — débloque une synchro sans attendre l'expiration. Bouton dans VosEcoutesPanel
snoozeTrack(uri)               // useCallback([dbReady]) — « réécouter plus tard » : renvoie le titre en FIN de liste de façon persistante (UPDATE id = MAX(id)+1, l'ordre par défaut est id ASC) + déplace l'item en fin de feed. Bouton ⏰ dans FeedItem (avant la croix rouge)
removeFromFeed(uri)            // useCallback([dbReady]) — marque le titre écouté (UPDATE listened=1 + listened_at + incrément stats) + retire du feed → apparaît dans l'Historique et compte dans les stats. Idempotent via listenedUrisRef. Plus de DELETE
setTrackLiked(uri, bool)       // useCallback([dbReady]) — UPDATE liked en DB + stats.total_liked ±1 + recharge likedTracks/listenStats + met à jour feed array
syncInitialLikes()             // vérifie /me/library/contains par batch de 40 URIs (max 300 tracks), sleep 400ms entre batchs, TTL 24h
navigateFeed(dir)              // useCallback([markListened]) — dir=-1 prev, +1 next — joue le titre adjacent dans filteredFeed (+ markListened du titre quitté en dir>0)
markListened(uri)              // useCallback([dbReady]) — marque un titre du feed écouté (UPDATE tracks/stats + saveDB + recharge stats/history + retrait animé), idempotent via listenedUrisRef. Appelé par le clic « Suivant » des 3 players → corrige le double-clic rapide où l'URI intermédiaire échappe au poll 5s
resetFilters()                 // remet filterType='all', sortBy='default', artistSearch='', genreFilter=''
logout()
seek(positionMs)
setLoopEnabled(bool)
setModuleEnabled(id, bool)     // active/désactive un module (Paramétrage) — met à jour moduleToggles (state) + localStorage spotifyplus_modules ; les onglets se masquent/réapparaissent en direct
setDelayChoice(n)
```

### Paramétrage des modules (activer / désactiver les onglets)
- Constante module-level `TOGGLE_MODULES` = liste des modules basculables `{ id, label, icon, color, desktop, mobile }` — **Historique / Artistes / Météo / Finance / TV Time / To do / Maps / Mot de passe / Remember / Stats** (Stats = desktop `stats` / mobile `statspanel`, `#ee6a48` ; les modules cœur Scrapping / À propos ne sont PAS basculables). `desktop`/`mobile` = clé d'onglet sur chaque layout (elles diffèrent pour Remember : `remember` desktop, `rappel` mobile).
- Helpers module-level `loadModuleToggles()` / `saveModuleToggles(v)` (localStorage `spotifyplus_modules` = `{ [id]: bool }`) + `moduleEnabled(toggles, id)` (clé absente = activé, seul `false` désactive).
- **UI** : section repliable **« ⚙️ PARAMÉTRAGE »** (`StatsCollapse`) dans `VosEcoutesPanel` (À propos), sous « ↧ EXPORTER MES DONNÉES » : une ligne par module avec un **toggle switch** (via `setModuleEnabled`). Un module désactivé disparaît des onglets de `WebApp` (filtre `disabledDesktop` sur `navItems`) et `MobileApp` (filtre `disabledMobile` sur `overflowTabs`) ; si l'onglet actif est désactivé → retour auto à `scrapping` (effet).
- **Export/Import** : inclus dans `buildBackup`/`applyBackup` (clé `modules`, REMPLACÉ à la restauration) + **export dédié ⚙️ Paramétrage** (`EXPORT_SECTIONS`, fields `['modules']`). `restoreBackup` rafraîchit `moduleToggles` sans reload.

### Compteur `dailyScrapings` + fenêtre 24h `quotaUntil`
- Persisté dans `localStorage` clé `spotifyplus_daily_scrapings` : `{ count: N, until: timestamp_ms, last: timestamp_ms }` (helpers `loadQuota()` / `saveQuota(count, until, last)`)
- `until` = fin de la fenêtre glissante de 24h, posée **uniquement quand `count` atteint 100** (`0` sinon). `last` = instant du **dernier scrap**. `loadQuota()` remet `count` à 0 si `until` est dépassé (fenêtre 100 expirée) **OU** si `Date.now() - last ≥ 24h` (inactivité — reset même en dessous de 100)
- Chargé au démarrage de l'app (dans l'init useEffect) → `setDailyScrapings(q.count)` + `setQuotaUntil(q.until)` si encore actif
- Incrémenté dans `startSync` **après la requête albums de chaque artiste** (un échec ne consomme pas le quota) ; `last = Date.now()` est posé au même moment
- **Plus de remise à zéro à minuit** : le compteur est réinitialisé soit 24h après le 100e artiste (fenêtre bloquante), soit **24h après le dernier scrap** s'il y a eu de l'inactivité sans atteindre 100
- Affiché dans la carte **Artistes** de `ScrapingStatusPanel` (`X/100 sur 24h`)
- Utilisé dans `NextCallPanel` pour **"Temps total de la session"** : `(100 - dailyScrapings) × délai moyen (delayChoice + 2s)` — temps restant pour finir les 100 artistes de la fenêtre, affiché uniquement pendant une synchro active

---

## MobilePlayer — player 50vh

- Remplace `MiniPlayer` sur mobile (affiché quand `now` est défini)
- **Like/unlike** : au changement d'URI, `isLiked` est initialisé depuis le local (`likedTracks` + `feed`). `GET /me/library/contains` n'est appelé **que si le titre est inconnu localement** (titre hors feed) — économise 1 requête par changement de titre dans le cas courant. Écritures via `librarySave`/`libraryRemove` + `libraryScopeAlert` sur 403.
- **Clic next** : seek à 25% de `duration_ms` après 400ms (laisse le temps à Spotify de démarrer)
- **SeekBar** : support touch complet (`onTouchStart` + `touchmove`/`touchend` sur `window`, `passive:false`) + `touchAction:'none'` pour bloquer le scroll pendant le drag
- **Bouton loop** : alterne entre boucle (icône accent + "1") et auto-avance (icône muted) — partagé avec `loopEnabled` du store, synchronisé avec le PlayerBar desktop
- **Hauteur** : `25vh` avec `minHeight:160px`
- Position dans le feed affichée (`currentIndex + 1 / filteredFeed.length`)

---

## Rate limit Spotify (guard module-level, persisté)

Variables `_rlUntil` (timestamp ms, **relu depuis localStorage `spotifyplus_blocked_until` au chargement du module**) et `_rlNotify` (callback React → `setBlockedUntil`).
`apiGet`, `apiPut`, `apiDel` vérifient `_rlUntil` avant chaque appel et retournent `{ error:'rate_limited', rate_limited: true }` si bloqué (jamais `null` : les callers distinguent "bloqué" de "204 rien ne joue").
Sur 429 → `_rlSet(retryMs)` **persiste la fenêtre en localStorage** et bloque TOUS les appels jusqu'à expiration — y compris après un F5.
`_parseRetryAfterMs(raw, fallback)` blinde le parsing du header `Retry-After` contre `NaN`.

---

## Bugs connus / fixes appliqués

### releaseInRange — précision variable de `release_date`
Une `release_date` Spotify peut avoir la précision `day` (`"2026-05-10"`), `month` (`"2026-05"`) ou `year` (`"2026"`). `new Date("2026")` → **1er janvier** : une nouveauté datée à l'année seule passait sous le `cutoff` (ex. `2026-03-15`) et était **ratée**. Le helper module-level `releaseInRange(dateStr, cutoff, ceiling)` traite la date comme une **période `[début, fin]`** (année → 1er janv.→31 déc., mois → 1er→dernier jour) et garde la sortie si la période **chevauche** `[cutoff, ceiling]`. Utilisé aux **deux** sites de filtrage de `startSync` (comptage « aucune sortie » + boucle des albums). `rDate` (= `new Date(release_date)`) reste calculé uniquement pour le **libellé** affiché.

### Auto-avance « restart » — garde anti-vol de lecture
L'effet 2 (redémarrage du même titre : `now.current < 5` après avoir été proche de la fin) exige désormais `prevNowRef.current.uri === now.uri` (**même URI** au tick précédent). Sans cette garde, un **changement de titre** ou un **seek manuel au début** d'un titre du feed était pris pour un rebouclage → `playTrack` écrasait le choix de l'utilisateur. (Même logique que la garde `nearEnd` de l'effet 1.)

### Écran tout noir au chargement — erreur de compilation Babel (redéclaration `days`)
Toute l'app est dans **un seul `<script type="text/babel">`** compilé au runtime par Babel standalone. **La moindre erreur de syntaxe fait échouer la compilation du script ENTIER → React ne monte jamais → `#root` reste vide → écran tout noir** (même pas le « // CHARGEMENT… », qui est déjà du React). Cas rencontré : `fetchMeteoDetail(lat, lon, days = 3)` (paramètre `days` ajouté pour le bouton Light/Full) avait déjà un `const days = d.time.map(...)` dans son corps → `SyntaxError: Identifier 'days' has already been declared`. Corrigé en renommant la variable locale en `daysArr` (`return { days: daysArr, hourly }`). **⚠ Il n'y a AUCUN filet de sécurité** : pas de build tool qui validerait la syntaxe avant déploiement, et aucun message d'erreur visible à l'écran. Après toute modif de `index.html`, vérifier que le script compile (charger la page et confirmer que `#root` n'est pas vide) avant de commit.

### Reset mensuel — mois LOCAL, pas UTC
Le reset de `listened_this_month` compare `last_reset_month` à `curMonth`. `curMonth` est désormais calculé en **local** (`getFullYear()`/`getMonth()`) et non via `toISOString().slice(0,7)` (UTC) — sinon le compteur mensuel bascule à minuit UTC (≈ 01h/02h en France) au lieu de minuit local.

### Service worker — ne cache que les réponses OK (v5) + fetch app shell no-store (v6)
Le handler network-first de l'app shell met en cache le `fetch` réseau **uniquement si `res.ok`** : sinon une page 404/5xx (GitHub Pages en maintenance) écrasait `./index.html` en cache et était servie hors-ligne à la place de l'app. Cache bumpé `spotifyplus-v4` → `spotifyplus-v5`.
**v6** : le `fetch` réseau de l'app shell passe de `fetch(e.request)` à `fetch(e.request.url, { cache: 'no-store' })`. GitHub Pages sert `index.html` avec `Cache-Control: max-age=600` → le network-first récupérait une copie **périmée depuis le cache HTTP du navigateur** et la version affichée restait figée (ex. bloquée en 3.0.0) même après un nouveau déploiement + fermeture de l'app. Le `no-store` force la récupération du dernier déploiement. On fetch **par URL** (pas `e.request`) car une `Request` en mode `navigate` + init lève une exception. Cache bumpé `spotifyplus-v5` → `spotifyplus-v6`.

### Optimisations sync + countdowns
- **Feed batché par artiste** : dans `startSync`, les items d'un artiste sont accumulés dans `artistFeedItems` puis insérés en **un seul `setFeed`** en fin d'artiste (au lieu d'une recopie du tableau `feed` par album).
- **Refresh Artistes throttlé** : `loadArtistsFromDB()` relit toute la table → helper `refreshArtists()` (throttle 1×/1.5 s) remplace les `setArtists(loadArtistsFromDB())` intra-boucle. `endSync` garde un refresh final inconditionnel.
- **Horloge partagée** : `useSharedTick(active)` (+ `_clock` module-level) = **un seul `setInterval` 1 s** pour tous les countdowns (quota 24h, blocage 429) au lieu d'un intervalle par composant. Utilisé par `ScrapingStatusPanel` et `DateRangePanel`.
- **Non appliqués volontairement** : précompiler le JSX (casserait l'architecture mono-fichier `type="text/babel"` sans build) ; debounce temporel de `saveDB` (interdit — risque de perte de données si l'OS tue l'onglet mobile).

### Coffre MDP — URL assainie avant rendu (`safeHref`)
Le champ `url` d'une entrée est rendu en `<a href>`. Comme la CSP autorise `'unsafe-inline'`, une URL `javascript:` s'exécuterait au clic dans l'origine de l'app (vecteur : import d'un backup malveillant, `importVault` ne validait que la structure du blob). `safeHref(url)` n'autorise QUE `http:`/`https:` (ajoute `https://` si aucun schéma) et retourne `null` sinon → le lien devient un simple `<span>` non cliquable. Bloque `javascript:`/`data:`/`vbscript:`.

### apiGet — HTTP 204 No Content
`/me/player/currently-playing` retourne 204 quand rien ne joue (pas de body).
Sans le guard `if (res.status === 204) return null`, `.json()` lève une exception → catch silencieux → `setNow(null)` jamais appelé → `now?.uri` ne change pas → auto-avance ne se déclenche jamais.
**Toujours retourner `null` sur 204 dans `apiGet`** — et `{ rate_limited: true }` (pas `null`) quand le guard bloque, pour que le tick puisse distinguer les deux cas.

### TV Time — bouton loupe qui cherchait en « Séries » (event passé comme type)
Le bouton de recherche était `onClick={search}` : React passe l'**évènement de clic** en 1er argument, donc `search(event)` posait `type = event` → `path` retombait sur `/search/tv` (séries only) **et** `media_type: r.media_type || event` faisait échouer le `.filter(=== 'movie'|'tv')` → **0 résultat**. Symptôme : sur mobile (où l'on tape le bouton au lieu d'Entrée), « Les deux » renvoyait 0, « Films » marchait, puis rebasculer sur « Les deux » via `pickSearchType` (qui appelle `search('all')` correctement) affichait enfin les résultats — d'où l'impression d'un bug de filtre au démarrage + « pas d'étoiles » (les seuls résultats vus venaient du filtre Films). Corrigé : `onClick={() => search()}` + garde défensive dans `search` (`typeArg` ignoré s'il n'est pas `'all'|'movie'|'tv'`). Les étoiles sont le **même composant partagé** desktop/mobile : elles apparaissent dès qu'un résultat correspond à un titre déjà suivi.

### FeedItem — clé React stable
`key={item.id}` (l'URI, unique) et **jamais** `key={item.id + i}` : avec l'index concaténé, retirer un titre changeait la clé de tous les suivants → remount complet de centaines de lignes, `React.memo` inopérant.

### Curseur de navigation optimiste — `_effUri` (clic suivant rapide)
Le poll player n'interroge Spotify que **toutes les 5s** → `now.uri` reste périmé jusqu'à 5s après un clic suivant/précédent. Sans correctif, un **2e clic « suivant » dans cette fenêtre** recalculait `currentIndex` sur l'ANCIEN titre (`now.uri` pas encore rafraîchi) et **relançait le même titre** (re-seek à 25%) au lieu de passer au titre d'après.
**Fix** : module-level `let _navUri/_navAt` ; `playTrack()` les pose à chaque lecture (tout passe par `playTrack` : players, `navigateFeed`, bouton play de FeedItem, auto-avance). `_effUri(pollUri)` renvoie `_navUri` tant que `Date.now() - _navAt < 6500ms` (poll 5s + marge), sinon `pollUri`. **Les 3 players (`MobilePlayer`, `PlayerBar`, `CompactPlayer`) ET `navigateFeed` calculent `currentIndex` via `_effUri(now?.uri)`** — un clic rapide repart donc bien du titre réellement lancé.

### PlayerBar — tableau cohérent
`currentIndex` est calculé sur `filteredFeed` → le label doit être lu dans `filteredFeed[currentIndex]`, pas `feed[currentIndex]` (mauvais titre affiché dès qu'un filtre/tri est actif).

### SeekBar — race condition useEffect
Les listeners `mousemove`/`mouseup` doivent être attachés **directement dans `onMouseDown`**, pas dans un `useEffect`. Sinon un clic rapide (mousedown + mouseup) se termine avant que React ait re-rendu et attaché les listeners.

### sql.js — params positionnels
`_db.run(sql, params)` attend un tableau de valeurs positionnelles pour les `?`. Ne jamais passer un objet `{ key: val }`.

### Babel — var hoisting
Babel compile `const`/`let` en `var`. Les **callbacks** de `useEffect` qui référencent des variables déclarées plus bas dans la même fonction body fonctionnent car ils sont exécutés après le rendu complet (les vars sont initialisées).

**⚠️ MAIS le tableau de deps est évalué PENDANT le rendu** : `useEffect(() => {...}, [feed])` placé **avant** `const [feed] = useState(...)` reçoit `[undefined]` à chaque rendu → React croit que les deps ne changent jamais → l'effet ne tourne qu'une fois. C'était le bug de `feedRef` (figé sur `[]` pour toujours). **Règle : tout hook avec un tableau de deps doit être déclaré APRÈS les variables qu'il liste.**

**⚠️ `useMemo` est encore plus strict** : son callback s'exécute **pendant le rendu**, pas après. Les variables référencées doivent donc être déclarées **avant** le `useMemo` dans le source. Ex : `filteredFeed` (useMemo) doit être déclaré après `feed` (useState), sinon `feed` vaut `undefined` au premier rendu.

### Limites connues
- iOS/Safari : stockage IndexedDB limité à ~50 Mo, purge automatique si l'app n'est pas ouverte 7 jours → privilégier Android
- sql.js charge toute la DB en RAM au démarrage — purger régulièrement les `listened = 1`
- Données sur un seul appareil, pas de sync multi-devices
