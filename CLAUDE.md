# ⚠️ RÈGLES ABSOLUES

- **Git** : committer et pousser directement sur `main` (pas de branche), sauf demande contraire. **Un commit distinct par demande.**
- **Docs** : `CLAUDE.md` (technique) et `README.md` (utilisateur), mis à jour dans le **même commit** que la modif, chacun indépendamment — **uniquement si** elle rend une phrase FAUSSE ou ajoute un piège / un comportement qu'on ne devine pas en lisant le code : nouveau module, origine CSP, clé de stockage, changement d'API tierce, comportement visible par l'utilisateur (→ `README.md`). Correctif, refacto, valeur ajustée, une source de plus dans une liste déjà décrite : **ne toucher à aucun des deux**, le message de commit raconte mieux. **Les garder COURTS** : l'état actuel et les pièges, jamais l'historique des correctifs (il est dans git) ; **corriger ou remplacer** une ligne plutôt qu'en ajouter, et préférer le commentaire dans le code au paragraphe ici — ces fichiers sont relus intégralement à chaque session.
- **Éditer le dépôt principal**, jamais un worktree temporaire (`.claude/worktrees/*`).
- **⚠ Pas de build tool** : toute l'app est UN seul `<script type="text/app-source" id="app-src">` (type inconnu du navigateur ⇒ **jamais exécuté ni compilé tout seul**). Le **script d'amorçage** avant `</body>` décide : code déjà compilé trouvé dans le cache `spotifyplus-compiled` ⇒ injecté tel quel et **Babel n'est même pas téléchargé** ; sinon Babel est chargé à la demande, compile avec le preset **`react` SEUL, jamais `env`** (ES5 = 4× plus lent), options `comments:false` + `retainLines:true` (une ligne compilée = sa ligne source ; **jamais `compact:true`**, plus aucune erreur ne serait localisable), puis mémorise le résultat. Clé = `APP_VERSION` + longueur ; l'**empreinte FNV-1a** de la source, rangée dans l'en-tête de l'entrée, est **vérifiée après le démarrage** : différente ⇒ purge + **un** rechargement (`spotifyplus_boot_rehash`) — le filet d'un bump oublié. La moindre erreur de syntaxe fait échouer toute la compilation → React ne monte jamais. **Filet d'amorçage** : un écouteur `error` global écrit la vraie erreur sur `#boot`, et un chien de garde de 45 s le voit encore là — si le code venait du CACHE il purge et recharge **une** fois (`spotifyplus_boot_retry`, sessionStorage), sinon il se contente du message (recompiler donnerait la même source cassée). **Après toute modif d'`index.html`, vérifier que la page monte avant de commit.**
- `APP_VERSION` (constante en tête d'`index.html`, **valeur à y lire**) : **à incrémenter à la main à chaque commit**. `MAJ.MIN.U` dérivé du nombre de commits N du projet : `patch = N%10`, `minor = floor(N/10)%10`, `major = floor(N/100)`.

---

# Spotify+ · Hub de Pierre

PWA **sans backend** sur GitHub Pages, données 100 % locales (sql.js SQLite WASM + IndexedDB). Chaque artiste suivi est scrapé depuis **sa propre** `last_scraped_at` ; un artiste **jamais scrapé prend sa dernière ANNÉE** de sorties. Feed écouté titre par titre : fin de titre → marqué écouté + auto-avance + disparition animée. Aucun ajout auto en playlist. C'est aussi un **hub perso multi-modules**, avec un **ordre unique partout** (onglets desktop, menu ⋯ mobile, accueil) : alphabétique, puis Stats et À propos.

**⚠ Ce fichier est un index, pas un manuel** : chaque piège y est nommé par son symbole et **détaillé en commentaire au-dessus de ce symbole** dans `index.html`. Aller lire le commentaire avant de toucher au code concerné.

## Fichiers

| Fichier | Rôle |
|---|---|
| `index.html` | **App complète** (React 18 CDN + Babel + sql.js), ~35 000 lignes, tout en un fichier |
| `manifest.json` | Config PWA |
| `service-worker.js` | **DEUX caches** : `spotifyplus-v<N>` (app shell, **bumpé à chaque déploiement**) et `spotifyplus-vendor-v1` (`vendor/` + `manifest.json` + les 2 icônes, **bumpé seulement quand l'un d'eux change** — `primeVendor()` est incrémental, AJOUTER une entrée ne demande aucun bump). `doodle.js` / `sport-data.js` / `dobble-art.js` sont dans le précache de l'app shell. `data/*.json` en **stale-while-revalidate** ; app shell en **network-first plafonné à `SHELL_TIMEOUT_MS` (1,5 s)** avec `{cache:'no-cache'}` (piège 31) : au-delà on sert le cache, le téléchargement continue en fond, et un ETag différent ⇒ `postMessage('shell-updated')` **au seul client servi** (`resultingClientId`) → bandeau `#newver`. Clé normalisée `./index.html` ; **l'app shell = le chemin EXACT de `self.registration.scope` (ou + `index.html`)**, jamais « tout ce qui finit par `/` ». Une ressource locale hors précache est cachée au premier accès (dans l'app shell, donc purgée au déploiement suivant), sauf le worker. ⚠ `activate` supprime tout cache dont le nom ≠ le sien — **sauf `spotifyplus-compiled` et `VENDOR`**. ⚠ Le cross-origin ne passe plus par le worker. |
| `vendor/react*.production.min.js` | React 18.3.1 auto-hébergé (fichiers du paquet npm) |
| `vendor/fonts.css` + `fonts/*.woff2` | DM Sans (variable 400–900) et DM Mono auto-hébergées, sous-ensemble latin |
| `vendor/doodle.js` | **Moteur Doodle Jump** (constantes `D_*`, génération, dessin : ~7 000 lignes SANS JSX), **lazy** (`loadDoodle()`), précaché avec l'app shell. ⚠ Script classique ⇒ globales lexicales : **aucune lecture au niveau module** d'`index.html`, aucun nom redéclaré des deux côtés ; `GameDoodle` est le garde (`window.DOODLE_READY`). ⚠ La **✨ Création** n'est PAS dans `D_TILES` (donc jamais débloquée ni tirée) : elle naît de DEUX dalles qui se touchent (`doodleCreaFuse`) **ou** de la 🌈 Multicolore qui, faute de place libre, se téléporte SUR une dalle (`doodleRainbowHop`) — un seul point de naissance, `doodleCreaBorn`, sous un seul garde-fou, `doodleCreaReady` (une seule à l'écran). ⚠ **Un SEUL mode** : ni « Classique » ni `spotifyplus_doodle_mode`, et la ✨ Création pilote le doodler (`doodleCreaStep`, **appelée juste avant le test d'atterrissage** — c'est ce test qui joue l'effet de chaque dalle visitée, aucun effet n'est réécrit) |
| `vendor/sport-data.js` | `SPORT_GROUPS` (catalogue Sport), **lazy** (`loadVendorScript`), mêmes règles ; garde `SportPanel` (`window.SPORT_DATA_READY`) |
| `vendor/dobble-art.js` | **Les 57 objets de Dobble**, chacun dessiné en **3 versions A/B/C** (43 Ko), **lazy** (`loadDobbleArt()`), précaché avec l'app shell ; garde `GameDobble` (`window.DOBBLE_ART_READY`, posé **seulement si le compte est 57**). ⚠ **IIFE** comme `motus-words.js`, et non des globales lexicales comme `doodle.js` : les noms courts qu'il emploie (`K`, `O`) entreraient en collision avec index.html (`C`, le thème). Un dessin = une liste de FORMES en tableaux compacts (`['c',cx,cy,r,fill]`…), rendues en SVG par `dobShape()` — jamais de balisage, donc jamais d'`innerHTML`. ⚠ Le blanc y sert de GOMME (morsure du cookie) : il ne marche que parce que la carte est blanche. ⚠ **Trois dessins viennent du web** (érable et fromage : Twemoji CC-BY 4.0 ; igloo : Font Awesome Free CC BY 4.0) : chemins repris TELS QUELS, d'où le `vb` d'`add()` qui garde le viewBox de la source au lieu d'un `d` rééchelonné à la main — **l'attribution en tête du fichier est exigée par les deux licences**, ne pas la retirer |
| `vendor/sql-wasm.js` / `.wasm` | sql.js 1.10.2 auto-hébergé |
| `vendor/leaflet.js` / `.css` | Leaflet 1.9.4, **lazy** (`loadLeaflet()`) |
| `vendor/motus-words.js` / `motus-dico.js` | Données Motus (~1,1 Mo), **lazy** (`loadMotusData()`), IIFE → `window.MOTUS_WORDS` / `MOTUS_DICO` |
| `data/actu.json` | Sources Actu pré-fetchées par `update-data.yml` (cron 2 h) via `scripts/fetch_actu.py`. Lu same-origin ; > 24 h → repli fetchers client (proxies CORS). Clé `via` = les relais publics empruntés par section (vide = direct) → bandeau `ActuViaNotice` |
| `data/indices.json` | Indices boursiers via `scripts/fetch_indices.py` (Stooq→Yahoo), **même workflow**, séance seulement (06–22 UTC, lun–ven). La valeur vient d'un **relais public** : `check_quote` refuse un non-nombre ou un écart > 25 % avec le relevé précédent, et la clé `via` de chaque cours nomme le relais (écrite dans le JSON, **jamais affichée**) |
| `data/macrocal.json` | Calendrier macro de la semaine (BullRun → Macroéconomie) via `scripts/fetch_macrocal.py`, **même workflow**. ⚠ Il existe PARCE QUE le flux ne s'atteint pas depuis une page (pas de CORS) : le runner n'a pas cette contrainte. Lu same-origin en premier ; > `MACROCAL_STALE` (4 j) ou absent ⇒ repli sur l'appel direct puis les proxies. `clean_events` normalise (`ts` en **secondes**, `stars` 0-3) et **refuse un lot** de moins de `MIN_EVENTS` ou hors fenêtre ⇒ fichier précédent conservé |
| `scripts/commit_data.sh` | Commit des trois JSON. Chacun réécrit son `updated_at` ⇒ **un run = un diff** : le commit du jour est **amendé + force-push** tant qu'il est en tête ⇒ **1 seul commit/jour** (jamais d'amende par-dessus un commit humain ; `--force-with-lease`). ⚠ Côté local, `git pull` sur `main` peut diverger → `git pull --rebase` |

## Stack & sécurité

- **React 18.3.1 auto-hébergé** (`vendor/`) ; **Babel standalone 7.29.7** via unpkg, chemin froid seulement — **version épinglée + SRI sha384 + crossorigin**. Ne jamais changer sa version sans recalculer le hash.
- **sql.js 1.10.2 auto-hébergé** : `initSqlJs({ locateFile: f => './vendor/' + f })`.
- **⚠ Un secret ne traverse JAMAIS un relais public sans accord explicite.** La clé Riot est le seul cas (l'API Riot n'ouvre pas le CORS) : `lolApiGet` tente d'abord le DIRECT avec `X-Riot-Token`, puis les relais **uniquement** si `spotifyplus_lol_relay === 'on'` (case à cocher du ⚙ LoL, **absent = non**). Steam / Google CSE / Alpha Vantage / TwelveData mettent aussi leur clé en query string, mais vont **directement** chez le propriétaire de l'API — c'est le protocole prévu, rien à changer.
- **⚠ CSP** (`<meta>` du `<head>`) = **source de vérité, y lire les listes à jour**. **Toute nouvelle origine (fetch / script / image / iframe) DOIT y être ajoutée**, sinon blocage silencieux. ⚠ `img-src` finit par un joker `https:` **non retirable** (Actu, Histoire et Collection affichent des images d'hôte arbitraire par nature) : ces `<img>`-là portent `referrerPolicy="no-referrer"`, et la page est en `strict-origin-when-cross-origin`. Particularités : `'unsafe-inline'`/`'unsafe-eval'` pour Babel et `'wasm-unsafe-eval'` pour sql.js · `accounts.google.com` **sans SRI** (script GIS non versionné) · `blob:` dans `img-src` (photos Drive au Bearer → `createObjectURL`) · `connect-src` = miroir d'`API_LIST` (~50 origines).
- **IndexedDB** : binaire SQLite (`spotifyplus_db`), connexion unique cachée (`_idbPromise`), `navigator.storage.persist()` au login. Cinq bases séparées (piège 20) : `spotifyplus_note_media` (pièces jointes des notes), `spotifyplus_tvtime_db` (la liste TV Time — elle pesait 9 Mo sur le quota de 10 que se partage tout le Hub), `spotifyplus_cards_db` (les sous-collections de CARTES de Collection, une entrée par sous-collection — un import ManaBox, c'est des dizaines de milliers de lignes) `spotifyplus_photo_cache` (**cache local des photos Drive**, une entrée par `fileId` + les index `index:<Dossier>` ; **hors sauvegarde**, tout y est re-téléchargeable) et `spotifyplus_sport_img_db` (**une base SQLite à part** portant les planches du module Sport, cf. sa ligne ; **hors sauvegarde**).
- **`lsSet(key, value)`** : écriture localStorage centralisée (warn + alerte 1×/session sur quota plein) — voir piège 14.
- **Garde multi-onglets** : `BroadcastChannel('spotifyplus_tabs')` → bandeau `otherTab` (chaque `saveDB()` exporte toute la DB).

## Auth Spotify

```js
CLIENT_ID    = '672e41f0308f4378b4f2331844e08b20'
REDIRECT_URI = 'https://pierre-portfolio.github.io/NewSpotifyRelease/'
SCOPES = 'user-follow-read user-follow-modify user-read-private user-read-currently-playing ' +
         'user-read-playback-state ' +
         'user-modify-playback-state user-library-read user-library-modify ' +
         'playlist-read-private playlist-read-collaborative'
```

- **OAuth 2.0 PKCE 100 % client**, token en localStorage, `state` anti-CSRF. `refreshToken()` sous mutex (`_refreshPromise`) : refresh tokens à usage unique + rotation ; sans refresh token ou `invalid_grant` → `logoutClear()` + reload. ⚠ **Aucune 2e méthode d'auth n'est possible** : Client Credentials exige un `client_secret` (dépôt public) et n'a aucun contexte utilisateur.
- `apiGet` / `apiPut` / `apiDel` : retry 1× après refresh sur 401 · `null` sur 204 · **throw sur 429** · `{rate_limited:true}` sous fenêtre 429. **`apiPut` garde le corps d'un 403** (`{error:'forbidden', status, message, reason}`) : Spotify y met la vraie raison.
- **Scopes mémorisés** (`spotify_scopes`) → `spotifyHasScope(s)`. ⚠ **`null` = INCONNU** (jeton antérieur) : traiter `!== true`, jamais `=== false`. Un jeton **ne gagne pas** un scope au refresh, seule une reconnexion l'accorde.
- **Diagnostic de connexion** : toute cause d'échec est posée par `authErrSet`, relue par `authErrTake` (**sessionStorage** : `refreshToken()` recharge la page), affichée en bandeau rouge (`authError`). ⚠ Cause n°1 : `/me` en **403 = compte absent de *User Management* du Dashboard**.
- **Mode hors ligne** : un `/me` raté ne jette pas la session ; bouton « 📴 Continuer sans Spotify » toujours affiché → `enterOffline()` = `setUser(meCacheLoad())` + `offline` + `bootLocal()`. Profil caché dans `spotifyplus_me` (hors backup, re-fetchable).
- **`bootLocal()`** = toute l'init NON-Spotify (persistance, `syncRemembersToTodos`, `syncSpeedrunToTodoFromCache`, `notifyDueReminders`, `tvSyncDaily`, `initDB`, reset mois/année, feed, stats, reprise de synchro, quota, fenêtre 429, `setDbReady`). Appelée par le chemin normal **et** l'entrée hors ligne. Deps `[]` : ne référence que setters et refs.

## Base locale (4 tables)

```sql
tracks (id PK, spotify_uri UNIQUE, artist_name, title, release_title, release_type,
        release_date, cover_url, duration_ms, listened DEFAULT 0, liked DEFAULT 0,
        listened_at, added_at DEFAULT datetime('now'))

artists_scraped (spotify_id PK, last_scraped_at, name, image_url, popularity, followers,
        genres /*JSON*/, spotify_url, last_release_count DEFAULT 0, total_tracks_added DEFAULT 0,
        last_scan_status /*ok|partial|error|gone*/, scan_count DEFAULT 0, following DEFAULT 1)

stats (id=1 CHECK, total_listened, listened_this_month, listened_this_year,
        last_reset_month /*YYYY-MM*/, last_reset_year, total_listened_ms, total_liked)

purged_uris (spotify_uri PK)  -- jamais réinsérés par un re-scan
```

Migrations idempotentes dans `initDB()` (ALTER TABLE en try/catch). Accès : `dbRun`/`dbAll`/`dbGet`, chargements `load*FromDB`, `loadFeedFromDB({artistSearch, filterType, sortBy, genreFilter})` **filtre et trie EN SQL** puis LIMIT 1000 (balaie toute la base). Item feed : `{ id, spotifyUri, label, artist, title, subtitle, date, rawDate, image, isNew, liked, duration_ms }`.

`saveDB()` (export→IndexedDB, **sérialisé** via `_savePromise`/`_saveQueued`, **jamais debouncé** : perte de données mobile) après chaque artiste scrapé, chaque écoute, chaque purge, `endSync`, `visibilitychange→hidden`. Toujours `INSERT OR IGNORE` pour `tracks` ; **UPSERT `ON CONFLICT(spotify_id) DO UPDATE`** pour `artists_scraped` (jamais `INSERT OR REPLACE`).

**Migration API fév. 2026 → `/me/library`** : `/me/tracks/contains`, `PUT/DELETE /me/tracks` **et `PUT/DELETE /me/following` (abonnement artiste)** sont **supprimés**. Remplaçants : `libraryContains(uris)` → `GET /me/library/contains?uris=` (max 40) · `librarySave`/`libraryRemove` → `PUT/DELETE /me/library?uris=` (**URIs en query string même pour PUT**, pas de body) · `libraryScopeAlert(result)` sur 403. **Suivi d'artiste** : `spotifyFollowArtists`/`spotifyUnfollowArtists` → `PUT/DELETE /me/library?uris=spotify:artist:<id>` (scope `user-follow-modify`). ⚠ **Plafond `LIBRARY_URIS_MAX` = 40 URIs**, pas les 50 de `SPOTIFY_IDS_MAX` (`/v1/artists?ids=`) — tout lot se découpe par 40. ⚠ **Un endpoint retiré répond 403 comme une permission absente** : `apiGet`/`apiPut` **et `apiDel`** gardent le corps du 403, et l'UI CITE le `message` de Spotify au lieu d'affirmer une cause (`deltaRefusedMsg`). Le `GET /me/following` n'a pas bougé. `syncInitialLikes()` : jusqu'à 300 tracks par batch de 40, TTL 24 h.

## Scraping (`startSync`)

**`syncState`** : `'idle' | 'running' | 'paused' | 'rl_waiting'` ; `setSync(st)` pose synchroniquement `syncStateRef.current` puis le state, `scraping`/`paused`/`rlWaiting` sont **dérivés**. Démarrage refusé (avant toute requête) si l'état n'est pas `idle`, si `Date.now() < _rlUntil` ou si le quota 24 h est atteint. **`checkpoint()`** est attendu avant **chaque** appel (via `apiGetSafe`) : une seule boucle d'attente pour la pause manuelle **et** la fenêtre 429 — aucune requête pendant une pause. `endSync(reason)` : reset + `saveDB()` final ; `'completed'` nettoie `spotifyplus_sync_progress`, les autres raisons laissent « ↩ Reprendre ».

**Rate-limit** : sur le **1er 429**, `_rlSet(max(Retry-After, 15 min))` — fenêtre persistée `spotifyplus_blocked_until`, relue au chargement du module, donc elle survit au F5 et s'applique à **tous** les appels, player compris. Boot pendant la fenêtre : `/me` → `rate_limited` → mode connecté dégradé (`user = null`) au lieu d'éjecter.

**Boucle artiste** :
- **Premier scan = la dernière ANNÉE GLISSANTE** (`firstScrapeFrom()`, `FIRST_SCRAPE_MONTHS`) et **UNE SEULE page d'albums**, exactement comme un artiste déjà connu (qui garde `cutoff = last_scraped_at`). ⚠ Jamais toute la discographie en déroulant `next` : ce seraient des centaines de titres d'un coup dans le feed et des dizaines de requêtes. Le prix assumé : plus de dix sorties en un an ⇒ les plus anciennes échappent — le journal le DIT (`d'autres pages existent, non lues`).
- ⚠ `FULL_SCRAPE_FROM` n'est plus une profondeur de scan mais un **ÉTAT** : « premier scan pas terminé ». C'est `lightISO` (et non `cutoffISO`) que l'écriture « légère » pose — sinon un artiste coupé par un 429 cesserait d'être « jamais scanné » alors que son scan n'est pas fini.
- `/artists/{id}/albums?include_groups=album,single&limit=10&market=FR` (piège 21) → filtre `releaseInRange` (piège 19) → `/albums/{id}/tracks?limit=50`. Dédoublonnage `seenUris` + `knownUrisRef` + `purged_uris` : album skippé seulement si `uris.every` connu.
- **Artistes sans nom** (ligne d'avant la colonne `name`, ou restaurée d'une sauvegarde qui ne portait que `spotify_id`+`name`) : plus dans `/me/following` ⇒ aucun scan ne repasse les remplir. Bouton « 🔍 Identifier » du panneau Artistes → `GET /v1/artists?ids=` (50 max, `null` en place d'un id disparu) : trouvé ⇒ métadonnées écrites, sinon `last_scan_status='gone'` puis « 🗑 Supprimer » = `DELETE FROM artists_scraped` **+ le désabonnement `/me/library`** (voir la migration fév. 2026 ; suppression locale faite quand même si Spotify refuse).
- **Désabonnement = `following=0`, JAMAIS un DELETE** : la ligne garde sa date et ses compteurs, l'artiste n'est plus scanné (la boucle ne visite que `/me/following`, d'où le X/X déjà juste) et les deux UPSERT reposent `following=1` ⇒ un **réabonnement repart de la date d'origine, sans rien redemander**. Deux sources : le bouton ⊘ de Delta Artiste (`artistUnfollowKeepData` — crée la ligne à la sentinelle `FULL_SCRAPE_FROM` si l'artiste n'a jamais été scrapé) et le **balayage de fin de boucle**, ⚠ joué **uniquement** après un passage COMPLET parti de la 1re page : après une reprise ou un quota atteint, `followedIds` est partiel et archiverait des artistes bien suivis.
- **Écriture « légère »** dès la liste d'albums lue (`last_scan_status='partial'`, date jamais avancée) → l'artiste apparaît même si un 429 coupe ; **UPSERT final** après artiste terminé (avance `last_scraped_at`, `status='ok'`, cumule les compteurs). Liste inaccessible → `status='error'`, date non avancée.
- Progression dans `spotifyplus_sync_progress` après chaque artiste ; `resumeSync()` repart sur la bonne page.

**Quota 100 artistes / fenêtre glissante 24 h** : `spotifyplus_daily_scrapings` = `{count, until, last}` (`loadQuota`/`saveQuota`/`ensureQuotaWindow`, auto-réparants). `dailyCount` incrémenté **APRÈS** la requête albums (un throw ne brûle plus le quota).

**Scraping automatique** : `musicAutoTry()` part 4 s après le boot puis se re-teste toutes les 10 min ; `musicAutoDue()` est **100 % local, aucune requête**. ⚠ La journée n'est marquée **que** par `endSync('completed')` — le quota se libérant par fenêtre glissante, la suite peut repartir le même jour. Coupé si `offline` ou `otherTab`. Préférence `MUSIC_AUTO_LS` (**absent = activé, seule la valeur `'off'` désactive**).

**Découvertes de la semaine** : **aucun import automatique**. Deux chemins : le bouton violet `dwManualAccess` (`{force:true}`) et la **capture par la lecture** `dwCaptureFromContext` (lit la file déjà interrogée par le poll player, aucune requête dédiée). ⚠ Spotify refuse souvent le CONTENU d'une playlist éditoriale (403) : la capture par la lecture est le vrai filet.

## Écoute / auto-avance

**Marquage** : `now?.uri` change (URI précédente dans le feed & `dbReady`) → `listened=1` + `stats` + `saveDB` + `recordListen()` + animation 600 ms ; `listenedUrisRef` anti-doublon ; écritures DB **hors** de l'updater `setFeed` (item lu via `feedRef.current`). Les players capturent l'URI quittée AVANT de jouer la suivante puis appellent `markListened` (idempotent) — sinon le double-clic rapide échappe au poll 5 s. ⚠ `markListened` n'utilise **que** la position réelle, jamais le repli mémorisé par `navigateFeed`.

**3 mécanismes d'auto-avance** (triple couverture, `advancedForRef` anti-doublon, `prevNowRef` = tick précédent), **placés AVANT le `return` sur `listenedUrisRef`** :
1. Effet `now?.uri` : URI null/hors feed **et** titre quitté proche de la fin (`nearEnd`, garde anti-vol de lecture)
2. Effet `now?.current` : `remaining ≤ 3 s` & titre dans le feed
3. Effet `now?.playing` : transition `true→false` & `remaining ≤ 8 s` — **cas principal** ⚠ la fin d'un titre est un `200 + is_playing:false` sur la **même URI**, donc ni 1 ni 2 ne se déclenchent.

**Poll player** : chaîne de `setTimeout` (`PLAYER_POLL_*`), **une requête en vol** et 10 s de plafond par tick (sinon une réponse en retard remet `now` en arrière), 5 s en lecture, 15 s après 2 min sans rien qui joue — `playTrack` réveille le poll (`_playerPollKick`) ; bail-out si `now` inchangé, aucun appel onglet caché (rattrapage au retour), fenêtre 429 → garde l'état affiché. Curseur optimiste `_effUri(pollUri)` → `_navUri` tant que `< 6500 ms` (le poll est périmé jusqu'à 5 s).

**`playTrack`** : `PUT /me/player/play` **sans `device_id`** n'aboutit que s'il existe un appareil *actif au sens de Spotify* (un Spotify ouvert mais en pause ne l'est plus) ; sur **404 ou 403** → `GET /me/player/devices` puis rejeu sur `?device_id=`. `spotifyPickDevice` **écarte les `is_restricted`** (ils refusent toute commande de l'API Web). ⚠ `spotifyDevices` en try/catch (`apiGet` throw sur 429, sinon l'auto-avance casse). ⚠ **Throttle `_playAlert` 30 s** : `playTrack` est appelée sans geste utilisateur par l'auto-avance.

## Stats / purge

`stats` : `total_listened`, `listened_this_month/year`, `total_listened_ms`, `total_liked` (likes posés **dans l'app**, jamais par `syncInitialLikes`). Reset mois/année au démarrage via `last_reset_*` (mois **LOCAL**).

**`purgeListened`** : URIs → `purged_uris` (INSERT OR IGNORE) puis `DELETE FROM tracks WHERE listened=1` ; `stats` non touchée, le journal `spotifyplus_listens` survit. **`removeFromFeed`** (croix × / swipe gauche) traite le titre **comme écouté** (pas de DELETE) → il apparaît dans l'Historique ; idempotent, **Undo** 5 s (`undoRemove()`).

## Store (`StoreProvider`)

Détient l'auth, le feed (+ `filteredFeed`, `filteredFeedIndex` Map URI→index et `feedIndex` Map URI→item du feed COMPLET), les stats, les artistes, les filtres, les toggles de modules et les états de synchro/quota — **liste exacte dans `StoreProvider`**.

- **⚠ TROIS contextes, pas un** : `useStore()` (données stables) · `useNow()` (titre en cours, change toutes les 5 s) · `useSyncInfo()` (`nextCall`/`logs`/`statusMsg`/`currentArtist`/`eta`, lus par les 3 panneaux du collapse ⚙ Scraping). **Ne JAMAIS remettre dans `api` une valeur qui bouge à intervalle régulier** : elle recrée l'objet du store et re-rend tous ses abonnés (canvas de jeu compris).
- ⚠ Changement de `filterType`/`sortBy`/`artistSearch`/`genreFilter` → effet debouncé 150 ms qui re-query `loadFeedFromDB` ; `lastQueryRef` skippe le 1er run et les re-renders sans changement.
- **Modules** : `TOGGLE_MODULES` = `{id, label, icon, color, desktop, mobile}` ; `moduleEnabled` / `moduleOnHome` = **absent ⇒ activé/affiché, seul `false` désactive** (`spotifyplus_modules` / `_home_modules`). Onglet actif désactivé → retour à `home`.
- **Barre de lecture** : `usePlayerHidden()` réunit DEUX causes de masquage — le compteur `_hidePlayer.n` (verrou temporaire de `useHidePlayerBar`, jeux plein écran) et la préférence permanente `PLAYERBAR_LS` (`spotifyplus_playerbar`, Paramétrage d'À propos ; **absent ⇒ affichée, seule la valeur `'off'` masque**). ⚠ Le poll player continue de tourner : seul l'AFFICHAGE disparaît. Le ⌄ de `MobilePlayer` REPLIE au lieu de masquer (`PLAYERFOLD_LS`, **absent ⇒ déplié**) : il reste un bandeau d'une ligne, qui porte le ⌃ du retour — **seul moyen de redéployer**.
- **IA par module** (bouton 🤖, `ModuleAiModal`) : `AI_MODULES` = **la table qui décide** (module absent ⇒ 🤖 grisé). `spotifyplus_module_ai` = `{modId:{provId:false}}`, **absent ⇒ autorisé**. `aiChain(needVision, modId)` filtre la chaîne : décocher **raccourcit les replis**, ça ne change ni la requête ni le parsing → changer d'IA ne peut pas casser un module. Chaîne vide malgré des clés → `AI_OFF_MSG` (≠ `NO_KEY`). ⚠ Cache `_modAiCache` → **`moduleAiInvalidate()` après toute écriture hors `saveModuleAi`**.
- **Optimisations** : feed batché par artiste (1 seul `setFeed`), refresh Artistes throttlé 1,5 s, horloge partagée `useSharedTick`/`_clock`, index `shownByType` de Vêtement, initialiseurs `useState` paresseux sur toute lecture de localStorage. **Interdit** : précompiler le JSX (casse le mono-fichier).

## Sauvegarde / restauration

**`buildBackup()`** (synchrone) → une clé par section + les clés/pseudos d'API (**liste exacte dans le code** ; y ajouter toute nouvelle section persistée). **`buildBackupFull()`** = + `note_media` (IndexedDB, donc **asynchrone** — téléchargement et 3 destinations cloud). Export partiel : `EXPORT_SECTIONS` → `downloadBackup(sec)`.

**Chiffrement par défaut** (`BACKUP_ENC_LS`, **absent = chiffré** ; seule la valeur `'off'` laisse en clair ; annuler la saisie ⇒ `confirm` « exporter en clair ? », refus ⇒ `null`) : `backupSeal(data)` scelle en `{app, version, exported_at, scope, encrypted:true, enc}` avec la primitive du coffre (`mdpEncrypt`), `backupOpen(data)` ouvre. ⚠ `backupSeal` s'applique **APRÈS** le découpage d'un export partiel, aux **4** sorties (fichier + 3 clouds) ; `backupOpen` est appelé dans **`restoreBackup`** seul — c'est l'unique entonnoir des 4 chemins de restauration. ⚠ La phrase de passe n'est stockée nulle part : oubliée = fichier illisible. `backupSeal` renvoie `null` si les deux saisies diffèrent (rien n'est écrit) et l'objet **tel quel** si l'option est inactive.

**Règles de fusion** (`restoreBackup`, complet ou partiel, clés absentes ignorées) :
- ⚠ **Toute clé d'objet venue du FICHIER passe par `safeKey`** (`__proto__`/`constructor`/`prototype` refusés) : `obj['__proto__'] = v` ne crée pas une propriété, il remplace le prototype de l'objet. Les fusions par id utilisent une `Map` quand c'est possible.
- **⚠ Cumuls et compteurs : au MAX, JAMAIS en somme** (artistes, `games.hi`, `api_calls`) — la sauvegarde contient déjà l'historique local, additionner **doublerait** à chaque ré-import.
- Union / le plus avancé : tvtime, listens (cap 5000 ; **format sur disque = tableau de NOMBRES bruts**, `listenTs` lit aussi les anciens `{ts}`), notes (`updatedAt` le plus récent), finance, food (`weightMerge` pour les pesées), collection / revente / note_media (par id, **le local gagne**).
- Remplacés : todos, rappels, remembers, maps, modules, home_modules, prefs, mdp_vault (backup dans `spotifyplus_mdp_vault_prev`), clés/pseudos si non vides. Dates d'artistes **jamais régressées** ; `music` : `listened`/`liked` au MAX.
- ⚠ **Un `localStorage` plein ne lève RIEN** (`lsSet` renvoie `false`, `put` avale le throw) : la restauration s'annoncerait réussie sans rien écrire. `applyBackup` renvoie donc `failed[]` → `restoreFailMsg(res)` dans les 4 UI. TV Time, le plus gros objet du backup, n'écrit plus en localStorage mais dans sa base : son échec passe par `tvFlushDone()` (attendu dans `restoreBackup`) et `tvCacheReload()`.
- **⚠ Volontairement EXCLUS** : tous les caches re-fetchables (actu, indices, cours, Steam, LoL, chess, speedrun, github, yt_subs, delta, displate, clés de jour des synchros auto) et **toutes les images** (voir piège 15).

**3 destinations** (UI partagée `DropboxSync`/`GoogleDriveSync`/`PCloudSync`, prop `mode='save'|'restore'`) :
- **Dropbox** : OAuth PKCE offline. `state = 'dropbox.<nonce>'` (`dropbox_state`), nonce obligatoire (le `'dropbox'` nu est refusé : alerte anti-CSRF).
- **Google Drive** : **Google Identity Services, modèle « token »** (le flux implicite par redirection n'existe plus). `loadGis()` charge lazy `accounts.google.com/gsi/client` (**non versionné ⇒ pas de SRI**), **préchargé au boot** pour que `requestAccessToken()` parte SYNCHRONE d'un clic (sinon popup bloquée). Scopes séparés `GDRIVE_SCOPE_BASE` (`drive.file`) / `GDRIVE_SCOPE_READ` (`drive.readonly`, Finance → Mes Actifs) ; **YouTube a son propre consentement et son propre jeton** (`ytg_*`). ⚠ **`include_granted_scopes: false` obligatoire** (sinon Google recolle les scopes déjà accordés → combinaison interdite `youtube` + `drive.file` → erreur 400). ⚠ **Aucun repli par redirection** ; échec → `gdriveLoginFailed` / `ytgLoginFailed`. ⚠ **La PWA installée casse** (jeton renvoyé vers `storagerelay://`) → se connecter **une fois depuis un onglet Chrome normal**.
- **pCloud** : OAuth **implicite** (le flux `code` exige un client_secret). ⚠ **2 régions** (`api.` / `eapi.pcloud.com`) mémorisées dans `pcloud_host` — la mauvaise donne « invalid access token ». ⚠ **L'API répond TOUJOURS en HTTP 200** avec `result` ≠ 0 → `pcloudApi()` teste `result`, pas `res.ok`. Hôte de contenu éphémère `c###.pcloud.com` (joker CSP).

**Photos → Google Drive** (`HUB_Pierre/<Module>`) : un fichier par fiche **nommé avec son id** → le lien fiche ↔ photo est porté par le NOM, le Drive fait autorité (helpers `gdrive*`). Un fichier privé n'a pas d'URL affichable → téléchargement au Bearer + `createObjectURL` (**d'où `blob:` dans `img-src`**). ⚠ **`gdriveBlobUrl` va mémoire → `spotifyplus_photo_cache` → Drive** : une photo déjà vue ne repart JAMAIS chercher Drive (demande utilisateur), et Vêtement garde en plus son index fiche→`fileId` dans cette base (`photoIndexGet`/`photoIndexPut`) pour afficher ses photos **Drive déconnecté**. Contrepartie assumée : une photo remplacée depuis un AUTRE appareil reste l'ancienne — `gdriveBlobForget` purge les deux caches pour un remplacement fait ICI, et le bouton « ↻ Re-télécharger » de Vêtement vide la base (`photoCacheClear`).

## Modules

| Module | Composant / couleur | Stockage | Notes |
|---|---|---|---|
| **Musique** | `MusiquePanel`, violet | (base SQLite) | 6 collapses `defaultOpen={false}` : ⚙ Scraping · ⏳ En attente · ❤ Likés · 🕘 Historique · 🎤 Artistes suivis · 🔀 Delta (bouton ▶ Analyser + 3 sous-collapses : Delta Playlist et Delta Artiste, RÉSULTATS d'un seul scan, puis 🤖 IA — qui repart de ce scan, donc inutilisable sans lui) |
| **Actu** | `ActuPanel`, bleu-indigo | caches 30 min, `spotifyplus_actu_ia`, `_actu_bug`, `_yt_top_seen` | Actualité · **Bug & Pannes** · **🐂 BullRun** · **🗺️ Cartographie** · Fuites de données · Vidéos YouTube (OAuth séparé, lecture dans le `YtFullscreenPlayer` de TV Time) ; trait rouge « Fin des nouveautés » posé au rang de la vidéo qui ouvrait l'onglet à la visite précédente — `_yt_top_seen` garde un ID (jamais un rang, la liste s'allonge par le haut) par onglet, écrit par `YtTopSeenRecorder`, donc **seulement si le collapse a été déplié**, tandis que le repère affiché est figé au montage. Hors sauvegarde. `data/actu.json` prioritaire, repli proxies CORS.<br>**Bug & Pannes** — ⚠ **Downdetector n'a aucune API publique** et bloque les robots : la pastille vient des pages de statut OFFICIELLES d'`API_STATUS_HOSTS`, **source unique** reprise par `DD_SERVICES`, `API_HOST_ID` et la CSP ; `DD_KIND` dit comment lire chaque hôte (Statuspage `/api/v2/status.json`, Instatus `/summary.json`, `gworkspace` = Gmail + Drive dans UN `incidents.json`, **relayé**). ⚠ `ddFetchStates` fait **une requête par HÔTE**, pas par service. La **courbe des signalements** (relayée, `ddParseReports`, cache `graphs` 30 min) sert aussi de pastille aux services sans page officielle via `ddFromReports` — volume récent au-dessus de la médiane du reste, **zéro requête de plus** ; ⚠ ce n'est PAS un constat d'éditeur (libellés séparés, `reported:true`, chiffres cités), une page officielle qui répond garde la priorité, garde-fou `DD_REPORT_FLOOR`, et le compteur « N perturbés » passe par `ddIsDown`, jamais par `ind !== 'none'`.<br>**BullRun** (`BullRunPanel`) — 5 collapses TOUS repliés ⇒ zéro requête tant qu'on n'ouvre rien ; cache module-level `_bullrunSum` 10 min. ⚠ **Aucune page tierce embarquée** : tout est recalculé ici, le site de référence tient dans un `SiteLink` (`BULLRUN_SITES`). `BullrunMacroCal` = les publications de la SEMAINE par jour local, heure et ★, **filtrées par `macroCalKeep`** (★★+ pour `MACROCAL_EU_US`, ★★★ ailleurs — ⚠ filtre appliqué APRÈS le test de disponibilité, sinon une semaine calme déclencherait le repli distant), lues dans `data/macrocal.json` en premier ; la note DIT quelle source a servi. ⚠ `BullrunRainbow` ne porte QUE le bitcoin et ne touche PLUS à CoinGecko (365 j d'historique public) : **Blockchain.com** `charts/market-price?timespan=all` (⚠ `x` en SECONDES) → **Stooq CSV**, régression log maison, la ligne nomme sa source et son nombre de points. `RainbowChart` : ⚠ l'échelle se cale sur le prix + les arêtes extrêmes du JOUR, jamais sur toute l'étendue des bandes (elles divergent à l'origine et écraseraient la courbe).<br>**Cartographie** — douze fiches à plat dans un seul collapse, gabarit `CartoCard` = `[titre] … [↻] [▶]`, lien `↗ site` rendu EN TÊTE DU CONTENU. ⚠ **Chaque fiche arrive REPLIÉE** : le `open` vit dans `useCartoSummary`, à côté du chargement — une fiche repliée ne demande RIEN, ouvrir Cartographie ne lance aucun relevé (🚇 Transport porte son propre repli : sans lui l'application d'IDFM se chargerait à chaque ouverture ; 🌌 Aurores n'a ni repli ni ↻, elle n'a plus rien à montrer que son lien). Le ↻ déplie au passage ; cache `_cartoSum` 15 min. ⚠ **Aucune note explicative sous les chiffres** : une réserve qui compte se dit dans le `sub` d'une ligne. ⚠ **DEUX fiches ne lisent plus RIEN** (13.21.17) : 🌌 Aurores est un `CartoLinkCard` — son seul contenu est le lien SpaceWeatherLive, tout le reste (Kp, vent solaire, Bz, éruptions, ovale auroral, et donc l'hôte NOAA SWPC) a été retiré d'`API_LIST` et de la CSP — et 🚇 Transport est la carte IDFM embarquée, sans aucune requête. Sources des dix autres : séismes USGS · communes `geo.api.gouv.fr` (⚠ `cartoSearchCommunes` n'est qu'une RECHERCHE ; l'enrichissement de la seule commune de TÊTE vit dans `cartoInseeCommune`, que 📊 INSEE SEULE appelle — l'y remettre ferait payer trois requêtes à 💧 Eau, 📸 Photos et `useCartoCity`. ⚠ Une donnée absente n'a PAS de ligne, jamais un « — » ni un 0 de remplissage) · air et **pollens** Open-Meteo · eau Hub'Eau (⚠ code INSEE RÉSOLU par `cartoSearchCommunes`, jamais en dur ; ⚠ dans `WATER_STATES` le test « non conforme » vient EN PREMIER, il contient « conforme ») · conflits **UCDP** (⚠ version dans le CHEMIN → cascade `UCDP_VERSIONS`) · tournages **opendata.paris.fr** (⚠ tri sur `date_debut`, cascade d'`order_by`, un champ inexistant rend un 400) · photos **Wikimedia Commons** (⚠ `origin=*` obligatoire) · orbites **Celestrak** (HTML → `DOMParser` via `corsFetchText` ; ⚠ `SPACE_ORIGINS` est le SEUL jeu de coordonnées en dur). `CartoWaterAirMap` / `CartoMiniMap` : Leaflet lazy, molette désactivée (panneau qui défile), surcouche refaite sur SIGNATURE, ⚠ une surcouche peut rendre une **fonction de nettoyage** rappelée avant la reconstruction suivante et au démontage. `useCartoPick` + `CartoPickRow` : toucher une ligne la DÉSIGNE (point blanc `CARTO_PICK_DOT`, `CARTO_PICK` valant l'orange de « Dégradé ») ; le chiffre garde la couleur de sa mesure. `useCommuneSuggest` : ⚠ part du nom DÉJÀ dans le champ (sinon le menu s'ouvre au montage), `onMouseDown` et non `onClick` (le blur fermerait le menu). `useCartoCity` (💧 Eau, 🌫️ Air, 🏥 Santé) : la ville cherchée est un état LOCAL, jamais versé dans `METEO_LOCS`. ⚠ **Aucun de ces hôtes n'est joignable côté agent** (piège 32) : parsing défensif, un lot vide lève une erreur NOMMÉE, jamais un chiffre inventé.<br>`ActuAiSummary` : résumé écrit **à partir des titres passés dans le prompt** (jamais de la mémoire du modèle, jamais `opts.tools`) ; `_actuIa`, 1 génération/jour, cache **hors backup**. ⚠ Vue à `ACTU_SEEN_RATIO`, à la fin ou cochée 👁 → `ytSeenMarkAnywhere` + `ytWlDropSeen` ; vidéo trop récente ⇒ `ytVideoListWith` l'INSÈRE dans ses `videos` (la fiche ne rend que `videos`), garde-fou `ytItemAcceptsVideo` |
| **Alertes** | `RappelPanel`, `#6f7bf5` | `spotifyplus_rappels`, `_rappels_time`, `_rappels_notes`, `_remember_custom`, `_garanties`, `_anniv_custom`, `_speedrun_todo_added` | Rubriques fixes + **trois listes LIBRES** : 🔔 Mes rappels (`_remember_custom`) et 🛡️ Mes garanties (`_garanties`) **en tête**, 🎂 Mes anniversaires (`_anniv_custom`) **dans la rubrique 🎂 Anniversaire**, qui n'a AUCUNE ligne figée (les dates orphelines des anciennes prédéfinies sont purgées une fois au boot par `rappelPurgeGone` — ⚠ liste EXPLICITE `RAPPEL_GONE`, jamais un préfixe : `anniv.` est celui de la liste libre) (option `bare` de `listeLibre`, qui rend la liste sans son collapse). Vides à l'installation, même gabarit, clés d'alerte `custom.<id>`, `garantie.<id>` et `anniv.<id>` — ⚠ trois préfixes DISTINCTS, deux lignes nées dans la même milliseconde se voleraient sinon leur tâche To do. La croix de « 📅 Dates à venir » porte un descripteur `perso:{list,id}` et non plus un id par liste + sections auto (Frigo, Steam) + calendrier 🗓. `RAPPEL_NOTES` = champs de rubrique en **texte libre**, ⚠ sans date donc **hors du circuit d'alerte** (badge, calendrier, notification et To do dérivent de `RAPPEL_SECTIONS` seul). `RAPPEL_RECUR` / `rappelNextDate` (la date stockée reste celle d'origine) — ⚠ **tout anniversaire récurre de 12 mois par sa CLÉ** (`isAnnivKey` / `rappelRecurOf`) : il n'est jamais « en retard », et ses jalons d'alerte sont `ANNIV_ALERTS` = jour J et J-3 SEULEMENT (prévenu à J-3, pas besoin de l'être à J-1) via `rappelAlertDue`. Sa saisie est un champ **JJ/MM** (`dayMonthInput`, sans heure) — ⚠ le stockage reste un `YYYY-MM-DD` (l'année en cours est posée pour nous) et la valeur n'est validée **qu'à la sortie du champ**, « 24/1 » étant un état valide en cours de saisie de « 24/12 ». File speedrun non vide ⇒ tâche « Aujourd'hui » du To do (`syncSpeedrunToTodo` : marqueur `srKey`, 1 création/jour, file vidée ⇒ tâche et marqueur retirés) |
| **API** | `APIPanel`, `#6a78f2` | `spotifyplus_api_export`, `_api_calls` | `API_LIST` = miroir de `connect-src`. Clés inline (`API_KEY_LS`, valeur = chaîne **ou tableau** pour 2 champs), ligne rouge si clé manquante, export JSON |
| **Bon Plan** | `ComparePanel`, bleu-indigo | `spotifyplus_cmp_watch` | **3 sources dans la même liste** : CheapShark, `cmpSteamDeals`, `cmpEpicDeals` (⚠ tester la fenêtre `startDate`/`endDate`, une promo finie reste dans la réponse). ⚠ Prix hétérogènes (USD converti vs euros réels) → `__eur` / `px()` / tri `cmpEurVal` ; ⚠ `/deals` renvoie **une ligne par boutique** → regroupement `cmpTitleKey` ; `cmpMergeEditions` (Deluxe/Ultimate = `gameID` distincts) ; ☆ réservé aux jeux à `gameID` CheapShark |
| **Collection** | `CollectionPanel`, `#5a95e6` | `spotifyplus_collection` + **IndexedDB `spotifyplus_cards_db`** (cartes) | 7 catégories, moteur commun `CollectionSub`. `collAdd` incrémente `qty` sur id existant, `collEdit` corrige une fiche **sans jamais toucher son `id`** (clé de dédoublonnage ET nom du fichier Drive) ; `img` = **URL distante, jamais une image encodée**. Photos d'analyse IA → Drive `HUB_Pierre/Collection` (`collPhotoUpload`, nom = id ASSAINI par `collPhotoKey` — les ids portent `:` et des accents) ; index `_collPhotos` listé 1×/session, Drive absent ⇒ photo non conservée.<br>**Import Finance** (`CollFinanceImport`, `actifsSheetRead` partagé avec le portefeuille crypto) pour Displate / Pop / Montre : une colonne = un objet (l'en-tête est l'id ET le nom), la ligne datée la plus récente donne le prix, une colonne à 0 est écartée. ⚠ La cellule N'EST PAS toujours des euros — le facteur vient du `Total` de la ligne. ⚠ **`actifsFindFile` ne trouve QUE les Google Sheets NATIFS** : un `.xlsx`/`.ods` est invisible pour l'API Sheets → sonde `actifsFindForeign` + erreur `NOTNATIVE:<format>:<nom>` rendue par `actifsNotNativeMsg`.<br>**Cartes** — ⚠ les deux sous-collections de `CARD_SUBS` ne vivent PAS dans `spotifyplus_collection` mais dans `spotifyplus_cards_db` : `collLoad()` les y recolle dans la même map et `collSave()` les y redirige (aucun appelant ne change), mais tant que `_cards.ready` est faux (`cardsPreload`, appelé par `bootLocal`) `cardsSet` REFUSE d'écrire, comme TV Time. Deux plafonds : `COLLECTION_MAX` ne compte que le localStorage, les cartes ont `CARDS_MAX`. « Ma collection » est groupée PAR EXTENSION (`cardGroups`), triée par `CARD_SORTS` (% complété par défaut ; choix dans le module-level `_cardSort`, aucune clé de stockage — ⚠ sans total catalogue pas de pourcentage, sans date de sortie pas de date : la ligne passe en FIN, jamais à 0 % ni au 1er janvier 1970), cartes rangées par NUMÉRO (`cardNumCmp` : comparaison numérique partie par partie, préfixes et suffixes gérés). `X / Y` = numéros DISTINCTS possédés sur le total de l'extension ; ⚠ **catalogue injoignable ⇒ le X s'affiche SEUL**, jamais un total deviné (`cardSetsLoad` : 1 requête par jeu et par session, au premier dépliage). ⚠ **ACCORDÉON** : une seule extension ouverte à la fois — c'est ce qui borne le coût, et ce qui tient 20 000 cartes (repliée, une extension ne rend aucune vignette). ⚠ Liste **Manquantes** (`CARD_MISS_MODES` : `'off'` par défaut · `'inline'` · `'apart'`) : hors `'off'`, le catalogue de la SEULE extension ouverte est lu (`cardMissLoad`, cache `_cardMiss`) et les numéros absents sont rendus en grisé, non cliquables (5ᵉ argument `ghost` de `card`, **sur la fiche et non sur la grille** : en `'inline'` manquantes et possédées partagent la MÊME grille, donc la même pagination). L'attente, l'échec et le compte se rendent AU-DESSUS de la grille dans les deux modes. ⚠ `cardSetCode`/`cardNum` déduisent extension et numéro de l'id `pkm:<ext>-<num>` pour les fiches sans `setId` ni `num`.<br>⚠ **TOUT est replié à l'arrivée**, aucun effet n'ouvre une sous-collection — d'où le bilan d'import écrit en toutes lettres sous la barre. Chaque vignette est une **fiche** (`.coll-grid`, 4 par ligne sous 560 px, `.coll-grid-wide` pour les collections à nom) : pas de ✏️, quantité en pastille, numéro et cote (`collEurShort`) sous une carte, l'extension n'apparaissant que sur un RÉSULTAT. ⚠ **UNE SEULE BARRE, collante, deux métiers** : ce qu'on tape FILTRE en direct, ⏎ cherche au CATALOGUE (`sub.search` et `sub.searchSets` partent ensemble par `Promise.all`). `sub.addSet` (`pokeSetCards`) verse l'extension via `collAddMany` — une carte déjà possédée est ignorée, **sa quantité n'est pas incrémentée** ; `pokeCardRow` est la conversion unique partagée avec `pokeSearch` (deux conversions auraient créé des doublons d'id). `sub.barBtn` n'existe que là où il y a un fichier à importer — Magic seul.<br>⚠ **L'import Magic n'accepte QUE du CSV** (ManaBox ou Delver Lens ; colonnes devinées par mots-clés, séparateur déduit), rendu par DEUX composants qui partagent `_mtgImp` : `MtgImportBtn` (le 📥) et `MtgTools` (cote 💰 + colonnes retenues). Les fichiers de SAUVEGARDE (`.backup` SQLCipher, `.dlens`/`.bin`) sont des bases internes chiffrées, non lues — mais **choisissables EXPRÈS**, sinon le message qui explique pourquoi serait inatteignable ; un binaire est reconnu par son CONTENU (`mtgLooksBinary`). Id aligné sur `magicSearch` (`mtg:<id Scryfall>`) dès que le CSV le donne ; l'image n'est **jamais stockée** (`mtgImgUrl`). Réimporter MET À JOUR les quantités, 💰 passe par `POST /cards/collection` (75 max/lot).<br>**🃏 Analyse Cardmarket** (`CollectionCardmarket` → `MtgSetSection`, état `_mtgSet`, aucune clé de stockage) : l'analyse LÉGÈRE ne lit que Scryfall, qui publie **un seul prix par carte, sans état ni langue** — les règles Good/NM et français n'existent que dans l'analyse POUSSÉE, qui lit les offres via `corsFetchText`. ⚠ Celle-ci est **best effort et doit le rester** (Cloudflare) : toute carte non lue est COMPTÉE ET AFFICHÉE, jamais valorisée à 0, et les liens 🛒 marchent dans tous les cas. ⚠ `mtgSetBestPerSeller` réduit chaque vendeur à UNE offre avant le regroupement, sinon un vendeur listant la même carte en GD et en NM comptait double |
| **Emploi** | `EmploiPanel`, `#57a0e0` | `spotifyplus_emploi`, `_adzuna_id`, `_adzuna_key` | 🎯 Critères de l'emploi rêvé (formulaire = **seule source de vérité**, `EMPLOI_CRIT_KEYS` = liste blanche écrite en localStorage) + 📋 Offres, dont le **seul** champ est la 🤖 demande en langage libre : `emploiSearchJobs(crit)` interroge The Muse · Remotive · Arbeitnow · Jobicy (keyless) + **Adzuna** (2 identifiants gratuits, seule à couvrir la France et à filtrer salaire/ancienneté côté serveur). ⚠ `emploiReject` = critères DURS (**lieu**, fraîcheur, exclusions, salaire, contrat, télétravail), `emploiScore` = critères SOUPLES (secteur, techno, langue, expérience) — **une donnée manquante de l'OFFRE ne rejette jamais**. Le LIEU est non négociable (`emploiLocOk`, comparaison sans accents sur le seul champ lieu) : hors zone, sans lieu ou « anywhere » ⇒ écartée, sauf à saisir « Remote » parmi les lieux. Puis `emploiAiRank` **réordonne** (⚠ l'IA CLASSE, elle ne cherche pas : un `id` inconnu est ignoré, une offre oubliée garde son rang local). ⚠ La 🤖 recherche en langage libre ne cherche pas non plus : `emploiAiQuery` traduit la phrase en critères, validés contre la liste blanche (`emploiAiCrit`), qui forment un **calque à usage unique** posé sur le formulaire et **jamais persisté** |
| **Finance** | `FinancePanel`, cyan | `spotifyplus_finance_history` | État module-level `_finance`. 4 modes **lazy** : Light / Full / Calculatrice / Mes Actifs. ⚠ **`BullRunPanel` n'est PAS ici** : il vit dans **Actu → 📰 Actualité**, juste au-dessus de Bourse & crypto. Relevé quotidien `finAutoDue` → `finHistSnapshot` (tout le Light + les seules lignes Full INDICES et STOCK PICKING). ⚠ Son déclencheur est un effet **à part** de celui du scraping musique (comme la pluie) : ni `dbReady` ni `offline` ne le gardent — Finance ne doit rien à Spotify ni à la base, l'y remettre rouvrirait les trous d'historique des sessions ouvertes hors ligne |
| **Frigo** | `FrigoPanel`, `#47a8de` | `spotifyplus_frigo` | 3 compartiments + péremption + assistant IA (photo → ingrédients, recettes) |
| **Histoire** | `HistoirePanel`, `#41b0d8` | `spotifyplus_histoire` | Ville + photos → fiche d'œuvre par IA (recherche web ancrée) + Google Custom Search. Fil de questions (`chat` DANS la fiche, donc sauvegardé) ; `histChatPrompt` rappelle TOUJOURS ville + œuvre |
| **Jeux** | `JeuxPanel`, `#39b2d1` | `spotifyplus_game_hi_*`, `tierlist-v1`, `_flip7`, `_doodle_hi_reset`, `_hi_purged_v9` | 27 mini-jeux **100 % locaux et hors ligne**. ⚠ **Dobble** (`GameDobble`, 2 joueurs sur UN écran, zone du haut tournée à 180°) ne tire PAS ses cartes au hasard : `dobbleDeck()` construit le **plan projectif d'ordre 7** (57 cartes de 8 symboles, exactement UN symbole commun à deux cartes quelconques) — `57 = 7²+7+1` et `8 = 7+1` sont UN SEUL réglage avec le nombre d'objets de `vendor/dobble-art.js`, on n'en change pas un sans les autres. Seule la carte du MILIEU est ronde et dispersée (PRNG graine = index de la carte, sinon les 8 symboles sauteraient à chaque rendu) ; celle qu'un joueur a DEVANT lui est un rectangle de 4 × 2 symboles debout (`DobHandCard`, l'orientation venant du parent tourné à 180°). Une erreur coûte une TUILE de plus, piochée au talon des 16 cartes non distribuées (`dobDraw`, qui remélange quand il est vide), SAUF si le milieu vient de changer (`DOB_GRACE_MS`) : le joueur visait l'ancien, il est alors seulement bloqué `DOB_PENALTY_MS` — jamais les deux malus à la fois ; `DOB_PICK` dit la version de dessin retenue par objet (**id absent ⇒ A**) — SEULE table à toucher pour changer un dessin d'avis, les trois versions restant dans le vendor. ⚠ **Des chiffres et des lettres** (`GameChiffresLettres`, 1 à 8) n'ARBITRE pas : il tire, chronomètre et dévoile, le gagnant de la manche est désigné À LA MAIN (demande utilisateur) — donc aucune clé de stockage, rien à ajouter à la sauvegarde. `cdlSolve` rend le compte exact ou le plus proche (exhaustif, mémo `seen` sur le multi-ensemble de plaques : ~70 ms au pire, sans lui la page figeait) et il est appelé à l'ARRIVÉE sur l'écran de résultat, jamais pendant un rendu ; `cdlWordsFrom` relit le dictionnaire de Motus (`loadMotusData`), ⚠ **6 à 9 lettres SEULEMENT** — l'écran le dit, un mot de 4 ou 10 lettres valable n'y est pas. ⚠ **Flip 7** ne simule rien : c'est le TABLEAU DES SCORES d'une partie de cartes réelle (victoire à `FLIP7_GOAL`), donc une **donnée** et non un record — `spotifyplus_flip7` voyage dans la sauvegarde (clé `flip7`, REMPLACÉE à la restauration : unir deux listes de manches en inventerait) et s'exporte en CSV. `useImmersiveLock(true)` ; `useHidePlayerBar(true)` ; `useNoPinchZoom(active)` (Osu) — ⚠ écouteurs **non passifs** obligatoires. ⚠ `_doodle_hi_reset` = migration jouée **une seule fois**, l'effacer remet le record à 0. ⚠ Retirer un jeu ⇒ ajouter son id à `REMOVED_GAMES` (cumulatif, **un jeu qui revient y reste** : la purge tourne avant la 1re partie et efface le record de l'ancienne version) et **renommer** `HI_PURGE_LS` (`_v10`…), sinon la purge ne repart pas là où l'ancienne a déjà tourné |
| **Maps** | `MapsPanel`, jaune | `spotifyplus_maps` | Import Google Timeline (2 formats), Leaflet lazy. ⚠ L'app ne trace jamais la position |
| **Météo** | `MeteoPanel`, bleu | `spotifyplus_meteo_rain_notified` | Open-Meteo sans clé, **3 lieux** (`METEO_LOCS`), Light 3 j / Full 7 j. ⚠ `METEO_LOCS` est lue par QUATRE cartes — Météo, 🌫️ Qualité de l'air, 💧 Qualité de l'eau et 🤧 Pollens & allergies : y retirer un lieu le retire des quatre. Ne pas la confondre avec `FR_CITIES`, le repli hors-ligne du reverse-geocoding, qui doit rester dense. `meteoRainCheck()` notifie une pluie dans les 2 h **1× par ÉPISODE** (clé = heure de DÉBUT des heures pluvieuses contiguës). Géoloc **seulement si déjà autorisée**, sinon `METEO_LOCS[0]` |
| **Mot de Passe** | `GestionMdpPanel`, ambre | `spotifyplus_mdp_vault` | AES-GCM 256 + PBKDF2 600k. **Deux espaces Perso / Pro** dans UN coffre : le bouton en tête du panneau porte le nom de l'espace affiché et bascule au clic (`MDP_SPACES`, module-level `_mdpSpace`, **aucune clé de stockage** — le coffre se reverrouille au reload). ⚠ `entries` reste la liste COMPLÈTE (c'est elle qu'on rechiffre), `spaceEntries` est la vue — tout ce qui s'AFFICHE, check-up compris, part d'elle, et une entrée AJOUTÉE naît dans l'espace affiché. ⚠ `space` absent ⇒ Perso (lire par `mdpSpaceOf`, jamais `e.space` nu) : rien n'est migré, et le blob reste `{entries, tags}` ⇒ rien à changer aux sauvegardes. ⚠ Ce n'est PAS un cloisonnement de sécurité (même mot de passe maître) ; `syncLeakVaultTodos` voit les deux espaces, comme il le doit. Les collapses de THÈME arrivent **repliés** ; un filtre actif (recherche, criticité, tag) les ouvre — ⚠ `defaultOpen` n'étant lu qu'au montage, c'est un SUFFIXE DE CLÉ qui remonte les collapses à l'entrée et à la sortie du filtre. ⚠ La clé dérivée est CACHÉE en mémoire (`_mdpKeyCache`, 4 max, vidé au verrouillage/déconnexion) et `mdpEncrypt` REJOUE le sel connu : **ce qui doit rester neuf à chaque chiffrement, c'est l'IV, pas le sel**. Le blob chiffré contient `{entries, tags}` → suivi par les sauvegardes. Verrouillage auto 10 min. ⚠ Mot de passe oublié = irrécupérable. 🩺 **`mdpCheckup`** (bouton sous « déverrouillé », JAMAIS automatique) : réutilisation exacte, variantes de même racine (`mdpRoot`), force/motifs, expiration, fiches incomplètes ou en double, **HIBP Pwned Passwords en k-anonymity** (`api.pwnedpasswords.com/range/<5 car. du SHA-1>` — ni le mot de passe, ni son hash entier, ni le compte ne sortent) et **sites piratés** via `/api/v3/breaches` (public, sans clé). ⚠ Un contrôle réseau muet est ANNONCÉ comme tel : « rien trouvé » ne doit jamais se lire « rien ». ⚠ Le test « le site répond-il ? » est une `<img>` sur son favicon, **directe chez lui, jamais par un relais**, et sur case à cocher : `connect-src` ne listera jamais les domaines des comptes. ⚠ HIBP `/breachedaccount` est PAYANT — hors sujet ici. Le bilan vit en mémoire seule |
| **Note** | `NotePanel`, `#6fd345` | `spotifyplus_notes`, `_notes_enc_check`, IndexedDB `spotifyplus_note_media` | 2 espaces (clair / crypté au mot de passe **global**), sauvegarde auto 700 ms. La note ne porte que les ids des pièces jointes, les contenus vivent en IndexedDB (chiffrés dans l'espace privé) |
| **Revente** | `ReventePanel`, `#86d941` | `spotifyplus_revente` | Annonces Vinted/Leboncoin/eBay par IA. ⚠ Publication automatique **impossible sans backend** |
| **Santé** (id `food`) | `NourriturePanel`, `#9bd93f` | `_food_profile`, `_food_days`, `_food_meals` (local seul), `_food_weights`, `_food_favs`, `_food_sport`, `_food_forecast` | Calories (Open Food Facts + IA + photo de plat), macros, pesées. Chaque repas est ré-éditable (nom, catégorie, **quantité `g`**, kcal, macros ; champ de macro vidé ⇒ la macro redevient INCONNUE, jamais 0). ⚠ Changer la quantité refait un **produit en croix** sur kcal et macros à partir d'un couple de RÉFÉRENCE figé, redéfini dès qu'une valeur est corrigée à la main ; quantité inconnue (repas d'avant `g`, non lisible dans le « (N g) » du libellé par `foodMealGrams`) ⇒ aucun recalcul. ⭐ Favoris : valeurs /100 g épinglées (id = `nom|marque`, jamais d'image), recherche LOCALE dédiée. 🏃 `FoodSportSection` : sport + durée + intensité → kcal brûlées (`foodSportKcal` = MET × poids × heures, `FOOD_SPORTS`/`FOOD_INTENS` ; `manual:true` = kcal corrigées à la main, plus jamais recalculées). 🖼️ **capture de séance** (Strava & co, galerie seule) → `foodStravaAnalyze` : l'IA **vision** lit la course sur l'image et estime les kcal **pour le physique du profil**, ⚠ bornées ×0,4–×2,5 de la formule MET (une durée mal lue partirait sinon dans l'objectif du jour) ; séance **proposée**, enregistrée au + avec `manual:true` + `ai` (✨ sur la ligne). ⚠ Le sport ne se soustrait PAS des repas : il RELÈVE l'objectif du jour (`targetEff = target + dayBurn`), `_food_days` reste le total avalé. ⚠ Un SEUL sélecteur de jour (chips sous le profil) commande la carte 🍽️ Nourriture ET la carte 🏃 Sport — elles parlent toujours de la même journée. 📉 `FoodForecastSection` (sous l'historique) : projection de perte de poids jusqu'à une échéance. ⚠ La perte n'est PAS 100 % de gras — `fatShare` (**90 % par défaut** — le scénario protéines + renforcement ; 75 % est le cas « déficit seul », le pire des trois) mélange 7 700 kcal/kg de tissu adipeux et **1 800 kcal/kg de masse maigre**, d'où une densité mixte bien inférieure à 7 700. ⚠ Le poids de départ vient de la DERNIÈRE PESÉE quand il y en a une, jamais du réglage saisi ; l'axe de la courbe part du PLUS ANCIEN de `cfg.createdAt` (tampon posé UNE fois au 1er montage, dans un effet — `loadForecast` est un initialiseur `useState`, donc appelé pendant un rendu) et de la 1re pesée : sans le tampon l'axe démarrait à aujourd'hui tant qu'il n'y avait qu'une pesée, sans rien à montrer ; réglage d'avant 13.14.0 ⇒ tampon absent ⇒ aujourd'hui, jamais une date inventée. ⚠ Toute écriture du journal de pesées passe par **`weightsPublish`**, jamais `saveWeights` nu : ⚖️ Poids et 📉 Prévision sont montés en même temps et abonnés à la même liste (`useWeights`) |
| **Sport** | `SportPanel`, `#c3d93f` | `spotifyplus_sport`, `_sport_times` (rendu dans Stats) + **SQLite `spotifyplus_sport_img_db`** (planches) | 9 groupes musculaires × 70 exercices illustrés (Wikimedia Commons), appui long → fiche animée. **Cache SQLite des images** : vue une fois, une planche est relue en base (`sportImgResolve` → `useSportImg`, URL blob) et le module s'affiche HORS LIGNE. ⚠ Base SQLite **à part**, jamais une table de `spotifyplus_db` : `saveDB()` réexporte TOUT à chaque écoute. ⚠ `useSportImg` rend `null` tant que la base n'a pas répondu — partir sur l'URL distante rallumerait le réseau pour une image que le cache allait servir. ⚠ `SportExoDemo` résout TOUTES ses phases d'un coup (un hook par phase est impossible, leur nombre change d'un exercice à l'autre). ⚠ Plafonds `SPORT_IMG_MAX_ONE` / `_MAX_TOTAL` : cache plein ⇒ retour au réseau, rien ne casse. ⚠ `upload.wikimedia.org` a dû rejoindre `connect-src` (il n'était que dans `img-src`) |
| **To do** | `TodoPanel`, vert | `spotifyplus_todos`, `_todos_done`, `_todos_trash`, `_leak_todo` | 7 rubriques en carrousel. 🏷️ **Tags de tâche** (`TODO_TAGS` : 📍 Massy bleu, 💼 Travail rouge) : DOUBLE TAP sur le libellé = tag suivant (rien → Massy → Travail → rien), et le bouton à GAUCHE du ♻️ suit le MÊME cycle. Un champ `tag` sur la tâche, **aucune nouvelle clé** donc rien à ajouter à la sauvegarde ; ⚠ toujours lire par **`todoTagOf`**, jamais `t.tag` nu — les tâches d'avant 13.20.5 portent un booléen `massy`. Le filtre n'est **jamais persisté** (on arrive toujours sur tout) et s'applique DANS la rubrique courante, où une tâche ajoutée naît avec le tag affiché — sinon elle disparaîtrait en s'écrivant. ⚠ `touchAction:'manipulation'` sur le libellé, sans quoi le double tap zoome la page. ♻️ = les 3 dernières supprimées, **restaurables** (`restoreTrash`) : ⚠ `recordTodoDone` rend son horodatage, stocké en `doneTs` dans la corbeille, pour que `todoDoneUndo` retire LA BONNE entrée du journal. Dans `daily` et `cecile` le × **valide** au lieu de supprimer ; appui long 3 s dans `daily` = supprimer la récurrence. `today` alimenté par `syncRemembersToTodos`, `syncSpeedrunToTodo`, `syncTvEpisodeAlerts` et `syncLeakVaultTodos` (une fuite d'Actu touchant un site du coffre ⇒ « 🔒 Site hacker vérifie ton compte : X » ; ⚠ **coffre déverrouillé obligatoire**, d'où l'appel au déverrouillage ET à l'ouverture d'Actu, marqueur `_leak_todo` site|date hors sauvegarde = 1 tâche par fuite même supprimée). ⚠ **Santé coche le `daily`** : `todoDailyCheck(libellé)` valide pour AUJOURD'HUI toute tâche Quotidien dont le texte CONTIENT le libellé (sans accents ni casse) — pesée du jour → « se peser », `FOOD_TODO_MEALS` repas → « nourriture », séance de sport d'id `course` → « courir ». Il ne CRÉE jamais la tâche, et il est idempotent |
| **TV Time** | `TvTimePanel`, ambre | **IndexedDB `spotifyplus_tvtime_db`** (la liste) + `spotifyplus_tvtime_eps`, `_yt_progress`, `_yt_comment_likes`, `_tv_ep_todo`, `_tv_ep_notified` | Séries/films (TMDB), chaînes YouTube, livres (Google Books + Open Library). ⚠ **La liste vit dans sa propre base IndexedDB**, `loadTvItems`/`saveTvItems` restent SYNCHRONES sur le cache mémoire (les 43 appelants n'ont pas bougé) et seule la persistance est asynchrone : `tvItemsPreload()` — appelé par `bootLocal` AVANT tout le reste — déménage la liste au premier passage et n'efface la copie localStorage qu'après une écriture CONFIRMÉE. ⚠ **`_tvReady` est le verrou anti-perte** : tant que la base n'a pas répondu, `saveTvItems` REFUSE d'écrire *et ne touche pas au cache* — sinon une liste vide (lecture avant préchargement) s'afficherait puis partirait en base. La restauration confirme l'écriture avec `tvFlushDone()`. ⚠ La vignette d'une vidéo n'est **jamais stockée** : `ytStill(v)` la déduit de l'id (`i.ytimg.com/vi/<id>/mqdefault.jpg`), `tvStripStills` purge l'ancienne une fois. Onglets `all｜tv｜movie｜youtube｜shorts｜book` (`_tvtime_filter`). ⚠ YT et Short partagent le MÊME bloc de rendu, découpé par `ytInTab` = ce qu'il RESTE à voir (Shorts **et** vidéos longues en attente ⇒ le vidéaste est dans les deux ; replis : contenu de la chaîne, puis tag `shortsOnly`) — `ytWatching`/`ytDone` restent entières. Source de vérité = `tvWatchedSet`. ⚠ Un item hérité re-dérive son set à chaque refresh (l'épisode se cocherait tout seul à sa sortie) → **`tvFreezeWatchedSet` le fige AVANT tout refresh**. `YtFullscreenPlayer` : la fin d'une vidéo la valide, position dans `_yt_progress` (⚠ l'API YouTube n'en donne aucune ; hors sauvegarde). `tvSyncDaily` reprenable. ⚠ **🔔 `epAlert`** (fiche, séries seulement) → `syncTvEpisodeAlerts`, **100 % local** (il relit `nextEp`, déjà rafraîchi par `tvSyncDaily`) : tâche To do « Aujourd'hui » la VEILLE d'un épisode, notification la veille ET le jour même. Deux marqueurs distincts, hors sauvegarde — `_tv_ep_todo` (1 tâche par épisode, même supprimée) et `_tv_ep_notified` (1 notif par jour et par épisode) ; la clé d'épisode porte sa DATE, donc un report rouvre une occurrence. ⚠ **L'API Data v3 ne sait PAS liker un commentaire** → le 👍 pose une marque locale (`_yt_comment_likes`, hors sauvegarde) et ouvre le commentaire sur youtube.com (`&lc=`) |
| **Vêtement** | `VetementPanel`, `#f59a2e` | `spotifyplus_vetements`, `_vetement_prix`, `_vetement_img` + IndexedDB `spotifyplus_photo_cache` | 35 types en sous-catégories (`brand.groupe` = clé de `VET_GROUPS`, `''` = « Possédé »). ⚠ Ne JAMAIS renommer une clé de type : la fiche deviendrait orpheline. **Photos sur Google Drive**, gardées ensuite sur l'appareil (cache + index, cf. Sauvegarde). ⚠ `syncDrive({force:true})` est le SEUL chemin qui relit Drive quand l'index local existe. Filtres + recherche par image (IA). 2 réglages d'affichage persistés : 💶 masque les prix, 🖼️ grille de photos (`'on'` = images, absent = liste) |
| **Stats** | `StatsPanel` | `spotifyplus_sport_times` | Collapses imbriqués, **titres en ordre alphabétique**. Lecture seule PARTOUT SAUF le collapse 🏋️ SPORT (`SportTimesSection`, entre Santé et To do) : un temps LIBRE saisi à la main par distance de `SPORT_RUN_KM` — rien n'est calculé, et la clé voyage dans la sauvegarde (section Sport) |
| **À propos** | `VosEcoutesPanel` | — | Paramétrage · **Autorisations** · Lancement automatique · **Limite data** (`DataLimitPanel` : poids PAR MODULE des deux réserves — localStorage ventilé par `LSU_RULES` (icône/couleur reprises de `TOGGLE_MODULES`, ligne dépliable = ses clés une par une ; ⚠ **trois modules ont une ventilation interne colorée** — TV Time par famille de fiches (`lsuTvSplit`), Collection en 🟣 Magic / 🔴 Pokémon / 📦 Autre (`lsuCollSplit`, les deux premiers venant de `cardsBy`) et Musique en ⏳ En attente / 🎧 Écoutés / ⚙️ Autre (`lsuMusiqueSplit` : chaque titre pesé à ses colonnes texte EN SQL, « Autre » étant le RESTE mesuré de la base, jamais une règle de trois)) + `lsuIdbScan` pour les cinq postes lourds d'IndexedDB : base SQLite → Musique, liste → TV Time, pièces jointes → Note, base Cartes → Collection, cache des planches → Sport ; ⚠ le plafond n'est pas une constante devinée, `lsuProbeFree` le SONDE par dichotomie sur une clé jetable — même unité que l'usage, l'octet UTF-16, donc le % reste juste quel que soit le moteur ; ⚠ `navigator.storage.estimate()` ne donne QUE le total de l'origine, arrondi et en retard) · Exporter · Sauvegardes cloud · Restaurer · Réinit quota · Purger · Déconnecter |

**Autorisations externes** : `AUTH_PERMS` = **la table unique** (Drive, Drive lecture seule, YouTube, Dropbox, pCloud, notifications, géoloc, stockage persistant) ; `AuthPermsPanel` la rend en **cases à cocher**, **uniquement dans À propos**. Cocher = `ask`, décocher = `revoke` — que les 4 connexions OAuth savent faire (effacement du jeton local, puis **reload** : les panneaux `*Sync` voisins lisent leur état une seule fois). ⚠ Une permission NAVIGATEUR ne se rend pas depuis la page : case cochée et verrouillée. ⚠ **Aucun « tout autoriser » n'est possible** : une popup OAuth doit partir du même tick qu'un clic, et Google refuse `youtube` + `drive.file` ensemble. États navigateur relus au montage et sur `visibilitychange`.

**Layout** : `WebApp` (desktop, onglets en haut) · `MobileApp` (barre haut : profil + menu ⋯) · `CompactPlayer` (viewport court) · `HubHome` (accueil, grille dérivée de `TOGGLE_MODULES`) · `Home` (login).

**IA** : **8 fournisseurs en chaîne de replis** (repli AUTOMATIQUE, signalé par le **badge `AiFallbackBadge`** alimenté par `aiMeta()` = `{ai, fb}` capturés au même instant — variables module-level que l'appel suivant écrase), façade unique **`aiGenerate(modId, parts, opts)`** — **seul point d'entrée des modules**. Ordre : `gemini → groq → openrouter → mistral → cerebras → zai → cohere → cloudflare`, filtré par les IA cochées pour `modId`. `AI_PROVIDERS` porte `vision` (4 seulement) et `ls2`/`url` fonction (Cloudflare = 2 champs). ⚠ Une requête avec image **saute** les fournisseurs sans vision (sinon réponse inventée). ⚠ `opts.tools` (recherche web ancrée) **n'existe que chez Gemini** → sinon `NO_GROUNDING` (≠ `NO_KEY`, que les appelants testent pour abandonner) et l'appelant rejoue sans outil. `aiAnyKey` garde l'UI, `aiLastProvider()` nomme celui qui a servi. ⚠ **Le modèle Gemini n'est PAS figé** : `GEMINI_MODELS` est essayée dans l'ordre (404/403/5xx → suivant, un 5xx rejoué une fois : panne Google), le gagnant mémorisé pour la session. Google retire ses modèles ⇒ « Erreur IA (HTTP 404) » clé pourtant valide ; une clé morte, c'est un **400**. N'y mettre que des **Flash** (les Pro sont payants depuis avril 2026).

---

## ⚠ Pièges (les relire avant de coder)

**Compilation / React**
1. **Écran figé = erreur Babel ou erreur au niveau module.** Le filet d'amorçage affiche la cause sur `#boot` et purge le cache de compilation une fois, mais il ne répare RIEN dans la source : toujours vérifier que la page monte avant de commit.
2. **Babel hoisting** : tout hook avec tableau de deps (et tout `useMemo`) doit être déclaré **APRÈS** les variables qu'il liste, sinon deps `[undefined]` figées. Une `const` module-level lue plus haut = **TDZ = écran noir**. Contourner par une ref (`reloadFeedRef`, `startSyncRef`, `goRef`). ⚠ Dans `vendor/doodle.js` la même TDZ (typiquement une const lue par `D_TILES`, **construit au chargement**) ne donne pas d'écran noir mais « **Impossible de charger le jeu (réseau ?)** » : le module entier échoue, `DOODLE_READY` n'est jamais posé, et le fichier est pourtant arrivé intact. Le vérifier avant de commit : `node -e "global.window={};global.document={createElement:()=>({getContext:()=>({})})};global.localStorage={getItem:()=>null};require('./vendor/doodle.js')"`.
3. **Ne pas mélanger raccourci et propriété longue CSS** (`border` + `borderColor`) : React **supprime** la longue au re-render.
4. **Clé de liste = l'URI/l'id**, jamais `id + index` (remount + `React.memo` inopérant). ⚠ Ne pas passer l'index en **prop** non plus : il change pour tous les items suivant une suppression et fait échouer `React.memo`. Le numéro de rang du feed vient d'un **compteur CSS** (`.feed-list` / `.feed-row` / `.feed-num`).
5. **`StatsCollapse` démonte ses enfants à la fermeture** : rien n'est monté ni requêté tant qu'il est replié — mais tout état à préserver doit vivre **module-level** (pattern `_finance` / `_delta` / `_coll` / `_tvSync` : objet + `subs:Set` + publieur).
6. **Refs anti-closure** obligatoires dans les boucles `rAF`/`setInterval` et les écouteurs posés par élément (sinon index/filtres figés).
7. **Un nouvel onglet mobile DOIT avoir son bloc de rendu dans `MobileApp`** (sinon page noire), en plus de l'entrée dans `allOverflowTabs` — et les anciennes valeurs de `spotifyplus_active_tab`/`_view` doivent être remappées.
8. `selfHeading:true` dans `SECTION` **et** heading retiré de `MobileApp`, sinon le titre s'affiche en double.
9. **`position:fixed` est piégé par tout ancêtre `transform`** → `ReactDOM.createPortal(document.body)`. ⚠ **Réciproque** : en **plein écran NATIF**, le navigateur ne peint QUE le sous-arbre de l'élément plein écran → un portal sur `document.body` y est **invisible** : rendre la feuille **en enfant** du lecteur, comme `YtCommentsPanel` / `YtSummarySheet`.
10. **`useImmersiveLock(active)`** sur tout overlay plein écran : sans lui, une rotation fait chuter `innerHeight`, `Shell` remplace `MobileApp` par `CompactPlayer` et **démonte l'overlay**. `MOBILE_LOCK_TABS` = les onglets qui héritent du mode compact (aujourd'hui `musique` seul).
11. **Appui long** : ref `{timer, fired}`, `fired` neutralise le `click` qui suit le relâchement ; bloquer `onContextMenu` + `userSelect`/`WebkitTouchCallout` sur mobile.
12. **Un swipe et un défilement se terminent par un `click` synthétique** → garde `moved` (8 px sur l'un ou l'autre axe) avant d'agir.
13. **Une iframe cross-origin avale tous les événements** : pour capter un geste par-dessus, poser une couche de capture transparente (et lui laisser une réserve pour les contrôles natifs). Aucun moyen de relayer un clic dedans.

**Données / stockage**
14. **`lsSet` écrit la valeur telle quelle** → un objet devient `"[object Object]"`. Toujours `JSON.stringify` soi-même, et **lire le booléen de retour** (quota plein = rien n'a été écrit : ne pas afficher un ajout fantôme). ⚠ **Et `localStorage` LÈVE à la simple LECTURE** quand les données de site sont bloquées (Safari privé, Chrome cloisonné) : toute lecture atteignable depuis un **rendu** (corps de composant, initialiseur `useState`, effet) passe par **`lsGet`**, jamais par `localStorage.getItem` nu — sinon le throw démonte le sous-arbre React, voire empêche l'app de monter.
15. **Ne jamais stocker d'images** (data URL) en localStorage ni dans le backup : quota ~5 Mo partagé par tout le Hub. Photos → Drive, médias lourds → IndexedDB dédiée.
16. **Clé de jour toujours LOCALE** : `localDayStr()` (helper canonique, `todoTodayStr` en est un alias), jamais `toISOString()` (UTC → bascule vers 01-02 h en France, donc un jour de décalage).
17. **Fusion des sauvegardes : au MAX ou en union, jamais en somme.**
18. **`_db.run(sql, params)`** = tableau **positionnel** pour les `?`, jamais un objet.
19. `releaseInRange` : `release_date` peut être `YYYY`, `YYYY-MM` ou `YYYY-MM-DD` → traiter comme une **période**, `new Date("2026")` donne le 1er janvier.
20. **Une base séparée plutôt qu'un store de plus** dans IndexedDB : ajouter un store impose un changement de version, qu'un onglet resté ouvert bloque (`blocked`).

**Réseau / API**
21. **Spotify albums : `limit` max = 10** (`limit=20` → 400).
22. **Migration Spotify fév. 2026** : les entrées de playlist ont été renommées **`track` → `item`** — mais **`/me/tracks` utilise toujours `track`**. Résolveur partagé `it.item || it.track || it`.
23. **Un endpoint qui répond `items: []` n'est pas une preuve de vide** : essayer toute la cascade (`/items` puis `/tracks`) et garder la première réponse **non vide**.
24. **Le statut HTTP ne suffit pas à diagnostiquer** : « commentaires désactivés » et « quota épuisé » sont tous deux des 403 → lire `error.errors[0].reason`.
25. **`fetch('blob:…')` et `fetch('data:…')` sont bloqués par `connect-src`** (qui ne liste que des origines https) → décoder à la main ou passer par `<img>` + canvas.
26. **`FileReader.readAsDataURL` et non `URL.createObjectURL`** pour une image qui sera affichée : `img-src` autorise `data:` mais un blob URL non déclaré est bloqué.
27. **Les sites institutionnels refusent souvent l'`<iframe>`** (X-Frame-Options / frame-ancestors) et rendent un cadre MORT — et **aucun code ne peut le détecter** : le refus déclenche quand même `onload` et le document est illisible cross-origin. La vue reste donc partout un RÉSUMÉ recalculé ici, le site tenant dans un `SiteLink` (« ↗ nom du site », nouvel onglet). **Une seule exception** : 🚇 Transport, dont la fiche EST l'`<iframe>` de la carte IDFM. Son rattrapage est le `SiteLink` « ↗ Île-de-France Mobilités » posé juste au-dessus du cadre — **ne pas le retirer, c'est le seul recours si le cadre est mort**. N'en rouvrir une qu'à cette condition, après avoir testé les en-têtes dans un VRAI navigateur (le proxy de l'agent ne les voit pas, piège 32) et ajouté l'origine à `frame-src`. (🌌 Aurores, elle, n'embarque RIEN : SpaceWeatherLive refuse l'iframe, la fiche se réduit à son lien.)
28. **`AbortController` sur tout fetch tiers** (les proxies CORS et les pages lourdes bloquent sinon indéfiniment).

**Sécurité**
29. **`dangerouslySetInnerHTML` proscrit** → `DOMParser` pour extraire du texte d'un HTML tiers.
30. **`safeHref` avant tout `<a href>`** (la CSP autorise `unsafe-inline`, donc un `javascript:` s'exécuterait) — y compris sur une URL proposée par une IA.
31. **Service worker** : ne cacher que `res.ok` (sinon un 404 écrase l'app), app shell en **network-first plafonné à 1,5 s** avec `{cache:'no-cache'}` ; **jamais `no-store`** ni un retour au cache-**first** (les PWA installées ne recevraient plus les mises à jour). ⚠ Notifier `matchAll()` afficherait le bandeau sur la page SUIVANTE : un téléchargement de fond survit à la page qui l'a lancé.

**Environnement d'agent**
32. **Le proxy réseau bloque de nombreux hôtes** (Google News, LinkedIn, Steam, CoinGecko, docs.google.com, YouTube, instagram…) → écrire un parsing défensif et **dire explicitement « non vérifiable côté agent »** plutôt que d'affirmer.
33. **unpkg est bloqué** : pour tester la page complète dans Chromium, servir React/Babel en local et bloquer le service worker (`serviceWorkers:'block'`), sinon il court-circuite les routes de test.

## Limites connues

- **iOS/Safari** : IndexedDB ~50 Mo, purge automatique si l'app n'est pas ouverte pendant 7 jours → privilégier Android.
- sql.js charge toute la base en RAM ; `saveDB()` réexporte **tout** à chaque écriture → purger régulièrement, ne rien y mettre de lourd.
- Pas de synchronisation multi-appareils automatique (Dropbox / Drive / pCloud manuels).
- Le réglage « Version pour ordinateur » de Chrome Android est **impossible à désactiver depuis une page web** : seulement contourné (`useIsMobile` force le layout mobile + bandeau d'aide).
